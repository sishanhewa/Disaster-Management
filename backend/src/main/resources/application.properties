spring.datasource.url=${DB_URL:jdbc:postgresql://104.208.67.128:5432/river_levels_empty}
spring.datasource.username=${DB_USER:river_user}
spring.datasource.password=${DB_PASSWORD:ITP_SE_03}
spring.datasource.hikari.maximum-pool-size=2
spring.datasource.hikari.minimum-idle=1
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.cache.type=redis
server.port=8080
jwt.access-secret=${JWT_ACCESS_SECRET:sidms-dev-jwt-secret-key-change-me-in-production-asap!!}
jwt.access-expiration-ms=900000
jwt.refresh-expiration-ms=2592000000
ai.service.url=http://localhost:8000
app.frontend.url=http://localhost:5173
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
logging.level.com.sidms=INFO
management.endpoints.web.exposure.include=health,info,metrics
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:demo}
cloudinary.api-key=${CLOUDINARY_API_KEY:demo}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:demo}
api.open-meteo-base-url=https://api.open-meteo.com/v1
api.open-meteo-keys=${OPEN_METEO_KEY_1:},${OPEN_METEO_KEY_2:}
api.open-weather-map-keys=${OPENWEATHER_API_KEY_1:},${OPENWEATHER_API_KEY_2:},${OPENWEATHER_API_KEY_3:}

api.arcgis-base-url=https://services.arcgis.com/J7ZFXmR8rSmQ3FGf/arcgis/rest/services
api.arcgis-keys=${ARCGIS_KEY_1:},${ARCGIS_KEY_2:}

rivernet.api.key=${RIVERNET_API_KEY:}
api.rivernet-keys=${RIVERNET_KEY_1:},${RIVERNET_KEY_2:}

api.open-meteo-calls-per-minute=600
api.batch-size=50

app.sync.lazy-start=${APP_SYNC_LAZY_START:false}
app.sync.weather.enabled=${APP_SYNC_WEATHER_ENABLED:true}
app.sync.weather.current-enabled=${APP_SYNC_WEATHER_CURRENT_ENABLED:true}
app.sync.weather.openmeteo-fallback-enabled=${APP_SYNC_WEATHER_OPENMETEO_FALLBACK_ENABLED:false}
app.sync.weather.owm-fallback-max-calls-per-run=${APP_SYNC_WEATHER_OWM_FALLBACK_MAX_CALLS_PER_RUN:300}
app.sync.weather.forecast-max-units=${APP_SYNC_WEATHER_FORECAST_MAX_UNITS:-1}
app.sync.weather.aqi-enabled=${APP_SYNC_WEATHER_AQI_ENABLED:false}
app.sync.weather.aqi-max-calls-per-run=${APP_SYNC_WEATHER_AQI_MAX_CALLS_PER_RUN:0}
app.sync.yrno.endpoint-mode=${APP_SYNC_YRNO_ENDPOINT_MODE:complete}

# Intervals (default match existing code)
app.sync.weather.interval=1800000
app.sync.flood.interval=900000
app.sync.meteo.interval=900000
app.sync.alerts.interval=900000
app.sync.warming.interval=900000

# Initial Delays (populated by SIDMSApplication if lazy-start is true)
app.sync.weather.initial-delay=${APP_SYNC_WEATHER_INITIAL_DELAY:0}
app.sync.flood.initial-delay=${APP_SYNC_FLOOD_INITIAL_DELAY:0}
app.sync.meteo.initial-delay=${APP_SYNC_METEO_INITIAL_DELAY:0}
app.sync.alerts.initial-delay=${APP_SYNC_ALERTS_INITIAL_DELAY:0}
app.sync.warming.initial-delay=${APP_SYNC_WARMING_INITIAL_DELAY:10000}
app.sync.cache-evict.initial-delay=${APP_SYNC_CACHE_EVICT_INITIAL_DELAY:5000}
app.sync.debug.verbose=${APP_SYNC_DEBUG_VERBOSE:false}

# JAXA FTP credentials (resolved from .env/System properties)
jaxa.ftp.host=${JAXA_FTP_HOST:hokusai.eorc.jaxa.jp}
jaxa.ftp.user=${JAXA_FTP_USER:}
jaxa.ftp.pass=${JAXA_FTP_PASS:}

# Email (SMTP)
spring.mail.host=mail.xetuwiz.de
spring.mail.port=587
spring.mail.username=no-reply@xetuwiz.de
spring.mail.password=Noreplypassword1#
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Enable email notifications
app.notifications.email.enabled=true

# SMS Gateway (Android SMS Gate)
sms.gateway.cloud.url=https://api.sms-gate.app/3rdparty/v1
sms.gateway.username=RDMABK
sms.gateway.password=Smsgatewaypassword1#
sms.gateway.sim-number=1

# Enable SMS notifications
app.notifications.sms.enabled=true

# Enable SMS verification (separate from notifications)
app.verification.sms.enabled=true