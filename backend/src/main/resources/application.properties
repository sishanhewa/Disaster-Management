spring.application.name=backend

# VPS PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://104.208.67.128:5432/river_levels
spring.datasource.username=river_user
spring.datasource.password=ITP_SE_03
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Web CORS configuration could be added via code or properties
