import { r as __decorate } from "./tslib.es6-DlxpVI88.js";
import { A as has, n as n$4, t as r$5, y as i$10 } from "./Error-CzxduO2m.js";
import { T as N$1, W as t$9 } from "./typedArrayUtil-BAuNmygZ.js";
import { t as f$5 } from "./request-CuG5cxow.js";
import { C as y$5, D as n$5, S as w$1, U as t$10, b as s$9, f as d$6, j as u$2, o as L, t as $, u as T, v as m$6 } from "./promiseUtils-DhYhergm.js";
import { t as e$5 } from "./MapUtils-CBkGGs30.js";
import { C as m$7, l as l$6, n as c$3, t as a$4 } from "./decorators-DE7S5xmd.js";
import { c as c$4, t as b } from "./Accessor-kDoDKy4v.js";
import "./tracking-DBoczQof.js";
import "./ObservableBase-Bz6iwe5p.js";
import { t as t$11 } from "./nextTick-CSKTK1TU.js";
import "./scheduling-DiUcWka1.js";
import "./PooledArray-ChtfzjBt.js";
import { t as i$11 } from "./Evented-GLJbxWO5.js";
import "./SimpleObservable-CNlRjEs1.js";
import "./Collection-BAJSKCip.js";
import "./JSONSupport-BUaD4jSd.js";
import "./jsonMap-CFSDFmi6.js";
import "./Ellipsoid-DzO_iHAj.js";
import "./assets-BZbzeyNa.js";
import "./pe-BLztJ5xc.js";
import "./spatialReferenceUtils-b3vCEkpS.js";
import "./units-Dg-cK1vO.js";
import { t as S$1 } from "./SpatialReference-rIfb2LrD.js";
import "./Point-B7zMqEx6.js";
import "./Extent-CquIzaXp.js";
import "./locale-BdrQIP_a.js";
import "./apiKeyUtils-Bv2Uwsd3.js";
import "./messages-BSXJ_xjI.js";
import "./basemapDefinitions-CGK-Ctsz.js";
import { s as l$7 } from "./reactiveUtils-DRpp6Nmg.js";
import "./timeZoneUtils-CBNjS1ZG.js";
import "./UnknownTimeZone-Dk-CZx5e.js";
import "./date-BGzzeGV1.js";
import "./timeUtils-LVAIYsCb.js";
import "./TimeExtent-bDAyL7B5.js";
import "./colorUtils-BC0_8aMM.js";
import { y as r$6 } from "./mathUtils-hEBUcrMa.js";
import "./Color-C99QAF80.js";
import "./Clonable-D_RHUyXD.js";
import { n as n$6 } from "./uuid-CI605U6Y.js";
import "./Polygon-CCBjbbXT.js";
import "./curveUtils-CfkOAT4m.js";
import "./coordsUtils-DXLB9bAf.js";
import { E as z$1, f as f$6, o as N$2, t as A, v as o$5 } from "./aaBoundingRect-CgUWvAgv.js";
import "./common-BxLRDsKd.js";
import "./vec2-BPF6SpMH.js";
import "./curveExtent--ue9-x0m.js";
import "./mat3-CPqND9LM.js";
import "./mat3f64-DZZP34-L.js";
import "./Polyline-Cv0nwof6.js";
import "./vec3f64-CwISzc_v.js";
import "./Multipoint-B5Liskmz.js";
import "./spatialReferenceEllipsoidUtils-qNeWENaq.js";
import "./GeographicTransformation-D90zE-j2.js";
import "./geodesicConstants-C0TscDSm.js";
import "./projectBuffer-CV6RkXdH.js";
import "./projectionUtils-CmEsVWfk.js";
import { f as u$3 } from "./screenUtils-BR-xd7ya.js";
import "./utils-3ndlmaCD.js";
import "./mat4-CCf33Vjt.js";
import "./basemapUtils-C5xoGB-C.js";
import "./Cyclical-BTNbmw1N.js";
import "./jsonUtils-D_oLUjKv.js";
import "./typeUtils-DaICxhuY.js";
import "./Version-CjTddL5F.js";
import "./utils-5irCjX9t.js";
import "./utils-Ch7GqCap.js";
import "./fieldType-D7SwLPxF.js";
import "./sql-Cyp7eZa9.js";
import "./fieldUtils-CC2YSmV6.js";
import "./SimpleMarkerSymbol-BjFFaoyw.js";
import "./mat4f64-BA1Qbgtv.js";
import "./DoubleArray-EEc6IyGQ.js";
import { C as i$12 } from "./aaBoundingBox-CzeY9F8R.js";
import "./textUtils-B4iTDAON.js";
import "./TextSymbol-CsSnkPMD.js";
import "./SimpleFillSymbol-CbXKKnxp.js";
import "./Field-jzopk-Sr.js";
import "./Queue-CM8W5OTt.js";
import { i as c$5, o as l$8, r as u$4 } from "./workers-BjS-6PTj.js";
import "./number-DwLpDjta.js";
import "./intl-1FbLkipu.js";
import "./zscale-Cit8BV12.js";
import "./queryZScale-BhSMSSYh.js";
import "./displayFilterUtils-DQYkMjND.js";
import "./DynamicDataLayer-Nl0N-nbb.js";
import { t as R } from "./Query-aOayEcb1.js";
import { t as m$8 } from "./QuantizationParameters-BoZFfmfD.js";
import "./StatisticDefinition-DCvGQn-e.js";
import "./defaults-BIYIh1Ct.js";
import { r as y$6 } from "./diffUtils-D9XuwFJT.js";
import "./NormalizationBinParametersMixin-BMz0fNea.js";
import "./labelUtils-CbCLFptS.js";
import "./MemCache-DQgW8nin.js";
import "./LRUCache-C0A4Jg0w.js";
import { t as _$4 } from "./FieldsIndex-FII40DPp.js";
import "./enum-D9ePJlKL.js";
import "./TimeOnly-DiAMH6GI.js";
import { a as u$5 } from "./ArcadeExpression-DAdhL71a.js";
import { t as s$10 } from "./DictionaryScriptEvaluator-CHGIJh_p.js";
import "./defaultCIMValues-DmZscRIy.js";
import "./utils-CwgvNNZ_.js";
import "./vec4-DVix-cmy.js";
import "./vec4f64-SXri5KT8.js";
import "./heatmapUtils-CKd_Sdiu.js";
import { t as z$2 } from "./TileInfo-Dm0DlKvz.js";
import "./TileKey-DNAwECdW.js";
import "./UpdatingHandles-BpejPsAZ.js";
import "./signal-DCDIpEz3.js";
import "./ReactiveMap-B1BORGbU.js";
import "./vec2f64-BKe4utUH.js";
import "./mat2d-BuUJVbP4.js";
import "./normalizeUtilsCommon-gtN1A7xM.js";
import { n as L$1 } from "./normalizeUtils-BbPgVXXO.js";
import "./mat2df32-D4Q05fSu.js";
import "./vec2f32-D_bzcz_y.js";
import "./debugFlags-CzS8-qb6.js";
import "./Scheduler-PPZHCbsQ.js";
import { t as e$6 } from "./TileKey-CWP4O_FK.js";
import { t as h$5 } from "./TileInfoView-BxjD5r_v.js";
import "./ScheduledQueueProcessor-CgHEIqY6.js";
import "./TileStrategy-2ufKPmL7.js";
import "./rbush-CqCIXys4.js";
import { d as u$6, o as a$5, s as i$13, u as s$11 } from "./quantizationUtils-C-TMvCYs.js";
import { t as e$7 } from "./arcadeUtils-BS6_jCyT.js";
import { t as a$6 } from "./Tile-Cxu-kR0E.js";
import "./definitions-BxssUXCo.js";
import "./enums-DUaXkkTm.js";
import { n as i$14 } from "./memoryEstimations-BBFGLDPz.js";
import { t as s$12 } from "./OptimizedGeometry-CNYohxaW.js";
import { C as o$6, _ as lt, b as rt, d as X, h as et, i as D, l as S$2, s as J, x as ut } from "./featureConversionUtils-BQ5ifpAj.js";
import "./WhereClause-CROVW3Le.js";
import { a as t$12, i as r$7, r as i$15, t as _$5 } from "./streamLayerUtils-5M96awbW.js";
import "./closestPointOnCurve-DOaJ7IXx.js";
import { n as u$7 } from "./QueueProcessor-CWKnNCOB.js";
import "./FlatGeometry-D0n_NdSI.js";
import { t as e$9 } from "./geodeticCurveType-CAiC8BEg.js";
import { a as u$8, i as s$13, r as i$16 } from "./operatorGeodeticDensify-C-B7b3bE.js";
import "./bundle-BzzL5SC6.js";
import "./WhereClauseCache-CbTZRh0W.js";
import "./clientSideDefaults-BMp3ST94.js";
import "./generateRendererUtils-C-FDEpEz.js";
import "./utils-3D591xuo.js";
import { t as n$7 } from "./pbf-Czx4fZ_r.js";
import { n as U$1, r as f$7 } from "./pbfQueryUtils-CbCn_UG1.js";
import "./queryUtils-imz_pa9S.js";
import { a as l$9, i as j$1, o as m$9 } from "./query-IgT1qZDA.js";
import "./timeSupport-B81HKeWW.js";
import { t as e$10 } from "./optimizedFeatureQueryEngineAdapter-Pxwx0I21.js";
import { t as W$1 } from "./QueryEngine-Ccc05g61.js";
import { n as f$8 } from "./projectionSupport-qG0SGMeB.js";
import "./utils-Dgqqelok.js";
import "./utils-nvlqepdT.js";
import "./queryUtils-CNTJGLMY.js";
import "./FixedIntervalBinParameters-CbmEfZTf.js";
import { t as a$7 } from "./normalizeUtilsSync-Cj2_7db3.js";
import "./date-Dr7Yyuw6.js";
import "./geojson-DtGUaoDu.js";
import "./sourceUtils-IJgEL_Ke.js";
import { a as R$1 } from "./ogcFeatureUtils-XConhfWS.js";
import { t as s$14 } from "./loadParquetModule-C6PNN_hg.js";
import "./GeometryUtils-B-zPj-EF.js";
import "./utils-DtAoCWzC.js";
import "./AlignedVertexSpec-DByvPz6j.js";
import { n as r$8 } from "./createConnection-BCuG0ZNa.js";
import { a as s$15, c as l$10, i as t$13, l as i$17, n as n$9, o as d$7, r as n$8, s as r$9, t as o$7 } from "./FeatureSetReaderParquet-9w3F5ML1.js";
import "./labelPoint-IgtWrSUL.js";
import { a as u$9, o as O, s as a$8 } from "./callExpressionWithCursor-D5J-jWwC.js";
import { t as i$18 } from "./FeatureMetadata-BHJp6_d8.js";
import "./fontUtils-CPPDvNws.js";
import "./CIMSymbolHelper-BFA0d3St.js";
import "./rasterizingUtils-C2t5_kHq.js";
import "./BoundingBox-wqZcYwRQ.js";
import "./BidiEngine-Dr3hg5XU.js";
import "./libtess-DgzzZQ3y.js";
import "./TurboLine-BNuOc56H.js";
import "./templateUtils-CEt6V42d.js";
import { t as n$10 } from "./UpdateTracking2D-BU2X0KCG.js";
import "./dataViewUtils-D2k9_zlf.js";
import "./constants-Dbjt-7cW.js";
import { t as t$14 } from "./CircularArray-Daxpyh3i.js";
import { n as u$10, t as e$11 } from "./FeatureIdInfo-B0YGalvX.js";
import { a as s$16, i as p$1, r as b$1, s as n$11, t as l$11 } from "./ComputedAttributeStorage-DBhSWnRM.js";
import "./PieChartMeshWriter-BcgYYaeu.js";
import { n as r$10, t as x$2 } from "./MeshWriterRegistry-BKSfpYiC.js";
//#region node_modules/@arcgis/core/views/2d/layers/features/PipelineConnectionHandlers.js
var r$4 = class {
	constructor(e) {
		this._client = e, this.layerView = this._client.createInvokeProxy(""), this.container = this._client.createInvokeProxy("container"), this._eventLog = this._client.createInvokeProxy("eventLog");
	}
	onEvent(r) {
		"error" === r.type && r.error && "toJSON" in r.error && (r.error = r.error.toJSON()), l$8(this._eventLog.onEvent(r));
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/engine/webgl/mesh/MultiTileMeshData.js
var r$3 = 1, i$9 = 2, n$3 = 4, h$4 = 8, o$4 = 16, c$2 = 32, a$3 = 64, f$4 = 128;
function l$5(e) {
	switch (e) {
		case r$3:
		case h$4:
		case c$2: return -1;
		case i$9:
		case a$3: return 0;
		case n$3:
		case o$4:
		case f$4: return 1;
	}
}
function u$1(e) {
	switch (e) {
		case r$3:
		case i$9:
		case n$3: return -1;
		case h$4:
		case o$4: return 0;
		case c$2:
		case a$3:
		case f$4: return 1;
	}
}
var m$5 = h$4 | 33, _$3 = o$4 | 132, d$5 = i$9 | 5, p = a$3 | 160;
var g$2 = class {
	constructor(e, s, r, i, n = 0) {
		this.tileKey = e, this._bufferingEnabled = s, this._sizeHint = n, this._meshes = {
			self: new n$11(this.id, this._sizeHint),
			neighbors: new Array()
		}, this._currentRecordOverlaps = 0, this._currentEntityOverlaps = 0;
		const h = i ? 1 : 0;
		this._copyBufferedDataIntoSelf = r && this._bufferingEnabled && e.level === h;
	}
	get id() {
		return this.tileKey.id;
	}
	get requiresRefresh() {
		return this._meshes.self.requiresRefresh || this._meshes.neighbors.some((e) => e.requiresRefresh);
	}
	set requiresRefresh(e) {
		this._meshes.self.requiresRefresh = e;
	}
	vertexStart() {
		return this._meshes.self.vertexStart() ?? 0;
	}
	vertexCount() {
		return this._meshes.self.vertexCount();
	}
	indexCount() {
		return this._meshes.self.indexCount();
	}
	indexEnsureSize(e) {
		this._meshes.self.indexEnsureSize(e);
	}
	entityStart(e, s = e) {
		this._currentEntityOverlaps = 0, this._meshes.self.entityStart(e, s);
	}
	entityRecordCount() {
		return this._meshes.self.entityRecordCount();
	}
	entityEnd() {
		if (this._meshes.self.entityEnd(), this._bufferingEnabled) {
			if (this._copyBufferedDataIntoSelf) return;
			for (let e = 0; e < 8; e++) {
				const s = 1 << e;
				this._currentEntityOverlaps & s && this._meshes.neighbors[e].entityEnd();
			}
		}
	}
	recordStart(e, s, t) {
		this._currentRecordOverlaps = 0, this._meshes.self.recordStart(e, s, t);
	}
	recordEnd(e = 0) {
		const s = this._meshes.self.recordEnd(this._currentRecordOverlaps);
		return s && 0 !== this._currentRecordOverlaps ? (this._copyIntoNeighbors(), this._currentEntityOverlaps |= this._currentRecordOverlaps, !0) : s;
	}
	recordBounds(e, s, t, r) {
		this._bufferingEnabled && this._addOverlap(e, s, t, r);
	}
	recordCount() {
		return this._meshes.self.recordCount();
	}
	metricStart(e) {
		this._meshes.self.metricStart(e);
	}
	metricBoxWrite(e) {
		this._meshes.self.metricBoxWrite(e);
	}
	metricEnd() {
		this._meshes.self.metricEnd();
	}
	vertexWrite(e) {
		this._meshes.self.vertexWrite(e);
	}
	vertexWriteF32(e) {
		this._meshes.self.vertexWriteF32(e);
	}
	vertexWriteRegion(e) {
		this._meshes.self.vertexWriteRegion(e);
	}
	indexWrite(e) {
		this._meshes.self.indexWrite(e);
	}
	serialize(s) {
		const t = {
			message: [],
			transferList: []
		}, r = this._meshes.self.serialize();
		return t.message.push({
			tileId: this.tileKey.id,
			requiresRefresh: this.requiresRefresh,
			...r.message
		}), t.transferList.push(...r.transferList), this._meshes.neighbors.forEach((r, i) => {
			const n = r.serialize(), h = 1 << i, o = l$5(h), c = u$1(h), a = new e$6(this.tileKey).getNormalizedNeighbor(o, c, s);
			t.message.push({
				tileId: a.id,
				requiresRefresh: this.requiresRefresh,
				...n.message
			}), t.transferList.push(...n.transferList);
		}), t;
	}
	_addOverlap(e, t, r, i) {
		const n = Math.min(512 / 2, r), h = Math.min(512 / 2, i), o = 255 ^ ((e < 0 + n ? _$3 : e >= 512 - n ? m$5 : _$3 | m$5) | (t < 0 + h ? p : t >= 512 - h ? d$5 : p | d$5));
		this._currentRecordOverlaps |= o;
	}
	_copyIntoNeighbors() {
		for (let e = 0; e < 8; e++) {
			const r = 1 << e;
			if (!!(this._currentRecordOverlaps & r)) {
				if (this._copyBufferedDataIntoSelf) {
					const e = -l$5(r) * 512, t = -u$1(r) * 512;
					if (0 !== t) continue;
					this._meshes.self.copyLast(e, t);
					continue;
				}
				if (!this._meshes.neighbors[e]) {
					const s = Math.floor(this._sizeHint / 16);
					this._meshes.neighbors[e] = new n$11(r, s);
				}
				const i = this._meshes.neighbors[e], n = -l$5(r) * 512, h = -u$1(r) * 512;
				i.copyLastFrom(this._meshes.self, n, h);
			}
		}
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/engine/webgl/mesh/factories/MeshFactory.js
var s$8 = class {};
//#endregion
//#region node_modules/@arcgis/core/views/2d/engine/webgl/util/Matcher.js
var t$8 = class t$8 {
	constructor() {
		this._defaultResult = null, this._backgroundFillResult = null;
	}
	static async from(e, s) {
		const l = new t$8();
		return l.setDefault(await e.createMeshWriters(s.meshes)), l;
	}
	size() {
		return 1;
	}
	getDefault() {
		return this._defaultResult;
	}
	setDefault(t) {
		this._defaultResult = t;
	}
	getBackgroundFill() {
		return this._backgroundFillResult;
	}
	setBackgroundFill(t) {
		this._backgroundFillResult = t;
	}
	hasArcadeDependency(t) {
		return this._defaultResult?.some((e) => e.hasArcadeDependency(t)) ?? !1;
	}
	match(t, e, s) {
		const l = this.doMatch(t, e) || this.getDefault();
		if (l && l.length > 0) {
			const t = this.getBackgroundFill();
			if (t) return [...t, ...l];
		}
		return l;
	}
	getSortKey(t, e) {
		return 0;
	}
	doMatch(t, e) {
		return null;
	}
	async fetchResources(t, e) {}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/engine/webgl/util/DictionaryMatcher.js
var e$4 = class e$4 extends t$8 {
	static async fromDictionaryRenderer(r, o) {
		return new e$4(r, await s$10.from(o.dictionaryInfo, o.userConfig, o.fieldMap));
	}
	constructor(t, r) {
		super(), this._context = t, this._evaluator = r, this._controlStringToPromise = /* @__PURE__ */ new Map(), this._controlStringToGroup = /* @__PURE__ */ new Map();
	}
	async fetchResources(t, r) {
		const e = r.getCursor(), o = /* @__PURE__ */ new Set();
		for (; e.next();) {
			const t = this._evaluateControlString(e);
			t && o.add(t);
		}
		const n = Array.from(o.values()).map((r) => this._ensureGroup(t, r));
		await Promise.all(n);
	}
	match(t, r) {
		const e = this._evaluateControlString(t);
		return e ? this._controlStringToGroup.get(e) : null;
	}
	_evaluateControlString(t) {
		const r = t.readLegacyFeatureWorldSpace();
		return this._evaluator.evaluate(r, 0, t.fields, null);
	}
	_ensureGroup(t, r) {
		let e = this._controlStringToPromise.get(r);
		return e ?? (e = this._fetchGroup(t, r), this._controlStringToPromise.set(r, e)), e;
	}
	async _fetchGroup(t, r) {
		const e = await t.fetchDictionaryResourceImmediate({
			type: "dictionary-request",
			controlString: r
		});
		if (!e) return;
		const o = await this._context.createMeshWriters(e.meshes);
		this._controlStringToGroup.set(r, o);
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/engine/webgl/util/IntervalMatcher.js
var t$7 = class t$7 extends t$8 {
	constructor(e, t) {
		super(), this._intervals = [], this._isMaxInclusive = t, this._field = e;
	}
	static async fromIntervalSchema(e, s) {
		const a = new t$7(await e.storage.createComputedField(s), s.isMaxInclusive);
		await Promise.all(s.intervals.map(async (t) => {
			const s = await e.createMeshWriters(t.meshes);
			a.add(t, s);
		}));
		const i = await e.createMeshWriters(s.defaultSymbol);
		a.setDefault(i);
		const n = await e.createMeshWriters(s.backgroundFill);
		return a.setBackgroundFill(n), a;
	}
	add(e, t) {
		this._intervals.push({
			interval: e,
			result: t
		}), this._intervals.sort((e, t) => e.interval.min - t.interval.min);
	}
	size() {
		return super.size() + this._intervals.length;
	}
	hasArcadeDependency(e) {
		return this._field?.hasArcadeDependency(e) || this._intervals.some((t) => t.result.some((t) => t.hasArcadeDependency(e)));
	}
	doMatch(e, t) {
		const s = this._field?.read(e, t);
		if (null == s || isNaN(s) || s === Infinity || s === -Infinity) return null;
		for (let r = 0; r < this._intervals.length; r++) {
			const { interval: e, result: t } = this._intervals[r], a = s >= e.min, i = this._isMaxInclusive ? s <= e.max : s < e.max;
			if (a && i) return t;
		}
		return null;
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/engine/webgl/util/LabelMatcher.js
var s$7 = class s$7 extends t$8 {
	static async fromLabelSchema(e, a) {
		const r = a.classes.map(async (s) => {
			const a = await e.createMeshWriters(s.meshes);
			return {
				minScale: s.minScale,
				maxScale: s.maxScale,
				meshes: a,
				expression: null,
				where: await e.storage.createWhereClause(s.where)
			};
		});
		return new s$7(await Promise.all(r));
	}
	constructor(e) {
		super(), this._labels = e;
	}
	match(e, s, a) {
		if (!this._labels.length) return null;
		const r = this._getLabels(s.$view.scale), t = [];
		for (const l of r) l.where && !l.where(e, a) || t.push(...l.meshes);
		return t;
	}
	hasArcadeDependency(e) {
		return this._labels.some((s) => s.meshes.some((s) => s.hasArcadeDependency(e)));
	}
	_getLabels(e) {
		return this._labels.filter((s) => this._validForTileScale(s, e));
	}
	_validForTileScale(e, s) {
		const a = s - s / 4, r = s + s / 2;
		return (!e.minScale || e.minScale >= a) && (!e.maxScale || e.maxScale <= r);
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/engine/webgl/util/MapMatcher.js
var t$6 = class t$6 extends t$8 {
	constructor(e, t) {
		super(), this._defaultSymbolSortKey = 0, this._nullResult = null, this._resultsMap = /* @__PURE__ */ new Map(), this._fields = [], this._fields = e, this._separator = t || "";
	}
	static async fromMatcherSchema(e, s) {
		const l = s.expression ? [e.storage.createComputedField({ expression: s.expression })] : [
			s.field ? e.storage.createComputedField({ field: s.field }) : null,
			s.field2 ? e.storage.createComputedField({ field: s.field2 }) : null,
			s.field3 ? e.storage.createComputedField({ field: s.field3 }) : null
		], i = new t$6((await Promise.all(l)).filter((e) => !!e), s.fieldDelimiter), a = await e.createMeshWriters(s.defaultSymbol);
		i.setDefault(a);
		const u = await e.createMeshWriters(s.backgroundFill);
		return i.setBackgroundFill(u), await Promise.all(s.map.map(async (t, s) => {
			const l = await e.createMeshWriters(t.symbol);
			"<Null>" === t.value ? i.setNullResult(l) : i.add(t.value, l, s + 1);
		})), i;
	}
	setNullResult(e) {
		this._nullResult = e;
	}
	getSortKey(e, t) {
		const s = this._getValueFromFields(e, t);
		if (null == s || "" === s || "<Null>" === s) return 0;
		const l = this._resultsMap.get(s.toString());
		return l ? l.sortKey : this._defaultSymbolSortKey;
	}
	add(e, t, s) {
		this._resultsMap.set(e.toString(), {
			meshWriters: t,
			sortKey: s
		}), this._defaultSymbolSortKey = Math.max(this._defaultSymbolSortKey, s + 1);
	}
	size() {
		return super.size() + this._resultsMap.size;
	}
	hasArcadeDependency(e) {
		return this._fields.some((t) => t.hasArcadeDependency(e)) || [...this._resultsMap.values()].some((t) => t.meshWriters.some((t) => t.hasArcadeDependency(e))) || this._nullResult?.some((t) => t.hasArcadeDependency(e)) || !1;
	}
	doMatch(e, t) {
		const s = this._getValueFromFields(e, t);
		if (null !== this._nullResult && (null == s || "" === s || "<Null>" === s)) return this._nullResult;
		if (null == s) return null;
		const l = s.toString();
		return this._resultsMap.get(l)?.meshWriters;
	}
	_getValueFromFields(e, t) {
		const s = [];
		for (const l of this._fields) {
			const r = l.read(e, t);
			null == r || "" === r ? s.push("<Null>") : s.push(r);
		}
		return s.join(this._separator);
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/engine/webgl/util/createMatcher.js
async function n$2(e, n) {
	switch (n.type) {
		case "simple":
		case "heatmap":
		case "dot-density":
		case "pie-chart": return t$8.from(e, n);
		case "interval": return t$7.fromIntervalSchema(e, n);
		case "dictionary": return e$4.fromDictionaryRenderer(e, n);
		case "label": return s$7.fromLabelSchema(e, n);
		case "map": return t$6.fromMatcherSchema(e, n);
		case "subtype": return h$3.fromSubtypes(e, n);
		case "cluster": return i$8.fromClusterSchema(e, n);
		case "track": return o$3.fromTrackSchema(e, n);
		default: throw new Error("Impl");
	}
}
var h$3 = class h$3 extends t$8 {
	constructor(e, t) {
		super(), this._subMatchers = e, this._subtypeField = t;
	}
	static async fromSubtypes(e, t) {
		const r = /* @__PURE__ */ new Map(), a = [];
		for (const s in t.renderers) {
			const c = parseInt(s, 10), h = n$2(e, t.renderers[s]).then((e) => r.set(c, e));
			a.push(h);
		}
		return await Promise.all(a), new h$3(r, t.subtypeField);
	}
	match(e, t, r) {
		const a = e.readAttribute(this._subtypeField), s = this._subMatchers.get(a);
		return s ? s.match(e, t, r) : null;
	}
	hasArcadeDependency(e) {
		for (const t of this._subMatchers.values()) if (t.hasArcadeDependency(e)) return !0;
		return !1;
	}
};
var i$8 = class i$8 extends t$8 {
	static async fromClusterSchema(e, t) {
		const [r, a] = await Promise.all([n$2(e, t.feature), n$2(e, t.cluster)]);
		return new i$8(r, a);
	}
	constructor(e, t) {
		super(), this._featureMatcher = e, this._clusterMatcher = t;
	}
	match(e, t, r) {
		return 1 === e.readAttribute("cluster_count") ? this._featureMatcher.match(e, t, r) : this._clusterMatcher.match(e, t, r);
	}
	hasArcadeDependency(e) {
		return this._featureMatcher.hasArcadeDependency(e) || this._clusterMatcher.hasArcadeDependency(e);
	}
};
var o$3 = class o$3 extends t$8 {
	static async fromTrackSchema(e, t) {
		const [r, a, s] = await Promise.all([
			n$2(e, t.previousObservation),
			n$2(e, t.latestObservation),
			n$2(e, t.trackLine)
		]);
		return new o$3(r, a, s);
	}
	constructor(e, t, r) {
		super(), this._previousObservationMatcher = e, this._latestObservationMatcher = t, this._trackLineMatcher = r;
	}
	match(t, r, a) {
		switch (t.readAttribute(r$7)) {
			case 0: return this._trackLineMatcher.match(t, r, a);
			case 1: return this._latestObservationMatcher.match(t, r, a);
			case 2: return this._previousObservationMatcher.match(t, r, a);
		}
		return null;
	}
	hasArcadeDependency(e) {
		return this._trackLineMatcher.hasArcadeDependency(e) || this._latestObservationMatcher.hasArcadeDependency(e) || this._previousObservationMatcher.hasArcadeDependency(e);
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/engine/webgl/mesh/factories/FeatureMeshFactory.js
var s$6 = class s$6 extends s$8 {
	static async create(e, o) {
		return new s$6(await n$2(e, o.symbology), o.labels ? await n$2(e, o.labels) : null);
	}
	constructor(e, t) {
		super(), this._symbology = e, this._labels = t;
	}
	destroy() {}
	async enqueueMatcherRequests(e, t) {
		await Promise.all([this._symbology.fetchResources(e, t), this._labels?.fetchResources(e, t)]);
	}
	enqueueWriterRequests(e, t, s, o) {
		const r = this._symbology.match(t, s, o);
		if (r) {
			for (const o of r) o.enqueueRequest(e, t, s);
			if (this._labels) {
				const r = this._labels.match(t, s, o);
				if (!r) return;
				for (const o of r) o.enqueueRequest(e, t, s);
			}
		}
	}
	write(e, t, s, o, r, c, a) {
		const n = this._symbology.match(s, o, r);
		if (!n) return;
		for (const i of n) i.write(e, t, s, o, c);
		if (e.entityRecordCount() >= 1 && this._labels) {
			const i = this._labels.match(s, o, r);
			if (!i) return;
			for (const r of i) r.setReferences(n), r.write(e, t, s, o, c, a);
		}
	}
	getSortKey(e, t) {
		return this._symbology.getSortKey(e, t);
	}
	hasArcadeDependency(e) {
		return !(!this._symbology.hasArcadeDependency(e) && !this._labels?.hasArcadeDependency(e));
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/engine/webgl/util/MatcherContext.js
var s$5 = class {
	constructor(s, t, r, e, i) {
		this.storage = s, this.proxy = t, this.viewParams = r, this.registry = e, this.fieldsMap = i;
	}
	async createMeshWriters(s) {
		const t = s.map((s) => this.registry.createMeshWriter(this.storage, this.proxy, this.viewParams, s, this.fieldsMap));
		return Promise.all(t);
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/ProcessorTileMessageQueue.js
var e$3 = class {
	constructor(e) {
		this._outstandingMessages = [], this._queue = new u$7({
			concurrency: e.concurrency,
			process: (s) => e.process(s)
		});
	}
	async push(s) {
		if (s.end) return await Promise.all(this._outstandingMessages), await this._queue.push(s), void (this._outstandingMessages = []);
		const e = this._queue.push(s);
		return this._outstandingMessages.push(e), e;
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/aggregation/ComputedAggregateField.js
var s$4 = class s$4 {
	static async create(i, e) {
		if ("count" === e.statisticType) {
			const i = new r$10(1);
			return new s$4(e.name, e.alias, e.type, e.statisticType, i);
		}
		const a = await i.createComputedField({
			expression: e.onStatisticExpression?.expression,
			field: e.onStatisticField
		});
		return new s$4(e.name, e.alias, e.type, e.statisticType, a);
	}
	constructor(t, s, i, e, a) {
		this.name = t, this.alias = s, this.type = i, this.statisticType = e, this.computed = a;
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/processor/AProcessorStrategy.js
var e$2 = class {
	constructor(e) {
		this.subscription = e, this.handledChunks = /* @__PURE__ */ new Set();
	}
	destroy() {}
};
var t$5 = class {
	constructor(e, t, s) {
		this._source = e, this._attributeStore = t, this._sqlOptions = s, this._sendStates = /* @__PURE__ */ new Map();
	}
	destroy() {}
	get enablePixelBuffering() {
		return !0;
	}
	get isAggregate() {
		return !1;
	}
	get usedMemory() {
		return 0;
	}
	updateRequiredLevel(e, t) {}
	onSubscribe(e) {
		const t = this.createState(e);
		this._sendStates.set(e.key.id, t), this.updateChunks();
	}
	onUnsubscribe(e) {
		this._sendStates.get(e.key.id)?.destroy(), this._sendStates.delete(e.key.id);
	}
	get hasSubscribers() {
		return this._sendStates.size > 0;
	}
	requiresInvalidation() {
		return !1;
	}
	invalidate() {
		const e = Array.from(this._sendStates.values());
		this._sendStates.clear();
		for (const t of e) t.destroy(), this.onSubscribe(t.subscription);
	}
	invalidateAttributeData(e) {}
	hasArcadeDependency(e) {
		return !1;
	}
	getFeatureObjectIdsForAggregate(e) {
		throw new Error("InternalError: AggregateId lookup not supported");
	}
	getDisplayIds(e) {
		return this.displayMap(e, (e) => e, (e) => e);
	}
	getDisplayAndObjectIds(e) {
		return this.displayMap(e, (e) => e, (e, t, s) => [e, s]);
	}
	afterUpdateChunks() {}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/processor/AAggregateStrategy.js
var i$7 = class extends t$5 {
	constructor(r, t, s, n, i) {
		super(r, t, i), this.spatialReference = s, this.aggregateFields = n, this._arcadeDependencies = /* @__PURE__ */ new Set(), this.events = new i$11(), this.featureAdapter = e$10;
		for (const e of n) u$5(this._arcadeDependencies, e.computed);
	}
	get aggregateQueryEngine() {
		return this._aggregateQueryEngine || (this._aggregateQueryEngine = new W$1({
			featureStore: this,
			fieldsIndex: this._metadata.fieldsIndex,
			geometryType: this._metadata.geometryType,
			featureIdInfo: this._metadata.featureIdInfo,
			spatialReference: this.spatialReference
		})), this._aggregateQueryEngine;
	}
	get isAggregate() {
		return !0;
	}
	removeChunks(e) {}
	hasArcadeDependency(e) {
		return this._arcadeDependencies.has(e);
	}
	forEach(e) {
		return this.forEachAggregateWorldSpace(e);
	}
	forEachInBounds(e, r) {}
	forEachBounds(e, a) {
		const s = i$12();
		for (const r of e) {
			const e = ut(s, r.geometry);
			e && a(e);
		}
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/sources/FeatureSourceMessage.js
var e$1 = class {
	constructor(e, i, t, s, r) {
		this.subscription = e, this.reader = i, this.clear = t, this.end = s, this.debugInfo = r, this.type = "append";
	}
	get id() {
		return this.subscription.tile.id;
	}
	createMessage(e, i, t) {
		return {
			type: "append",
			clear: this.clear,
			id: this.id,
			append: e,
			end: this.end,
			debugInfo: this.debugInfo,
			subscriptionVesrion: this.subscription.version,
			version: i,
			attributeEpoch: t
		};
	}
};
var i$6 = class {
	constructor(e, i, t, s, r) {
		this.subscription = e, this.reader = i, this.remove = t, this.end = s, this.debugInfo = r, this.type = "update";
	}
	get id() {
		return this.subscription.tile.id;
	}
	createMessage(e, i, t) {
		return {
			type: "update",
			id: this.id,
			modify: e,
			debugInfo: this.debugInfo,
			remove: this.remove,
			version: i,
			subscriptionVesrion: this.subscription.version,
			end: this.end,
			attributeEpoch: t
		};
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/processor/BinningStrategy.js
var g$1 = class extends e$2 {
	constructor(e, t) {
		super(e), this.bins = /* @__PURE__ */ new Map(), this.featureCache = /* @__PURE__ */ new Map(), this.done = !1, this._store = t;
	}
	reset() {
		this.destroy(), this.done = !1;
	}
	destroy() {
		const e = this.subscription.tile.key.level;
		for (const t of this.featureCache.keys()) this._store.releaseDisplayIdForObjectId(`${t}.${e}`);
		this.bins.clear(), this.featureCache.clear(), this.handledChunks.clear();
	}
	get tile() {
		return this.subscription.tile;
	}
	*featuresWorldSpace() {
		for (const e of this.featureCache.values()) {
			const t = e.clone();
			t.geometry && lt(t.geometry, this.subscription.tile.transform), yield t;
		}
	}
};
var y$4 = class y$4 extends i$7 {
	static async create(t, s, a, i, n) {
		const l = s.metadata.outSpatialReference, d = new l$11({ spatialReference: l }), c = await Promise.all(t.fields.map(async (e) => s$4.create(d, e))), p = t.featureFilter ? await p$1.create({
			geometryType: s.metadata.geometryType,
			hasM: !1,
			hasZ: !1,
			timeInfo: s.metadata.timeInfo,
			fieldsIndex: s.metadata.fieldsIndex,
			spatialReference: l,
			filterJSON: t.featureFilter
		}) : null;
		return "geohash" === t.index.type && await f$8(l, S$1.WGS84), new y$4(t, p, i, c, l, s, a, n);
	}
	constructor(e, t, s, a, r, o, n, l) {
		super(o, n, r, a, l), this._schema = e, this._featureFilter = t, this._arcadeContextInfo = s, this._metadata = i$18.createFeature({
			geometryType: "esriGeometryPolygon",
			featureIdInfo: {
				type: "object-id",
				fieldName: "aggregateId"
			},
			fieldsIndex: new _$4(e.fields).toJSON(),
			globalIdField: null,
			spatialReference: o.metadata.spatialReference,
			outSpatialReference: o.metadata.outSpatialReference,
			subtypeField: null,
			subtypes: null,
			timeInfo: null,
			timeReferenceUnknownClient: null,
			dateFieldsTimeZone: null,
			typeIdField: null,
			types: null
		});
	}
	createState(e) {
		return new g$1(e, this._attributeStore);
	}
	async *applyOverrideUpdate(e) {
		for (const t of this._sendStates.values()) {
			t.reset();
			yield new e$1(t.subscription, u$9.empty(this._source.metadata), !0, !1, {});
		}
	}
	displayMap(e, t, s) {
		const a = new Map(e.map((e) => [t(e), e])), r = [];
		for (const i of this._sendStates.values()) for (const e of i.featuresWorldSpace()) {
			const { objectId: t, displayId: i } = e, o = a.get(t);
			if (null != o) {
				const e = s(i, o, t);
				r.push(e), a.delete(t);
			}
		}
		return r;
	}
	getDisplayFeatures(e) {
		const s = new Set(e), a = /* @__PURE__ */ new Set(), r = [];
		for (const i of this._sendStates.values()) for (const e of i.featuresWorldSpace()) s.has(e.displayId) && !a.has(e.objectId) && (e.geometry && r.push({
			...D(e, this._metadata.geometryType, !1, !1),
			displayId: e.displayId
		}), a.add(e.objectId));
		return {
			features: [],
			aggregates: r,
			tracks: []
		};
	}
	getFeatureObjectIdsForAggregate(e) {
		for (const t of this._sendStates.values()) for (const s of t.bins.values()) if (s.id === e) return Array.from(s.containedObjectIds);
		return [];
	}
	async *updateChunks() {
		for (const e of this._sendStates.values()) yield* this._update(e, this._source);
	}
	forEachAggregateWorldSpace(e) {
		const t = /* @__PURE__ */ new Set();
		for (const s of this._sendStates.values()) for (const a of s.featuresWorldSpace()) t.has(a.objectId) || (e(a), t.add(a.objectId));
	}
	_createIndexOptions(e) {
		switch (this._schema.index.type) {
			case "geohash": return {
				type: "geohash",
				fields: this.aggregateFields,
				featureFilter: this._featureFilter,
				geohashLevel: this._schema.index.fixBinLevel,
				spatialReference: this.spatialReference,
				arcadeContextInfo: this._arcadeContextInfo,
				scale: e.scale,
				sqlOptions: this._sqlOptions
			};
			case "grid": {
				const t = this._schema.index.fixedBinLevel, s = null != t ? e.tileInfoView.getLODInfoAt(t).scale : e.scale;
				return {
					type: "grid",
					fields: this.aggregateFields,
					cellSize: this._schema.index.size,
					featureFilter: this._featureFilter,
					spatialReference: this.spatialReference,
					arcadeContextInfo: this._arcadeContextInfo,
					scale: s,
					sqlOptions: this._sqlOptions
				};
			}
		}
	}
	async *_update(e, t) {
		const { handledChunks: s, subscription: r, bins: i, featureCache: o } = e, n = r.tile;
		if (e.done) return;
		for (const a of t.chunks()) {
			if (s.has(a.chunkId)) continue;
			s.add(a.chunkId);
			const t = a.queryInfo;
			if ("tileId" in t) {
				const e = new e$6(t.tileId);
				if (e.level !== n.level || e.world !== n.key.world) continue;
			}
			a.getAggregateIndex(this._createIndexOptions(e.tile)).putBounded(i, e.tile.extent, e.tile.resolution);
		}
		const l = [], d = r.tile.transform, u = r.tile.key.level;
		for (const c of i.values()) {
			let e = o.get(c.id);
			if (e) e.attributes = c.getAttributes();
			else {
				const t = c.getGeometry(this.spatialReference, d);
				e = new o$6(t, c.getAttributes(), null, c.id), t || (e.centroid = c.getGeometricCentroid(this.spatialReference, d)), e.displayId = this._attributeStore.createDisplayIdForObjectId(`${e.objectId}.${u}`), o.set(c.id, e);
			}
			l.push(e);
		}
		this.events.emit("changed"), e.done = !t.updateTracking.updating;
		const f = u$9.fromOptimizedFeatures(l, this._metadata, d), p = f.getCursor(), g = e.subscription.tile.createArcadeEvaluationOptions(this._arcadeContextInfo);
		for (; p.next();) this._attributeStore.setAttributeData(p.getDisplayId(), p, g, this._sqlOptions);
		yield new i$6(e.subscription, f, [], e.done, {});
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/processor/support/WithDisplayId.js
var s$3 = class {
	constructor(s, t) {
		this.inner = s, this.displayId = t;
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/processor/ClusterStrategy.js
var m$4 = 128;
var y$3 = class extends e$2 {
	constructor(e) {
		super(e), this.didSend = !1, this.done = !1;
	}
};
var S = class {
	constructor(e, t, s, i, r) {
		this._level = e, this._scale = t, this._indexOptions = s, this._clusterRadius = i, this._store = r, this._cells = /* @__PURE__ */ new Map(), this._handledChunks = /* @__PURE__ */ new Set(), this._statistics = /* @__PURE__ */ new Map(), this._clusters = /* @__PURE__ */ new Map();
	}
	destroy() {
		this._clearClusters();
	}
	_clearClusters() {
		for (const e of this._clusters.values()) this._store.releaseDisplayIdForObjectId(e.inner.id);
		this._clusters.clear();
	}
	*aggregatesWorldSpace() {
		for (const e of this._clusters.values()) yield new o$6(e.inner.getCentroid(null), e.inner.getAttributes(), null, e.inner.id, e.displayId);
	}
	clusters() {
		return this._clusters.values();
	}
	updateChunks(e, t) {
		let s = !1;
		for (const a of e) {
			const e = a.queryInfo;
			if ("tileId" in e) {
				if (new e$6(e.tileId).level !== this._level) continue;
			}
			if (!this._handledChunks.has(a.normalizedChunkId)) {
				this._handledChunks.add(a.normalizedChunkId), s = !0;
				a.getAggregateIndex({
					...this._indexOptions,
					scale: this._scale
				}).put(this._cells);
			}
		}
		const i = {
			xMin: Infinity,
			yMin: Infinity,
			xMax: -Infinity,
			yMax: -Infinity
		}, r = r$9(this._indexOptions.spatialReference, this._scale), n = this._indexOptions.cellSize;
		for (const { subscription: a } of t) {
			const e = a.tile.bounds, t = Math.floor(e[0] * r / n), s = Math.floor(e[1] * r / n), l = Math.ceil(e[2] * r / n), o = Math.ceil(e[3] * r / n);
			i.xMin = Math.min(i.xMin, t), i.yMin = Math.min(i.yMin, s), i.xMax = Math.max(i.xMax, l), i.yMax = Math.max(i.yMax, o);
		}
		return null != this._lastCellBounds && i.xMin === this._lastCellBounds.xMin && i.yMin === this._lastCellBounds.yMin && i.yMin === this._lastCellBounds.yMin && i.yMax === this._lastCellBounds.yMax || (s = !0, this._lastCellBounds = i), s && this._clusterCells(i), s;
	}
	async updateStatistics(e) {
		let t = !1;
		for (const s of this._clusters.values()) s.inner.count > 1 && (t = this._updateAggregateStatistics(this._statistics, s.inner) || t);
		if (t) {
			const t = Array.from(this._statistics.entries()).map(([e, t]) => ({
				fieldName: e,
				minValue: t.minValue,
				maxValue: t.maxValue
			}));
			await e.container.updateStatistics(this._level, t);
		}
	}
	createAggregateFeatures(e, i) {
		const n = e.subscription, a = [], l = n.tile.transform;
		for (const o of this._clusters.values()) {
			let e = o.inner.getCentroidX(l);
			const i = o.inner.getCentroidY(l), d = n.tile.lod, u = d.wrap ? d.worldSize[0] : null, c = 1 === o.inner.count ? o.inner.firstObjectId : o.inner.id, h = o.displayId;
			if (null != u) if (1 === u) {
				const l = new o$6(new s$12([], [e, i]), o.inner.getAttributes(), null, c, h);
				l.geometry.coords[0] -= 512, a.push(l);
				const u = new o$6(new s$12([], [e, i]), o.inner.getAttributes(), null, c, h);
				u.geometry.coords[0] += 512, a.push(u);
			} else e > 768 ? e -= u * 512 : e < -256 && (e += u * 512);
			if (e < 512 + m$4 && e >= -m$4 && i < 512 + m$4 && i >= -m$4) {
				const n = new o$6(new s$12([], [e, i]), o.inner.getAttributes(), null, c, h);
				a.push(n);
			}
		}
		return u$9.fromOptimizedFeatures(a, i, n.tile.transform);
	}
	_clusterCells(e) {
		let t = Array.from(this._cells.values());
		t = t.sort((e, t) => t.count - e.count);
		const s = [];
		for (const a of this._clusters.values()) s.push(a.inner.id);
		this._clusters.clear();
		const i = this._clusterRadius * (1 / r$9(this._indexOptions.spatialReference, this._scale)), r = 1 + this._clusterRadius / this._indexOptions.cellSize, n = /* @__PURE__ */ new Set();
		for (const l of t) {
			if (n.has(l.id)) continue;
			if (l.gridX < e.xMin || l.gridX > e.xMax || l.gridY < e.yMin || l.gridY > e.yMax) continue;
			const t = this._store.createDisplayIdForObjectId(l.id), s = new s$3(l.clone(), t);
			n.add(l.id), this._clusters.set(l.id, s);
			const o = l.centroidXWorld, d = l.centroidYWorld;
			for (let e = l.gridY - r; e <= l.gridY + r; e++) for (let t = l.gridX - r; t <= l.gridX + r; t++) {
				if (e === l.gridY && t === l.gridX) continue;
				const r = this._cells.get(l$10.createId(t, e));
				if (!r || n.has(r.id)) continue;
				const u = Math.abs(r.centroidXWorld - o), c = Math.abs(r.centroidYWorld - d);
				u < i && c < i && (s.inner.merge(r), n.add(r.id));
			}
		}
		for (const a of s) this._store.releaseDisplayIdForObjectId(a);
	}
	_updateAggregateStatistics(e, t) {
		let s = !1;
		for (const i of t.statistics.values()) {
			if ("esriFieldTypeString" === i.field.type) continue;
			const t = i.value, r = i.field, n = e.get(r.name);
			if (n) {
				const { minValue: e, maxValue: i } = n, r = Math.min(n.minValue, t), a = Math.max(n.maxValue, t);
				e === r && i === a || (n.minValue = r, n.maxValue = a, s = !0);
				continue;
			}
			e.set(r.name, {
				minValue: t,
				maxValue: t
			}), s = !0;
		}
		return s;
	}
};
var x$1 = class x$1 extends i$7 {
	static async create(e, t, s, i, r, a, l, o) {
		const d = s.metadata.outSpatialReference, u = new l$11({ spatialReference: d }), c = {
			type: "grid",
			fields: await Promise.all(t.fields.map(async (e) => s$4.create(u, e))),
			spatialReference: d,
			featureFilter: t.featureFilter ? await p$1.create({
				geometryType: s.metadata.geometryType,
				hasM: !1,
				hasZ: !1,
				timeInfo: s.metadata.timeInfo,
				fieldsIndex: s.metadata.fieldsIndex,
				spatialReference: d,
				filterJSON: t.featureFilter
			}) : null,
			cellSize: t.clusterRadius / 4,
			arcadeContextInfo: r,
			sqlOptions: a
		};
		return new x$1(e, t.clusterRadius, c, t.fields, s, i, a, l, o);
	}
	constructor(e, t, s, r, n, a, l, o, d) {
		super(n, a, s.spatialReference, s.fields, l), this._connection = e, this._clusterRadius = t, this._indexOptions = s, this._requiredLevel = o, this._requiredScale = d, this._cellsPerScale = /* @__PURE__ */ new Map(), this._metadata = i$18.createFeature({
			geometryType: "esriGeometryPoint",
			featureIdInfo: {
				type: "object-id",
				fieldName: "aggregateId"
			},
			fieldsIndex: new _$4([
				...r,
				...this._source.metadata.fieldsIndex.fields,
				{
					name: "aggregateId",
					alias: "aggregateId",
					type: "esriFieldTypeOID"
				}
			]).toJSON(),
			globalIdField: null,
			spatialReference: n.metadata.spatialReference,
			outSpatialReference: n.metadata.outSpatialReference,
			subtypeField: null,
			subtypes: null,
			timeInfo: null,
			timeReferenceUnknownClient: null,
			dateFieldsTimeZone: null,
			typeIdField: null,
			types: null
		});
	}
	get enablePixelBuffering() {
		return !1;
	}
	invalidate() {
		super.invalidate();
		for (const e of this._cellsPerScale.values()) e.destroy();
		this._cellsPerScale.clear();
	}
	updateRequiredLevel(e, t) {
		this._requiredLevel = e, this._requiredScale = t;
	}
	createState(e) {
		return new y$3(e);
	}
	async *applyOverrideUpdate(e) {
		for (const t of this._cellsPerScale.values()) t.destroy();
		this._cellsPerScale.clear();
		for (const t of this._sendStates.values()) t.done = !1;
	}
	displayMap(e, t, s) {
		const i = new Map(e.map((e) => [t(e), e])), r = [], n = this._getClusterState(this._requiredLevel, this._requiredScale);
		for (const a of n.clusters()) {
			const e = i.get(a.inner.id);
			if (null != e) {
				const t = s(a.displayId, e, a.inner.id);
				r.push(t), i.delete(a.inner.id);
				continue;
			}
			if (1 === a.inner.count) {
				const { firstObjectId: e } = a.inner, t = e ? i.get(e) : null;
				if (null != t) {
					const n = s(a.displayId, t, e);
					r.push(n), i.delete(e);
				}
			}
		}
		return r;
	}
	getDisplayFeatures(t) {
		const s = new Set(t), i = /* @__PURE__ */ new Set(), r = [], n = [], a = this._getClusterState(this._requiredLevel, this._requiredScale);
		for (const l of a.aggregatesWorldSpace()) if (s.has(l.displayId) && !i.has(l.displayId)) {
			const t = D(l, this._metadata.geometryType, !1, !1);
			if (i.add(l.displayId), 1 === t.attributes.cluster_count) {
				r.push({
					...t,
					displayId: l.displayId
				});
				continue;
			}
			n.push({
				...t,
				displayId: l.displayId
			});
		}
		return {
			features: r,
			aggregates: n,
			tracks: []
		};
	}
	getFeatureObjectIdsForAggregate(e) {
		const t = this._getClusterState(this._requiredLevel, this._requiredScale);
		for (const s of t.clusters()) if (s.inner.id === e) return Array.from(s.inner.containedObjectIds);
		return [];
	}
	async *updateChunks() {
		const e = this._source.chunks();
		if (!e.length) return;
		const t = this._getClusterState(this._requiredLevel, this._requiredScale), s = Array.from(this._sendStates.values()).filter((e) => e.subscription.tile.level === this._requiredLevel);
		if (t.updateChunks(e, s) || !this._source.updateTracking.updating) for (const n of s) n.subscription.tile.level === this._requiredLevel && (n.didSend = !1, n.done = !1);
		const i = Array.from(this._sendStates.values()).filter((e) => e.done).map((e) => e.subscription.tile.key), r = new Set(i);
		for (const n of this._sendStates.values()) {
			if (this._source.updateTracking.updating) {
				if (i.some((e) => e.containsChild(n.subscription.tile.key))) continue;
				if (n.subscription.tile.key.getChildKeys().every((e) => r.has(e))) continue;
			}
			n.didSend || n.subscription.tile.level !== this._requiredLevel || (n.didSend = !0, yield* this._update(n, t, this._source));
		}
		await t.updateStatistics(this._connection);
	}
	forEachAggregateWorldSpace(e) {
		if (null == this._requiredLevel || null == this._requiredScale) return;
		const t = this._getClusterState(this._requiredLevel, this._requiredScale);
		for (const s of t.aggregatesWorldSpace()) e(s);
	}
	_getClusterState(e, t) {
		if (null == e || null == t) throw new Error("InternalError: Level and scale must be defined");
		let s = this._cellsPerScale.get(t);
		return s || (s = new S(e, t, this._indexOptions, this._clusterRadius, this._attributeStore), this._cellsPerScale.set(t, s)), s;
	}
	async *_update(e, t, s) {
		if (e.done) return;
		const i = t.createAggregateFeatures(e, this._metadata);
		this.events.emit("changed"), e.done = !s.updateTracking.updating;
		const r = i.getCursor(), n = e.subscription.tile.createArcadeEvaluationOptions(this._indexOptions.arcadeContextInfo);
		for (; r.next();) this._attributeStore.setAttributeData(r.getDisplayId(), r, n, this._sqlOptions);
		yield new e$1(e.subscription, i, !0, e.done, {});
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/processor/FeatureUpdateStrategy.js
var d$4 = class extends e$2 {};
var i$5 = class extends t$5 {
	constructor(e, t, s, r) {
		super(e, t, r), this._arcadeContextInfo = s, this.handledChunks = /* @__PURE__ */ new Set(), this.handledChunksForIdCreation = /* @__PURE__ */ new Set(), this.handledChunksForAttributeData = /* @__PURE__ */ new Set(), this._streamLayerDeferredObjectIdsToRemove = [];
	}
	destroy() {
		super.destroy();
		for (const e of this._source.chunks()) this._cleanupChunkIds(e);
	}
	invalidateAttributeData(e) {
		this.handledChunksForAttributeData.clear(), this._arcadeContextInfo = e, null != this._evalOptions && (this._evalOptions = e$7(this._evalOptions.$view.scale, e));
	}
	onSubscribe(e) {
		super.onSubscribe(e), this._evalOptions = e.tile.createArcadeEvaluationOptions(this._arcadeContextInfo);
	}
	createState(e) {
		return new d$4(e);
	}
	get aggregateQueryEngine() {
		return null;
	}
	displayMap(e, t, s) {
		const r = new Map(e.map((e) => [t(e), e])), a = [];
		for (const o of this._source.chunks()) {
			const e = o.reader.getCursor();
			for (; e.next();) {
				const t = e.getObjectId(), o = e.getDisplayId(), n = r.get(t);
				if (null != n) {
					const e = s(o, n, t);
					a.push(e), r.delete(t);
				}
			}
		}
		return a;
	}
	getDisplayFeatures(e) {
		const t = new Set(e), s = /* @__PURE__ */ new Set(), r = [];
		for (const a of this._source.chunks()) {
			const e = a.reader.getCursor();
			for (; e.next();) {
				const a = e.getObjectId(), o = e.getDisplayId();
				t.has(o) && !s.has(a) && (r.push({
					...e.readLegacyFeatureWorldSpace(),
					displayId: o
				}), s.add(a));
			}
		}
		return {
			features: r,
			aggregates: [],
			tracks: []
		};
	}
	async *applyOverrideUpdate(e) {
		const t = [];
		for (const s of e.modified.values()) {
			const e = this._attributeStore.createDisplayIdForObjectId(s.objectId);
			s.displayId = e, t.push(e);
		}
		const r = u$9.fromOptimizedFeatures(Array.from(e.modified.values()), this._source.metadata).getCursor();
		for (; r.next();) this._attributeStore.setAttributeData(r.getDisplayId(), r, this._evalOptions, this._sqlOptions);
		const o = [];
		for (const s of e.removed) {
			const e = this._attributeStore.getDisplayIdForObjectId(s);
			null != e && o.push(e);
		}
		has("esri-2d-update-debug") && console.debug("FeatureUpdateStrategy.applyLocalEdit", {
			message: e,
			modifiedDisplayIds: t,
			removedDisplayIds: o
		});
		const d = n$8.fromFeatures(Array.from(e.modified.values()), this._source.metadata);
		this.handledChunks.add(d.chunkId), this.handledChunksForAttributeData.add(d.chunkId), this.handledChunksForIdCreation.add(d.chunkId);
		for (const a of this._sendStates.values()) a.handledChunks.add(d.chunkId), yield new i$6(a.subscription, null, t, !1, d.queryInfo);
		for (const a of this._sendStates.values()) {
			const e = d.getTileReader(a.subscription.tile);
			yield new i$6(a.subscription, e, o, !1, d.queryInfo);
		}
		for (const s of e.removed) this._attributeStore.releaseDisplayIdForObjectId(s);
	}
	async *updateChunks() {
		if (this._source.chunks().length) {
			this._updateAttributeData();
			for (const e of this._sendStates.values()) yield* this._update(e);
		}
	}
	removeChunks(e) {
		for (const t of e) this.handledChunks.delete(t.chunkId), this.handledChunksForAttributeData.delete(t.chunkId), this._cleanupChunkIds(t);
	}
	afterUpdateChunks() {
		for (const e of this._streamLayerDeferredObjectIdsToRemove) this._attributeStore.releaseDisplayIdForObjectId(e);
		this._streamLayerDeferredObjectIdsToRemove = [];
	}
	_cleanupChunkIds(e) {
		if (this.handledChunksForIdCreation.has(e.chunkId)) {
			const t = e.reader.getCursor();
			for (; t.next();) {
				const e = t.getObjectId();
				this._source.isStream ? this._streamLayerDeferredObjectIdsToRemove.push(e) : this._attributeStore.releaseDisplayIdForObjectId(e);
			}
			this.handledChunksForIdCreation.delete(e.chunkId);
		}
	}
	_updateAttributeData() {
		for (const e of this._source.chunks()) {
			const { chunkId: t, reader: s } = e;
			if (!this.handledChunksForIdCreation.has(t)) {
				this.handledChunksForIdCreation.add(t);
				const e = s.getCursor();
				for (; e.next();) {
					const t = this._attributeStore.createDisplayIdForObjectId(e.getObjectId());
					e.setDisplayId(t);
				}
			}
		}
		for (const e of this._source.chunks()) if (this._attributeStore.referencesScale() || !this.handledChunksForAttributeData.has(e.chunkId)) {
			this.handledChunksForAttributeData.add(e.chunkId);
			const t = e.reader.getCursor();
			for (; t.next();) {
				const e = t.getDisplayId();
				this._attributeStore.setAttributeData(e, t, this._evalOptions, this._sqlOptions);
			}
		}
	}
	*_update(e) {
		const { subscription: t, handledChunks: s } = e;
		for (const a of this._source.chunks()) {
			const { chunkId: o } = a;
			if (s.has(o) || !this.handledChunksForIdCreation.has(o) || !this.handledChunksForAttributeData.has(o)) continue;
			s.add(o);
			const n = a.getTileReader(t.tile);
			n && (yield new e$1(e.subscription, n, !1, a.end, a.queryInfo));
		}
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/processor/TrackStrategy.js
var C;
var G = () => n$4.getLogger("esri.views.2d.layers.features.processor.TrackStrategy"), P = 32;
var W = class {
	constructor(t, e, s, r, i) {
		this.chunkIndex = t, this.featureIndex = e, this.objectId = s, this.displayId = r, this.time = i;
	}
};
var z = class {
	static getOid(t) {
		return t$12 + t;
	}
	constructor(t, e, s, r, i, a, o, n) {
		this._schema = t, this.trackId = e, this.objectId = s, this.displayId = r, this._fields = i, this._spatialReference = a, this._metadata = o, this._isStream = n, this._maxDisplayDuration = this._schema.maxDisplayDuration > 0 ? this._schema.maxDisplayDuration : Infinity, this._maxDisplayObservationsPerTrack = this._schema.maxDisplayObservationsPerTrack >= 1 ? this._schema.maxDisplayObservationsPerTrack : Infinity, this._observationRecords = [], this._nextObservationRecords = [], this._trackLinePath = [], this._bounds = [], this._trackLineGeometry = new s$12();
	}
	get _trackLineAttributes() {
		const t = {
			...this._latestObservationFeature?.attributes,
			aggregateId: this.objectId,
			[r$7]: 0
		};
		if (null != this._statistics) for (const e of this._statistics.values()) t[e.field.name] = e.value;
		return t;
	}
	get _startTimeField() {
		return this._metadata.timeInfo?.startTimeField;
	}
	get length() {
		return this._observationRecords.length;
	}
	*observations() {
		yield* this._observationRecords;
	}
	*previousObservations() {
		for (let t = 0; t < this._observationRecords.length - 1; t++) yield this._observationRecords[t];
	}
	get latestObservationFeature() {
		return this._latestObservationFeature;
	}
	get latestObservationRecord() {
		return this._latestObservationRecord;
	}
	stageObservation(t, e) {
		this._nextObservationRecords.push(new W(t, e.getIndex(), e.getObjectId(), e.getDisplayId(), null != this._startTimeField ? e.readAttributeAsTimestamp(this._startTimeField) : null));
	}
	commitObservations(t, e, r) {
		const i = new Set(this._nextObservationRecords.map((t) => t.objectId)), a = this._observationRecords.filter((t) => !i.has(t.objectId)).map((t) => t.objectId);
		let o, n;
		switch (this._observationRecords = [], this._trackLinePath = [], this._isStream || null == this._startTimeField || this._nextObservationRecords.sort((t, e) => {
			const s = t.time, r = e.time;
			return null != s && null != r ? s - r : 0;
		}), this._schema.timeField) {
			case "startTimeField":
				o = this._metadata.timeInfo?.startTimeField;
				break;
			case "endTimeField":
				o = this._metadata.timeInfo?.endTimeField;
				break;
			case "timeReceived": o = this._isStream ? i$15 : null;
		}
		n = this._isStream ? r?.end ?? Date.now() : r?.end ?? -Infinity;
		const l = e.map((t) => t.reader.getCursor());
		let c;
		for (let d = this._nextObservationRecords.length - 1; d >= 0 && !(this._observationRecords.length >= this._maxDisplayObservationsPerTrack); d--) {
			const t = this._nextObservationRecords[d], e = l[t.chunkIndex];
			n$5(e), e.setIndex(t.featureIndex);
			const r = null != o ? e.readAttributeAsTimestamp(o) : null;
			(null != r ? n - r : 0) >= this._maxDisplayDuration || (this._commitObservation(t, e), c ??= t);
		}
		if (null != c) {
			const { chunkIndex: e, featureIndex: r } = c, i = `${c.objectId}.latest`, o = t.createDisplayIdForObjectId(i), n = l[e];
			n$5(n), n.setIndex(r);
			const d = new o$6(n.readGeometryWorldSpace(), {
				...n.readAttributes(),
				[r$7]: 1
			}, null, i, o);
			this._latestObservationFeature && a.push(this._latestObservationFeature.objectId), this._latestObservationFeature = d, this._latestObservationRecord = c;
		} else this._latestObservationFeature = null;
		return this._trackLineGeometry = q$1(this._trackLinePath, this._spatialReference), this._bounds = N(this._trackLineGeometry), this._nextObservationRecords = [], a;
	}
	updateStatistics(t, e) {
		this._statistics = i$17.create(this._fields);
		const r = t.map((t) => t.reader.getCursor());
		for (const { chunkIndex: i, featureIndex: a } of this._observationRecords) {
			const t = r[i];
			n$5(t), t.setIndex(a), this._statistics.insert(t, e);
		}
	}
	overlapsTile(t) {
		for (const e of this._bounds) if (A(e, t.bounds, P)) return !0;
		return !1;
	}
	getLatestObservationFeatureForTile(t) {
		if (null == this._latestObservationFeature) return null;
		const { objectId: e, displayId: s, geometry: r, attributes: i } = this._latestObservationFeature, a = rt(r, this._metadata.geometryType, t.subscription.tile.transform) ?? new s$12(), o = o$5(Infinity, Infinity, -Infinity, -Infinity);
		E(a, (t, e) => f$6(o, [t, e]));
		if (!z$1(o, o$5(0, 0, 512, 512))) return null;
		return new o$6(a, i, null, e, s);
	}
	getTrackLineFeatureForTile(t) {
		return new o$6(rt(this._trackLineGeometry, "esriGeometryPolyline", t.subscription.tile.transform) ?? new s$12(), this._trackLineAttributes, null, this.objectId, this.displayId);
	}
	getTrackLineOptimizedFeature() {
		return new o$6(this._trackLineGeometry, this._trackLineAttributes, null, this.objectId, this.displayId);
	}
	getTrackLineDisplayFeature() {
		const { _trackLineGeometry: t, _trackLineAttributes: e, displayId: s } = this;
		return {
			geometry: J(t, "esriGeometryPolyline", !1, !1),
			attributes: e,
			displayId: s
		};
	}
	_commitObservation(t, e) {
		const s = e.readCentroidWorldSpace();
		let r = s?.coords[0], i = s?.coords[1];
		s ?? (r = e.readXWorldSpace(), i = e.readYWorldSpace()), null != r && null != i && (this._observationRecords.unshift(t), this._trackLinePath.unshift([r, i]));
	}
};
var M = class extends e$2 {
	constructor(t) {
		super(t), this.done = !1;
	}
};
var U = class U extends i$7 {
	static async create(t, e, s, a, o) {
		const n = e.metadata.outSpatialReference, l = new l$11({ spatialReference: n }), c = await Promise.all(t.fields.map(async (t) => s$4.create(l, t))), d = t.featureFilter ? await p$1.create({
			geometryType: e.metadata.geometryType,
			hasM: !1,
			hasZ: !1,
			timeInfo: e.metadata.timeInfo,
			fieldsIndex: e.metadata.fieldsIndex,
			spatialReference: n,
			filterJSON: t.featureFilter
		}) : null;
		return n.isWrappable || s$13() || await Promise.all([
			import("./apiConverter-BiH9EVj2.js").then((n) => n.s),
			import("./jsonConverter-C7YfydKv.js").then((n) => n.s),
			i$16()
		]).then(([t, e, s]) => {
			C = {
				fromGeometryToGXGeometry: e.fromGeometryToGXGeometry,
				toGeometry: e.toGeometry,
				fromSpatialReference: t.fromSpatialReference
			};
		}), new U(t, e, s, n, c, d, a, o);
	}
	constructor(t, e, s, r, i, a, o, n) {
		super(e, s, r, i, n), this._schema = t, this._featureFilter = a, this._arcadeContextInfo = o, this._tracks = /* @__PURE__ */ new Map(), this._handledChunks = /* @__PURE__ */ new Set(), this._metadata = e.metadata.weakCloneWithAdditionalFields([{
			name: r$7,
			alias: "trackPart",
			type: "esriFieldTypeSmallInteger"
		}]), this._trackLineMetadata = i$18.createFeature({
			geometryType: "esriGeometryPolyline",
			featureIdInfo: {
				type: "object-id",
				fieldName: "aggregateId"
			},
			fieldsIndex: {
				fields: [
					...this._source.metadata.fieldsIndex.fields,
					...this.aggregateFields,
					{
						name: r$7,
						alias: "trackPart",
						type: "esriFieldTypeSmallInteger"
					},
					{
						name: "aggregateId",
						alias: "aggregateId",
						type: "esriFieldTypeOID"
					}
				],
				timeZoneByFieldName: null
			},
			globalIdField: null,
			spatialReference: e.metadata.spatialReference,
			outSpatialReference: e.metadata.outSpatialReference,
			subtypeField: null,
			subtypes: null,
			timeInfo: e.metadata.timeInfo,
			timeReferenceUnknownClient: null,
			dateFieldsTimeZone: null,
			typeIdField: null,
			types: null
		});
	}
	destroy() {
		super.destroy(), this._clear();
	}
	get _isStream() {
		return this._source.isStream;
	}
	get enablePixelBuffering() {
		return !0;
	}
	get isAggregate() {
		return !1;
	}
	requiresInvalidation() {
		return !0;
	}
	invalidate() {
		super.invalidate(), this._clear();
	}
	createState(t) {
		return new M(t);
	}
	async *applyOverrideUpdate(t) {
		G().error("Applying override to tracks is not supported");
	}
	displayMap(t, e, s) {
		const r = new Map(t.map((t) => [e(t), t])), i = [];
		for (const a of this._tracks.values()) {
			const t = r.get(a.objectId);
			if (null != t) {
				const e = s(a.displayId, t, a.objectId);
				i.push(e), r.delete(a.objectId);
				continue;
			}
			const e = a.latestObservationFeature;
			if (e?.objectId) {
				const t = r.get(e.objectId);
				if (null != t) {
					const a = s(e.displayId, t, e.objectId);
					i.push(a), r.delete(e.objectId);
					continue;
				}
			}
			for (const o of a.observations()) {
				const t = r.get(o.objectId);
				if (null != t) {
					const e = s(o.displayId, t, o.objectId);
					i.push(e), r.delete(o.objectId);
				}
			}
		}
		return i;
	}
	getDisplayFeatures(t) {
		const e = new Set(t), s = [], r = [], i = this._source.chunks().map((t) => t.reader.getCursor());
		for (const a of this._tracks.values()) {
			if (e.has(a.displayId) && r.push(a.getTrackLineDisplayFeature()), null != a.latestObservationFeature && e.has(a.latestObservationFeature.displayId)) {
				const { displayId: t, chunkIndex: e, featureIndex: r } = a.latestObservationRecord, o = i[e];
				o.setIndex(r), s.push({
					displayId: t,
					...o.readLegacyFeatureWorldSpace()
				});
			}
			for (const { displayId: t, chunkIndex: r, featureIndex: o } of a.observations()) if (e.has(t)) {
				const e = i[r];
				e.setIndex(o), s.push({
					displayId: t,
					...e.readLegacyFeatureWorldSpace()
				});
			}
		}
		return {
			features: s,
			aggregates: [],
			tracks: r
		};
	}
	getFeatureObjectIdsForAggregate(t) {
		for (const e of this._tracks.values()) if (e.objectId === t) return Array.from(e.observations(), (t) => t.objectId);
		return [];
	}
	async *updateChunks() {
		0 === this._handledChunks.size && this._rebuildTracks();
		for (const t of this._sendStates.values()) yield* this._update(t);
	}
	forEachAggregateWorldSpace(t) {
		for (const e of this._tracks.values()) t(e.getTrackLineOptimizedFeature());
	}
	_clear() {
		for (const t of this._source.chunks()) if (this._handledChunks.has(t.chunkId)) {
			const e = t.reader.getCursor();
			for (; e.next();) {
				const t = e.getObjectId();
				this._attributeStore.releaseDisplayIdForObjectId(t);
			}
		}
		this._handledChunks.clear();
		for (const t of this._tracks.values()) this._removeTrack(t);
		this._tracks.clear();
	}
	_rebuildTracks() {
		const t = this._source.chunks();
		if (!t.length) return;
		const e = this._metadata.timeInfo?.trackIdField;
		if (null == e) return;
		const s = /* @__PURE__ */ new Set();
		for (let r = 0; r < t.length; r++) {
			const i = t[r];
			if (this._handledChunks.has(i.chunkId)) continue;
			this._handledChunks.add(i.chunkId);
			const a = i.reader.getCursor();
			for (; a.next();) {
				const t = a.getObjectId();
				a.setDisplayId(this._attributeStore.createDisplayIdForObjectId(t));
				const i = a.readAttribute(e);
				if (null != i && null != t && (null === this._featureFilter || this._featureFilter.check(a, this._sqlOptions))) {
					if (!this._tracks.has(i)) {
						const t = z.getOid(i), e = this._attributeStore.createDisplayIdForObjectId(t), s = new z(this._schema, i, t, e, this.aggregateFields, this.spatialReference, this._source.metadata, this._isStream);
						this._tracks.set(i, s);
					}
					this._tracks.get(i).stageObservation(r, a), s.add(i);
				}
			}
		}
		for (const r of this._tracks.values()) if (s.has(r.trackId)) {
			const e = r.commitObservations(this._attributeStore, t, this._featureFilter?.timeExtent);
			for (const t of e) this._attributeStore.releaseDisplayIdForObjectId(t);
			r.updateStatistics(t, e$7(1, this._arcadeContextInfo));
		} else this._removeTrack(r);
	}
	_removeTrack(t) {
		this._tracks.delete(t.trackId), this._attributeStore.releaseDisplayIdForObjectId(t.objectId), null != t.latestObservationFeature && this._attributeStore.releaseDisplayIdForObjectId(t.latestObservationFeature.objectId);
	}
	*_update(e) {
		if (e.done) return;
		e.done = !this._source.updateTracking.updating;
		const s = [], r = [];
		for (const t of this._tracks.values()) if (t.length > 0) {
			if (this._schema.showLatestObservation) {
				const r = t.getLatestObservationFeatureForTile(e);
				null != r && s.push(r);
			}
			this._schema.showTrackLine && t.overlapsTile(e.subscription.tile) && r.push(t.getTrackLineFeatureForTile(e));
		}
		const i = u$9.fromOptimizedFeatures(s, this._metadata, e.subscription.tile.transform), a = u$9.fromOptimizedFeatures(r, this._trackLineMetadata, e.subscription.tile.transform);
		let o = [];
		if (this._schema.showPreviousObservations) {
			const s = this._source.chunks().map(() => []);
			for (const t of this._tracks.values()) for (const { chunkIndex: e, featureIndex: r } of t.previousObservations()) s[e].push(r);
			o = this._source.chunks().map((t, r) => {
				const i = t.getTileReader(e.subscription.tile);
				if (null == i) return null;
				const a = d$7.from(i, s[r]);
				return a.setProcessorAttributes({ [r$7]: 2 }), "esriGeometryPoint" !== a.geometryType && null != a.getInTransform() || a.setTransformForDisplay(e.subscription.tile.transform), a;
			}).filter(N$1);
		}
		this.events.emit("changed");
		const n = e.subscription.tile.createArcadeEvaluationOptions(this._arcadeContextInfo), l = a.getCursor();
		for (; l.next();) this._attributeStore.setAttributeData(l.getDisplayId(), l, n, this._sqlOptions);
		for (const t of o) {
			const e = t.getCursor();
			for (; e.next();) this._attributeStore.setAttributeData(e.getDisplayId(), e, n, this._sqlOptions);
		}
		const c = i.getCursor();
		for (; c.next();) this._attributeStore.setAttributeData(c.getDisplayId(), c, n, this._sqlOptions);
		yield new e$1(e.subscription, a, !1, !1, {});
		for (const t of o) yield new e$1(e.subscription, t, !1, !1, {});
		yield new e$1(e.subscription, i, !1, e.done, {});
	}
};
function q$1(t, e) {
	if (t.length < 2) return S$2({ paths: [t] }, !1, !1);
	if (e.isWrappable) {
		let s = !1;
		for (let r = 1; r < t.length; r++) {
			const i = t[r][0], a = L$1(i, t[r - 1][0], e);
			i !== a && (t[r][0] = a, s = !0);
		}
		if (s) {
			const s = a$7({
				paths: [t],
				spatialReference: e
			});
			if (null != s) return S$2({ paths: s.paths }, !1, !1);
		}
		return S$2({ paths: [t] }, !1, !1);
	}
	const s = C.fromGeometryToGXGeometry({
		hasM: !1,
		hasZ: !1,
		paths: [t]
	}), r = C.fromSpatialReference(e);
	if (null != r) {
		const t = u$8(s, 1e6, r, e$9.geodesic);
		if (null != t) {
			const e = C.toGeometry(t, r);
			if (null != e && "paths" in e) return S$2({ paths: e.paths }, !1, !1);
		}
	}
	return S$2({ paths: [t] }, !1, !1);
}
function E(t, e) {
	const s = 2, { coords: r, lengths: i } = t;
	if (!i.length) return void e(r[0], r[1]);
	let a = 0;
	for (let o = 0; o < i.length; o++) {
		const t = i[o];
		let n = 0, l = 0;
		for (let i = 0; i < t; i++) n += r[s * (i + a)], l += r[s * (i + a) + 1], e(n, l);
		a += t;
	}
}
function N(t) {
	const { lengths: e, coords: s } = t, r = 2;
	if (!e.length) return [N$2()];
	const i = [];
	let a = 0;
	for (let o = 0; o < e.length; o++) {
		const t = e[o], n = N$2();
		i.push(n);
		for (let e = 0; e < t; e++) {
			const t = s[r * (e + a)], i = s[r * (e + a) + 1];
			f$6(n, [t, i]);
		}
		a += t;
	}
	return i;
}
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/Processor.js
var j = class {
	constructor(e, t, s, r) {
		this._connection = e, this._source = t, this._requiredLevel = s, this._requiredScale = r, this._version = 1, this._registry = new x$2(), this._proxy = new s$16({
			fetch: (e, t) => this._connection.layerView.fetch(e, t),
			fetchDictionary: (e, t) => this._connection.layerView.fetchDictionary(e, t)
		}), this._attributeStore = new b$1({
			isLocal: !1,
			update: (e) => l$8(this._connection.container.updateAttributeView(e))
		});
	}
	destroy() {
		this._proxy.destroy(), this._strategy?.destroy(), this._attributeStore.destroy();
	}
	get hasClusters() {
		return "cluster" === this._schema?.mesh.strategy.type;
	}
	get aggregateQueryEngine() {
		return this._strategy?.aggregateQueryEngine;
	}
	get usedMemory() {
		let e = 0;
		return e += this._attributeStore.usedMemory, this._strategy && (e += this._strategy.usedMemory), e;
	}
	get version() {
		return this._version;
	}
	getDisplayFeatures(e) {
		return this._strategy ? this._strategy.getDisplayFeatures(e) : {
			features: [],
			aggregates: [],
			tracks: []
		};
	}
	getDisplayIds(e) {
		const t = {};
		return this._strategy ? (this._strategy.displayMap(e, (e) => e, (e, s, r) => {
			t[r] = e;
		}), t) : t;
	}
	getFeatureObjectIdsForAggregate(e) {
		return this._strategy ? this._strategy.getFeatureObjectIdsForAggregate(e) : [];
	}
	onSubscribe(e) {
		this._strategy?.onSubscribe(e);
	}
	onUnsubscribe(e) {
		this._strategy?.onUnsubscribe(e);
	}
	requiresInvalidation() {
		return this._strategy?.requiresInvalidation() ?? !1;
	}
	updateRequiredLevel(e, t) {
		this._strategy?.updateRequiredLevel(e, t), this._requiredLevel = e, this._requiredScale = t;
	}
	async update(e, s, r, i, o) {
		const n = e.processor, c = i$10(this._schema?.storage, n.storage), h = i$10(this._schema?.mesh.properties, n.mesh.properties), u = i$10(this._schema?.mesh.factory, n.mesh.factory), p = i$10(this._schema?.mesh.strategy, n.mesh.strategy), y = I(this._schema?.expressionProperties, n.expressionProperties), _ = y.some((e) => this._attributeStore.hasArcadeDependency(e)), l = y.some((e) => this._factory?.hasArcadeDependency(e) ?? !1), m = y.some((e) => this._strategy?.hasArcadeDependency(e)) || this._strategy?.isAggregate && _, f = l || m, b = h || u || p;
		if (!(c || b || l || _ || m) && !i) return !1;
		has("esri-2d-update-debug") && console.debug(`Version[${this._version}] SymbolProcessor.update`, {
			changes: y$6(this._schema, n),
			schema: n
		}), this._schema = n;
		const w = new l$11({
			fields: this._source.metadata.fieldsIndex,
			spatialReference: this._source.metadata.outSpatialReference
		}), j = { currentUser: n.mesh.properties.currentUser };
		if ((c || b || _) && (await this._attributeStore.update(n.storage, w, this._source.metadata, s), this._strategy?.invalidateAttributeData(v(n))), !i && !b && !f) return !1;
		(p || h || f) && await this._updateStrategy(n.mesh.strategy, o, v(n), j), this._updateSortKey(w, "sortKey" in n.mesh.properties ? n.mesh.properties.sortKey : null);
		const x = "dictionary" === n.mesh.factory.symbology.type ? n.mesh.factory.symbology.fieldMap : null, M = new s$5(w, this._proxy, r, this._registry, x);
		return (u || "dictionary" === n.mesh.factory.symbology.type) && (this._factory = await s$6.create(M, n.mesh.factory)), this._version = s, !0;
	}
	async applyOverrideUpdate(e, t) {
		if (!this._strategy) return;
		const s = this._strategy.applyOverrideUpdate(e);
		for await (const i of s) try {
			await this._process(i, t);
		} catch (r) {}
	}
	async updateChunks(e) {
		await this._doUpdateChunks(e), this._strategy?.afterUpdateChunks();
	}
	async removeChunks(e) {
		this._strategy?.removeChunks(e), this._attributeStore.incrementDisplayIdGeneration();
	}
	updateHighlight({ highlights: e }) {
		if (!this._strategy || !this._strategy.hasSubscribers) return void this._attributeStore.setHighlight(e.map(({ objectId: e, highlightFlags: t }) => ({
			objectId: e,
			highlightFlags: t,
			displayId: -1
		})), e);
		const t = this._strategy.displayMap(e, ({ objectId: e }) => e, (e, { highlightFlags: t }, s) => ({
			objectId: s,
			displayId: e,
			highlightFlags: t
		}));
		this._attributeStore.setHighlight(t, e);
	}
	invalidate() {
		this._strategy && this._strategy.invalidate();
	}
	async _doUpdateChunks(e) {
		if (!this._strategy) return;
		const t = this._strategy.updateChunks(), r = [], i = /* @__PURE__ */ new Map();
		for await (const o of t) {
			let t = i.get(o.id);
			t ?? (t = new e$3({
				concurrency: 16,
				process: (t) => this._process(t, e)
			}), i.set(o.id, t));
			const a = t.push(o).catch((e) => m$6(e));
			r.push(a);
		}
		try {
			await Promise.all(r);
		} catch (a) {}
		has("esri-2d-update-debug") && console.log("SendUpdates"), this._attributeStore.sendUpdates(), has("esri-2d-update-debug") && console.log("SendUpdates.await");
	}
	async _updateStrategy(e, t, s, r) {
		switch (this._strategy?.destroy(), e.type) {
			case "feature":
				this._strategy = new i$5(this._source, this._attributeStore, s, r);
				break;
			case "binning":
				this._strategy = await y$4.create(e, this._source, this._attributeStore, s, r);
				break;
			case "cluster":
				this._strategy = await x$1.create(this._connection, e, this._source, this._attributeStore, s, r, this._requiredLevel, this._requiredScale);
				break;
			case "track": this._strategy = await U.create(e, this._source, this._attributeStore, s, r);
		}
		for (const i of t) this._strategy.onSubscribe(i);
	}
	async _updateSortKey(t, s) {
		if (this._sortInfo = u$2(this._sortInfo?.computed), null != s) {
			const e = s.byRenderer ? null : await t.createComputedField(s);
			this._sortInfo = {
				...s,
				computed: e
			};
		}
	}
	async _process(e, t) {
		const s = e.subscription;
		if (has("esri-2d-update-debug")) {
			const t = s.tile;
			console.debug(`Version[${this._version}] Tile[${t.key.id}, end=${e.end}] Processor._process`);
		}
		const i = { currentUser: this._schema?.mesh.properties.currentUser };
		await this._fetchResources(e, i), s$9(s.signal);
		const a = await this._write(e, s.tile.createArcadeEvaluationOptions(v(this._schema)), i, t), o = s.tile.tileInfoView.getLODInfoAt(s.tile.key);
		s$9(s.signal);
		const { message: n, transferList: c } = a.serialize(o), h = {
			objectIdMap: null,
			inner: e.createMessage(n, this._version, this._attributeStore.epoch)
		};
		if (this._schema?.mesh.properties.returnMeshObjectId) {
			h.objectIdMap = {};
			const t = e.reader?.getCursor();
			if (t) for (; t.next();) h.objectIdMap[t.getDisplayId()] = t.getObjectId();
		}
		if (s$9(s.signal), await this._connection.container.onMessage(h, {
			signal: s.signal,
			transferList: c
		}), this._attributeStore.sendUpdates(), has("esri-2d-update-debug")) {
			const t = s.tile;
			console.debug(`Version[${this._version}] Tile[${t.key.id}, end=${e.end}] Processor._process.await`);
		}
	}
	async _fetchResources(e, t) {
		await this._fetchMatcherResources(e), await this._fetchWriterResources(e, t);
	}
	async _fetchMatcherResources(e) {
		if (e.reader) return this._factory.enqueueMatcherRequests(this._proxy, e.reader);
	}
	async _fetchWriterResources(e, t) {
		if (!e.reader) return;
		const s = e.reader.getCursor(), r = e.subscription.tile.createArcadeEvaluationOptions(v(this._schema));
		for (; s.next();) this._factory.enqueueWriterRequests(this._proxy, s, r, t);
		await this._proxy.fetchEnqueuedResources();
	}
	async _write(e, t, s, a) {
		const o = e.subscription.tile, n = e.reader?.getCursor(), c = n?.getSize() ?? 0, h = o.tileInfoView.tileInfo.isWrappable, g = o.tileInfoView.spatialReference.isWGS84, p = new g$2(o.key, this._strategy.enablePixelBuffering, h, g, c);
		if (!n) return p;
		const y = x(o.transform, a), d = o.createArcadeEvaluationOptions(v(this._schema));
		let _ = 0;
		for (; n.next();) {
			++_ % 1e3 || (await T(0), s$9(e.subscription));
			const a = this._getSortKeyValue(n, t);
			p.entityStart(n.getDisplayId(), a);
			try {
				this._factory.write(p, this._proxy, n, d, s, o.level, y);
			} catch (l) {}
			p.entityEnd();
		}
		return p;
	}
	_getSortKeyValue(e, t) {
		if (!this._sortInfo) return 0;
		const { computed: s, order: r, byRenderer: i } = this._sortInfo, a = i ? this._factory.getSortKey(e, t) : s?.read(e, t);
		return null == a || isNaN(a) ? 0 : a * ("asc" === r ? -1 : 1);
	}
};
function I(e, t) {
	const s = [];
	return e?.timeExtent?.start === t.timeExtent?.start && e?.timeExtent?.end === t.timeExtent?.end || s.push("timeProperties"), s;
}
function v(e) {
	const { timeZone: t } = e?.mesh.properties ?? {}, { timeExtent: s } = e?.expressionProperties ?? {};
	return {
		timeZone: t,
		timeExtent: s
	};
}
function x(e, t) {
	const s = u$6(e), r = [];
	for (const i of t.rings) {
		const e = [];
		for (const t of i) e.push([i$13(s, t[0]), a$5(s, t[1])]);
		r.push(e);
	}
	return { rings: r };
}
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/FeatureStoreStatistics.js
var t$4 = class t$4 {
	static from(e) {
		let n = 0, r = 0, o = 0;
		return e.forEach((t) => {
			const e = t._readGeometry();
			e && (r += e.isPoint ? 1 : e.lengths.reduce((t, e) => t + e, 0), o += e.isPoint ? 1 : e.lengths.length, n += 1);
		}), new t$4(n, r, o);
	}
	constructor(t, e, n) {
		this.featureCount = t, this.vertexCount = e, this.ringCount = n;
	}
	toJSON() {
		return {
			featureCount: this.featureCount,
			ringCount: this.featureCount,
			vertexCount: this.featureCount
		};
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/sources/StreamMessenger.js
var i$4 = 2500;
var r$2 = class extends b {
	constructor(e) {
		super(), this._connection = e, this._enabledEventTypes = /* @__PURE__ */ new Set(), this._updateInfo = {
			websocket: 0,
			client: 0
		}, this._lastTime = performance.now(), this._queuedCommands = [], this.addHandles([l$7(() => this._strategy?.connectionStatus ?? "disconnected", (e) => {
			this._layerView.setProperty({
				propertyName: "pipelineConnectionStatus",
				value: e
			});
		}, { initial: !0 }), l$7(() => this._strategy?.errorString || null, (e) => this._layerView.setProperty({
			propertyName: "pipelineErrorString",
			value: e
		}), { initial: !0 })]);
	}
	destroy() {
		this._strategy = null, this.removeAllHandles();
	}
	get _layerView() {
		return this._connection.layerView;
	}
	set strategy(e) {
		this._strategy ?? this._resetUpdateInfo(performance.now());
		const t = "event-handles";
		this.removeHandles(t), null != e && (this.addHandles([
			e.events.on("data-received", (e) => this._onFeature(e)),
			e.events.on("message-received", (e) => this._onWebSocketMessage(e)),
			e.events.on("features-updated", (e) => this._onUpdate(e)),
			e.events.on("tick", () => this._onTick())
		], t), this._queuedCommands.forEach((t) => t(e)), this._queuedCommands = []), this._strategy = e;
	}
	updateCustomParameters(e) {
		null != e && this._callOrEnqueue((t) => t.updateCustomParameters(e));
	}
	sendMessageToSocket(e) {
		this._callOrEnqueue((t) => t.sendMessageToSocket(e));
	}
	sendMessageToClient(e) {
		this._callOrEnqueue((t) => t.sendMessageToClient(e));
	}
	enableEvent(e, t) {
		t ? this._enabledEventTypes.add(e) : this._enabledEventTypes.delete(e);
	}
	disconnect() {
		this._strategy?.disconnect();
	}
	connect() {
		this._strategy?.connect();
	}
	clear() {
		this._strategy?.clear();
	}
	_onWebSocketMessage(e) {
		this._enabledEventTypes.has("message-received") && this._layerView.emitEvent({
			name: "message-received",
			event: e
		});
	}
	_onFeature(e) {
		this._updateInfo.websocket++, this._enabledEventTypes.has("data-received") && this._layerView.emitEvent({
			name: "data-received",
			event: {
				attributes: e.attributes,
				centroid: e.centroid,
				geometry: e.geometry
			}
		});
	}
	_onUpdate(e) {
		this._updateInfo.client += e;
	}
	_onTick() {
		const e = performance.now(), t = e - this._lastTime;
		if (t > i$4) {
			const s = Math.round(this._updateInfo.client / (t / 1e3)), n = Math.round(this._updateInfo.websocket / (t / 1e3));
			this._resetUpdateInfo(e), this._layerView.emitEvent({
				name: "update-rate",
				event: {
					client: s,
					websocket: n
				}
			});
		}
	}
	_resetUpdateInfo(e) {
		this._lastTime = e, this._updateInfo.client = 0, this._updateInfo.websocket = 0;
	}
	_callOrEnqueue(e) {
		null != this._strategy ? e(this._strategy) : this._queuedCommands.push(e);
	}
};
__decorate([a$4()], r$2.prototype, "_strategy", void 0), r$2 = __decorate([c$3("esri.views.2d.layers.features.sources.StreamMessenger")], r$2);
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/sources/strategies/ALoadStrategy.js
var r$1 = class {
	constructor() {
		this._requiresInvalidation = !1;
	}
	get requiresInvalidation() {
		return this._requiresInvalidation;
	}
	requireInvalidation() {
		this._requiresInvalidation = !0;
	}
};
var e = class {
	constructor(r, e, o) {
		this._context = o, this._controller = new AbortController(), this.metadata = i$18.createFeature(r), this._schema = e;
	}
	destroy() {
		this._controller.abort(), this.store.destroy();
	}
	get store() {
		return this._context.store;
	}
	get _connection() {
		return this._context.connection;
	}
	get _options() {
		return { signal: this._controller.signal };
	}
	get _signal() {
		return this._controller.signal;
	}
	async applyOverride(t) {
		this._onOverride(), await this.store.applyOverride(t);
	}
	takeOverrideUpdate() {
		return this.store.takeOverrideUpdate();
	}
	unsafeSetQueryHistoricMoment(t) {
		throw new Error("InternalError: LoadStrategy does not support query info");
	}
	async queryByObjectId(t, r) {
		throw new Error("InternalError: LoadStrategy does not support fetching");
	}
	prepareCacheUpdate(t, r) {}
	applyCacheUpdate() {
		return null;
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/sources/strategies/support/snapshotUtils.js
async function o$2(e, n, o, a = {}) {
	const s = (await Promise.allSettled(o.map((r) => t$3(e, n, r, a)))).filter((r) => "rejected" === r.status).map((r) => r.reason);
	if (s.length) throw new r$5("featurelayer-query", "Encountered errors when downloading data", { errors: s });
}
async function t$3(r, o, t, a = {}) {
	const s = `${r.chunkPrefix ?? ""}${t.num}`, i = new t$13(await r.fetch(t.query, a, { chunkId: s }), t.query.inner.toJSON(), t.num, !1);
	i.chunkId = i.normalizedChunkId = s, s$9(a), o.insert(i);
}
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/sources/FeatureCache.js
var o$1 = 8e3;
var h$2 = class {
	constructor(h, a, c, n) {
		this.store = h, this.queryInfo = a, this._options = c, this._fetch = n, this._nextBatch = /* @__PURE__ */ new Set(), this._fetchFeatures = L(async () => {
			if (0 === this._nextBatch.size || this._options.signal?.aborted) return;
			const r = Array.from(this._nextBatch);
			this._nextBatch.clear(), r.length > o$1 && n$4.getLogger("esri.views.2d.layers.FeatureLayerView2D").warn(new r$5("highlight-too-many-features", `highlight is limited to ${o$1} features on large layers configured with a display filter to avoid performance issues`));
			const h = this.queryInfo.objectIdsQueryPageSize, a = Math.ceil(o$1 / h), c = Math.min(a, Math.ceil(r.length / h)), n = Array.from({ length: c }, (t, e) => {
				const s = e * h, i = Math.min(s + h, r.length);
				return {
					num: e,
					query: this.queryInfo.createObjectIdsQuery(r.slice(s, i))
				};
			});
			try {
				await o$2({
					chunkPrefix: "cache." + n$6(),
					fetch: this._fetch
				}, this.store, n, this._options);
			} catch (f) {}
		});
	}
	prepareCacheUpdate(t, e) {
		if (e) for (const r of e) this._nextBatch.delete(r);
		for (const r of t) this._nextBatch.add(r);
	}
	applyCacheUpdate() {
		return 0 === this._nextBatch.size || this._options.signal?.aborted ? null : this._fetchFeatures().catch(() => {});
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/support/FeatureSetReaderPBFHeader.js
var n$1 = 268435455;
var i$3 = class {
	constructor() {
		this.hasFeatures = !1, this.exceededTransferLimit = !1, this.fieldCount = 0, this.featureCount = 0, this.idFieldIndices = [], this.vertexCount = 0, this.offsets = {
			attributes: new Array(),
			geometry: new Array()
		}, this.centroid = new Array();
	}
	get usedMemory() {
		let t = 0;
		return t += i$14(this.idFieldIndices), t += i$14(this.offsets.attributes), t += i$14(this.offsets.geometry), t += i$14(this.centroid), this.displayIds && (t += i$14(this.displayIds)), this.groupIds && (t += i$14(this.groupIds)), t;
	}
};
function a$2(e, a, c) {
	const d = 9, f = 12, u = 13, p = 15, g = e.asUnsafe(), h = g.pos(), l = new i$3();
	let m = 0, y = 0;
	const I = 1, k = 2, b = 4, x = 3;
	let w = null;
	const L = [];
	for (; g.next();) switch (g.tag()) {
		case f:
			w = g.processMessage(U$1);
			break;
		case d:
			l.exceededTransferLimit = g.getBool();
			break;
		case u: {
			const e = g.processMessage(f$7);
			e.index = m++, L.push(e);
			break;
		}
		case p: {
			const e = g.getLength(), t = g.pos() + e, s = l.offsets.geometry, r = l.centroid;
			for (s.push(0), r.push(n$1), r.push(n$1); g.pos() < t && g.next();) switch (g.tag()) {
				case I: {
					l.offsets.attributes.push(g.pos());
					const e = g.getLength();
					g.skipLen(e);
					break;
				}
				case k:
					if (a) {
						const e = g.getLength(), t = g.pos() + e;
						for (; g.pos() < t && g.next();) switch (g.tag()) {
							case x: {
								g.getUInt32();
								const e = g.getSInt64(), t = g.getSInt64();
								l.centroid[2 * y] = e, l.centroid[2 * y + 1] = t;
								break;
							}
							default: g.skip();
						}
					} else {
						l.offsets.geometry[y] = g.pos();
						const e = g.getLength();
						l.vertexCount += e, g.skipLen(e);
					}
					break;
				case b: {
					const e = g.getLength(), t = g.pos() + e;
					for (; g.pos() < t && g.next();) switch (g.tag()) {
						case x: {
							g.getUInt32();
							const e = g.getSInt64(), t = g.getSInt64();
							l.centroid[2 * y] = e, l.centroid[2 * y + 1] = t;
							break;
						}
						default: g.skip();
					}
					break;
				}
				default: g.skip();
			}
			y++, l.hasFeatures = !0;
			break;
		}
		default: g.skip();
	}
	l.fields = new _$4(L), l.featureCount = y, l.fieldCount = m;
	const C = e$11(c);
	return l.idFieldIndices = Array.from(C, (e) => l.fields.get(e)?.index), l.transform = w, l.displayIds = new Uint32Array(l.featureCount), l.groupIds = new Uint16Array(l.featureCount), g.move(h), l;
}
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/support/FeatureSetReaderPBF.js
var o = 268435455, d$3 = 128, u = 128e3, c$1 = {
	small: {
		delta: new Int32Array(d$3),
		decoded: new Int32Array(d$3)
	},
	small64: {
		delta: new Float64Array(0),
		decoded: new Float64Array(0)
	},
	large: {
		delta: new Int32Array(u),
		decoded: new Int32Array(u)
	},
	large64: {
		delta: new Float64Array(0),
		decoded: new Float64Array(0)
	}
};
function l$4(e, t) {
	return t ? e <= c$1.small64.delta.length ? c$1.small64 : (e <= c$1.large64.delta.length || (c$1.large64.delta = new Float64Array(Math.round(1.25 * e)), c$1.large64.decoded = new Float64Array(Math.round(1.25 * e))), c$1.large64) : e <= c$1.small.delta.length ? c$1.small : (e <= c$1.large.delta.length || (c$1.large.delta = new Int32Array(Math.round(1.25 * e)), c$1.large.decoded = new Int32Array(Math.round(1.25 * e))), c$1.large);
}
function _$2(t) {
	try {
		const e = 2, r = new n$7(new Uint8Array(t), new DataView(t));
		for (; r.next();) {
			if (r.tag() === e) return g(r.getMessage());
			r.skip();
		}
	} catch (r) {
		throw new r$5("query:parsing-pbf", "Error while parsing FeatureSet PBF payload", { error: r });
	}
	return null;
}
function g(e) {
	const t = 1;
	for (; e.next();) {
		if (e.tag() === t) return e.getMessage();
		e.skip();
	}
	return null;
}
function f$3(e) {
	const t = 1, r = 2, s = 3, n = 4, a = 5, i = 6, h = 7, o = 8, d = 9, u = e.getLength(), c = e.pos() + u;
	for (; e.pos() < c && e.next();) switch (e.tag()) {
		case t: return e.getString();
		case r: return e.getFloat();
		case s: return e.getDouble();
		case n: return e.getSInt32();
		case a: return e.getUInt32();
		case i: return e.getInt64();
		case h: return e.getUInt64();
		case o: return e.getSInt64();
		case d: return e.getBool();
		default: return e.skip(), null;
	}
	return null;
}
function y$2(e, t, r, s, n) {
	if (!e) return !1;
	return 0 === t * n - s * r && t * s + r * n > 0;
}
var m$3 = class m$3 extends O {
	static fromBuffer(e, t, r = !1) {
		const s = t.geometryType, n = _$2(e);
		return new m$3(n, a$2(n, "esriGeometryPoint" === s, t.featureIdInfo), t, r);
	}
	constructor(e, t, r, s) {
		super(r), this._use64Bit = s, this._hasNext = !1, this._isPoints = !1, this._featureIndex = -1, this._featureOffset = 0, this._cache = {
			area: 0,
			unquantGeometry: void 0,
			geometry: void 0
		}, this._parseCaches = new Array(), this._geometryType = r.geometryType, this._reader = e, this._header = t, this._hasNext = t.hasFeatures, this._isPoints = "esriGeometryPoint" === r.geometryType;
	}
	get _size() {
		return this._header.featureCount;
	}
	get fields() {
		return this._header.fields;
	}
	get geometryType() {
		return this._geometryType;
	}
	get hasZ() {
		return !1;
	}
	get hasM() {
		return !1;
	}
	get hasFeatures() {
		return this._header.hasFeatures;
	}
	get hasNext() {
		return this._hasNext;
	}
	get exceededTransferLimit() {
		return this._header.exceededTransferLimit;
	}
	getSize() {
		return this._size;
	}
	getInTransform() {
		return this._header.transform;
	}
	getCursor() {
		return this.copy();
	}
	getIndex() {
		return this._featureIndex;
	}
	setIndex(e) {
		this._cache.area = 0, this._cache.unquantGeometry = void 0, this._cache.geometry = void 0, this._featureIndex = e;
	}
	getAttributeHash() {
		let e = "";
		for (const t of this._header.fields.fields) e += this._readAttributeAtIndex(t.index) + ".";
		return e;
	}
	getObjectId() {
		if (1 === this._header.idFieldIndices.length) return this._readAttributeAtIndex(this._header.idFieldIndices[0]);
		const e = this._header.idFieldIndices.map((e) => this._readAttributeAtIndex(e));
		return JSON.stringify(e);
	}
	getDisplayId() {
		return this._header.displayIds[this._featureIndex];
	}
	setDisplayId(e) {
		this._header.displayIds[this._featureIndex] = e;
	}
	readGeometryArea() {
		return this._cache.area || this._readGeometry(!0), this._cache.area;
	}
	copy() {
		const t = new m$3(this._reader.clone(), this._header, this.metadata, this._use64Bit);
		return this.copyInto(t), t;
	}
	next() {
		for (this._cache.area = 0, this._cache.unquantGeometry = void 0, this._cache.geometry = void 0; ++this._featureIndex < this._size && !this._getExists(););
		return this._featureIndex < this._size;
	}
	get usedMemory() {
		return 32 + (this._cache.geometry?.usedMemory ?? 0);
	}
	get underlyingMemory() {
		return super.underlyingMemory + this._reader.usedMemory + this._header.usedMemory;
	}
	_readX() {
		return this._header.centroid[2 * this._featureIndex];
	}
	_readY() {
		return this._header.centroid[2 * this._featureIndex + 1];
	}
	_readServerCentroid() {
		const e = this._header.centroid[2 * this._featureIndex], t = this._header.centroid[2 * this._featureIndex + 1];
		return e === o ? null : new s$12([], [e, t]);
	}
	_readGeometry(e = !1) {
		if (void 0 === this._cache.geometry) {
			let r = null;
			if (this._isPoints) {
				if (this._header.centroid[2 * this._featureIndex] === o) return null;
				const e = this._header.centroid[2 * this._featureIndex], t = this._header.centroid[2 * this._featureIndex + 1];
				r = new s$12([], [e, t]);
			} else {
				const s = this._header.offsets.geometry[this._featureIndex], n = this._reader;
				if (0 === s) return null;
				n.move(s);
				try {
					r = e ? this._parseGeometryForDisplay(n) : this._parseGeometry(n);
				} catch (t) {
					return null;
				}
			}
			return 0 === r?.coords.length && (r = null), this._cache.geometry = r, r;
		}
		return this._cache.geometry;
	}
	_readAttribute(e, t) {
		const r = this._header.fields.get(e);
		if (null == r) return;
		const s = this._readAttributeAtIndex(r.index), n = this._header.fields.isDateField(r.name);
		return t ? null == s ? s : n ? new Date(s) : s : s;
	}
	_readAttributes() {
		const e = {};
		for (const t of this._header.fields.fields) e[t.name] = this._readAttributeAtIndex(t.index);
		return e;
	}
	copyInto(e) {
		super.copyInto(e), e._featureIndex = this._featureIndex, e._featureOffset = this._featureOffset, e._hasNext = this._hasNext, e._parseCaches = this._parseCaches;
	}
	_readAttributeAtIndex(e) {
		let t = this._parseCaches[e];
		if (t || (t = new a$8(this.getSize()), this._parseCaches[e] = t), t.has(this._featureIndex)) return t.get(this._featureIndex);
		const r = this._header.offsets.attributes[this._featureIndex * this._header.fieldCount + e], s = this._reader;
		s.move(r);
		const n = f$3(s);
		return t.set(this._featureIndex, n), n;
	}
	_readGeometryDeltaDecoded(e = !1) {
		if (void 0 === this._cache.unquantGeometry) {
			const t = this._readGeometry(e);
			if (!t) return this._cache.unquantGeometry = void 0, null;
			if (!this.getInTransform()) return this._cache.unquantGeometry = t, t;
			const r = l$4(t.coords.length, this._use64Bit).decoded, s = t.clone(r), n = s.coords;
			let a = 0;
			for (const e of s.lengths) {
				for (let t = 1; t < e; t++) {
					const e = 2 * (a + t), r = 2 * (a + t - 1);
					n[e] += n[r], n[e + 1] += n[r + 1];
				}
				a += e;
			}
			return this._cache.unquantGeometry = s, s;
		}
		return this._cache.unquantGeometry;
	}
	_parseGeometry(e) {
		const t = 2, r = 3, s = e.asUnsafe(), a = s.getLength(), i = s.pos() + a, h = [], o = [];
		for (; s.pos() < i && s.next();) switch (s.tag()) {
			case t: {
				const e = s.getUInt32(), t = s.pos() + e;
				for (; s.pos() < t;) o.push(s.getUInt32());
				break;
			}
			case r: {
				const e = s.getUInt32(), t = s.pos() + e;
				for (h.push(s.getSInt64()), h.push(s.getSInt64()), this.hasZ && s.getSInt64(), this.hasM && s.getSInt64(); s.pos() < t;) h.push(s.getSInt64()), h.push(s.getSInt64()), this.hasZ && s.getSInt64(), this.hasM && s.getSInt64();
				break;
			}
			default: s.skip();
		}
		return new s$12(o, h, this.hasZ, this.hasM);
	}
	_parseGeometryForDisplay(e) {
		const r = 2, s = 3, a = e.asUnsafe(), i = a.getLength(), h = a.pos() + i, o = [], d = [];
		let u = 0, c = 0, _ = null, g = 0;
		const f = "esriGeometryPolygon" === this.geometryType, m = "esriGeometryPolyline" === this.geometryType, I = f ? 3 : m ? 2 : 1, p = f || m;
		for (; a.pos() < h && a.next();) switch (a.tag()) {
			case r: {
				const e = a.getUInt32(), t = a.pos() + e;
				for (; a.pos() < t;) {
					const e = a.getUInt32();
					o.push(e), u += e;
				}
				_ = l$4(2 * u, this._use64Bit).delta;
				break;
			}
			case s: {
				a.getUInt32();
				const e = 2 + (this.hasZ ? 1 : 0) + (this.hasM ? 1 : 0);
				n$5(_);
				for (const t of o) {
					if (c + e * t > _.length) {
						for (let e = 0; e < t; e++) a.getSInt64(), a.getSInt64(), this.hasZ && a.getSInt64(), this.hasM && a.getSInt64();
						continue;
					}
					let r = 0, s = a.getSInt64(), n = a.getSInt64();
					this.hasZ && a.getSInt64(), this.hasM && a.getSInt64(), _[c++] = s, _[c++] = n, r += 1;
					for (let e = 1; e < t; e++) {
						const e = a.getSInt64(), t = a.getSInt64(), i = s + e, h = n + t;
						g += -.5 * (i - s) * (h + n), this.hasZ && a.getSInt64(), this.hasM && a.getSInt64(), 0 === e && 0 === t || y$2(p, _[c - 2], _[c - 1], e, t) ? (_[c - 2] += e, _[c - 1] += t) : (_[c++] = e, _[c++] = t, r += 1), s = i, n = h;
					}
					r >= I ? d.push(r) : c -= r * e;
				}
				break;
			}
			default: a.skip();
		}
		return this._cache.area = g, d.length ? new s$12(d, _, this.hasZ, this.hasM) : null != _ ? this._createDeltaQuantizedExtrudedGeometry(_[0], _[1]) : null;
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/sources/strategies/support/queryAdapters.js
var l$3 = class {
	constructor(t, e) {
		this.service = t, this._metadata = e;
	}
	destroy() {}
};
function f$2(t, e, r) {
	switch (t.type) {
		case "memory": return new d$2(t, e, r);
		case "ogc": return new w(t, e);
		case "feature-service": return t.queryMetadata.supportsFormatPBF && has("featurelayer-pbf") ? new h$1(t, e) : new _$1(t, e);
	}
}
async function y$1(t) {
	const e = new c$5();
	return await e.open(t, {}), e;
}
var d$2 = class extends l$3 {
	constructor(t, e, r) {
		super(t, e), this._ports = [], this._loaded = this._load(r);
	}
	destroy() {
		this._loaded.finally(() => {
			this._client.close(), this._client = null;
			for (const t of this._ports) t.close();
		}).catch(() => {});
	}
	async _load(t) {
		this._ports = await t.layerView.openMemoryPorts(), this._client = await y$1(this._ports);
	}
	async executeQuery(t, e) {
		await this._loaded;
		const r = await this._client.invoke("queryFeatures", t.toJSON(), e);
		return u$9.fromFeatureSet(r, this._metadata);
	}
};
var h$1 = class extends l$3 {
	async executeQuery(r, a) {
		const s = await m$9(this.service.source, r, a), i = !r.quantizationParameters;
		try {
			return m$3.fromBuffer(s, this._metadata, i);
		} catch (o) {
			const s = await j$1(this.service.source, r, "pbf", a ?? {}), { data: { url: i } } = await f$5(s.url, {
				responseType: "native-request-init",
				...s.options
			});
			throw new r$5("featurelayer:query", "Failed to parse response", {
				url: i,
				error: o,
				query: r.toJSON()
			});
		}
	}
};
var _$1 = class extends l$3 {
	async executeQuery(t, e) {
		const { source: r, queryMetadata: o } = this.service;
		if (null != t.quantizationParameters && !o.supportsQuantization) {
			const o = t.clone(), n = s$11(o.quantizationParameters);
			o.quantizationParameters = null;
			const p = X(await l$9(r, o, this._metadata.spatialReference, e), this._metadata.featureIdInfo);
			return et(n, p), u$9.fromOptimizedFeatureSet(p, this._metadata);
		}
		const n = await l$9(r, t, this._metadata.spatialReference, e);
		return "esriGeometryPoint" === this._metadata.geometryType && (n.features = n.features?.filter((t) => {
			if (null != t.geometry) {
				const e = t.geometry;
				return Number.isFinite(e.x) && Number.isFinite(e.y);
			}
			return !0;
		})), u$9.fromFeatureSet(n, this._metadata);
	}
};
var w = class extends l$3 {
	async executeQuery(t, e) {
		if (t.quantizationParameters && !this.service.queryMetadata.supportsQuantization) {
			const r = t.clone(), s = s$11(r.quantizationParameters);
			r.quantizationParameters = null;
			const n = await R$1(this.service.source, t, e);
			return et(s, n), u$9.fromOptimizedFeatureSet(n, this._metadata);
		}
		const r = await R$1(this.service.source, t, e);
		return u$9.fromOptimizedFeatureSet(r, this._metadata);
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/sources/strategies/AFeatureLoadStrategy.js
var l$2 = class extends e {
	constructor(e, t, r) {
		super(e.metadata, t, r), this._service = e, this._didApplyOverride = !1, this._queue = new u$7({
			concurrency: 32,
			process: async (e) => {
				const t = {
					signal: e.options?.signal,
					query: e.query.customParameters,
					useQueue: !0
				};
				return this._adapter.executeQuery(e.query.inner, t);
			}
		}), this._queryInfo = u$10.create(e, {
			...t.full,
			...t.partial
		}, this.metadata), this._adapter = f$2(e, this.metadata, r.connection), this._lastEditDate = e.queryMetadata.lastEditDate;
	}
	destroy() {
		super.destroy(), this._adapter.destroy();
	}
	unsafeSetQueryHistoricMoment(e) {
		this._queryInfo.updateHistoricMoment(e);
	}
	async tryUpdate(e, t) {
		if (i$10(this.availableFields, t.availableFields)) {
			if (this._didApplyOverride || await this._queryLastEditDateChanged()) return !1;
			await this._updateFields(t.availableFields);
		}
		return this._schema.partial = t, !0;
	}
	async queryByObjectId(e, t) {
		if (0 === e.length) return u$9.empty(this.metadata);
		const r = this._queryInfo.createQuery({ objectIds: e });
		return r.inner.outFields = t, this._fetch(r, null, null);
	}
	get availableFields() {
		return this._schema.partial.availableFields;
	}
	get definitionExpression() {
		return this._schema.full.definitionExpression;
	}
	_onOverride() {
		this._didApplyOverride = !0;
	}
	async _updateFields(e) {
		this._queryInfo.updateFields(e);
		const r = Array.from(this.store.chunks()).map(async (e) => {
			const t = R.fromJSON(e.queryInfo.queryJSON);
			if (t) try {
				return await this._tryUpdateFields(e.reader, t, { chunkId: e.chunkId }), null;
			} catch (r) {
				return r;
			}
		}), s = (await Promise.all(r)).filter((e) => e);
		if (s.length) throw new r$5("featurelayer-query", "Encountered errors when downloading fields", { errors: s });
	}
	async _fetch(e, t, r) {
		const s = await this._enqueue(e, t);
		return await this._tryUpdateFields(s, e.inner, r), s;
	}
	async _queryLastEditDateChanged() {
		if (null == this._lastEditDate) return !1;
		if (!("source" in this._service)) return !1;
		const t = this._service.source, r = {
			...t.query,
			f: "json"
		}, s = (await f$5(t.path, {
			query: r,
			responseType: "json"
		})).data.editingInfo.lastEditDate;
		return s !== this._lastEditDate && (this._lastEditDate = s, !0);
	}
	async _tryUpdateFields(e, s, i) {
		const a = this._queryInfo.createPatchFieldsQuery(s, e, i);
		if (!a) return;
		const n = await this._enqueue(a, this._options);
		n.getSize() === e.getSize() ? e.joinAttributes(n) : n$4.getLogger("esri.views.2d.layers.features.sources.strategies.AFetchLoadStrategy").error(new r$5("featurelayer-query", `Failed to join features. Expected a count of ${e.getSize()} features, but got ${n.getSize()}`, {
			query: a.inner.toJSON(),
			debugInfo: i
		}));
	}
	async _enqueue(e, t) {
		return this._connection.onEvent({ type: "fetchStart" }), this._queue.push({
			query: e,
			options: t
		}).finally(() => {
			this._connection.onEvent({
				type: "fetchEnd",
				done: 0 === this._queue.length
			});
		});
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/sources/strategies/AFeatureTileLoadStrategy.js
var s$2 = class extends l$2 {
	constructor(t, s, h) {
		super(t, s, h), this._chunksById = /* @__PURE__ */ new Map(), this._featureCache = new h$2(this.store, this._queryInfo, this._options, this._fetch.bind(this));
	}
	prepareCacheUpdate(e, t) {
		return this._featureCache.prepareCacheUpdate(e, t);
	}
	applyCacheUpdate() {
		return this._featureCache.applyCacheUpdate();
	}
	unload(e) {
		this._removeChunks(e.tile);
	}
	_addChunk(e) {
		const t = e.tile.id;
		this._chunksById.has(t) || this._chunksById.set(t, []);
		const s = e.size();
		(s || e.first || e.end) && (has("esri-2d-update-debug") && console.debug(`Chunk[${e.chunkId}] ATileLoadStrategy.addChunk [count=${s}]`), this._chunksById.get(t).push(e), this.store.insert(e));
	}
	_removeChunks(e) {
		const t = this._chunksById.get(e.key.id) ?? [];
		for (const s of t) has("esri-2d-update-debug") && console.debug(`Tile[${e.key.id}] Chunk[${s.chunkId}] ATileLoadStrategy.removeChunk`), this.store.remove(s);
		this._chunksById.delete(e.key.id);
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/sources/strategies/chunks/DrillDownTileSourceChunk.js
var i$2 = class extends s$15 {
	constructor(e, i, t, r, s, h) {
		super(), this._reader = e, this._queryJSON = i, this._tile = t, this._sourceTile = r, this._sourceTileDepth = s, this._end = h, this.chunkId = `${this._tile.key.id}.${this._sourceTile?.key.id}${this._end ? "e" : ""}`, this.normalizedChunkId = `${this._tile.key.normalizedId}.${this._sourceTile?.key.normalizedId}${this._end ? "e" : ""}`;
	}
	get queryInfo() {
		return {
			type: "drill-down-tile",
			chunkId: this.chunkId,
			tileId: this._tile.key.id,
			queryJSON: this._queryJSON,
			sourceTileDepth: this._sourceTileDepth,
			sourceTileId: this._sourceTile?.key.id,
			size: this.size(),
			end: this.end
		};
	}
	get first() {
		return 0 === this._sourceTileDepth;
	}
	get reader() {
		return this._reader;
	}
	get end() {
		return this._end;
	}
	get tile() {
		return this._tile;
	}
	get isTiled() {
		return !0;
	}
	getTileReader(e) {
		return this._tile.key.id === e.key.id ? this.reader : null;
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/sources/strategies/FeatureDrillDownTileLoadStrategy.js
var a$1 = has("featurelayer-query-max-depth");
var l$1 = class {
	constructor(e, o) {
		this.subscription = e, this._tileIdToResult = /* @__PURE__ */ new Map(), this._controller = new AbortController(), this._handles = t$10([w$1(e.signal, () => this._controller.abort()), w$1(o, () => this._controller.abort())]);
	}
	destroy() {
		this._controller.abort(), this._handles.remove();
	}
	get(t) {
		return this._tileIdToResult.get(t);
	}
	set(t, e) {
		this._tileIdToResult.set(t, e);
	}
	get options() {
		return { signal: this._controller.signal };
	}
};
var n = class extends s$2 {
	constructor() {
		super(...arguments), this._loadStates = /* @__PURE__ */ new Map();
	}
	destroy() {
		super.destroy();
		for (const t of this._loadStates.values()) t.destroy();
		this._loadStates.clear();
	}
	get about() {
		return {
			willQueryAllFeatures: !1,
			willQueryFullResolutionGeometry: !1
		};
	}
	async load(t) {
		this._loadStates.has(t.key.id) || this._loadStates.set(t.key.id, new l$1(t, this._options));
		const s = this._loadStates.get(t.key.id);
		let o;
		try {
			for await (const o of this._fetchChunkInfos(s, t.tile, 0)) {
				const { queryJSON: t, reader: i, sourceTile: a, sourceTileDepth: l, tile: n } = o, h = new i$2(i, t, n, a, l, !1);
				s$9(s.options), this._addChunk(h);
			}
		} catch (n) {
			o = n;
		}
		s$9(s.options);
		const a = new i$2(u$9.empty(this.metadata), null, t.tile, null, -1, !0);
		if (this._addChunk(a), o) throw o;
	}
	unload(t) {
		super.unload(t), this._loadStates.get(t.key.id)?.destroy(), this._loadStates.delete(t.key.id);
	}
	async *_fetchChunkInfos(t, e, s) {
		let o = t.get(e.id);
		const r = !!o;
		if (o || (o = await this._fetchChunkInfo(t, e, s), t.set(e.id, o)), o.reader.exceededTransferLimit && s < has("featurelayer-query-max-depth")) for (const i of e.createChildTiles()) yield* this._fetchChunkInfos(t, i, s + 1);
		else r || (yield o);
	}
	async _fetchChunkInfo(t, e, s) {
		const o = t.subscription.tile.getQuantizationParameters(), r = this._queryInfo.createTileQuery(e, {
			returnExceededLimitFeatures: s === a$1,
			quantizationParameters: o
		});
		return {
			reader: await this._fetch(r, t.options, { chunkId: e.id }),
			queryJSON: r.inner.toJSON(),
			tile: t.subscription.tile,
			sourceTile: e,
			sourceTileDepth: s
		};
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/sources/strategies/chunks/PagedTileSourceChunk.js
var t$2 = class extends s$15 {
	constructor(e, t, i, r, s) {
		super(), this._reader = e, this._queryJSON = t, this._tile = i, this._page = r, this._end = s, this.chunkId = `${this._tile.key.id}.${this._page}${this.end ? "e" : ""}`, this.normalizedChunkId = `${this._tile.key.normalizedId}.${this._page}${this.end ? "e" : ""}`;
	}
	get queryInfo() {
		return {
			type: "paged-tile",
			chunkId: this.chunkId,
			tileId: this._tile.key.id,
			queryJSON: this._queryJSON,
			page: this._page,
			size: this.size(),
			end: this.end
		};
	}
	get reader() {
		return this._reader;
	}
	get first() {
		return 0 === this._page;
	}
	get end() {
		return this._end;
	}
	get page() {
		return this._page;
	}
	get tile() {
		return this._tile;
	}
	get isTiled() {
		return !0;
	}
	getTileReader(e) {
		return this._tile.key.id === e.key.id ? this.reader : null;
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/sources/strategies/FeaturePagedTileLoadStrategy.js
var l = class {
	constructor(t, o) {
		this.subscription = t, this._pages = /* @__PURE__ */ new Set(), this._controller = new AbortController(), this._done = !1, this._handles = t$10([w$1(t.signal, () => this._controller.abort()), w$1(o, () => this._controller.abort())]);
	}
	destroy() {
		this._controller.abort(), this._handles.remove();
	}
	get pageStart() {
		let e = -1;
		for (const t of this._pages.values()) e = Math.max(e, t);
		return e + 1;
	}
	get done() {
		return this._done;
	}
	get options() {
		return { signal: this._controller.signal };
	}
	add(e, t) {
		this._pages.add(e), this._done = this._done || t;
	}
};
var c = class extends s$2 {
	constructor() {
		super(...arguments), this._loadStates = /* @__PURE__ */ new Map();
	}
	destroy() {
		super.destroy();
		for (const e of this._loadStates.values()) e.destroy();
		this._loadStates.clear();
	}
	get about() {
		return {
			willQueryAllFeatures: !1,
			willQueryFullResolutionGeometry: !1
		};
	}
	async load(e) {
		const o = e$5(this._loadStates, e.key.id, () => new l(e, this._options));
		for await (const t of this._fetchPages(o)) this._addChunk(t);
	}
	unload(e) {
		super.unload(e), this._loadStates.get(e.key.id)?.destroy(), this._loadStates.delete(e.key.id);
	}
	async *_fetchPages(e) {
		let t;
		try {
			for await (const t of this._concurrentPageStream(e)) s$9(e.options), 0 !== t.size() && (yield t);
		} catch (r) {
			t = r;
		}
		if (t && d$6(t) || (yield new t$2(u$9.empty(this.metadata), null, e.subscription.tile, -1, !0)), t) throw t;
	}
	async *_concurrentPageStream(e) {
		const t = has("featurelayer-query-tile-concurrency"), o = this._pageStreamAll(e), s = [];
		let r = !1, a = 1;
		for (; !r;) {
			const e = [];
			for (; !r && s.length < a;) {
				const t = o.next();
				if (!t.value) {
					r = !0;
					break;
				}
				const a = t.value;
				a.then((e) => {
					e.reader.exceededTransferLimit || (r = !0);
				}).catch((e) => {
					r = !0;
				}).finally(() => {
					s.splice(s.indexOf(a), 1);
				}), s.push(a), e.push(a);
			}
			for (const t of e) yield t;
			s.length && await Promise.race(s), a < t && (a += 1);
		}
	}
	*_pageStreamAll(e) {
		const t = Math.ceil(has("featurelayer-query-tile-max-features") / this._queryInfo.getPageSize(!0));
		for (let o = 0; o < t; o++) yield this._downloadPage(o, e);
	}
	async _downloadPage(e, t) {
		s$9(t.options);
		const s = t.subscription.tile, r = this._queryInfo.createPagedTileQuery(s, e), a = await this._fetch(r, t.options, { chunkId: `${s.id}-${e}` });
		return s$9(t.options), new t$2(a, r.inner.toJSON(), s, e, !1);
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/sources/strategies/FeatureSnapshotLoadStrategy.js
var i$1 = class extends l$2 {
	constructor(e, t, r) {
		super(e, t, r);
	}
	get about() {
		return {
			willQueryAllFeatures: !0,
			willQueryFullResolutionGeometry: !0
		};
	}
	load(e) {
		return this._promise ??= this._download(), this._promise;
	}
	unload(e) {}
	async _download() {
		const e = this._schema.snapshotInfo.initialTolerance, t = e ? new m$8({
			mode: "view",
			originPosition: "upper-left",
			tolerance: e
		}) : null;
		await this._downloadStreaming(t), null != t && await this._downloadRefresh();
	}
	async _downloadStreaming(t) {
		try {
			for await (const e of this._fetchPages(t)) this.store.insert(e);
		} catch (r) {
			throw new r$5("featurelayer-query", "Encountered error when downloading data", { error: r });
		}
	}
	async _downloadRefresh() {
		try {
			const e = [];
			for await (const t of this._fetchPages(null)) e.push(t);
			this.store.clear();
			for (const t of e) this.store.insert(t);
			this.store.refresh();
		} catch (t) {
			throw new r$5("featurelayer-query", "Encountered error when downloading data", { error: t });
		}
	}
	async *_fetchPages(e) {
		let r;
		try {
			for await (const t of this._concurrentPageStream(e)) 0 !== t.size() && (yield t);
		} catch (o) {
			r = o;
		}
		if (r && d$6(r) || (yield new t$13(u$9.empty(this.metadata), null, -1, !0)), r) throw r;
	}
	async *_concurrentPageStream(e) {
		const t = has("featurelayer-snapshot-concurrency"), r = this._pageStreamAll(e), o = [];
		let a = !1, s = 1;
		for (; !a;) {
			const e = [];
			for (; !a && o.length < s;) {
				const t = r.next();
				if (!t.value) {
					a = !0;
					break;
				}
				const s = t.value;
				s.then((e) => {
					e.reader.exceededTransferLimit || (a = !0);
				}).catch((e) => {
					a = !0;
				}).finally(() => {
					o.splice(o.indexOf(s), 1);
				}), o.push(s), e.push(s);
			}
			for (const t of e) yield t;
			o.length && await Promise.race(o), s < t && (s += 1);
		}
	}
	*_pageStreamAll(e) {
		const t = Math.ceil(this._schema.snapshotInfo.maxFeatureCount / this._queryInfo.getPageSize(!1));
		for (let r = 0; r < t; r++) yield this._downloadPage(r, e);
	}
	async _downloadPage(e, t) {
		s$9(this._options);
		const o = this._queryInfo.createPagedQuery(e, t), n = new t$13(await this._fetch(o, this._options, { chunkId: e.toString() }), o.inner.toJSON(), e, !1);
		return s$9(this._options), n;
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/sources/strategies/chunks/ParquetSnapshotSourceChunk.js
var s$1 = class extends s$15 {
	constructor(e, t, s, r, i, n) {
		super(), this._metadata = e, this._reader = t, this._page = s, this._end = r, this._fileIndex = i, this._fileUrl = n, this.chunkId = `${this._fileIndex}.${this._page}${this.end ? "e" : ""}`, this.normalizedChunkId = this.chunkId;
	}
	get reader() {
		return this._reader ?? u$9.empty(this._metadata);
	}
	get first() {
		return 0 === this._page;
	}
	get end() {
		return this._end;
	}
	get queryInfo() {
		return {
			type: "snapshot",
			chunkId: this.chunkId,
			page: this._page,
			size: this.size(),
			end: this.end
		};
	}
	get isTiled() {
		return !1;
	}
	async updateFields(e, t, s) {
		return this._reader?.updateFields(e, this._fileUrl, t, s);
	}
	getTileReader(e) {
		const t = this.queryFeaturesInBounds(e.bounds);
		return t.setTransformForDisplay(e.transform), t;
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/sources/strategies/ParquetSnapshotLoadStrategy.js
var h = 8;
var d$1 = class extends e {
	constructor(t, e, s, i) {
		super(t.metadata, e, s), this._service = t, this._files = /* @__PURE__ */ new Map(), this._loaded = this._load(i);
	}
	destroy() {
		super.destroy(), this._loaded.finally(() => {
			this._client.close(), this._client = null;
			for (const t of this._ports) t.close();
		}).catch(() => {});
	}
	get about() {
		return {
			willQueryAllFeatures: !0,
			willQueryFullResolutionGeometry: !0
		};
	}
	get availableFields() {
		return this._schema.partial.availableFields;
	}
	get definitionExpression() {
		return this._schema.full.definitionExpression;
	}
	async tryUpdate(s, i) {
		if (await this._loaded, i$10(this.availableFields, i.availableFields) && await this._updateFields(i.availableFields), i$10(this._schema.partial.urls, i.urls)) {
			for (const t of i.urls) this._files.has(t) || await this._loadFile(t);
			for (const e of this._files.keys()) if (!i.urls.includes(e)) throw new r$5("unsupported", "Removing parquet files is not supported", {
				previous: this._schema.partial.urls,
				next: i.urls
			});
		}
		return this._schema.partial = i, !0;
	}
	async load(t) {
		return this._loaded;
	}
	unload(t) {}
	_onOverride() {}
	async _load(t) {
		this._ports = await t.layerView.openMemoryPorts(), this._client = await u$4(this._ports, { maxNumberOfConcurrentJobs: 20 }), this._proxy = this._client.createInvokeProxy(), this._indexMap = await this._proxy.getIndexMap(), await this._download();
	}
	async _updateFields(t) {
		const s = c$4(new Set(t), new Set(this.availableFields));
		if (null != this._service.metadata.fieldsIndex && s.size) {
			const t = Array.from(this.store.insertedChunks()).map((t) => t.updateFields(this._proxy, Array.from(s), this._options));
			await Promise.all(t);
		}
	}
	async _loadFile(t) {
		const e = await s$14(), i = await this._proxy.getFileId(t);
		this._files.set(t, i), s$9(this._options);
		const a = await this._proxy.queryStream(t, {
			type: "snapshot",
			outFields: Array.from(this.availableFields),
			returnGeometry: !0,
			outSpatialReference: this.metadata.outSpatialReference.toJSON(),
			where: this.definitionExpression
		}, this._options);
		let o = 0;
		const d = [], p = async (r = 0) => {
			const h = await this._proxy.getStreamNext(t, a, this._options);
			if (s$9(this._options), null == h) return;
			const d = e.ParquetChunk.deserialize(new Uint8Array(h)), u = new o$7(this.metadata, this._indexMap, d, i), c = new s$1(this.metadata, u, o++, !1, i, t);
			return this.store.insert(c), p(r + 1);
		};
		for (let s = 0; s < h; s++) d.push(p());
		await Promise.all(d);
	}
	async _download() {
		try {
			await Promise.all(this._schema.partial.urls.map((t) => this._loadFile(t)));
			const t = new s$1(this.metadata, null, -1, !0, 0, "");
			this.store.insert(t);
		} catch (t) {
			throw console.error(t), t;
		}
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/sources/strategies/chunks/ParquetSourceChunk.js
var i = class extends s$15 {
	constructor(e, t, i, r, s, d, h) {
		super(), this._metadata = e, this._reader = t, this._tile = i, this._fileId = r, this._fileUrl = s, this._page = d, this._end = h, this.chunkId = `${this._tile.key.id}.${this._fileId}.${this._page}${this.end ? "e" : ""}`, this.normalizedChunkId = `${this._tile.key.normalizedId}.${this._fileId}.${this._page}${this.end ? "e" : ""}`;
	}
	get reader() {
		return this._reader ?? u$9.empty(this._metadata);
	}
	get first() {
		return 0 === this._page;
	}
	get end() {
		return this._end;
	}
	get tile() {
		return this._tile;
	}
	get queryInfo() {
		return {
			type: "parquet",
			chunkId: this.chunkId,
			queryJSON: null,
			page: this._page,
			size: this.size(),
			tileId: this._tile.id,
			end: this.end
		};
	}
	get isTiled() {
		return !0;
	}
	async updateFields(e, t, i) {
		return this._reader?.updateFields(e, this._fileUrl, t, i);
	}
	getTileReader(e) {
		return this._tile.key.id === e.key.id ? this.reader : null;
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/sources/strategies/ParquetTileLoadStrategy.js
var m$2 = 2, _ = 4;
var f$1 = class {
	constructor(t, s) {
		this.subscription = t, this.chunks = [], this.ids = [], this._controller = new AbortController(), this._handles = t$10([w$1(t.signal, () => this._controller.abort()), w$1(s, () => this._controller.abort())]);
	}
	destroy() {
		this._controller.abort(), this._handles.remove();
	}
	get options() {
		return { signal: this._controller.signal };
	}
	addBinaryData(t) {
		this.chunks.push(t);
	}
	addSourceChunkId(t) {
		this.ids.push(t);
	}
};
var y = class extends e {
	constructor(t, e, s, i) {
		if (super(t.metadata, e, s), this._service = t, this._loadStates = /* @__PURE__ */ new Map(), this._files = /* @__PURE__ */ new Map(), this._queue = new u$7({
			concurrency: 32,
			process: (t) => this._loadTile(t)
		}), !t.geometryInfo.displayOptimization) throw new Error("InternalError: ParquetTileLoadStrategy only supports XZ-enabled parquet files");
		this._loaded = this._load(i);
	}
	destroy() {
		super.destroy(), this._loaded.finally(() => {
			this._client.close(), this._client = null;
			for (const t of this._ports) t.close();
		}).catch(() => {});
	}
	get about() {
		return {
			willQueryAllFeatures: !0,
			willQueryFullResolutionGeometry: !0
		};
	}
	get availableFields() {
		return this._schema.partial.availableFields;
	}
	get definitionExpression() {
		return this._schema.full.definitionExpression;
	}
	async tryUpdate(e, s) {
		await this._loaded;
		let r = !0;
		if (i$10(this.availableFields, s.availableFields) && await this._updateFields(s.availableFields), i$10(this._schema.partial.urls, s.urls)) {
			for (const t of s.urls) this._files.has(t) || (r = !1, await this._insert(t));
			for (const e of this._files.keys()) if (!s.urls.includes(e)) throw new r$5("unsupported", "Removing parquet files is not supported", {
				previous: this._schema.partial.urls,
				next: s.urls
			});
		}
		return this._schema.partial = s, r;
	}
	async load(t) {
		return await this._loaded, e$5(this._loadStates, t.key.id, () => new f$1(t, this._options)), this._queue.push(t);
	}
	unload(t) {
		const e = this._loadStates.get(t.tile.id);
		if (null != e) {
			for (const t of e.ids) this.store.removeById(t);
			e.destroy(), this._loadStates.delete(t.tile.id);
		}
	}
	async _load(t) {
		const e = this._schema.partial.urls;
		this._ports = await t.layerView.openMemoryPorts(), this._client = await u$4(this._ports, { maxNumberOfConcurrentJobs: 20 }), this._proxy = this._client.createInvokeProxy(), this._indexMap = await this._proxy.getIndexMap(), await Promise.all(e.map((t) => this._insert(t)));
	}
	async _insert(t) {
		this._files.set(t, this._files.size);
	}
	_onOverride() {}
	async _updateFields(t) {
		const s = c$4(new Set(t), new Set(this.availableFields));
		if (null != this._service.metadata.fieldsIndex && s.size) {
			const t = Array.from(this.store.insertedChunks()).map((t) => t.updateFields(this._proxy, Array.from(s), this._options));
			await Promise.all(t);
		}
	}
	async _loadTile(t) {
		const e = t.tile, s = t.signal, i$19 = this._loadStates.get(e.id), a = new u$7({
			process: async (e) => {
				const s = await this._proxy.getFileId(e);
				return this._downloadFileData(t, e, s);
			},
			concurrency: m$2
		});
		await Promise.allSettled(Array.from(this._files.keys()).map((t) => a.push(t))), s$9(s);
		const l = new i(this.metadata, null, e, null, null, 0, !0);
		i$19.addSourceChunkId(l.chunkId), this.store.insert(l);
	}
	async _downloadFileData(t, e, s) {
		const i$20 = await s$14(), a = t.tile, o = t.signal, l = this._loadStates.get(a.id), n = await this._proxy.queryStream(e, {
			type: "tile",
			extent: a.extent,
			transform: a.normalizedTransform,
			outFields: Array.from(this.availableFields),
			returnGeometry: !0,
			outSpatialReference: this.metadata.outSpatialReference.toJSON(),
			scale: a.scale,
			where: this.definitionExpression
		}, { signal: o });
		let d = 0;
		const u = [], m = async (t = 0) => {
			const h = await this._proxy.getStreamNext(e, n, { signal: o });
			if (s$9(o), null == h) return;
			const u = i$20.ParquetChunk.deserialize(new Uint8Array(h)), _ = new o$7(this.metadata, this._indexMap, u, s), f = new i(this.metadata, _, a, s, e, d++, !1);
			return l.addBinaryData({
				data: u,
				fileUrl: e
			}), l.addSourceChunkId(f.chunkId), this.store.insert(f), m(t + 1);
		};
		for (let r = 0; r < _; r++) u.push(m());
		await Promise.all(u);
	}
};
//#endregion
//#region node_modules/@arcgis/core/layers/graphics/data/StreamFeatureManager.js
var d = 1e3;
var a = class {
	constructor(t, s, r, o, d = 128) {
		if (this._trackIdToObservations = /* @__PURE__ */ new Map(), this._idCounter = 0, this._lastPurge = performance.now(), this._addOrUpdated = /* @__PURE__ */ new Map(), this._removed = [], this._maxAge = 0, this._timeInfo = r, this._purgeOptions = o, this.store = t, "unique-id-composite" === s.type) throw new r$5("stream-layer", "composite uniqueIds are not supported");
		this.idField = s.fieldName, this.purgeInterval = d, this._useGeneratedIds = this.idField === _$5;
	}
	removeById(t) {
		this._removed.push(t);
	}
	removeByTrackId(t) {
		const e = this._trackIdToObservations.get(t);
		if (e) for (const s of e.entries) this._removed.push(s);
	}
	add(e) {
		if (this._useGeneratedIds) {
			const t = this._nextId();
			e.attributes[this.idField] = t, e.objectId = t;
		} else e.objectId = e.attributes[this.idField];
		const i = e.objectId;
		if (this._addOrUpdated.set(i, e), this._maxAge = Math.max(this._maxAge, e.attributes[this._timeInfo.startTimeField]), !this._timeInfo.trackIdField) return this._trackIdLessObservations ??= new t$14(1e5), void this._trackIdLessObservations.enqueue(i);
		const r = e.attributes[this._timeInfo.trackIdField];
		if (!this._trackIdToObservations.has(r)) {
			const i = r$6(null != this._purgeOptions?.maxObservations ? this._purgeOptions.maxObservations : d, 0, d);
			this._trackIdToObservations.set(r, new t$14(i));
		}
		const a = this._trackIdToObservations.get(r)?.enqueue(i);
		null != a && (this._addOrUpdated.has(a) ? this._addOrUpdated.delete(a) : this._removed.push(a));
	}
	checkForUpdates() {
		const t = this._getToAdd(), e = this._getToRemove(), s = performance.now(), i = s - this._lastPurge, d = Date.now();
		i >= this.purgeInterval && (this._purge(s), this._lastPurge = s);
		const a = [];
		if (null != e) for (const r of e) {
			const t = this.store.removeById(r);
			null != t && a.push(t);
		}
		const n = [];
		if (null != t) {
			const i = new Set(e ?? []);
			for (const e of t) i.has(e.objectId) || (e.attributes["__esri_timestamp__"] = s, e.attributes["__esri_time_received__"] = d, this.store.add(e), n.push(e));
		}
		return !(!n.length && !a?.length) && (this.store.update(n, a), !0);
	}
	_getToAdd() {
		if (!this._addOrUpdated.size) return null;
		const t = new Array(this._addOrUpdated.size);
		let e = 0;
		return this._addOrUpdated.forEach((s) => t[e++] = s), this._addOrUpdated.clear(), t;
	}
	_getToRemove() {
		const t = this._removed;
		return this._removed.length ? (this._removed = [], t) : null;
	}
	_nextId() {
		const t = this._idCounter;
		return this._idCounter = (this._idCounter + 1) % 4294967294 + 1, t;
	}
	_purge(t) {
		const e = this._purgeOptions;
		null != e && (this._purgeSomeByDisplayCount(e), this._purgeByAge(e), this._purgeByAgeReceived(t, e), this._purgeTracks());
	}
	_purgeSomeByDisplayCount(t) {
		if (!t.displayCount) return;
		let e = this.store.size;
		if (e > t.displayCount) {
			if (this._timeInfo.trackIdField) {
				for (const s of this._trackIdToObservations.values()) if (e > t.displayCount && s.size) {
					const t = s.dequeue();
					this._removed.push(t), e--;
				}
			}
			if (null != this._trackIdLessObservations) {
				let s = e - t.displayCount;
				for (; s-- > 0;) {
					const t = this._trackIdLessObservations.dequeue();
					null != t && this._removed.push(t);
				}
			}
		}
	}
	_purgeByAge(t) {
		const e = this._timeInfo?.startTimeField;
		if (!t.age || !e) return;
		const s = 60 * t.age * 1e3, i = this._maxAge - s;
		this.store.forEach((t) => {
			t.attributes[e] < i && this._removed.push(t.objectId);
		});
	}
	_purgeByAgeReceived(t, e) {
		if (!e.ageReceived) return;
		const s = t - 60 * e.ageReceived * 1e3;
		this.store.forEach((t) => {
			t.attributes["__esri_timestamp__"] < s && this._removed.push(t.objectId);
		});
	}
	_purgeTracks() {
		this._trackIdToObservations.forEach((t, e) => {
			0 === t.size && this._trackIdToObservations.delete(e);
		});
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/sources/StreamConnectionState.js
var s = class extends b {
	constructor(r) {
		super(r);
	}
	get connectionStatus() {
		return this.connection?.connectionStatus;
	}
	get errorString() {
		return this.connection?.errorString;
	}
};
__decorate([m$7()], s.prototype, "connection", void 0), __decorate([m$7()], s.prototype, "connectionStatus", null), __decorate([m$7()], s.prototype, "errorString", null), s = __decorate([l$6("esri.views.2d.layers.features.sources.StreamConnectionState")], s);
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/sources/StreamFeatureStore.js
var t$1 = class {
	constructor(e, t) {
		this._metadata = e, this._onUpdate = t, this._objectIdToFeature = /* @__PURE__ */ new Map();
	}
	get size() {
		return this._objectIdToFeature.size;
	}
	get reader() {
		return u$9.fromFeatures([...this._objectIdToFeature.values()], this._metadata);
	}
	add(e) {
		this._objectIdToFeature.set(e.objectId, e);
	}
	forEach(e) {
		this._objectIdToFeature.forEach(e);
	}
	removeById(e) {
		const t = this._objectIdToFeature.get(e);
		return t ? (this._objectIdToFeature.delete(e), t) : null;
	}
	clear() {
		this._objectIdToFeature = /* @__PURE__ */ new Map();
	}
	update(e, t) {
		this._onUpdate(e?.length ?? 0);
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/sources/strategies/chunks/StreamSourceChunk.js
var r = class extends s$15 {
	constructor(e) {
		super(), this._reader = e, this.chunkId = "stream-chunk", this.normalizedChunkId = "stream-chunk";
	}
	get reader() {
		return this._reader;
	}
	get first() {
		return !0;
	}
	get end() {
		return !0;
	}
	get queryInfo() {
		return {
			type: "stream",
			chunkId: this.chunkId,
			size: this.size(),
			end: this.end
		};
	}
	get isTiled() {
		return !1;
	}
	getTileReader(e) {
		const r = this.queryFeaturesInBounds(e.bounds);
		return r.setTransformForDisplay(e.transform), r;
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/sources/strategies/StreamLoadStrategy.js
var m$1 = class extends e {
	constructor(t, s$17, n) {
		super(t.metadata, s$17, n), this._service = t, this._connectionState = new s(), this._forceRefresh = !1, this.events = new i$11();
		const { timeInfo: c } = this.metadata, { purgeOptions: h } = s$17.full;
		this._stagingStore = new t$1(this.metadata.weakCloneWithAdditionalFields([{
			name: i$15,
			alias: "timeReceived",
			type: "esriFieldTypeDate"
		}]), (e) => this.events.emit("features-updated", e)), this._manager = new a(this._stagingStore, this.metadata.featureIdInfo, c, h), this.connect();
	}
	destroy() {
		super.destroy(), this.disconnect();
	}
	get about() {
		return {
			willQueryAllFeatures: !1,
			willQueryFullResolutionGeometry: !1
		};
	}
	get connectionStatus() {
		return this._connectionState.connectionStatus;
	}
	get errorString() {
		return this._connectionState?.errorString;
	}
	get availableFields() {
		return this._schema.full.availableFields;
	}
	get definitionExpression() {
		return this._schema.full.definitionExpression;
	}
	async tryUpdate(e, t) {
		const s = null != this._chunk;
		if (t.sourceRefreshVersion !== this._refreshVersion) {
			if (this._refreshVersion = t.sourceRefreshVersion, !this._manager.checkForUpdates() && s && !this._forceRefresh) return this.events.emit("tick"), !0;
			this._chunk && (this.store.remove(this._chunk), e.requireInvalidation()), this._forceRefresh = !1, this._chunk = new r(this._stagingStore.reader), this.store.insert(this._chunk), this.events.emit("tick");
		}
		return this._schema.partial = t, !0;
	}
	async load(e) {}
	unload(e) {}
	disconnect() {
		this._streamConnection = u$2(this._streamConnection), this._connectionState.connection = null, this._handlesGroup?.remove();
	}
	connect() {
		if (null != this._streamConnection) return;
		const { geometryType: e, spatialReference: s } = this.metadata, { maxReconnectionAttempts: i, maxReconnectionInterval: r, geometryDefinition: o, definitionExpression: a, customParameters: c } = this._schema.full;
		this._streamConnection = r$8(this._service.source, s, this._service.outSpatialReference, e, a, o, i, r, c), this._handlesGroup = t$10([this._streamConnection.on("data-received", (e) => this._onFeature(e)), this._streamConnection.on("message-received", (e) => this._onWebSocketMessage(e))]), this._connectionState.connection = this._streamConnection;
	}
	clear() {
		this._manager.checkForUpdates(), this._stagingStore.clear(), this._forceRefresh = !0;
	}
	updateCustomParameters(e) {
		this._streamConnection?.updateCustomParameters(e);
	}
	sendMessageToSocket(e) {
		this._streamConnection?.sendMessageToSocket(e);
	}
	sendMessageToClient(e) {
		this._streamConnection?.sendMessageToClient(e);
	}
	_onOverride() {}
	_onWebSocketMessage(e) {
		if ("type" in e) switch (e.type) {
			case "delete":
				if (e.objectIds) for (const t of e.objectIds) this._manager.removeById(t);
				if (e.trackIds) for (const t of e.trackIds) this._manager.removeByTrackId(t);
				break;
			case "clear": this.clear();
		}
		this.events.emit("message-received", e);
	}
	_onFeature(e) {
		try {
			this._manager.add(e), this.events.emit("data-received", e);
		} catch (t) {}
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/sources/FeatureSource.js
var f = class {
	constructor(e, t, r, s) {
		this._aggregateAdapter = e, this._subscriptions = t, this._connection = r, this._cachedObjectIds = s, this._updateTracking = new n$10({ debugName: "FeatureSource" }), this.store = new n$9();
	}
	destroy() {
		this._strategy?.destroy(), this._streamMessenger?.destroy(), this._updateTracking?.destroy(), this.store.destroy();
	}
	get metadata() {
		return this._strategy.metadata;
	}
	get streamMessenger() {
		return this._streamMessenger ?? this._initStreamMessenger(), this._streamMessenger;
	}
	get statistics() {
		return t$4.from(this.store);
	}
	get updateTracking() {
		return this._updateTracking;
	}
	get usedMemory() {
		return this.store.usedMemory;
	}
	get queryEngine() {
		if (!this._queryEngine) {
			if (!this.store || !this._strategy) return null;
			this._queryEngine = new W$1({
				featureStore: this.store,
				fieldsIndex: this.metadata.fieldsIndex,
				geometryType: this.metadata.geometryType,
				featureIdInfo: this.metadata.featureIdInfo,
				hasM: !1,
				hasZ: !1,
				spatialReference: this.metadata.outSpatialReference,
				aggregateAdapter: this._aggregateAdapter,
				timeInfo: this.metadata.timeInfo,
				definitionExpression: this._strategy?.definitionExpression,
				availableFields: this._strategy?.availableFields
			});
		}
		return this._queryEngine;
	}
	get isStream() {
		return "stream" === this._schema.type;
	}
	get hasQueryDisplayFilter() {
		if (!this._schema) return !1;
		switch (this._schema.type) {
			case "feature": return null != this._schema.strategy.full.displayFilterInfo;
			case "parquet":
			case "stream": return !1;
		}
	}
	chunks() {
		return Array.from(this.store.chunks());
	}
	prepareCacheUpdate(e, t) {
		const r = /* @__PURE__ */ new Set(), s = /* @__PURE__ */ new Set();
		for (const a of e) this._cachedObjectIds.has(a) || (this._cachedObjectIds.add(a), r.add(a));
		for (const a of t) this._cachedObjectIds.delete(a), s.add(a);
		this.hasQueryDisplayFilter && this._strategy.prepareCacheUpdate(r, s);
	}
	async applyCacheUpdate() {
		this.hasQueryDisplayFilter && await this._updateTracking.addPromise(this._strategy.applyCacheUpdate());
	}
	cleanup() {
		return this.store.cleanup();
	}
	onSubscribe(e) {
		if (this._connection.onEvent({
			type: "subscribe",
			tile: e.tile.id
		}), !this._strategy) return;
		const t = this._strategy.load(e);
		t.then(() => this._connection.onEvent({
			type: "loaded",
			tile: e.tile.id
		})).catch((t) => this._connection.onEvent({
			type: "error",
			tile: e.tile.id,
			error: t
		})), this._updateTracking.addPromise(t);
	}
	onResume(e) {
		this._updateTracking.addPromise(y$5(this._strategy?.load(e)));
	}
	onUnsubscribe(e) {
		this._connection.onEvent({
			type: "unsubscribe",
			tile: e.tile.id
		}), this._strategy?.unload(e);
	}
	async applyOverride(e) {
		await this._strategy?.applyOverride(e);
	}
	takeOverrideUpdate() {
		return this._strategy?.takeOverrideUpdate();
	}
	async update(r, s) {
		const a = this._schema;
		if (this._schema = r, this._queryEngine = u$2(this._queryEngine), a && a.type !== r.type) throw new Error("InternalError: Reconfiguring source types is not supported.");
		const i = new r$1();
		if (!a || i$10(a.service, r.service) || a.strategy.type !== r.strategy.type || i$10(r.strategy.full, a.strategy.full) || !await this._strategy.tryUpdate(i, r.strategy.partial)) {
			await this._updateStrategyType(this._schema.service, r, s);
			let e = null;
			return "postQueryFilter" in this._schema.strategy.full && this._schema.strategy.full.postQueryFilter && (e = await p$1.create({
				geometryType: this.metadata.geometryType,
				hasM: !1,
				hasZ: !1,
				timeInfo: this.metadata.timeInfo,
				fieldsIndex: this.metadata.fieldsIndex,
				spatialReference: this.metadata.outSpatialReference,
				filterJSON: this._schema.strategy.full.postQueryFilter
			})), await this.store.update({
				metadata: this.metadata,
				definitionExpression: this._schema.strategy.full.definitionExpression,
				postQueryFilter: e
			}), !0;
		}
		return i.requiresInvalidation;
	}
	unsafeSetQueryHistoricMoment(e) {
		"feature" === this._schema.type && (this._schema.strategy.full.historicMoment = e, this._strategy.unsafeSetQueryHistoricMoment(new Date(e)));
	}
	_initStreamMessenger() {
		this._streamMessenger ??= new r$2(this._connection);
	}
	async normalizeOverrides(e) {
		const t = {
			historicMoment: e.historicMoment,
			commands: {
				updateWeak: e.commands.updateWeak.map(o$6.fromJSON),
				removeWeak: e.commands.removeWeak,
				update: e.commands.update.map(o$6.fromJSON),
				remove: e.commands.remove,
				release: e.commands.release
			}
		}, r = e.commands.updateByIdWeak, s = await this._queryOptimizedFeatures(r);
		if (s.length !== r.length) {
			const e = new Set(s.map((e) => e.objectId));
			for (const s of r) e.has(s) || t.commands.removeWeak.push(s);
		}
		return t.commands.updateWeak.push(...s), t;
	}
	async _queryOptimizedFeatures(e) {
		if (0 === e.length) return [];
		const t = [], r = (await this._strategy.queryByObjectId(e, ["*"])).getCursor();
		for (; r.next();) t.push(r.readOptimizedFeatureWorldSpace());
		return t;
	}
	getObjectIdsFromGlobalIds(e) {
		const t = this.metadata.globalIdField;
		if (null == t) throw new Error("InternalError: Recieved an edit with globalIds, but not supported by the service");
		const r = this.store.mapObjectIdsFromGlobalIds(e, t).values();
		return Array.from(r);
	}
	async _updateStrategyType(e, t, r) {
		const a = this._createStrategy(e, t);
		this._connection.onEvent({
			type: "updateStrategyStart",
			about: a.about
		});
		const i = !!this._strategy;
		this.store.clear(), this._strategy?.destroy(), this._strategy = a, has("esri-2d-update-debug") && console.debug(`Version[${r}] FeatureSource.updateStrategy`, { strategy: a });
		const n = Array.from(this._subscriptions.values());
		if (!n.length) return void this._connection.onEvent({ type: "updateStrategyEnd" });
		const o = Promise.all(n.map((e) => this._strategy.load(e).then(() => this._connection.onEvent({
			type: "loaded",
			tile: e.tile.id
		})).catch((t) => this._connection.onEvent({
			type: "error",
			tile: e.tile.id,
			error: t
		}))));
		this._updateTracking.addPromise(o), this._strategy.prepareCacheUpdate(this._cachedObjectIds);
		try {
			i && await o;
		} catch (c) {
			m$6(c);
		}
		this._connection.onEvent({ type: "updateStrategyEnd" }), has("esri-2d-update-debug") && console.debug(`Version[${r}] FeatureSource.updateStrategyEnd`, { strategy: a });
	}
	_createStrategy(e, t) {
		const r = {
			connection: this._connection,
			store: this.store
		};
		switch (t.type) {
			case "feature": return this._createFeatureLoadStrategy(e, t.strategy, r);
			case "parquet": return this._createParquetLoadStrategy(e, t.strategy, r);
			case "stream": return this._createStreamLoadStrategy(e, t.strategy, r);
		}
	}
	_createFeatureLoadStrategy(e, t, r) {
		switch (t.type) {
			case "drill-down": return new n(e, t, r);
			case "paged-tile": return new c(e, t, r);
			case "snapshot": return new i$1(e, t, r);
		}
	}
	_createParquetLoadStrategy(e, t, r) {
		switch (t.type) {
			case "xz": return new y(e, t, r, this._connection);
			case "snapshot": return new d$1(e, t, r, this._connection);
		}
	}
	_createStreamLoadStrategy(e, t, r) {
		const s = new m$1(e, t, r);
		return this.streamMessenger.strategy = s, s;
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/sources/FeatureTileSubscription.js
var t = class {
	constructor(t, r) {
		this.tile = t, this.version = r, this._abortController = new AbortController();
	}
	get key() {
		return this.tile.key;
	}
	get signal() {
		return this._abortController.signal;
	}
	abort() {
		this._abortController.abort();
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/FeaturePipelineWorker.js
var m = class {
	constructor(e) {
		this.inner = e, this.resolver = $();
	}
};
var q = class {
	constructor() {
		this._aggregateAdapter = { getFeatureObjectIds: (e) => this._processor.getFeatureObjectIdsForAggregate(e) }, this._subscriptions = /* @__PURE__ */ new Map(), this._cachedObjectIds = /* @__PURE__ */ new Set(), this._updateRequested = !1, this._didSourceRefresh = !1, this._refreshRequested = !0, this._updateSubscriptionRequests = [], this._updateHighlightRequests = [];
	}
	destroy() {
		this._subscriptions.clear(), this._processor?.destroy(), this._source?.destroy(), this._handles?.remove(), this._updateOverridesRequest = null, this._tileInfoView = null;
	}
	onAttach(e) {
		has("esri-2d-update-debug") && console.debug("Pipeline.onAttach");
		const s = this._connection;
		this._tileInfoView = new h$5(z$2.fromJSON(e.tileInfoJSON)), this._viewExtent = e.initialViewExtent, this._source = new f(this._aggregateAdapter, this._subscriptions, s, this._cachedObjectIds), this._processor = new j(s, this._source, e.requiredLevel, e.requiredScale), this._handles = t$10([
			this._source.store.events.on("changed", () => this._requestUpdate()),
			this._source.store.events.on("refresh", () => this._requestRefresh()),
			l$7(() => this._source.updateTracking.updating, () => {
				this._requestUpdate(), y$5(this._connection.layerView.setUpdating({
					source: this._source.updateTracking.updating,
					pipeline: !0
				}));
			})
		]);
	}
	onDetach() {
		has("esri-2d-update-debug") && console.debug("Pipeline.onDetach"), this.destroy();
	}
	set remoteClient(e) {
		this._connection = new r$4(e);
	}
	get features() {
		const t = this._source?.queryEngine;
		if (!t) throw new r$5("no-queryEngine", "No query engine defined");
		return t;
	}
	get aggregates() {
		const t = this._processor?.aggregateQueryEngine;
		if (!t) throw new r$5("no-queryEngine", "No aggregate query engine defined");
		return t;
	}
	get streamMessenger() {
		return this._source.streamMessenger;
	}
	getUsedMemory() {
		return this._source.usedMemory + this._processor.usedMemory;
	}
	getDisplayFeatures(e) {
		return this._processor.getDisplayFeatures(e);
	}
	getDisplayIds(e) {
		return this._processor.getDisplayIds(e);
	}
	getObjectIdsFromGlobalIds(e) {
		return this._source.getObjectIdsFromGlobalIds(e);
	}
	updateStationaryExtent(e, t) {
		t && (this._refreshRequested = !0, this._requestUpdate()), this._viewExtent = e;
	}
	updateRequiredLevel(e, t) {
		this._processor.updateRequiredLevel(e, t), this._processor.hasClusters && this._requestUpdate();
	}
	async updateSchema(e, t) {
		return has("esri-2d-update-debug") && this._updateSchemaRequest && console.error("InternalError: Schema already updating"), this._updateSchemaRequest = new m({
			schema: e,
			version: t
		}), this._requestUpdate(), this._updateSchemaRequest.resolver.promise;
	}
	updateSubscriptions(e) {
		const t = new m(e);
		return this._updateSubscriptionRequests.push(t), this._requestUpdate(), t.resolver.promise;
	}
	updateHighlight(e) {
		const t = new m(e);
		return this._updateHighlightRequests.push(t), this._requestUpdate(), t.resolver.promise;
	}
	async onOverride(t) {
		if (null != this._updateOverridesRequest) throw new r$5("featurelayer", "InternalError - Already processing an edit");
		this._updateOverridesRequest = new m(t);
		const s = this._updateOverridesRequest.resolver.promise;
		return this._requestUpdate(), s;
	}
	queryStatistics() {
		return this._source.statistics.toJSON();
	}
	async queryVisibleFeatures(e, t) {
		return this.features.executeQuery(e, t);
	}
	async queryHeatmapStatistics(e) {
		const t = Math.round(u$3(e.radius));
		let s = Number.POSITIVE_INFINITY, i = Number.NEGATIVE_INFINITY;
		const r = "string" == typeof e.fieldOffset, o = e.fieldOffset ?? 0, n = Array.from(this._subscriptions.values()), u = this._source.chunks(), d = t ** 2, c = 3 / (Math.PI * d), p = 2 * t, l = Math.ceil(512 / p);
		for (const a of n) {
			const t = a.tile, n = new Float64Array(l * l);
			for (const s of u) {
				const i = s.getTileReader(t);
				if (!i) continue;
				const u = i.getCursor();
				for (; u.next();) {
					let t = 1;
					if (null != e.field) {
						const s = u.readAttribute(e.field);
						t = r ? -1 * +s : +s + o;
					}
					const s = u.readXForDisplay() / p, i = u.readYForDisplay() / p, a = Math.floor(s), h = Math.floor(i);
					if (a < 0 || h < 0 || a >= l || h >= l) continue;
					const _ = ((.5 + a - s) * p) ** 2 + ((.5 + h - i) * p) ** 2;
					if (_ > d) continue;
					const g = t * (c * (1 - _ / d) ** 2);
					n[h + a * l] += g;
				}
			}
			for (let e = 0; e < n.length; e++) s = Math.min(s, n[e]), i = Math.max(i, n[e]);
		}
		return {
			max: i,
			min: s
		};
	}
	async getSampleFeatures(e) {
		const t = this._source.chunks();
		if (t.reduce((e, t) => e + t.size(), 0) <= e.minFeatureCount) {
			if (!this._source.updateTracking.updating) {
				const e = [];
				return this._source.store.forEachUnsafe((t) => e.push(t.readLegacyFeatureWorldSpace())), e;
			}
			return null;
		}
		const s = /* @__PURE__ */ new Set(), i = [], r = t.map((e) => e.reader.getCursor()), o = new t$9(), u = 3 * e.sampleSize;
		for (let n = 0; n < u && i.length < e.sampleSize; n++) {
			const e = r[o.getIntRange(0, t.length - 1)];
			if (0 === e.getSize()) continue;
			const n = o.getIntRange(0, e.getSize() - 1);
			e.setIndex(n);
			const u = e.getObjectId();
			s.has(u) || (s.add(u), i.push(e.readLegacyFeatureWorldSpace()));
		}
		return i.length >= e.sampleSize ? i : null;
	}
	_requestUpdate() {
		this._updateRequested || (this._updateRequested = !0, t$11(() => this._scheduleNextUpdate()));
	}
	_requestRefresh() {
		this._didSourceRefresh = !0, this._requestUpdate();
	}
	_scheduleNextUpdate() {
		this._updateRequested && (this._ongoingUpdate || (this._ongoingUpdate = l$8(this._doUpdate()).finally(() => {
			this._ongoingUpdate = null, this._scheduleNextUpdate();
		}), this._updateRequested = !1));
	}
	_subscribe(e) {
		const t$15 = e.tileId;
		if (this._subscriptions.has(t$15)) return;
		has("esri-2d-update-debug") && console.debug(`Tile[${t$15}] Pipeline.subscribe`);
		const i = new t(new a$6(this._tileInfoView, t$15), e.version);
		this._subscriptions.set(t$15, i), this._source.onSubscribe(i), this._processor.onSubscribe(i);
	}
	_unsubscribe(e) {
		const t = this._subscriptions.get(e);
		t && (has("esri-2d-update-debug") && console.debug(`Tile[${e}] Pipeline.unsubscribe`), t.abort(), this._source.onUnsubscribe(t), this._processor.onUnsubscribe(t), this._subscriptions.delete(t.key.id));
	}
	async _doUpdate() {
		if (has("esri-2d-update-debug") && console.debug("Pipeline._doUpdateStart"), await this._connection.layerView.setUpdating({
			source: this._source.updateTracking.updating,
			pipeline: !0
		}), this._updateSubscriptionRequests.length) {
			const e = this._updateSubscriptionRequests;
			this._updateSubscriptionRequests = [];
			for (const t of e) this._doUpdateSubscriptions(t.inner), t.resolver.resolve();
		}
		if (this._updateHighlightRequests.length) {
			const e = this._updateHighlightRequests, t = /* @__PURE__ */ new Set(), s = /* @__PURE__ */ new Set();
			for (const i of e) for (const { objectId: e, highlightFlags: r } of i.inner.highlights) r ? (t.add(e), s.delete(e)) : (s.add(e), t.delete(e));
			this._source.prepareCacheUpdate(t, s);
		}
		const e = this._updateSchemaRequest;
		this._updateSchemaRequest = null;
		let t = !1;
		if (null != e) {
			const { schema: s, version: i } = e.inner;
			t = await this._doUpdateSchema(s, i);
		}
		this._processor.requiresInvalidation() && (t = !0), this._didSourceRefresh && (t = !0, this._didSourceRefresh = !1), this._refreshRequested && (this._refreshRequested = !1, t = !0), t && (this._processor.invalidate(), await this._connection.container.updateRenderState(this._processor.version));
		const s = this._updateOverridesRequest;
		if (this._updateOverridesRequest = null, null != s) {
			has("esri-2d-update-debug") && console.debug("Pipeline.applyOverride", s.inner), null != s.inner.historicMoment && this._source.unsafeSetQueryHistoricMoment(s.inner.historicMoment);
			const e = await this._source.normalizeOverrides(s.inner);
			await this._source.applyOverride(e), has("esri-2d-update-debug") && console.debug("Pipeline.endOverride", s.inner);
		}
		if (await this._source.applyCacheUpdate(), this._updateHighlightRequests.length) {
			const e = this._updateHighlightRequests;
			this._updateHighlightRequests = [];
			for (const t of e) this._processor.updateHighlight(t.inner), t.resolver.resolve();
		}
		const r = this._source.cleanup();
		this._processor.removeChunks(r);
		try {
			const e = this._source.takeOverrideUpdate();
			if (null != e && this._subscriptions.size) {
				has("esri-2d-update-debug") && console.debug("Pipeline.applyOverrideChangesStart"), await this._connection.container.lockForOverrides();
				try {
					await this._processor.applyOverrideUpdate(e, this._viewExtent);
				} catch (o) {
					has("esri-2d-update-debug") && console.debug("InternalError", o);
				}
				await this._connection.container.unlockForOverrides(), has("esri-2d-update-debug") && console.debug("Pipeline.applyOverrideChangesEnd");
			}
			this._subscriptions.size && (has("esri-2d-update-debug") && console.debug("Pipeline.updateChunksStart"), await this._processor.updateChunks(this._viewExtent), has("esri-2d-update-debug") && console.debug("Pipeline.updateChunksEnd"));
		} catch (o) {
			m$6(o);
		}
		s?.resolver.resolve(), e?.resolver.resolve(), null == e && t && await this._connection.container.trySwapRenderState(), this._connection.onEvent({
			type: "performance",
			usedMemory: this.getUsedMemory()
		}), this._updateRequested ? (has("esri-2d-update-debug") && console.debug("Pipeline._doUpdateEnd [updateRequested=true]"), await this._connection.layerView.setUpdating({
			source: this._source.updateTracking.updating,
			pipeline: !0
		})) : (has("esri-2d-update-debug") && console.debug("Pipeline._doUpdateEnd [updateRequested=false, After flush]"), await this._connection.layerView.setUpdating({
			source: this._source.updateTracking.updating,
			pipeline: this._updateRequested
		}));
	}
	async _doUpdateSchema(e, t) {
		has("esri-2d-update-debug") && console.debug(`Version[${t}] Pipeline.updateStart`, { schema: e });
		const s = { tileInfo: this._tileInfoView?.tileInfo }, i = await this._source.update(e.source, t), r = Array.from(this._subscriptions.values()), o = this._processor.update(e, t, s, i, r);
		return has("esri-2d-update-debug") && console.debug(`Version[${t}] Pipeline.updateEnd`), o;
	}
	_doUpdateSubscriptions(e) {
		has("esri-2d-update-debug") && console.debug("Pipeline.updateSubscriptions", e);
		for (const t of e.subscribe) this._subscribe(t);
		for (const t of e.unsubscribe) this._unsubscribe(t);
	}
};
//#endregion
export { q as default };

//# sourceMappingURL=FeaturePipelineWorker-CmtXGTNw.js.map