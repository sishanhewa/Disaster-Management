import { o as l$1 } from "./Error-CzxduO2m.js";
import "./typedArrayUtil-BAuNmygZ.js";
import "./request-CuG5cxow.js";
import { D as n$3, d as a$2, f as d$2 } from "./promiseUtils-DhYhergm.js";
import "./decorators-DE7S5xmd.js";
import { O as t$1 } from "./Accessor-kDoDKy4v.js";
import "./tracking-DBoczQof.js";
import "./ObservableBase-Bz6iwe5p.js";
import "./scheduling-DiUcWka1.js";
import "./PooledArray-ChtfzjBt.js";
import "./JSONSupport-BUaD4jSd.js";
import "./jsonMap-CFSDFmi6.js";
import "./Ellipsoid-DzO_iHAj.js";
import "./assets-BZbzeyNa.js";
import "./pe-BLztJ5xc.js";
import "./spatialReferenceUtils-b3vCEkpS.js";
import "./units-Dg-cK1vO.js";
import "./SpatialReference-rIfb2LrD.js";
import "./Point-B7zMqEx6.js";
import "./Extent-CquIzaXp.js";
import "./colorUtils-BC0_8aMM.js";
import "./mathUtils-hEBUcrMa.js";
import "./Color-C99QAF80.js";
import "./curveUtils-CfkOAT4m.js";
import "./coordsUtils-DXLB9bAf.js";
import "./aaBoundingRect-CgUWvAgv.js";
import "./common-BxLRDsKd.js";
import "./vec4-DVix-cmy.js";
import "./vec4f64-SXri5KT8.js";
import "./unitBezier-LifOR91C.js";
import "./definitions-BxssUXCo.js";
import "./enums-DUaXkkTm.js";
import "./memoryEstimations-BBFGLDPz.js";
import { t as e$3 } from "./earcut-CCI_bFcR.js";
import { t as n$4 } from "./pbf-Czx4fZ_r.js";
import "./colorUtils-RKWmAehh.js";
import { t as t$2 } from "./constants-30dzvtQT.js";
import { n as i$1, r as n$5, t as h$2 } from "./TileClipper-Cgjm662l.js";
import { c as f, d as o$1, f as p$1, i as a$3, l as h$3, m as s$2, o as d$3, p as r$5, s as e$4 } from "./GeometryUtils-B-zPj-EF.js";
import { c as m$2, l as s$3, u as x$1 } from "./utils-DtAoCWzC.js";
import "./AlignedVertexSpec-DByvPz6j.js";
import { i as V, r as T, t as l$2 } from "./StyleRepository-BmFgOHrz.js";
import { t as t$3 } from "./Rect-CJZWHuKk.js";
import { t as C } from "./BidiEngine-Dr3hg5XU.js";
import { n as f$1, t as c$2 } from "./libtess-DgzzZQ3y.js";
import { n as t$4, t as t$5 } from "./SourceLayerData-B61N1sGa.js";
import { t as _ } from "./TurboLine-BNuOc56H.js";
//#region node_modules/@arcgis/core/views/2d/engine/vectorTiles/ScriptUtils.js
function e$2(e) {
	return 746 === e || 747 === e || !(e < 4352) && (e >= 12704 && e <= 12735 || e >= 12544 && e <= 12591 || e >= 65072 && e <= 65103 && !(e >= 65097 && e <= 65103) || e >= 63744 && e <= 64255 || e >= 13056 && e <= 13311 || e >= 11904 && e <= 12031 || e >= 12736 && e <= 12783 || e >= 12288 && e <= 12351 && !(e >= 12296 && e <= 12305 || e >= 12308 && e <= 12319 || 12336 === e) || e >= 13312 && e <= 19903 || e >= 19968 && e <= 40959 || e >= 12800 && e <= 13055 || e >= 12592 && e <= 12687 || e >= 43360 && e <= 43391 || e >= 55216 && e <= 55295 || e >= 4352 && e <= 4607 || e >= 44032 && e <= 55215 || e >= 12352 && e <= 12447 || e >= 12272 && e <= 12287 || e >= 12688 && e <= 12703 || e >= 12032 && e <= 12255 || e >= 12784 && e <= 12799 || e >= 12448 && e <= 12543 && 12540 !== e || e >= 65280 && e <= 65519 && !(65288 === e || 65289 === e || 65293 === e || e >= 65306 && e <= 65310 || 65339 === e || 65341 === e || 65343 === e || e >= 65371 && e <= 65503 || 65507 === e || e >= 65512 && e <= 65519) || e >= 65104 && e <= 65135 && !(e >= 65112 && e <= 65118 || e >= 65123 && e <= 65126) || e >= 5120 && e <= 5759 || e >= 6320 && e <= 6399 || e >= 65040 && e <= 65055 || e >= 19904 && e <= 19967 || e >= 40960 && e <= 42127 || e >= 42128 && e <= 42191);
}
function c$1(e) {
	return !(e < 11904) && (e >= 12704 && e <= 12735 || e >= 12544 && e <= 12591 || e >= 65072 && e <= 65103 || e >= 63744 && e <= 64255 || e >= 13056 && e <= 13311 || e >= 11904 && e <= 12031 || e >= 12736 && e <= 12783 || e >= 12288 && e <= 12351 || e >= 13312 && e <= 19903 || e >= 19968 && e <= 40959 || e >= 12800 && e <= 13055 || e >= 65280 && e <= 65519 || e >= 12352 && e <= 12447 || e >= 12272 && e <= 12287 || e >= 12032 && e <= 12255 || e >= 12784 && e <= 12799 || e >= 12448 && e <= 12543 || e >= 65040 && e <= 65055 || e >= 42128 && e <= 42191 || e >= 40960 && e <= 42127);
}
function s$1(e) {
	switch (e) {
		case 10:
		case 32:
		case 38:
		case 40:
		case 41:
		case 43:
		case 45:
		case 47:
		case 173:
		case 183:
		case 8203:
		case 8208:
		case 8211:
		case 8231: return !0;
	}
	return !1;
}
function a$1(e) {
	switch (e) {
		case 9:
		case 10:
		case 11:
		case 12:
		case 13:
		case 32: return !0;
	}
	return !1;
}
var l = class {
	constructor(t, e, i, o, s, c, h) {
		this._glyphItems = t, this._maxWidth = e, this._lineHeight = i, this._letterSpacing = o, this._hAnchor = s, this._vAnchor = c, this._justify = h;
	}
	getShaping(s, c, h) {
		const l = this._letterSpacing, n = this._lineHeight, a = this._justify, r = this._maxWidth, m = [];
		let f = 0, p = 0;
		for (const t of s) {
			const e = t.codePointAt(0);
			if (null == e) continue;
			const i = h && e$2(e);
			let s;
			for (const t of this._glyphItems) if (s = t[e], s) break;
			m.push({
				codePoint: e,
				x: f,
				y: p,
				vertical: i,
				glyphMosaicItem: s
			}), s && (f += s.metrics.advance + l);
		}
		let g = f;
		if (r > 0) g = f / Math.max(1, Math.ceil(f / r));
		const y = s.includes("​"), d = [], x = m.length;
		for (let e = 0; e < x - 1; e++) {
			const o = m[e].codePoint, s = c$1(o);
			if (s$1(o) || s) {
				let t = 0;
				if (10 === o) t -= 1e4;
				else if (s && y) t += 150;
				else {
					40 !== o && 65288 !== o || (t += 50);
					const i = m[e + 1].codePoint;
					41 !== i && 65289 !== i || (t += 50);
				}
				d.push(this._buildBreak(e + 1, m[e].x, g, d, t, !1));
			}
		}
		const u = this._optimalBreaks(this._buildBreak(x, f, g, d, 0, !0));
		let M = 0;
		const _ = c ? -n : n;
		let I = 0;
		for (let t = 0; t < u.length; t++) {
			const i = u[t];
			let o = I;
			for (; o < i && a$1(m[o].codePoint);) m[o].glyphMosaicItem = null, ++o;
			let s = i - 1;
			for (; s > o && a$1(m[s].codePoint);) m[s].glyphMosaicItem = null, --s;
			if (o <= s) {
				const t = m[o].x;
				for (let i = o; i <= s; i++) m[i].x -= t, m[i].y = p;
				let e = m[s].x;
				m[s].glyphMosaicItem && (e += m[s].glyphMosaicItem.metrics.advance), M = Math.max(e, M), a && this._applyJustification(m, o, s);
			}
			I = i, p += _;
		}
		if (m.length > 0) {
			const t = u.length - 1, e = (a - this._hAnchor) * M;
			let i = (-this._vAnchor * (t + 1) + .5) * n;
			c && t && (i += t * n);
			for (const o of m) o.x += e, o.y += i;
		}
		return m.filter((t) => t.glyphMosaicItem);
	}
	static getTextBox(t, e) {
		if (!t.length) return null;
		let i = Infinity, o = Infinity, s = 0, c = 0;
		for (const l of t) {
			const t = l.glyphMosaicItem.metrics.advance, n = l.x, a = l.y - 17, r = n + t, m = a + e;
			i = Math.min(i, n), s = Math.max(s, r), o = Math.min(o, a), c = Math.max(c, m);
		}
		return {
			x: i,
			y: o,
			width: s - i,
			height: c - o
		};
	}
	static getBox(t) {
		if (!t.length) return null;
		let e = Infinity, i = Infinity, o = 0, s = 0;
		for (const c of t) {
			const { height: t, left: h, top: l, width: n } = c.glyphMosaicItem.metrics, a = c.x, r = c.y - (t - Math.abs(l)), m = a + n + h, f = r + t;
			e = Math.min(e, a), o = Math.max(o, m), i = Math.min(i, r), s = Math.max(s, f);
		}
		return {
			x: e,
			y: i,
			width: o - e,
			height: s - i
		};
	}
	static addDecoration(t, e) {
		const i = t.length;
		if (0 === i) return;
		const o = 3;
		let c = t[0].x + t[0].glyphMosaicItem.metrics.left, h = t[0].y;
		for (let n = 1; n < i; n++) {
			const i = t[n];
			if (i.y !== h) {
				const l = t[n - 1].x + t[n - 1].glyphMosaicItem.metrics.left + t[n - 1].glyphMosaicItem.metrics.width;
				t.push({
					codePoint: 0,
					x: c,
					y: h + e - o,
					vertical: !1,
					glyphMosaicItem: {
						sdf: !0,
						rect: new t$3(4, 0, 4, 8),
						metrics: {
							width: l - c,
							height: 2 + 2 * o,
							left: 0,
							top: 0,
							advance: 0
						},
						page: 0,
						code: 0
					}
				}), h = i.y, c = i.x + i.glyphMosaicItem.metrics.left;
			}
		}
		const l = t[i - 1].x + t[i - 1].glyphMosaicItem.metrics.left + t[i - 1].glyphMosaicItem.metrics.width;
		t.push({
			codePoint: 0,
			x: c,
			y: h + e - o,
			vertical: !1,
			glyphMosaicItem: {
				sdf: !0,
				rect: new t$3(4, 0, 4, 8),
				metrics: {
					width: l - c,
					height: 2 + 2 * o,
					left: 0,
					top: 0,
					advance: 0
				},
				page: 0,
				code: 0
			}
		});
	}
	_breakScore(t, e, i, o) {
		const s = (t - e) * (t - e);
		return o ? t < e ? s / 2 : 2 * s : s + Math.abs(i) * i;
	}
	_buildBreak(t, e, i, o, s, c) {
		let h = null, l = this._breakScore(e, i, s, c);
		for (const n of o) {
			const t = e - n.x, o = this._breakScore(t, i, s, c) + n.score;
			o <= l && (h = n, l = o);
		}
		return {
			index: t,
			x: e,
			score: l,
			previousBreak: h
		};
	}
	_optimalBreaks(t) {
		return t ? this._optimalBreaks(t.previousBreak).concat(t.index) : [];
	}
	_applyJustification(t, e, i) {
		const o = t[i], s = o.vertical ? 24 : o.glyphMosaicItem ? o.glyphMosaicItem.metrics.advance : 0, h = (o.x + s) * this._justify;
		for (let c = e; c <= i; c++) t[c].x -= h;
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/engine/vectorTiles/Placement.js
var g = .5, x = 2;
var m$1 = class {
	constructor(e, t, i = 0, n = -1, s = g) {
		this.x = e, this.y = t, this.angle = i, this.segment = n, this.minzoom = s;
	}
};
var d$1 = class {
	constructor(e, t, i, n, a, o = g, h = r$5) {
		this.anchor = e, this.labelAngle = t, this.glyphAngle = i, this.page = n, this.alternateVerticalGlyph = a, this.minzoom = o, this.maxzoom = h;
	}
};
var p = class {
	constructor(e, t, i, n, s, a, o, h, l, r, c, g) {
		this.tl = e, this.tr = t, this.bl = i, this.br = n, this.mosaicRect = s, this.labelAngle = a, this.minAngle = o, this.maxAngle = h, this.anchor = l, this.minzoom = r, this.maxzoom = c, this.page = g;
	}
};
var w = class {
	constructor(e) {
		this.shapes = e;
	}
};
var y$1 = class {
	getIconPlacement(t, s, a) {
		const o = new i$1(t.x, t.y), h = 0 === a.rotationAlignment, l = a.keepUpright;
		let r = a.rotate * s$2;
		h && (r += t.angle);
		const c = new w([]);
		return c.iconColliders = [], this._addIconPlacement(c, o, s, a, r), h && l && this._addIconPlacement(c, o, s, a, r + o$1), c;
	}
	_addIconPlacement(t, i, n, a, o) {
		const h = n.rasterizationScale, l = n.width / h, r = n.height / h, c = a.offset;
		let x = c[0], m = c[1];
		switch (a.anchor) {
			case 0:
				x -= l / 2, m -= r / 2;
				break;
			case 1:
				m -= r / 2;
				break;
			case 2:
				x -= l, m -= r / 2;
				break;
			case 3:
				x -= l / 2;
				break;
			case 4:
				x -= l / 2, m -= r;
				break;
			case 5: break;
			case 7:
				m -= r;
				break;
			case 6:
				x -= l;
				break;
			case 8: x -= l, m -= r;
		}
		const d = n.rect, w = 2 / h, y = x - w, f = m - w, b = y + d.width / h, u = f + d.height / h, I = new i$1(y, f), T = new i$1(b, u), k = new i$1(y, u), P = new i$1(b, f);
		if (0 !== o) {
			const e = Math.cos(o), t = Math.sin(o);
			I.rotate(e, t), T.rotate(e, t), k.rotate(e, t), P.rotate(e, t);
		}
		const N = new p(I, P, k, T, d, o, 0, 256, i, g, r$5, 0);
		t.shapes.push(N);
		{
			const e = a.size, n = a.padding, h = {
				xTile: i.x,
				yTile: i.y,
				dxPixels: x * e - n,
				dyPixels: m * e - n,
				hard: !a.optional,
				partIndex: 0,
				width: l * e + 2 * n,
				height: r * e + 2 * n,
				angle: o,
				minLod: g,
				maxLod: r$5
			};
			t.iconColliders.push(h);
		}
	}
	getTextPlacement(a, o, h, m) {
		const y = new i$1(a.x, a.y), { keepUpright: f, rotate: b, rotationAlignment: u, size: I } = m, T = b * s$2, k = 0 === u, P = f, N = m.padding;
		let z = g;
		const A = !k ? 0 : a.angle, _ = a.segment >= 0 && k, G = [], M = [], E = 4, L = !_;
		let V = Number.POSITIVE_INFINITY, v = Number.NEGATIVE_INFINITY, j = V, C = v;
		const F = (_ || k) && P, S = I / 24;
		let Y = !1;
		for (const e of o) if (e.vertical) {
			Y = !0;
			break;
		}
		let q, U = 0, O = 0;
		if (!_ && Y) {
			const e = l.getTextBox(o, m.lineHeight * 24);
			switch (m.anchor) {
				case 1:
					U = e.height / 2, O = -e.width / 2;
					break;
				case 2:
					U = -e.height / 2, O = e.width / 2;
					break;
				case 3:
					U = e.height / 2, O = e.width / 2;
					break;
				case 4:
					U = -e.height / 2, O = -e.width / 2;
					break;
				case 5:
					U = e.height;
					break;
				case 7:
					O = -e.width;
					break;
				case 6:
					O = e.width;
					break;
				case 8: U = -e.height;
			}
		}
		U += m.offset[0] * 24, O += m.offset[1] * 24;
		for (const i of o) {
			const o = i.glyphMosaicItem;
			if (!o || o.rect.isEmpty) continue;
			const l = o.rect, r = o.metrics, w = o.page;
			if (G && L) {
				if (void 0 !== q && q !== i.y) {
					let e, t, i, n;
					Y ? (e = -C + U, t = V + O, i = C - j, n = v - V) : (e = V + U, t = j + O, i = v - V, n = C - j);
					const o = {
						xTile: a.x,
						yTile: a.y,
						dxPixels: e * S - N,
						dyPixels: t * S - N,
						hard: !m.optional,
						partIndex: 1,
						width: i * S + 2 * N,
						height: n * S + 2 * N,
						angle: T,
						minLod: g,
						maxLod: r$5
					};
					G.push(o), V = Number.POSITIVE_INFINITY, v = Number.NEGATIVE_INFINITY, j = V, C = v;
				}
				q = i.y;
			}
			const f = [];
			if (_) {
				const e = .5 * o.metrics.width, n = (i.x + r.left - E + e) * S * 8;
				if (z = this._placeGlyph(a, z, n, h, a.segment, 1, i.vertical, w, f), P && (z = this._placeGlyph(a, z, n, h, a.segment, -1, i.vertical, w, f)), z >= x) break;
			} else f.push(new d$1(y, A, A, w, !1)), k && P && f.push(new d$1(y, A + o$1, A + o$1, w, !1));
			const b = i.x + r.left, u = i.y - 17 - r.top, I = b + r.width, B = u + r.height;
			let H, R, D, J, K, Q, W, X;
			if (!_ && Y) if (i.vertical) {
				const t = (b + I) / 2 - r.height / 2;
				H = new i$1(-((u + B) / 2 + r.width / 2) - E + U, t - E + O), R = new i$1(H.x + l.width, H.y + l.height), D = new i$1(H.x, R.y), J = new i$1(R.x, H.y);
			} else H = new i$1(-u + E + U, b - E + O), R = new i$1(H.x - l.height, H.y + l.width), D = new i$1(R.x, H.y), J = new i$1(H.x, R.y);
			else H = new i$1(b - E + U, u - E + O), R = new i$1(H.x + l.width, H.y + l.height), D = new i$1(H.x, R.y), J = new i$1(R.x, H.y);
			for (const t of f) {
				let n, s, o, h;
				if (t.alternateVerticalGlyph) {
					if (!K) {
						const t = (u + B) / 2 + O;
						K = new i$1((b + I) / 2 + U - r.height / 2 - E, t + r.width / 2 + E), Q = new i$1(K.x + l.height, K.y - l.width), W = new i$1(Q.x, K.y), X = new i$1(K.x, Q.y);
					}
					n = K, s = W, o = X, h = Q;
				} else n = H, s = D, o = J, h = R;
				const c = u, g = B, d = t.glyphAngle + T;
				if (0 !== d) {
					const e = Math.cos(d), t = Math.sin(d);
					n = n.clone(), s = s?.clone(), o = o?.clone(), h = h?.clone(), n.rotate(e, t), h?.rotate(e, t), s?.rotate(e, t), o?.rotate(e, t);
				}
				let w = 0, y = 256;
				if (_ && Y ? i.vertical ? t.alternateVerticalGlyph ? (w = 32, y = 96) : (w = 224, y = 32) : (w = 224, y = 96) : (w = 192, y = 64), M.push(new p(n, o, s, h, l, t.labelAngle, w, y, t.anchor, t.minzoom, t.maxzoom, t.page)), G && (!F || this._legible(t.labelAngle))) {
					if (L) b < V && (V = b), c < j && (j = c), I > v && (v = I), g > C && (C = g);
					else if (t.minzoom < x) {
						const e = {
							xTile: a.x,
							yTile: a.y,
							dxPixels: (b + U) * S - N,
							dyPixels: (c + U) * S - N,
							hard: !m.optional,
							partIndex: 1,
							width: (I - b) * S + 2 * N,
							height: (g - c) * S + 2 * N,
							angle: d,
							minLod: t.minzoom,
							maxLod: t.maxzoom
						};
						G.push(e);
					}
				}
			}
		}
		if (z >= x) return null;
		if (G && L) {
			let e, t, i, n;
			Y ? (e = -C + U, t = V + O, i = C - j, n = v - V) : (e = V + U, t = j + O, i = v - V, n = C - j);
			const o = {
				xTile: a.x,
				yTile: a.y,
				dxPixels: e * S - N,
				dyPixels: t * S - N,
				hard: !m.optional,
				partIndex: 1,
				width: i * S + 2 * N,
				height: n * S + 2 * N,
				angle: T,
				minLod: g,
				maxLod: r$5
			};
			G.push(o);
		}
		const B = new w(M);
		return G && G.length > 0 && (B.textColliders = G), B;
	}
	_legible(e) {
		const t = f(e);
		return t < 65 || t >= 193;
	}
	_placeGlyph(t, i, o, l, r, c, g, x, m) {
		let p = c;
		const w = p < 0 ? a$3(t.angle + o$1, e$4) : t.angle;
		let y = 0;
		o < 0 && (p *= -1, o *= -1, y = o$1), p > 0 && ++r;
		let f = new i$1(t.x, t.y), b = l[r], u = r$5;
		if (l.length <= r) return u;
		for (;;) {
			const e = b.x - f.x, t = b.y - f.y, n = Math.sqrt(e * e + t * t), s = Math.max(o / n, i), c = e / n, I = t / n, T = a$3(Math.atan2(I, c) + y, e$4);
			if (m.push(new d$1(f, w, T, x, !1, s, u)), g && m.push(new d$1(f, w, T, x, !0, s, u)), s <= i) return s;
			f = b.clone();
			do {
				if (r += p, l.length <= r || r < 0) return s;
				b = l[r];
			} while (f.isEqual(b));
			let k = b.x - f.x, P = b.y - f.y;
			const N = Math.sqrt(k * k + P * P);
			k *= n / N, P *= n / N, f.x -= k, f.y -= P, u = s;
		}
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/engine/vectorTiles/MemoryBuffer.js
var r$4 = class {
	constructor(r) {
		this._array = [], this._stride = r;
	}
	get array() {
		return this._array;
	}
	get index() {
		return 4 * this._array.length / this._stride;
	}
	get itemSize() {
		return this._stride;
	}
	get sizeInBytes() {
		return 4 * this._array.length;
	}
	reset() {
		this.array.length = 0;
	}
	toBuffer() {
		return new Uint32Array(this._array).buffer;
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/engine/vectorTiles/IndexMemoryBuffer.js
var r$3 = class extends r$4 {
	constructor() {
		super(12);
	}
	add(s, r, t) {
		const e = this.array;
		e.push(s), e.push(r), e.push(t);
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/engine/vectorTiles/VertexMemoryBuffer.js
var h = class extends r$4 {
	constructor(s) {
		super(s);
	}
	add(s, t, o, h, a, n, d, e, p, c, M, i) {
		const l = this.array;
		let m = s$3(s, t);
		l.push(m);
		const x = 31;
		m = m$2(Math.round(x * o), Math.round(x * h), Math.round(x * a), Math.round(x * n)), l.push(m), m = m$2(Math.round(x * d), Math.round(x * e), Math.round(x * p), Math.round(x * c)), l.push(m), m = s$3(M, 0), l.push(m), i && l.push(...i);
	}
};
var a = class extends r$4 {
	constructor(s) {
		super(s);
	}
	add(s, t, u) {
		const o = this.array;
		o.push(s$3(s, t)), u && o.push(...u);
	}
};
var n$2 = class extends r$4 {
	constructor(s) {
		super(s);
	}
	add(s, t, o, h, a, n, d) {
		const e = this.array, p = this.index;
		let c = s$3(s, t);
		e.push(c);
		const M = 15;
		return c = m$2(Math.round(M * o), Math.round(M * h), a, n), e.push(c), d && e.push(...d), p;
	}
};
var d = class extends r$4 {
	constructor(s) {
		super(s);
	}
	add(t, u, h, a, n, d, e, p, c, M, i, l) {
		const m = this.array;
		let x = s$3(t, u);
		m.push(x), x = s$3(Math.round(8 * h), Math.round(8 * a)), m.push(x), x = s$3(x$1(n / 4, d / 4), x$1(p, c)), m.push(x), x = s$3(x$1(0, f(e)), x$1(10 * M, Math.min(10 * i, 255))), m.push(x), l && m.push(...l);
	}
};
var e$1 = class extends r$4 {
	constructor(s) {
		super(s);
	}
	add(s, t, u, o, h) {
		const a = this.array, n = s$3(2 * s + u, 2 * t + o);
		a.push(n), h && a.push(...h);
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/engine/vectorTiles/buckets/BaseBucket.js
var e = class {
	constructor(e, s, r) {
		this.layerExtent = t$2, this._features = [], this.layer = e, this.zoom = s, this._spriteInfo = r, this._filter = e.getFeatureFilter();
	}
	pushFeature(t) {
		this._filter && !this._filter.filter(t, this.zoom) || this._features.push(t);
	}
	hasFeatures() {
		return this._features.length > 0;
	}
	getResources(t, e, s) {}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/engine/vectorTiles/buckets/CircleBucket.js
var t = class extends e {
	constructor(e, t, r, i, c) {
		super(e, t, r), this.type = 4, this._circleVertexBuffer = i, this._circleIndexBuffer = c;
	}
	get circleIndexStart() {
		return this._circleIndexStart;
	}
	get circleIndexCount() {
		return this._circleIndexCount;
	}
	processFeatures(e) {
		const t = this._circleVertexBuffer, r = this._circleIndexBuffer;
		this._circleIndexStart = 3 * r.index, this._circleIndexCount = 0;
		const i = this.layer, c = this.zoom;
		e && e.setExtent(this.layerExtent);
		for (const s of this._features) {
			const n = s.getGeometry(e);
			if (!n) continue;
			const l = i.circleMaterial.encodeAttributes(s, c, i);
			for (const e of n) if (e) for (const i of e) {
				const e = t.index;
				t.add(i.x, i.y, 0, 0, l), t.add(i.x, i.y, 0, 1, l), t.add(i.x, i.y, 1, 0, l), t.add(i.x, i.y, 1, 1, l), r.add(e, e + 1, e + 2), r.add(e + 1, e + 2, e + 3), this._circleIndexCount += 6;
			}
		}
	}
	serialize() {
		let e = 6;
		e += this.layerUIDs.length, e += this._circleVertexBuffer.array.length, e += this._circleIndexBuffer.array.length;
		const t = new Uint32Array(e), r = new Int32Array(t.buffer);
		let i = 0;
		t[i++] = this.type, t[i++] = this.layerUIDs.length;
		for (let c = 0; c < this.layerUIDs.length; c++) t[i++] = this.layerUIDs[c];
		t[i++] = this._circleIndexStart, t[i++] = this._circleIndexCount, t[i++] = this._circleVertexBuffer.array.length;
		for (let c = 0; c < this._circleVertexBuffer.array.length; c++) r[i++] = this._circleVertexBuffer.array[c];
		t[i++] = this._circleIndexBuffer.array.length;
		for (let c = 0; c < this._circleIndexBuffer.array.length; c++) t[i++] = this._circleIndexBuffer.array[c];
		return t.buffer;
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/engine/vectorTiles/buckets/FillBucket.js
var n$1 = class n$1 extends e {
	constructor(t, e, i, r, l, n, s) {
		super(t, e, i), this.type = 1, this._patternMap = /* @__PURE__ */ new Map(), this._fillVertexBuffer = r, this._fillIndexBuffer = l, this._outlineVertexBuffer = n, this._outlineIndexBuffer = s;
	}
	get fillIndexStart() {
		return this._fillIndexStart;
	}
	get fillIndexCount() {
		return this._fillIndexCount;
	}
	get outlineIndexStart() {
		return this._outlineIndexStart;
	}
	get outlineIndexCount() {
		return this._outlineIndexCount;
	}
	getResources(t, e, i) {
		const r = this.layer, l = this.zoom, n = r.getPaintProperty("fill-pattern");
		if (n) if (n.isDataDriven) for (const s of this._features) e(n.getValue(l, s), !0);
		else e(n.getValue(l), !0);
	}
	processFeatures(t) {
		this._fillIndexStart = 3 * this._fillIndexBuffer.index, this._fillIndexCount = 0, this._outlineIndexStart = 3 * this._outlineIndexBuffer.index, this._outlineIndexCount = 0;
		const e = this.layer, i = this.zoom, { fillMaterial: r, outlineMaterial: l, hasDataDrivenFill: n, hasDataDrivenOutline: s } = e;
		t && t.setExtent(this.layerExtent);
		const o = e.getPaintProperty("fill-pattern"), a = o?.isDataDriven;
		let f = !o && e.getPaintValue("fill-antialias", i);
		if (e.outlineUsesFillColor) {
			if (f && !e.hasDataDrivenOpacity) {
				const t = e.getPaintValue("fill-opacity", i), r = e.getPaintValue("fill-opacity", i + 1);
				t < 1 && r < 1 && (f = !1);
			}
			if (f && !e.hasDataDrivenColor) {
				const t = e.getPaintValue("fill-color", i), r = e.getPaintValue("fill-color", i + 1);
				t[3] < 1 && r[3] < 1 && (f = !1);
			}
		}
		const u = this._features, d = t?.validateTessellation;
		if (a) {
			const n = [];
			for (const a of u) {
				const u = o.getValue(i, a), d = this._spriteInfo[u];
				if (!d?.rect) continue;
				const h = r.encodeAttributes(a, i, e, d), x = f && s ? l.encodeAttributes(a, i, e) : [], c = a.getGeometry(t);
				n.push({
					ddFillAttributes: h,
					ddOutlineAttributes: x,
					page: d.page,
					geometry: c
				}), n.sort((t, e) => t.page - e.page);
			}
			for (const { ddFillAttributes: t, ddOutlineAttributes: i, page: r, geometry: l } of n) this._processFeature(l, f, e.outlineUsesFillColor, t, i, d, r);
		} else for (const h of u) {
			const o = n ? r.encodeAttributes(h, i, e) : null, a = f && s ? l.encodeAttributes(h, i, e) : null, u = h.getGeometry(t);
			this._processFeature(u, f, e.outlineUsesFillColor, o, a, d);
		}
	}
	serialize() {
		let t = 10;
		t += this.layerUIDs.length, t += this._fillVertexBuffer.array.length, t += this._fillIndexBuffer.array.length, t += this._outlineVertexBuffer.array.length, t += this._outlineIndexBuffer.array.length, t += 3 * this._patternMap.size + 1;
		const e = new Uint32Array(t), i = new Int32Array(e.buffer);
		let r = 0;
		e[r++] = this.type, e[r++] = this.layerUIDs.length;
		for (let s = 0; s < this.layerUIDs.length; s++) e[r++] = this.layerUIDs[s];
		e[r++] = this._fillIndexStart, e[r++] = this._fillIndexCount, e[r++] = this._outlineIndexStart, e[r++] = this._outlineIndexCount;
		const l = this._patternMap, n = l.size;
		if (e[r++] = n, n > 0) for (const [s, [o, a]] of l) e[r++] = s, e[r++] = o, e[r++] = a;
		e[r++] = this._fillVertexBuffer.array.length;
		for (let s = 0; s < this._fillVertexBuffer.array.length; s++) i[r++] = this._fillVertexBuffer.array[s];
		e[r++] = this._fillIndexBuffer.array.length;
		for (let s = 0; s < this._fillIndexBuffer.array.length; s++) e[r++] = this._fillIndexBuffer.array[s];
		e[r++] = this._outlineVertexBuffer.array.length;
		for (let s = 0; s < this._outlineVertexBuffer.array.length; s++) i[r++] = this._outlineVertexBuffer.array[s];
		e[r++] = this._outlineIndexBuffer.array.length;
		for (let s = 0; s < this._outlineIndexBuffer.array.length; s++) e[r++] = this._outlineIndexBuffer.array[s];
		return e.buffer;
	}
	_processFeature(t, e, i, r, l, s, o) {
		if (!t) return;
		const a = t.length, f = !l || 0 === l.length;
		if (e && (!i || f)) for (let n = 0; n < a; n++) this._processOutline(t[n], l);
		const u = 32;
		let d;
		for (let h = 0; h < a; h++) {
			const e = n$1._area(t[h]);
			e > u ? (void 0 !== d && this._processFill(t, d, r, s, o), d = [h]) : e < -u && void 0 !== d && d.push(h);
		}
		void 0 !== d && this._processFill(t, d, r, s, o);
	}
	_processOutline(t, e) {
		const i = this._outlineVertexBuffer, l = this._outlineIndexBuffer, n = l.index;
		let s, o, a;
		const f = new i$1(0, 0), u = new i$1(0, 0), d = new i$1(0, 0);
		let h = -1, x = -1, c = -1, _ = -1, y = -1, g = !1;
		const p = 0;
		let I = t.length;
		if (I < 2) return;
		const B = t[p];
		let V = t[I - 1];
		for (; I && V.isEqual(B);) --I, V = t[I - 1];
		if (!(I - p < 2)) {
			for (let r = p; r < I; ++r) {
				r === p ? (s = t[I - 1], o = t[p], a = t[p + 1], f.assignSub(o, s), f.normalize(), f.rightPerpendicular()) : (s = o, o = a, a = r !== I - 1 ? t[r + 1] : t[p], f.assign(u));
				const n = this._isClipEdge(s, o);
				-1 === _ && (g = n), u.assignSub(a, o), u.normalize(), u.rightPerpendicular();
				const B = f.x * u.y - f.y * u.x;
				d.assignAdd(f, u), d.normalize();
				const V = -d.x * -f.x + -d.y * -f.y;
				let m = Math.abs(0 !== V ? 1 / V : 1);
				m > 8 && (m = 8), B >= 0 ? (c = i.add(o.x, o.y, f.x, f.y, 0, 1, e), -1 === _ && (_ = c), h >= 0 && x >= 0 && c >= 0 && !n && l.add(h, x, c), x = i.add(o.x, o.y, m * -d.x, m * -d.y, 0, -1, e), -1 === y && (y = x), h >= 0 && x >= 0 && c >= 0 && !n && l.add(h, x, c), h = x, x = c, c = i.add(o.x, o.y, d.x, d.y, 0, 1, e), h >= 0 && x >= 0 && c >= 0 && !n && l.add(h, x, c), x = i.add(o.x, o.y, u.x, u.y, 0, 1, e), h >= 0 && x >= 0 && c >= 0 && !n && l.add(h, x, c)) : (c = i.add(o.x, o.y, m * d.x, m * d.y, 0, 1, e), -1 === _ && (_ = c), h >= 0 && x >= 0 && c >= 0 && !n && l.add(h, x, c), x = i.add(o.x, o.y, -f.x, -f.y, 0, -1, e), -1 === y && (y = x), h >= 0 && x >= 0 && c >= 0 && !n && l.add(h, x, c), h = x, x = c, c = i.add(o.x, o.y, -d.x, -d.y, 0, -1, e), h >= 0 && x >= 0 && c >= 0 && !n && l.add(h, x, c), h = i.add(o.x, o.y, -u.x, -u.y, 0, -1, e), h >= 0 && x >= 0 && c >= 0 && !n && l.add(h, x, c));
			}
			h >= 0 && x >= 0 && _ >= 0 && !g && l.add(h, x, _), h >= 0 && _ >= 0 && y >= 0 && !g && l.add(h, y, _), this._outlineIndexCount += 3 * (l.index - n);
		}
	}
	_processFill(r, l, n, s, o) {
		s = !0;
		let a;
		l.length > 1 && (a = []);
		let f = 0;
		for (const t of l) 0 !== f && a.push(f), f += r[t].length;
		const u = 2 * f, d = t$1.acquire();
		for (const t of l) {
			const e = r[t], i = e.length;
			for (let t = 0; t < i; ++t) d.push(e[t].x, e[t].y);
		}
		const h = e$3(d, a, 2);
		if (s && e$3.deviation(d, a, 2, h) > 0) {
			const { buffer: e, vertexCount: s } = f$1(d, l.map((t) => r[t].length));
			if (s > 0) {
				const t = this._fillVertexBuffer.index;
				for (let i = 0; i < s; i++) this._fillVertexBuffer.add(e[2 * i], e[2 * i + 1], n);
				for (let e = 0; e < s; e += 3) {
					const i = t + e;
					this._fillIndexBuffer.add(i, i + 1, i + 2);
				}
				if (void 0 !== o) {
					const t = this._patternMap, e = t.get(o);
					e ? e[1] += s : t.set(o, [this._fillIndexStart + this._fillIndexCount, s]);
				}
				this._fillIndexCount += s;
			}
		} else {
			const t = h.length;
			if (t > 0) {
				const e = this._fillVertexBuffer.index;
				let i = 0;
				for (; i < u;) this._fillVertexBuffer.add(d[i++], d[i++], n);
				let r = 0;
				for (; r < t;) this._fillIndexBuffer.add(e + h[r++], e + h[r++], e + h[r++]);
				if (void 0 !== o) {
					const e = this._patternMap, i = e.get(o);
					i ? i[1] += t : e.set(o, [this._fillIndexStart + this._fillIndexCount, t]);
				}
				this._fillIndexCount += t;
			}
		}
		t$1.release(d);
	}
	_isClipEdge(t, e) {
		return t.x === e.x ? t.x <= -64 || t.x >= 4160 : t.y === e.y && (t.y <= -64 || t.y >= 4160);
	}
	static _area(t) {
		let e = 0;
		const i = t.length - 1;
		for (let r = 0; r < i; r++) e += (t[r].x - t[r + 1].x) * (t[r].y + t[r + 1].y);
		return e += (t[i].x - t[0].x) * (t[i].y + t[0].y), .5 * e;
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/engine/vectorTiles/buckets/LineBucket.js
var i = 65535;
var s = class extends e {
	constructor(e, i, s, l, a) {
		super(e, i, s), this.type = 2, this._tessellationOptions = {
			pixelCoordRatio: 8,
			halfWidth: 0,
			offset: 0
		}, this._patternMap = /* @__PURE__ */ new Map(), this.tessellationProperties = {
			_lineVertexBuffer: null,
			_lineIndexBuffer: null,
			_ddValues: null
		}, this.tessellationProperties._lineVertexBuffer = l, this.tessellationProperties._lineIndexBuffer = a, this._lineTessellator = new _(n(this.tessellationProperties), r$2(this.tessellationProperties), e.canUseThinTessellation);
	}
	get lineIndexStart() {
		return this._lineIndexStart;
	}
	get lineIndexCount() {
		return this._lineIndexCount;
	}
	getResources(e, t, i) {
		const s = this.layer, n = this.zoom, r = s.getPaintProperty("line-pattern"), l = s.getPaintProperty("line-dasharray"), a = s.getLayoutProperty("line-cap");
		if (!r && !l) return;
		const o = a?.getValue(n) || 0, u = a?.isDataDriven, f = r?.isDataDriven, h = l?.isDataDriven;
		if (f || h) for (const p of this._features) t(f ? r.getValue(n, p) : this._getDashArrayKey(p, n, s, l, u, a, o));
		else if (r) t(r.getValue(n));
		else if (l) {
			const e = l.getValue(n);
			t(s.getDashKey(e, o));
		}
	}
	processFeatures(e) {
		this._lineIndexStart = 3 * this.tessellationProperties._lineIndexBuffer.index, this._lineIndexCount = 0;
		const t = this.layer, i = this.zoom, s = this._features, n = this._tessellationOptions, { hasDataDrivenLine: r, lineMaterial: l } = t;
		e && e.setExtent(this.layerExtent);
		const a = t.getPaintProperty("line-pattern"), o = t.getPaintProperty("line-dasharray"), u = a?.isDataDriven, f = o?.isDataDriven;
		let h;
		h = t.getLayoutProperty("line-cap");
		const p = h?.isDataDriven ? h : null, g = p ? null : t.getLayoutValue("line-cap", i), y = g || 0, d = !!p;
		h = t.getLayoutProperty("line-join");
		const c = h?.isDataDriven ? h : null, _ = c ? null : t.getLayoutValue("line-join", i);
		h = t.getLayoutProperty("line-miter-limit");
		const x = h?.isDataDriven ? h : null, V = x ? null : t.getLayoutValue("line-miter-limit", i);
		h = t.getLayoutProperty("line-round-limit");
		const D = h?.isDataDriven ? h : null, P = D ? null : t.getLayoutValue("line-round-limit", i);
		h = t.getPaintProperty("line-width");
		const m = h?.isDataDriven ? h : null, I = m ? null : t.getPaintValue("line-width", i);
		h = t.getPaintProperty("line-offset");
		const L = h?.isDataDriven ? h : null, B = L ? null : t.getPaintValue("line-offset", i);
		if (u || f) {
			const r = [];
			for (const n of s) {
				const s = u ? a.getValue(i, n) : this._getDashArrayKey(n, i, t, o, d, p, y), f = this._spriteInfo[s];
				if (!f?.rect) continue;
				const h = l.encodeAttributes(n, i, t, f), v = n.getGeometry(e);
				r.push({
					ddAttributes: h,
					page: f.page,
					cap: p ? p.getValue(i, n) : g,
					join: c ? c.getValue(i, n) : _,
					miterLimit: x ? x.getValue(i, n) : V,
					roundLimit: D ? D.getValue(i, n) : P,
					halfWidth: .5 * (m ? m.getValue(i, n) : I),
					offset: L ? L.getValue(i, n) : B,
					geometry: v
				});
			}
			r.sort((e, t) => e.page - t.page), n.textured = !0;
			for (const { ddAttributes: e, page: t, cap: i, join: s, miterLimit: l, roundLimit: a, halfWidth: o, offset: u, geometry: f } of r) n.capType = i, n.joinType = s, n.miterLimit = l, n.roundLimit = a, n.halfWidth = o, n.offset = u, this._processFeature(f, e, t);
		} else {
			if (a) {
				const e = a.getValue(i);
				if (!this._spriteInfo[e]?.rect) return;
			}
			n.textured = !(!a && !o), n.capType = g, n.joinType = _, n.miterLimit = V, n.roundLimit = P, n.halfWidth = .5 * I, n.offset = B;
			for (const a of s) {
				const s = r ? l.encodeAttributes(a, i, t) : null;
				p && (n.capType = p.getValue(i, a)), c && (n.joinType = c.getValue(i, a)), x && (n.miterLimit = x.getValue(i, a)), D && (n.roundLimit = D.getValue(i, a)), m && (n.halfWidth = .5 * m.getValue(i, a)), L && (n.offset = L.getValue(i, a));
				const o = a.getGeometry(e);
				this._processFeature(o, s);
			}
		}
	}
	serialize() {
		let e = 6;
		e += this.layerUIDs.length, e += this.tessellationProperties._lineVertexBuffer.array.length, e += this.tessellationProperties._lineIndexBuffer.array.length, e += 3 * this._patternMap.size + 1;
		const t = new Uint32Array(e), i = new Int32Array(t.buffer);
		let s = 0;
		t[s++] = this.type, t[s++] = this.layerUIDs.length;
		for (let l = 0; l < this.layerUIDs.length; l++) t[s++] = this.layerUIDs[l];
		t[s++] = this._lineIndexStart, t[s++] = this._lineIndexCount;
		const n = this._patternMap, r = n.size;
		if (t[s++] = r, r > 0) for (const [l, [a, o]] of n) t[s++] = l, t[s++] = a, t[s++] = o;
		t[s++] = this.tessellationProperties._lineVertexBuffer.array.length;
		for (let l = 0; l < this.tessellationProperties._lineVertexBuffer.array.length; l++) i[s++] = this.tessellationProperties._lineVertexBuffer.array[l];
		t[s++] = this.tessellationProperties._lineIndexBuffer.array.length;
		for (let l = 0; l < this.tessellationProperties._lineIndexBuffer.array.length; l++) t[s++] = this.tessellationProperties._lineIndexBuffer.array[l];
		return t.buffer;
	}
	_processFeature(e, t, i) {
		if (!e) return;
		const s = e.length;
		for (let n = 0; n < s; n++) this._processGeometry(e[n], t, i);
	}
	_processGeometry(e, t, s) {
		if (e.length < 2) return;
		const n = .001;
		let r, l, a = e[0], o = 1;
		for (; o < e.length;) r = e[o].x - a.x, l = e[o].y - a.y, r * r + l * l < n * n ? e.splice(o, 1) : (a = e[o], ++o);
		if (e.length < 2) return;
		const u = this.tessellationProperties._lineIndexBuffer, f = 3 * u.index;
		this._tessellationOptions.initialDistance = 0, this._tessellationOptions.wrapDistance = i, this.tessellationProperties._ddValues = t, this._lineTessellator.tessellate(e, this._tessellationOptions);
		const h = 3 * u.index - f;
		if (void 0 !== s) {
			const e = this._patternMap, t = e.get(s);
			t ? t[1] += h : e.set(s, [f + this._lineIndexCount, h]);
		}
		this._lineIndexCount += h;
	}
	_getDashArrayKey(e, t, i, s, n, r, l) {
		const a = n ? r.getValue(t, e) : l, o = s.getValue(t, e);
		return i.getDashKey(o, a);
	}
};
var n = (e) => (t, i, s, n, r, l, a, o, u, f, h) => (e._lineVertexBuffer.add(t, i, a, o, s, n, r, l, u, f, h, e._ddValues), e._lineVertexBuffer.index - 1), r$2 = (e) => (t, i, s) => {
	e._lineIndexBuffer.add(t, i, s);
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/engine/vectorTiles/buckets/SymbolBucket.js
var y = 10;
function m(e, t) {
	return e.iconMosaicItem && t.iconMosaicItem ? e.iconMosaicItem.page === t.iconMosaicItem.page ? 0 : e.iconMosaicItem.page - t.iconMosaicItem.page : e.iconMosaicItem && !t.iconMosaicItem ? 1 : !e.iconMosaicItem && t.iconMosaicItem ? -1 : 0;
}
var u = class u extends e {
	constructor(e, t, n, s, i, a, o, r, l) {
		super(t, n, l.getSpriteItems()), this.type = 3, this._markerMap = /* @__PURE__ */ new Map(), this._glyphMap = /* @__PURE__ */ new Map(), this._glyphBufferDataStorage = /* @__PURE__ */ new Map(), this._isIconSDF = !1, this._sourceTileKey = e, this._iconVertexBuffer = s, this._iconIndexBuffer = i, this._textVertexBuffer = a, this._textIndexBuffer = o, this._placementEngine = r, this._workerTileHandler = l;
	}
	get markerPageMap() {
		return this._markerMap;
	}
	get glyphsPageMap() {
		return this._glyphMap;
	}
	get symbolInstances() {
		return this._symbolInstances;
	}
	static {
		this._bidiEngine = new C();
	}
	getResources(e, t, s) {
		const i = this.layer, a = this.zoom;
		e && e.setExtent(this.layerExtent);
		const o = i.getLayoutProperty("icon-image"), r = i.getLayoutProperty("text-field");
		let l = i.getLayoutProperty("text-transform"), h = i.getLayoutProperty("text-font");
		const x = [];
		let c, d, f, g;
		o && !o.isDataDriven && (c = o.getValue(a)), r && !r.isDataDriven && (d = r.getValue(a)), l && l.isDataDriven || (f = i.getLayoutValue("text-transform", a), l = null), h && h.isDataDriven || (g = i.getLayoutValue("text-font", a), h = null);
		for (const y of this._features) {
			const m = y.getGeometry(e);
			if (!m || 0 === m.length) continue;
			let p, _;
			o && (p = o.isDataDriven ? o.getValue(a, y) : this._replaceKeys(c, y.values), p && t(p));
			let M = !1;
			if (r && (_ = r.isDataDriven ? r.getValue(a, y) : this._replaceKeys(d, y.values), _)) {
				switch (_ = _.replaceAll("\\n", "\n"), l && (f = l.getValue(a, y)), f) {
					case 2:
						_ = _.toLowerCase();
						break;
					case 1: _ = _.toUpperCase();
				}
				if (u._bidiEngine.hasBidiChar(_)) {
					let e;
					e = "rtl" === u._bidiEngine.checkContextual(_) ? "IDNNN" : "ICNNN", _ = u._bidiEngine.bidiTransform(_, e, "VLYSN"), M = !0;
				}
				if (_.length > 0) {
					h && (g = h.getValue(a, y));
					for (const e of g) {
						let t = s[e];
						t || (t = s[e] = /* @__PURE__ */ new Set());
						for (const e of _) {
							const n = e.codePointAt(0);
							null != n && t.add(n);
						}
					}
				}
			}
			if (!p && !_) continue;
			const b = i.getLayoutValue("symbol-sort-key", a, y), P = {
				feature: y,
				sprite: p,
				label: _,
				rtl: M,
				geometry: m,
				hash: (_ ? l$1(_) : 0) ^ (p ? l$1(p) : 0),
				priority: b,
				textFont: g
			};
			x.push(P);
		}
		this._symbolFeatures = x;
	}
	processFeatures(e) {
		e && e.setExtent(this.layerExtent);
		const n = this.layer, s = this.zoom, r = n.getLayoutValue("symbol-placement", s), l$3 = 0 !== r, d = n.getLayoutValue("symbol-spacing", s) * 8, y = n.getLayoutProperty("icon-image"), p = n.getLayoutProperty("text-field"), _ = y ? new V(n, s, l$3) : null, M = p ? new T(n, s, l$3) : null, b = this._workerTileHandler;
		let P;
		y && (P = b.getSpriteItems()), this._iconIndexStart = 3 * this._iconIndexBuffer.index, this._textIndexStart = 3 * this._textIndexBuffer.index, this._iconIndexCount = 0, this._textIndexCount = 0, this._markerMap.clear(), this._glyphMap.clear();
		const I = [];
		let A = 1;
		M?.size && (A = M.size / 24);
		const V$1 = M ? M.maxAngle * s$2 : 0, w = M ? M.size * 8 : 0;
		for (const o of this._symbolFeatures) {
			let e;
			_ && P && o.sprite && (e = P[o.sprite], e && e.sdf && (this._isIconSDF = !0));
			let n;
			e && _.update(s, o.feature);
			let f = 0;
			const g = o.label;
			if (g) {
				n$3(M), M.update(s, o.feature);
				const e = l$3 && 0 === M.rotationAlignment ? M.keepUpright : M.writingMode && M.writingMode.includes(1);
				let a = .5;
				switch (M.anchor) {
					case 5:
					case 1:
					case 7:
						a = 0;
						break;
					case 6:
					case 2:
					case 8: a = 1;
				}
				let r = .5;
				switch (M.anchor) {
					case 5:
					case 3:
					case 6:
						r = 0;
						break;
					case 7:
					case 4:
					case 8: r = 1;
				}
				let h = .5;
				switch (M.justify) {
					case 0:
						h = a;
						break;
					case 1:
						h = 0;
						break;
					case 3: h = 1;
				}
				const d = M.letterSpacing * 24, y = l$3 ? 0 : M.maxWidth * 24, m = M.lineHeight * 24;
				if (n = new l(o.textFont.map((e) => b.getGlyphItems(e)), y, m, d, a, r, h).getShaping(g, o.rtl, e), n && n.length > 0) {
					let e = 1e30, t = -1e30;
					for (const s of n) e = Math.min(e, s.x), t = Math.max(t, s.x);
					f = (t - e + 48) * A * 8;
				}
			}
			for (let t of o.geometry) {
				const s = [];
				if (1 === r) {
					if (n?.length && M?.size) {
						const e = M.size * 8 * (2 + Math.min(2, 4 * Math.abs(M.offset[1])));
						t = u._smoothVertices(t, e);
					}
					u._pushAnchors(s, t, d, f);
				} else 2 === r ? u._pushCenterAnchor(s, t) : 3 === o.feature.type ? u._pushCentroid(s, t) : s.push(new m$1(t[0].x, t[0].y));
				for (const i of s) {
					if (i.x < 0 || i.x > 4096 || i.y < 0 || i.y > 4096) continue;
					if (l$3 && f > 0 && 0 === M?.rotationAlignment && !u._honorsTextMaxAngle(t, i, f, V$1, w)) continue;
					const s = {
						shaping: n,
						line: t,
						iconMosaicItem: e,
						anchor: i,
						symbolFeature: o,
						textColliders: [],
						iconColliders: [],
						textVertexRanges: [],
						iconVertexRanges: []
					};
					I.push(s), this._processFeature(s, _, M);
				}
			}
		}
		I.sort(m), this._addPlacedGlyphs(), this._symbolInstances = I;
	}
	serialize() {
		let e = 14;
		e += this.layerUIDs.length, e += 3 * this.markerPageMap.size, e += 3 * this.glyphsPageMap.size, e += u._symbolsSerializationLength(this._symbolInstances), e += this._iconVertexBuffer.array.length, e += this._iconIndexBuffer.array.length, e += this._textVertexBuffer.array.length, e += this._textIndexBuffer.array.length;
		const t = new Uint32Array(e), n = new Int32Array(t.buffer), s = new Float32Array(t.buffer), [i, a, o] = this._sourceTileKey.split("/");
		let r = 0;
		t[r++] = this.type, t[r++] = this.layerUIDs.length;
		for (let l = 0; l < this.layerUIDs.length; l++) t[r++] = this.layerUIDs[l];
		t[r++] = this._isIconSDF ? 1 : 0, t[r++] = parseFloat(i), t[r++] = parseFloat(a), t[r++] = parseFloat(o), t[r++] = this.markerPageMap.size;
		for (const [l, [h, x]] of this.markerPageMap) t[r++] = l, t[r++] = h, t[r++] = x;
		t[r++] = this.glyphsPageMap.size;
		for (const [l, [h, x]] of this.glyphsPageMap) t[r++] = l, t[r++] = h, t[r++] = x;
		t[r++] = this._iconVertexBuffer.index / 4, t[r++] = this._textVertexBuffer.index / 4, r = u.serializeSymbols(t, n, s, r, this._symbolInstances), t[r++] = this._iconVertexBuffer.array.length;
		for (let l = 0; l < this._iconVertexBuffer.array.length; l++) n[r++] = this._iconVertexBuffer.array[l];
		t[r++] = this._iconIndexBuffer.array.length;
		for (let l = 0; l < this._iconIndexBuffer.array.length; l++) t[r++] = this._iconIndexBuffer.array[l];
		t[r++] = this._textVertexBuffer.array.length;
		for (let l = 0; l < this._textVertexBuffer.array.length; l++) n[r++] = this._textVertexBuffer.array[l];
		t[r++] = this._textIndexBuffer.array.length;
		for (let l = 0; l < this._textIndexBuffer.array.length; l++) t[r++] = this._textIndexBuffer.array[l];
		return t.buffer;
	}
	static _symbolsSerializationLength(e) {
		let t = 0;
		t += 1;
		for (const n of e || []) {
			t += 5, t += 1;
			for (const e of n.textColliders) t += y;
			for (const e of n.iconColliders) t += y;
			t += 1, t += 2 * n.textVertexRanges.length, t += 1, t += 2 * n.iconVertexRanges.length;
		}
		return t;
	}
	static serializeSymbols(e, t, n, s, i) {
		i = i || [], t[s++] = i.length;
		for (const a of i) {
			t[s++] = a.anchor.x, t[s++] = a.anchor.y, t[s++] = a.symbolFeature.hash, t[s++] = a.symbolFeature.priority, t[s++] = a.symbolFeature.feature.featureIndex, t[s++] = a.textColliders.length + a.iconColliders.length;
			for (const e of a.textColliders) t[s++] = e.xTile, t[s++] = e.yTile, t[s++] = e.dxPixels, t[s++] = e.dyPixels, t[s++] = e.hard ? 1 : 0, t[s++] = e.partIndex, n[s++] = e.minLod, n[s++] = e.maxLod, t[s++] = e.width, t[s++] = e.height;
			for (const e of a.iconColliders) t[s++] = e.xTile, t[s++] = e.yTile, t[s++] = e.dxPixels, t[s++] = e.dyPixels, t[s++] = e.hard ? 1 : 0, t[s++] = e.partIndex, n[s++] = e.minLod, n[s++] = e.maxLod, t[s++] = e.width, t[s++] = e.height;
			t[s++] = a.textVertexRanges.length;
			for (const [e, n] of a.textVertexRanges) t[s++] = e, t[s++] = n;
			t[s++] = a.iconVertexRanges.length;
			for (const [e, n] of a.iconVertexRanges) t[s++] = e, t[s++] = n;
		}
		return s;
	}
	_replaceKeys(e, t) {
		return e.replaceAll(/{([^{}]+)}/g, (e, n) => n in t ? t[n] : "");
	}
	_processFeature(e, t, n) {
		const { line: s, iconMosaicItem: i, shaping: a, anchor: o } = e, l = this.zoom, h = this.layer, x = !!i;
		let c = !0;
		x && (c = t?.optional || !i);
		const d = a && a.length > 0, f = !d || n?.optional;
		let g, y;
		if (x && (g = this._placementEngine.getIconPlacement(o, i, t)), (g || c) && (d && (y = this._placementEngine.getTextPlacement(o, a, s, n)), y || f)) {
			if (g && y || (f || c ? f || y ? c || g || (y = null) : g = null : (g = null, y = null)), y) {
				const t = h.hasDataDrivenText ? h.textMaterial.encodeAttributes(e.symbolFeature.feature, l, h) : null;
				if (this._storePlacedGlyphs(e, y.shapes, l, n.rotationAlignment, t), y.textColliders) {
					e.textColliders = y.textColliders;
					for (const e of y.textColliders) {
						e.minLod = Math.max(l + h$3(e.minLod), 0), e.maxLod = Math.min(l + h$3(e.maxLod), 25);
						const t = e.angle;
						if (t) {
							const n = Math.cos(t), s = Math.sin(t), i = e.dxPixels * n - e.dyPixels * s, a = e.dxPixels * s + e.dyPixels * n, o = (e.dxPixels + e.width) * n - e.dyPixels * s, r = (e.dxPixels + e.width) * s + e.dyPixels * n, l = e.dxPixels * n - (e.dyPixels + e.height) * s, h = e.dxPixels * s + (e.dyPixels + e.height) * n, x = (e.dxPixels + e.width) * n - (e.dyPixels + e.height) * s, c = (e.dxPixels + e.width) * s + (e.dyPixels + e.height) * n, d = Math.min(i, o, l, x), f = Math.max(i, o, l, x), g = Math.min(a, r, h, c), y = Math.max(a, r, h, c);
							e.dxPixels = d, e.dyPixels = g, e.width = f - d, e.height = y - g;
						}
					}
				}
			}
			if (g) {
				const n = h.hasDataDrivenIcon ? h.iconMaterial.encodeAttributes(e.symbolFeature.feature, l, h) : null;
				if (this._addPlacedIcons(e, g.shapes, l, i.page, 1 === t.rotationAlignment, n), g.iconColliders) {
					e.iconColliders = g.iconColliders;
					for (const e of g.iconColliders) {
						e.minLod = Math.max(l + h$3(e.minLod), 0), e.maxLod = Math.min(l + h$3(e.maxLod), 25);
						const t = e.angle;
						if (t) {
							const n = Math.cos(t), s = Math.sin(t), i = e.dxPixels * n - e.dyPixels * s, a = e.dxPixels * s + e.dyPixels * n, o = (e.dxPixels + e.width) * n - e.dyPixels * s, r = (e.dxPixels + e.width) * s + e.dyPixels * n, l = e.dxPixels * n - (e.dyPixels + e.height) * s, h = e.dxPixels * s + (e.dyPixels + e.height) * n, x = (e.dxPixels + e.width) * n - (e.dyPixels + e.height) * s, c = (e.dxPixels + e.width) * s + (e.dyPixels + e.height) * n, d = Math.min(i, o, l, x), f = Math.max(i, o, l, x), g = Math.min(a, r, h, c), y = Math.max(a, r, h, c);
							e.dxPixels = d, e.dyPixels = g, e.width = f - d, e.height = y - g;
						}
					}
				}
			}
		}
	}
	_addPlacedIcons(e, t, n, s, i, a) {
		const o = Math.max(n - 1, 0), l = this._iconVertexBuffer, h = this._iconIndexBuffer, x = this._markerMap;
		for (const c of t) {
			const t = i ? 0 : Math.max(n + h$3(c.minzoom), o), d = i ? 25 : Math.min(n + h$3(c.maxzoom), 25);
			if (d <= t) continue;
			const f = c.tl, g = c.tr, y = c.bl, m = c.br, u = c.mosaicRect, p = c.labelAngle, _ = c.minAngle, M = c.maxAngle, b = c.anchor, P = l.index, I = u.x, A = u.y, V = I + u.width, w = A + u.height, L = l.index;
			l.add(b.x, b.y, f.x, f.y, I, A, p, _, M, t, d, a), l.add(b.x, b.y, g.x, g.y, V, A, p, _, M, t, d, a), l.add(b.x, b.y, y.x, y.y, I, w, p, _, M, t, d, a), l.add(b.x, b.y, m.x, m.y, V, w, p, _, M, t, d, a), e.iconVertexRanges.length > 0 && e.iconVertexRanges[0][0] + e.iconVertexRanges[0][1] === L ? e.iconVertexRanges[0][1] += 4 : e.iconVertexRanges.push([L, 4]), h.add(P, P + 1, P + 2), h.add(P + 1, P + 2, P + 3), x.has(s) ? x.get(s)[1] += 6 : x.set(s, [this._iconIndexStart + this._iconIndexCount, 6]), this._iconIndexCount += 6;
		}
	}
	_addPlacedGlyphs() {
		const e = this._textVertexBuffer, t = this._textIndexBuffer, n = this._glyphMap;
		for (const [s, i] of this._glyphBufferDataStorage) for (const a of i) {
			const i = e.index, o = a.symbolInstance, r = a.ddAttributes, l = e.index;
			e.add(a.glyphAnchor[0], a.glyphAnchor[1], a.tl[0], a.tl[1], a.xmin, a.ymin, a.labelAngle, a.minAngle, a.maxAngle, a.minLod, a.maxLod, r), e.add(a.glyphAnchor[0], a.glyphAnchor[1], a.tr[0], a.tr[1], a.xmax, a.ymin, a.labelAngle, a.minAngle, a.maxAngle, a.minLod, a.maxLod, r), e.add(a.glyphAnchor[0], a.glyphAnchor[1], a.bl[0], a.bl[1], a.xmin, a.ymax, a.labelAngle, a.minAngle, a.maxAngle, a.minLod, a.maxLod, r), e.add(a.glyphAnchor[0], a.glyphAnchor[1], a.br[0], a.br[1], a.xmax, a.ymax, a.labelAngle, a.minAngle, a.maxAngle, a.minLod, a.maxLod, r), o.textVertexRanges.length > 0 && o.textVertexRanges[0][0] + o.textVertexRanges[0][1] === l ? o.textVertexRanges[0][1] += 4 : o.textVertexRanges.push([l, 4]), t.add(i, i + 1, i + 2), t.add(i + 1, i + 2, i + 3), n.has(s) ? n.get(s)[1] += 6 : n.set(s, [this._textIndexStart + this._textIndexCount, 6]), this._textIndexCount += 6;
		}
		this._glyphBufferDataStorage.clear();
	}
	_storePlacedGlyphs(e, t, n, s, i) {
		const a = Math.max(n - 1, 0), o = 1 === s;
		let l, h, x, c, d, f, g, y, m, u, p;
		for (const _ of t) {
			if (l = o ? 0 : Math.max(n + h$3(_.minzoom), a), h = o ? 25 : Math.min(n + h$3(_.maxzoom), 25), h <= l) continue;
			x = _.tl, c = _.tr, d = _.bl, f = _.br, g = _.labelAngle, y = _.minAngle, m = _.maxAngle, u = _.anchor, p = _.mosaicRect, this._glyphBufferDataStorage.has(_.page) || this._glyphBufferDataStorage.set(_.page, []);
			this._glyphBufferDataStorage.get(_.page).push({
				glyphAnchor: [u.x, u.y],
				tl: [x.x, x.y],
				tr: [c.x, c.y],
				bl: [d.x, d.y],
				br: [f.x, f.y],
				xmin: p.x,
				ymin: p.y,
				xmax: p.x + p.width,
				ymax: p.y + p.height,
				labelAngle: g,
				minAngle: y,
				maxAngle: m,
				minLod: l,
				maxLod: h,
				placementLod: a,
				symbolInstance: e,
				ddAttributes: i
			});
		}
	}
	static _pushAnchors(e, t, n, i) {
		n += i;
		let a = 0;
		const o = t.length - 1;
		for (let l = 0; l < o; l++) a += i$1.distance(t[l], t[l + 1]);
		let r = i || n;
		if (r *= .5, a <= r) return;
		const x = r / a;
		let c = 0, d = -(n = a / Math.max(Math.round(a / n), 1)) / 2;
		const f = t.length - 1;
		for (let s = 0; s < f; s++) {
			const i = t[s], a = t[s + 1], o = a.x - i.x, r = a.y - i.y, f = Math.sqrt(o * o + r * r);
			let g;
			for (; d + n < c + f;) {
				d += n;
				const t = (d - c) / f, y = p$1(i.x, a.x, t), m = p$1(i.y, a.y, t);
				void 0 === g && (g = Math.atan2(r, o)), e.push(new m$1(y, m, g, s, x));
			}
			c += f;
		}
	}
	static _pushCenterAnchor(e, t) {
		let n = 0;
		const i = t.length - 1;
		for (let l = 0; l < i; l++) n += i$1.distance(t[l], t[l + 1]);
		const a = n / 2;
		let o = 0;
		const r = t.length - 1;
		for (let s = 0; s < r; s++) {
			const n = t[s], i = t[s + 1], r = i.x - n.x, x = i.y - n.y, c = Math.sqrt(r * r + x * x);
			if (a < o + c) {
				const t = (a - o) / c, d = p$1(n.x, i.x, t), f = p$1(n.y, i.y, t), g = Math.atan2(x, r);
				e.push(new m$1(d, f, g, s, 0));
				return;
			}
			o += c;
		}
	}
	static _deviation(e, t, n) {
		const s = (t.x - e.x) * (n.x - t.x) + (t.y - e.y) * (n.y - t.y), i = (t.x - e.x) * (n.y - t.y) - (t.y - e.y) * (n.x - t.x);
		return Math.atan2(i, s);
	}
	static _honorsTextMaxAngle(e, t, n, i, a) {
		let o = 0;
		const r = n / 2;
		let l = new i$1(t.x, t.y), h = t.segment + 1;
		for (; o > -r;) {
			if (--h, h < 0) return !1;
			o -= i$1.distance(e[h], l), l = e[h];
		}
		o += i$1.distance(e[h], e[h + 1]);
		const x = [];
		let c = 0;
		const d = e.length;
		for (; o < r;) {
			const t = e[h];
			let n, r = h;
			do {
				if (++r, r === d) return !1;
				n = e[r];
			} while (n.isEqual(t));
			let l, f = r;
			do {
				if (++f, f === d) return !1;
				l = e[f];
			} while (l.isEqual(n));
			const g = this._deviation(t, n, l);
			for (x.push({
				deviation: g,
				distToAnchor: o
			}), c += g; o - x[0].distToAnchor > a;) c -= x.shift().deviation;
			if (Math.abs(c) > i) return !1;
			o += i$1.distance(n, l), h = r;
		}
		return !0;
	}
	static _smoothVertices(e, t) {
		if (t <= 0) return e;
		let n = e.length;
		if (n < 3) return e;
		const i = [];
		let a = 0, o = 0;
		i.push(0);
		for (let y = 1; y < n; y++) {
			const t = i$1.distance(e[y], e[y - 1]);
			t > 0 && (a += t, i.push(a), o++, o !== y && (e[o] = e[y]));
		}
		if (n = o + 1, n < 3) return e;
		t = Math.min(t, .2 * a);
		const r = e[0].x, l = e[0].y, h = e[n - 1].x, x = e[n - 1].y, c = i$1.sub(e[0], e[1]);
		c.normalize(), e[0].x += t * c.x, e[0].y += t * c.y, c.assignSub(e[n - 1], e[n - 2]), c.normalize(), e[n - 1].x += t * c.x, e[n - 1].y += t * c.y, i[0] -= t, i[n - 1] += t;
		const d = [];
		d.push(new i$1(r, l));
		const f = 1e-6, g = .5 * t;
		for (let y = 1; y < n - 1; y++) {
			let a = 0, o = 0, r = 0;
			for (let n = y - 1; n >= 0; n--) {
				const s = g + i[n + 1] - i[y];
				if (s < 0) break;
				const l = i[n + 1] - i[n], h = i[y] - i[n] < g ? 1 : s / l;
				if (h < f) break;
				const x = h * h, c = h * s - .5 * x * l, d = h * l / t, m = e[n + 1], u = e[n].x - m.x, p = e[n].y - m.y;
				a += d / c * (m.x * h * s + .5 * x * (s * u - l * m.x) - x * h * l * u / 3), o += d / c * (m.y * h * s + .5 * x * (s * p - l * m.y) - x * h * l * p / 3), r += d;
			}
			for (let s = y + 1; s < n; s++) {
				const n = g - i[s - 1] + i[y];
				if (n < 0) break;
				const l = i[s] - i[s - 1], h = i[s] - i[y] < g ? 1 : n / l;
				if (h < f) break;
				const x = h * h, c = h * n - .5 * x * l, d = h * l / t, m = e[s - 1], u = e[s].x - m.x, p = e[s].y - m.y;
				a += d / c * (m.x * h * n + .5 * x * (n * u - l * m.x) - x * h * l * u / 3), o += d / c * (m.y * h * n + .5 * x * (n * p - l * m.y) - x * h * l * p / 3), r += d;
			}
			d.push(new i$1(a / r, o / r));
		}
		return d.push(new i$1(h, x)), e[0].x = r, e[0].y = l, e[n - 1].x = h, e[n - 1].y = x, d;
	}
	static _pushCentroid(e, t) {
		const n = 0, s = 0, i = t$2, o = t$2, r = t.length - 1;
		let l = 0, x = 0, c = 0, d = t[0].x, f = t[0].y;
		d > i && (d = i), d < n && (d = n), f > o && (f = o), f < s && (f = s);
		for (let a = 1; a < r; a++) {
			let e = t[a].x, r = t[a].y, h = t[a + 1].x, g = t[a + 1].y;
			e > i && (e = i), e < n && (e = n), r > o && (r = o), r < s && (r = s), h > i && (h = i), h < n && (h = n), g > o && (g = o), g < s && (g = s);
			const y = (e - d) * (g - f) - (h - d) * (r - f);
			l += y * (d + e + h), x += y * (f + r + g), c += y;
		}
		l /= 3 * c, x /= 3 * c, isNaN(l) || isNaN(x) || e.push(new m$1(l, x));
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/engine/vectorTiles/TileParser.js
var k = class {
	constructor(t, r, o, a, n, l) {
		if (this._pbfTiles = {}, this._tileClippers = {}, this._client = o, this._tile = r, this._sourceDataMaxLOD = a, l) {
			this._styleLayerUIDs = /* @__PURE__ */ new Set();
			for (const e of l) this._styleLayerUIDs.add(e);
		}
		this._styleRepository = n, this._layers = this._styleRepository?.layers ?? [];
		const [u, f, p] = r.tileKey.split("/").map(parseFloat);
		this._level = u;
		const h = d$3(this._level);
		for (const c of Object.keys(t)) {
			const r = t[c];
			this._pbfTiles[c] = new n$4(new Uint8Array(r.protobuff), new DataView(r.protobuff));
			if (r.refKey) {
				const [e] = r.refKey.split("/").map(parseFloat), t = u - e;
				if (t > 0) {
					const e = (1 << t) - 1, r = f & e, i = p & e;
					this._tileClippers[c] = new h$2(t, r, i, 8, h);
				}
			}
			this._tileClippers[c] || (this._tileClippers[c] = new n$5());
		}
	}
	_canParseStyleLayer(e) {
		return !this._styleLayerUIDs || this._styleLayerUIDs.has(e);
	}
	async parse(e) {
		const t = c$2(), s = this._initialize(e), { returnedBuckets: i } = s;
		this._processLayers(s), this._linkReferences(s), this._filterFeatures(s);
		const o = [], c = /* @__PURE__ */ new Set(), a = (e, t) => {
			c.has(e) || (o.push({
				name: e,
				repeat: t
			}), c.add(e));
		}, n = {};
		for (const r of i) r.getResources(r.tileClipper, a, n);
		if (4 === this._tile.status) return [];
		const l = this._fetchResources(o, n, e);
		return Promise.all([...l, t]).then(() => this._processFeatures(s.returnedBuckets));
	}
	_initialize(e) {
		return {
			signal: e?.signal,
			sourceNameToTileData: this._parseTileData(this._pbfTiles),
			layers: this._layers,
			zoom: this._level,
			sourceNameToTileClipper: this._tileClippers,
			sourceNameToUniqueSourceLayerBuckets: {},
			sourceNameToUniqueSourceLayers: {},
			returnedBuckets: [],
			layerIdToBucket: {},
			referencerUIDToReferencedId: /* @__PURE__ */ new Map()
		};
	}
	_processLayers(e) {
		const { sourceNameToTileData: t, zoom: r, layers: s, sourceNameToTileClipper: i, sourceNameToUniqueSourceLayerBuckets: o, sourceNameToUniqueSourceLayers: c, returnedBuckets: a, layerIdToBucket: n, referencerUIDToReferencedId: l } = e, u = this._sourceDataMaxLOD;
		for (let f = s.length - 1; f >= 0; f--) {
			const e = s[f];
			if (r < u) {
				if (e.minzoom && r < Math.floor(e.minzoom) || e.maxzoom && r >= e.maxzoom) continue;
			} else if (e.maxzoom && r >= e.maxzoom) continue;
			if (0 === e.type || !this._canParseStyleLayer(e.uid) || !t[e.source] || !i[e.source]) continue;
			const p = t[e.source], h = i[e.source], m = e.sourceLayer, _ = p[m];
			if (_) {
				let t = c[e.source];
				if (t || (t = c[e.source] = /* @__PURE__ */ new Set()), t.add(e.sourceLayer), e.refLayerId) l.set(e.uid, e.refLayerId);
				else {
					const t = this._createBucket(e);
					if (t) {
						t.layerUIDs = [e.uid], t.layerExtent = _.extent, t.tileClipper = h;
						let r = o[e.source];
						r || (r = o[e.source] = {});
						let s = r[m];
						s || (s = r[m] = []), s.push(t), a.push(t), n[e.id] = t;
					}
				}
			}
		}
	}
	_linkReferences(e) {
		const { layerIdToBucket: t, referencerUIDToReferencedId: r } = e;
		r.forEach((e, r) => {
			t[e] && t[e].layerUIDs.push(r);
		});
	}
	_filterFeatures(e) {
		const { signal: r, sourceNameToTileData: s, sourceNameToUniqueSourceLayerBuckets: i, sourceNameToUniqueSourceLayers: c } = e, a = 10 * this._level, n = 10 * (this._level + 1), l = [], u = [];
		for (const t of Object.keys(c)) c[t].forEach((e) => {
			l.push(e), u.push(t);
		});
		for (let f = 0; f < l.length; f++) {
			const e = u[f], c = l[f];
			if (!s[e] || !i[e]) continue;
			const p = s[e][c], h = i[e][c];
			if (!h || 0 === h.length) continue;
			if (a$2(r)) return;
			let m = 0;
			const _ = p.getData();
			for (; _.nextTag(2);) {
				const e = _.getMessage(), t = new t$4(e, p, m++);
				e.release();
				const r = t.values;
				if (r) {
					const e = r._minzoom;
					if (e && e >= n) continue;
					const t = r._maxzoom;
					if (t && t <= a) continue;
				}
				for (const s of h) s.pushFeature(t);
			}
		}
	}
	_fetchResources(e, t, r) {
		const s = [], i = this._tile.getWorkerTileHandler();
		let o, c;
		e.length > 0 && (o = i.fetchSprites(e, this._client, r), s.push(o));
		for (const a in t) {
			const e = t[a];
			e.size > 0 && (c = i.fetchGlyphs(this._tile.tileKey, a, e, this._client, r), s.push(c));
		}
		return s;
	}
	_processFeatures(e) {
		const t = e.filter((e) => e.hasFeatures() || this._canParseStyleLayer(e.layer.uid));
		for (const r of t) r.processFeatures(r.tileClipper);
		return t;
	}
	_parseTileData(e) {
		const t = {};
		for (const r of Object.keys(e)) {
			const s = e[r], i = {};
			for (; s.next();) switch (s.tag()) {
				case 3: {
					const e = s.getMessage(), t = new t$5(e);
					e.release(), i[t.name] = t;
					break;
				}
				default: s.skip();
			}
			t[r] = i;
		}
		return t;
	}
	_createBucket(e) {
		switch (e.type) {
			case 0: return null;
			case 1: return this._createFillBucket(e);
			case 2: return this._createLineBucket(e);
			case 4: return this._createCircleBucket(e);
			case 3: return this._createSymbolBucket(e);
		}
	}
	_createFillBucket(e) {
		return new n$1(e, this._level, this._tile.getWorkerTileHandler().getSpriteItems(), new a(e.fillMaterial.getStride()), new r$3(), new n$2(e.outlineMaterial.getStride()), new r$3());
	}
	_createLineBucket(e) {
		return new s(e, this._level, this._tile.getWorkerTileHandler().getSpriteItems(), new h(e.lineMaterial.getStride()), new r$3());
	}
	_createCircleBucket(e) {
		return new t(e, this._level, this._tile.getWorkerTileHandler().getSpriteItems(), new e$1(e.circleMaterial.getStride()), new r$3());
	}
	_createSymbolBucket(e) {
		const t = this._tile;
		return new u(t.tileKey, e, this._level, new d(e.iconMaterial.getStride()), new r$3(), new d(e.textMaterial.getStride()), new r$3(), t.placementEngine, t.getWorkerTileHandler());
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/engine/vectorTiles/WorkerTile.js
var r$1 = class {
	constructor(t, s, r, i) {
		this.status = 0, this.placementEngine = new y$1(), this.tileKey = t, this.refKeys = s, this._workerTileHandler = r, this._styleRepository = i;
	}
	release() {
		this.tileKey = "", this.refKeys = null, this.status = 0, this._workerTileHandler = null;
	}
	async parse(e, s) {
		const r = s?.signal;
		if (null != r) {
			const t = () => {
				r.removeEventListener("abort", t), this.status = 4;
			};
			r.addEventListener("abort", t);
		}
		let i;
		const a = {
			bucketsWithData: [],
			emptyBuckets: null
		};
		try {
			i = await this._parse(e, s);
		} catch (h) {
			if (d$2(h)) throw h;
			return {
				result: a,
				transferList: []
			};
		}
		this.status = 2;
		const n = a.bucketsWithData, l = [];
		for (const t of i) if (t.hasFeatures()) {
			const e = t.serialize();
			n.push(e);
		} else l.push(t.layer.uid);
		const o = [...n];
		let u = null;
		return l.length > 0 && (u = Uint32Array.from(l), o.push(u.buffer)), a.emptyBuckets = u, {
			result: a,
			transferList: o
		};
	}
	setObsolete() {
		this.status = 4;
	}
	getLayers() {
		return this._workerTileHandler.getLayers();
	}
	getWorkerTileHandler() {
		return this._workerTileHandler;
	}
	async _parse(t, e) {
		const r = t.sourceName2DataAndRefKey;
		if (0 === Object.keys(r).length) return [];
		this.status = 3;
		return new k(r, this, e.client, t.sourceDataMaxLOD, this._styleRepository, t.styleLayerUIDs).parse(e);
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/engine/vectorTiles/WorkerTileHandler.js
var r = 25;
var o = class {
	constructor() {
		this._spriteInfo = {}, this._glyphInfo = {}, this._sourceDataMaxLOD = r;
	}
	reset() {
		return this._spriteInfo = {}, this._glyphInfo = {}, Promise.resolve();
	}
	getLayers() {
		return this._styleRepository?.layers ?? [];
	}
	async createTileAndParse(s, r) {
		const { key: o } = s, i = {};
		for (const e of Object.keys(s.sourceName2DataAndRefKey)) i[e] = s.sourceName2DataAndRefKey[e].refKey;
		const a = new r$1(o, i, this, this._styleRepository);
		try {
			return await a.parse({
				...s,
				sourceDataMaxLOD: this._sourceDataMaxLOD
			}, r);
		} catch (n) {
			if (a.setObsolete(), a.release(), !d$2(n)) throw n;
			return null;
		}
	}
	updateStyle(e) {
		if (!e || 0 === e.length || !this._styleRepository) return;
		const t = this._styleRepository;
		for (const s of e) {
			const e = s.type, r = s.data;
			switch (e) {
				case 0:
					t.setPaintProperties(r.layer, r.paint);
					break;
				case 1:
					t.setLayoutProperties(r.layer, r.layout);
					break;
				case 3:
					t.deleteStyleLayer(r.layer);
					break;
				case 2:
					t.setStyleLayer(r.layer, r.index);
					break;
				case 4: this._spriteInfo = {};
			}
		}
	}
	setStyle(e) {
		const { style: t, sourceDataMaxLOD: r } = e;
		this._styleRepository = new l$2(t), this._sourceDataMaxLOD = r, this._spriteInfo = {}, this._glyphInfo = {};
	}
	fetchSprites(e, t, s) {
		const r = [], o = this._spriteInfo;
		for (const i of e) void 0 === o[i.name] && r.push(i);
		return 0 === r.length ? Promise.resolve() : t.invoke("getSprites", r, { signal: s?.signal }).then((e) => {
			for (const t in e) o[t] = e[t];
		});
	}
	getSpriteItems() {
		return this._spriteInfo;
	}
	fetchGlyphs(e, t, s, r, o) {
		const i = [];
		let a = this._glyphInfo[t];
		return a ? s.forEach((e) => {
			a[e] || i.push(e);
		}) : (a = this._glyphInfo[t] = [], s.forEach((e) => i.push(e))), 0 === i.length ? Promise.resolve() : r.invoke("getGlyphs", {
			tileID: e,
			font: t,
			codePoints: i
		}, o).then((e) => {
			for (let t = 0; t < e.length; t++) e[t] && (a[t] = e[t]);
		});
	}
	getGlyphItems(e) {
		return this._glyphInfo[e];
	}
};
//#endregion
export { o as default };

//# sourceMappingURL=WorkerTileHandler-o1fZTGB3.js.map