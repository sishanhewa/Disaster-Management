import { r as __decorate } from "./tslib.es6-DlxpVI88.js";
import { A as has, n as n$1, t as r$2 } from "./Error-CzxduO2m.js";
import { T as N } from "./typedArrayUtil-BAuNmygZ.js";
import { V as I$2 } from "./request-CuG5cxow.js";
import { D as n$2, h as j, x as u$3 } from "./promiseUtils-DhYhergm.js";
import { n as c$2, t as a } from "./decorators-DE7S5xmd.js";
import "./Accessor-kDoDKy4v.js";
import "./tracking-DBoczQof.js";
import "./ObservableBase-Bz6iwe5p.js";
import "./scheduling-DiUcWka1.js";
import "./PooledArray-ChtfzjBt.js";
import "./Evented-GLJbxWO5.js";
import "./SimpleObservable-CNlRjEs1.js";
import "./Collection-BAJSKCip.js";
import "./collectionUtils-DQeMhtWS.js";
import "./JSONSupport-BUaD4jSd.js";
import "./Promise-Dhhz7kXA.js";
import "./Loadable-CQsALnOO.js";
import "./jsonMap-CFSDFmi6.js";
import "./Ellipsoid-DzO_iHAj.js";
import "./assets-BZbzeyNa.js";
import "./pe-BLztJ5xc.js";
import { a as I$3 } from "./spatialReferenceUtils-b3vCEkpS.js";
import "./units-Dg-cK1vO.js";
import { t as S$3 } from "./SpatialReference-rIfb2LrD.js";
import { t as _ } from "./Point-B7zMqEx6.js";
import { t as z } from "./Extent-CquIzaXp.js";
import { o as u$4 } from "./locale-BdrQIP_a.js";
import "./Portal-DYysvbhZ.js";
import "./apiKeyUtils-Bv2Uwsd3.js";
import "./reactiveUtils-DRpp6Nmg.js";
import "./Identifiable-D2tBaz7a.js";
import "./timeZoneUtils-CBNjS1ZG.js";
import "./UnknownTimeZone-Dk-CZx5e.js";
import "./date-BGzzeGV1.js";
import "./colorUtils-BC0_8aMM.js";
import "./mathUtils-hEBUcrMa.js";
import { t as g$1 } from "./Color-C99QAF80.js";
import "./opacityUtils-DgEZ8x-q.js";
import "./Clonable-D_RHUyXD.js";
import "./Polygon-CCBjbbXT.js";
import "./curveUtils-CfkOAT4m.js";
import "./coordsUtils-DXLB9bAf.js";
import "./aaBoundingRect-CgUWvAgv.js";
import "./common-BxLRDsKd.js";
import "./vec2-BPF6SpMH.js";
import "./curveExtent--ue9-x0m.js";
import "./mat3-CPqND9LM.js";
import "./mat3f64-DZZP34-L.js";
import "./Polyline-Cv0nwof6.js";
import "./vec3f64-CwISzc_v.js";
import "./Multipoint-B5Liskmz.js";
import "./spatialReferenceEllipsoidUtils-qNeWENaq.js";
import "./GeographicTransformation-D90zE-j2.js";
import "./geodesicConstants-C0TscDSm.js";
import "./projectBuffer-CV6RkXdH.js";
import "./projectionUtils-CmEsVWfk.js";
import "./jsonUtils-D_oLUjKv.js";
import "./typeUtils-DaICxhuY.js";
import "./utils-5irCjX9t.js";
import "./fieldType-D7SwLPxF.js";
import "./sql-Cyp7eZa9.js";
import "./fieldUtils-CC2YSmV6.js";
import "./PopupTemplate-8SH37QID.js";
import "./fieldFormatUtils-R1ptUFq7.js";
import "./ActionToggle-JH4srUd2.js";
import "./Graphic-D2G0Ykqt.js";
import "./SimpleMarkerSymbol-BjFFaoyw.js";
import "./typeUtils-DZkmoi8p.js";
import "./mat4f64-BA1Qbgtv.js";
import "./DoubleArray-EEc6IyGQ.js";
import "./aaBoundingBox-CzeY9F8R.js";
import "./symbolLayerUtils3D-BQRyZskR.js";
import "./textUtils-B4iTDAON.js";
import "./TextSymbol-CsSnkPMD.js";
import "./SimpleFillSymbol-CbXKKnxp.js";
import "./PictureMarkerSymbol-Crs5VdSs.js";
import "./layerContainerType-ZF61P2__.js";
import "./Field-jzopk-Sr.js";
import "./Queue-CM8W5OTt.js";
import { t as g$2 } from "./FeatureSet-Sjrap7hf.js";
import "./ElevationInfo-Bsg5AqQw.js";
import "./lengthUtils-DrG-JkjU.js";
import "./unitConversionUtils-dsyJpUwL.js";
import "./commonProperties-DQjThAJZ.js";
import "./MemCache-DQgW8nin.js";
import "./LRUCache-C0A4Jg0w.js";
import { t as z$1 } from "./TileInfo-Dm0DlKvz.js";
import "./TileKey-DNAwECdW.js";
import "./signal-DCDIpEz3.js";
import "./ReactiveMap-B1BORGbU.js";
import "./memoryEstimations-BBFGLDPz.js";
import "./QueueProcessor-CWKnNCOB.js";
import { t as T } from "./TilemapCache-BM1_PVUz.js";
import { p as n$3, t as C } from "./multidimensionalUtils-D_1JT4yA.js";
import { _ as t$1, c as C$1, d as N$1, f as Y, g as e, h as r$3, l as G, m as v$2, n as p$3, o as L, p as k$1, t as m$2, u as H, v as r$4 } from "./RasterInfo-DiWp8oA9.js";
import { i as i$1 } from "./pixelRangeUtils-DnVN3K4L.js";
import { t as c$3 } from "./PixelBlock-Dy0T84fY.js";
import { f as C$2, m as E$1, p as D$1 } from "./vectorFieldUtils-CU_o8r0z.js";
import "./stretchRendererUtils-BivIvjHF.js";
import { c as v$3, i as k$2, u as y$1 } from "./stretchUtils-DXnSQHhL.js";
import { n as d$3, r as f$2 } from "./fetchRasterInfo-DaLCT7MZ.js";
import "./RawBlockCache-DeKLg-1a.js";
import "./rasterProjectionHelper-CRTw0Nm9.js";
import { a as n$4, c as t$2, l as u$5, n as e$1, o as o$1, r as i$2, u as Z } from "./xmlUtilities-VvLKOeWa.js";
import { n as y$2, t as a$1 } from "./GCSShiftTransform-nfLvEATH.js";
//#region node_modules/@arcgis/core/layers/raster/datasets/covJSONParser.js
function i(e) {
	return [
		"x",
		"e",
		"east",
		"long",
		"longitude"
	].includes(e.toLowerCase());
}
function r$1(e) {
	return [
		"y",
		"n",
		"west",
		"lat",
		"latitude"
	].includes(e.toLowerCase());
}
function o(e) {
	const { axes: l } = e.domain, s = Object.keys(l), a = [], o = [];
	let u = -1, m = -1, c = [];
	for (let t = 0; t < s.length; t++) {
		const e = s[t];
		i(e) ? u = t : r$1(e) && (m = t);
		const n = l[e], d = [];
		if ("values" in n) {
			n.values.forEach((e) => d.push("string" == typeof e ? new Date(e).getTime() : e));
			const e = d[1] - d[0];
			a.push([d[0] - .5 * e, d[d.length - 1] + .5 * e]), o.push(e);
		} else {
			const { start: e, stop: t, num: l } = n, s = (t - e) / (l - 1);
			a.push([e - .5 * s, t + .5 * s]), o.push(s);
			for (let n = 0; n < l; n++) d.push(e + s * n);
		}
		c.push({
			name: e,
			values: d,
			extent: [d[0], d[d.length - 1]]
		});
	}
	u > -1 && -1 === m ? m = 0 === u ? 1 : 0 : m > -1 && -1 === u ? u = 0 === m ? 1 : 0 : -1 === m && -1 === u && (u = 0, m = 1), c = c.filter((e, t) => !(t === u || t === m));
	const { referencing: d } = e.domain, f = d.find((e) => e.coordinates.includes(s[u])).system.id, p = f?.slice(f.lastIndexOf("/") + 1), g = new S$3({ wkid: null == p || "CRS84" === p ? 4326 : Number(p) }), [b, y] = a[u], [x, j] = a[m], v = new z({
		xmin: b,
		xmax: y,
		ymin: x,
		ymax: j,
		spatialReference: g
	});
	return {
		width: Math.round(v.width / o[u]),
		height: Math.round(v.height / o[m]),
		extent: v,
		dimensions: c
	};
}
function u$2(e) {
	const t = u$4();
	return t ? e[t] ?? Object.values(e)[0] : Object.values(e)[0];
}
function m$1() {
	return Math.round(255 * Math.random());
}
function c$1(t) {
	const n = {}, { parameters: l } = t;
	if (!l) return n;
	for (const [s, a] of Object.entries(l)) {
		const { type: t, description: l, unit: i, categoryEncoding: r, observedProperty: o } = a;
		if ("Parameter" === t && (n[s] = {}, l && (n[s].description = u$2(l)), i && (n[s].unit = i.label ? u$2(i.label) : null, n[s].symbol = i.symbol?.value), r)) {
			const t = Object.entries(r).map((e, t) => ({
				OID: t,
				Value: Number(e[1]),
				ClassName: e[0].slice(e[0].lastIndexOf("/") + 1),
				Count: 1
			}));
			let l = !1;
			o?.categories?.length && (o.categories.forEach((n) => {
				if (!n.id) return;
				const s = n.id.slice(n.id.lastIndexOf("/") + 1), a = t.find((e) => e.ClassName === s);
				if (!a) return;
				if (a.Label = n.label ? u$2(n.label) : null, n.preferredColor) {
					const t = g$1.fromHex(n.preferredColor);
					t && (l = !0, a.Red = t.r, a.Green = t.g, a.Blue = t.b);
				}
			}), l && t.forEach((e) => {
				e.Red ?? (e.Red = m$1(), e.Green = m$1(), e.Blue = m$1());
			}));
			const a = {
				objectIdFieldName: "",
				fields: [
					{
						name: "OID",
						type: "esriFieldTypeOID",
						alias: "OID",
						domain: null
					},
					{
						name: "Value",
						type: "esriFieldTypeInteger",
						alias: "Value",
						domain: null
					},
					{
						name: "Count",
						type: "esriFieldTypeDouble",
						alias: "Count",
						domain: null
					},
					{
						name: "ClassName",
						type: "esriFieldTypeString",
						alias: "ClassName",
						domain: null,
						length: 50
					},
					{
						name: "Label",
						type: "esriFieldTypeString",
						alias: "Label",
						domain: null,
						length: 50
					}
				],
				features: t.map((e) => ({ attributes: e }))
			};
			l && a.fields.push({
				name: "Red",
				type: "esriFieldTypeInteger",
				alias: "Red",
				domain: null
			}, {
				name: "Green",
				type: "esriFieldTypeInteger",
				alias: "Green",
				domain: null
			}, {
				name: "Blue",
				type: "esriFieldTypeInteger",
				alias: "Blue",
				domain: null
			}), n[s].attributeTable = a;
		}
	}
	return n;
}
function d$2(e) {
	let t = Number.MAX_VALUE, n = -Number.MAX_VALUE;
	for (let l = 0; l < e.length; l++) {
		const s = e[l];
		null != s && (s < t && (t = s), s > n && (n = s));
	}
	return i$1(t, n);
}
function f$1(e, t, n) {
	const l = e.map((e, n) => ({
		name: e,
		count: t[n]
	})).sort((e, t) => e.name > t.name ? -1 : 1), s = (a = 1, (e) => a *= e.count);
	var a;
	const i = [...l.slice(1), {
		name: "",
		count: 1
	}].reverse().map(s).reverse();
	let r = 0;
	for (let o = e.length - 1; o >= 0; o--) r += i[l.findIndex(({ name: t }) => t === e[o])] * (n % t[o]), n = Math.floor(n / t[o]);
	return r;
}
function p$2(e) {
	const { width: t, height: n, extent: l, dimensions: s } = o(e), { ranges: i } = e, r = Object.keys(i).sort((e, t) => e < t ? -1 : 1), u = [];
	for (let a = 0; a < r.length; a++) {
		const e = r[a];
		s?.length && u.push({
			name: e,
			dimensions: s
		});
	}
	const m = c$1(e);
	u.forEach((e) => m[e.name] && Object.assign(e, m[e.name]));
	const p = u.length ? { variables: u } : void 0, h = [];
	for (let o = 0; o < r.length; o++) {
		const { values: l, dataType: u, axisNames: m, shape: c } = i[r[o]], p = c.length > 2 ? o * c.slice(0, -2).reduce((e, t) => e * t) : 0, g = m.slice(0, -2), b = c.slice(0, -2), y = "float" === u ? "f32" : d$2(l), x = t * n, j = l.length / x;
		for (let i = 0; i < j; i++) {
			const e = c$3.createEmptyBand(y, x), r = new Uint8Array(x).fill(255);
			let u = !1;
			const m = i * x;
			for (let t = 0; t < x; t++) {
				const n = l[m + t];
				null == n ? (r[t] = 0, u = !0) : e[t] = n;
			}
			if (0 === o || s?.length) {
				const l = new c$3({
					width: t,
					height: n,
					mask: u ? r : null,
					pixels: [e],
					pixelType: y
				});
				if (l.updateStatistics(), s?.length) h[f$1(g, b, i) + p] = l;
				else h.push(l);
			} else {
				const t = h[i];
				t.pixels.push(e), u ? t.mask && (t.mask = c$3.combineBandMasks([t.mask, r])) : t.mask = u ? r : null;
			}
		}
	}
	return {
		extent: l,
		pixelBlocks: h,
		multidimensionalInfo: p,
		attributeTable: Object.values(m).find((e) => e.attributeTable)?.attributeTable,
		bandNames: p ? void 0 : r
	};
}
//#endregion
//#region node_modules/@arcgis/core/layers/raster/datasets/InMemoryRaster.js
var f = class extends Z {
	constructor() {
		super(...arguments), this.datasetFormat = "MEMORY", this.source = null;
	}
	get url() {
		return "";
	}
	fetchRawTile(t, e, i, s = {}) {
		if (!this._pixelBlockTiles) {
			const { rasterInfo: a } = this, [o, r] = a.storageInfo.tileInfo.size, { sliceId: l } = s, { pixelBlocks: n } = this.source, p = {
				pixelBlock: null == l ? n[0] : n[l],
				useBilinear: "thematic" !== a.dataType,
				tileSize: {
					width: o,
					height: r
				},
				level: t,
				row: e,
				col: i
			}, c = this.rasterJobHandler ? this.rasterJobHandler.clipTile(p, s) : D$1(p);
			return Promise.resolve(c);
		}
		const a = this._pixelBlockTiles.get(`${t}/${e}/${i}`);
		return Promise.resolve(a);
	}
	async _open(t) {
		const e = this.source, { pixelBlocks: i, attributeTable: s, statistics: a, histograms: l, name: n, nativeExtent: m, transform: p, colormap: c } = e, u = i[0], { width: h, height: f, pixelType: x } = u, y = e.extent ?? new z({
			xmin: -.5,
			ymin: .5,
			xmax: h - .5,
			ymax: f - .5,
			spatialReference: new S$3({ wkid: 3857 })
		}), g = e.isPseudoSpatialReference ?? !e.extent, b = {
			x: y.width / h,
			y: y.height / f
		}, I = { ...e.keyProperties };
		e.dataType && (I.DataType = e.dataType), e.bandInfos && (I.BandProperties = e.bandInfos.map((t) => t.toJSON())), s && (I.DataType = "Thematic");
		const v = new m$2({
			width: h,
			height: f,
			pixelType: x,
			extent: y,
			nativeExtent: m,
			attributeTable: s,
			colormap: c,
			transform: p,
			pixelSize: b,
			spatialReference: y.spatialReference,
			bandCount: u.pixels.length,
			keyProperties: I,
			multidimensionalInfo: e.multidimensionalInfo,
			statistics: a,
			isPseudoSpatialReference: g,
			histograms: l
		});
		this.ioConfig.skipMapInfo && this.updateImageSpaceRasterInfo(v), this.createRemoteDatasetStorageInfo(v, 512, 512), this._set("rasterInfo", v), this.updateTileInfo(), v.multidimensionalInfo ? await this._buildMDimStats(e.pixelBlocks, v.multidimensionalInfo) : await this._buildInMemoryRaster(u, {
			width: 512,
			height: 512
		}, t), v.multidimensionalInfo || (this.source = null), this.datasetName = n;
	}
	async _buildInMemoryRaster(t, s, a) {
		const { rasterInfo: o } = this, r = o.storageInfo.maximumPyramidLevel ?? 0, l = "thematic" !== o.dataType, n = this.rasterJobHandler ? this.rasterJobHandler.split({
			pixelBlock: t,
			tileSize: s,
			maximumPyramidLevel: r,
			useBilinear: l
		}, a) : Promise.resolve(E$1(t, s, r, l)), m = null != o.statistics, u = null != o.histograms, d = await j([n, this.ioConfig.skipStatistics || m ? Promise.resolve({
			statistics: null,
			histograms: null
		}) : this.rasterJobHandler ? this.rasterJobHandler.estimateStatisticsHistograms({ pixelBlock: t }, a) : Promise.resolve(y$1(t))]);
		if (!d[0].value && d[1].value) throw new r$2("inmemory-raster:open", "failed to build in memory raster");
		this._pixelBlockTiles = d[0].value, m || (o.statistics = d[1].value?.statistics), u || (o.histograms = d[1].value?.histograms);
	}
	async _buildMDimStats(t, e, i) {
		for (let s = 0; s < e.variables.length; s++) {
			const a = e.variables[s];
			if (a.statistics) continue;
			const r = C(a.dimensions.map((t) => new n$3({
				variableName: a.name,
				dimensionName: t.name,
				values: [t.values?.[0] ?? t.extent?.[0]],
				isSlice: !0
			})), e), l = null == r ? null : t[r];
			if (null == l) continue;
			const m = this.rasterJobHandler ? await this.rasterJobHandler.computeStatisticsHistograms({ pixelBlock: l }, i) : k$2(l);
			a.statistics = m.statistics, a.histograms || (a.histograms = m.histograms);
		}
	}
};
__decorate([a({
	type: String,
	json: { write: !0 }
})], f.prototype, "datasetFormat", void 0), __decorate([a()], f.prototype, "source", void 0), __decorate([a()], f.prototype, "url", null), f = __decorate([c$2("esri.layers.raster.datasets.InMemoryRaster")], f);
var x$1 = f;
//#endregion
//#region node_modules/@arcgis/core/layers/raster/datasets/CovJSONRaster.js
var p$1 = class extends Z {
	constructor() {
		super(...arguments), this.datasetFormat = "CovJSON";
	}
	fetchRawTile(e, t, r, s = {}) {
		return this._inMemoryRaster.fetchRawTile(e, t, r, s);
	}
	async _open(e) {
		const { extent: t, pixelBlocks: r, multidimensionalInfo: s, attributeTable: a, bandNames: i } = await this._fetchData(e), { statistics: o, histograms: n } = k$2(r[0]), p = i?.map((e) => ({ BandName: e })), h = {
			DataType: a ? "Thematic" : s ? "Scientific" : "Generic",
			BandProperties: p
		}, d = new x$1({ source: {
			extent: t,
			pixelBlocks: r,
			attributeTable: a ? g$2.fromJSON(a) : null,
			multidimensionalInfo: s,
			statistics: o,
			histograms: n,
			keyProperties: h,
			isPseudoSpatialReference: !1
		} });
		await d.open(), this._inMemoryRaster = d;
		const u = this.source ? "" : this.url.slice(this.url.lastIndexOf("/") + 1);
		this._set("datasetName", u.slice(0, u.indexOf("."))), this._set("rasterInfo", d.rasterInfo);
	}
	async _fetchData(e) {
		const r = this.source ?? (await this.request(this.url, { signal: e?.signal })).data, s = "imagery-tile-layer:open-coverage-json";
		if ("coverage" !== r.type?.toLowerCase() || "grid" !== r.domain?.domainType?.toLowerCase()) throw new r$2(s, "Only coverage with Grid domain type is supported");
		if (!r.ranges) throw new r$2(s, "Missing ranges in the grid coverage data");
		if (!r.domain.referencing?.length) throw new r$2(s, "Missing domain referencing in the grid coverage data");
		const a = Object.values(r.ranges);
		for (let n = 0; n < a.length; n++) {
			const { axisNames: e, shape: r, type: l, values: c } = a[n];
			if (!("ndarray" === l.toLowerCase() && c?.length && e?.length && r?.length)) throw new r$2(s, "Only ranges with valid NdArray, axisNames, shape, and inline values are supported");
			if (!(i(e[e.length - 1]) && r$1(e[e.length - 2]))) throw new r$2(s, "Only row-major ordered pixel values are supported. X axis must be the last axis.");
		}
		return p$2(r);
	}
};
__decorate([a({
	type: String,
	json: { write: !0 }
})], p$1.prototype, "datasetFormat", void 0), __decorate([a({ constructOnly: !0 })], p$1.prototype, "source", void 0), p$1 = __decorate([c$2("esri.layers.raster.datasets.CovJSONRaster")], p$1);
var h$1 = p$1;
//#endregion
//#region node_modules/@arcgis/core/layers/raster/datasets/pamParser.js
function u$1(e, t) {
	if (!e || !t) return null;
	const n = [];
	for (let r = 0; r < e.length; r++) n.push(e[r]), n.push(t[r]);
	return n;
}
function c(e) {
	const t = e$1(e, "GeodataXform"), n = d$1(o$1(t, "SpatialReference/WKID") || t$2(t, "SpatialReference/WKT"));
	if ("typens:PolynomialXform" !== t.getAttribute("xsi:type")) return {
		spatialReference: n,
		transform: null
	};
	const r = o$1(t, "PolynomialOrder") ?? 1, a = u$5(t, "CoeffX/Double"), c = u$5(t, "CoeffY/Double"), m = u$5(t, "InverseCoeffX/Double"), p = u$5(t, "InverseCoeffY/Double"), S = u$1(a, c), C = u$1(m, p);
	return {
		spatialReference: n,
		transform: S && C && S.length && C.length ? new y$2({
			spatialReference: n,
			polynomialOrder: r,
			forwardCoefficients: S,
			inverseCoefficients: C
		}) : null
	};
}
function m(e) {
	const t = o$1(e, "NoDataValue"), n = e$1(e, "Histograms/HistItem"), r = o$1(n, "HistMin"), o = o$1(n, "HistMax"), f = o$1(n, "BucketCount"), u = t$2(n, "HistCounts")?.split("|").map((e) => Number(e));
	let c, m, d, p;
	n$4(e, "Metadata/MDI").forEach((e) => {
		const t = Number(e.textContent ?? e.nodeValue);
		switch (e.getAttribute("key").toUpperCase()) {
			case "STATISTICS_MINIMUM":
				c = t;
				break;
			case "STATISTICS_MAXIMUM":
				m = t;
				break;
			case "STATISTICS_MEAN":
				d = t;
				break;
			case "STATISTICS_STDDEV": p = t;
		}
	});
	const S = o$1(e, "Metadata/SourceBandIndex");
	return {
		noDataValue: t,
		histogram: u?.length && null != r && null != o ? {
			min: r,
			max: o,
			size: f || u.length,
			counts: u
		} : null,
		sourceBandIndex: S,
		statistics: null != c && null != m ? {
			min: c,
			max: m,
			avg: d,
			stddev: p
		} : null
	};
}
function d$1(e) {
	if (!e) return null;
	let r = Number(e);
	if (!isNaN(r) && 0 !== r) return new S$3({ wkid: r });
	if (e = String(e).trim(), I$3(e)) return new S$3({ wkt2: e });
	const s = e.toUpperCase();
	if (s.startsWith("COMPD_CS")) {
		if (!s.includes("VERTCS") || !s.includes("GEOGCS") && !s.startsWith("PROJCS")) return null;
		const n = s.indexOf("VERTCS"), a = s.indexOf("PROJCS"), i = a > -1 ? a : s.indexOf("GEOGCS");
		if (-1 === i) return null;
		const l = e.slice(i, e.lastIndexOf("]", n) + 1).trim(), o = e.slice(n, e.lastIndexOf("]")).trim();
		r = p(l);
		const f = new S$3(r ? { wkid: r } : { wkt: l }), u = p(o);
		return u && (f.vcsWkid = u), f;
	}
	return s.startsWith("GEOGCS") || s.startsWith("PROJCS") ? (r = p(e), new S$3(0 !== r ? { wkid: r } : { wkt: e })) : null;
}
function p(e) {
	const t = e.replaceAll("]", "[").replaceAll("\"", "").split("[").map((e) => e.trim()).filter((e) => "" !== e), n = t[t.length - 1].split(","), r = n[0]?.toLowerCase();
	if (("epsg" === r || "esri" === r) && e.endsWith("\"]]")) {
		const e = Number(n[1]);
		if (!isNaN(e) && 0 !== e) return e;
	}
	return 0;
}
function S$2(t) {
	if ("pamdataset" !== t?.documentElement.tagName?.toLowerCase()) return {};
	const n = {
		spatialReference: null,
		transform: null,
		metadata: {},
		rasterBands: [],
		statistics: null,
		histograms: null
	};
	t.documentElement.childNodes.forEach((e) => {
		if (1 === e.nodeType) {
			if (i$2(e, "SRS")) {
				if (!n.spatialReference) n.spatialReference = d$1(t$2(e));
			} else if (i$2(e, "Metadata")) if ("xml:ESRI" === e.getAttribute("domain")) {
				const { spatialReference: t, transform: r } = c(e);
				n.transform = r, n.spatialReference || (n.spatialReference = t);
			} else n$4(e, "MDI").forEach((e) => n.metadata[e.getAttribute("key")] = t$2(e));
			else if (i$2(e, "PAMRasterBand")) {
				const t = m(e);
				null != t.sourceBandIndex && null == n.rasterBands[t.sourceBandIndex] ? n.rasterBands[t.sourceBandIndex] = t : n.rasterBands.push(t);
			}
		}
	});
	const i = n.rasterBands;
	if (i.length) {
		n.statistics = !!i[0].statistics ? i.map((e) => e.statistics).filter(N) : null;
		n.histograms = !!i[0].histogram ? i.map((e) => e.histogram).filter(N) : null;
	}
	return n;
}
//#endregion
//#region node_modules/@arcgis/core/layers/raster/datasets/ImageAuxRaster.js
var h = class extends Z {
	fetchRawTile(t, e, s, r = {}) {
		return this._inMemoryRaster.fetchRawTile(t, e, s, r);
	}
	async _open(t) {
		const e = await this._fetchData(t);
		let { spatialReference: s, statistics: r, histograms: a, transform: o } = await this._fetchAuxiliaryData(t);
		const l = !s;
		l && (s = new S$3({ wkid: 3857 })), a?.length && null == r && (r = v$3(a));
		const { width: f, height: p } = e;
		let u = new z({
			xmin: -.5,
			ymin: .5 - p,
			xmax: f - .5,
			ymax: .5,
			spatialReference: s
		});
		const h = o ? o.forwardTransform(u) : u;
		let d = !0;
		if (o) {
			const t = o.forwardCoefficients;
			d = t && 0 === t[1] && 0 === t[2], d && (o = null, u = h);
		}
		const w = new x$1({
			source: {
				extent: h,
				nativeExtent: u,
				transform: o,
				pixelBlocks: [e],
				statistics: r,
				histograms: a,
				keyProperties: { DateType: "Processed" },
				isPseudoSpatialReference: l
			},
			ioConfig: {
				sampling: "closest",
				skipStatistics: !0
			}
		});
		this.ioConfig.skipMapInfo && (w.ioConfig.skipMapInfo = !0), await w.open(), w.source = null, this._set("rasterInfo", w.rasterInfo), this._inMemoryRaster = w;
	}
	async _fetchData(t) {
		const { data: s } = await this.request(this.url, {
			responseType: "array-buffer",
			signal: t?.signal
		}), r = L(s).toUpperCase();
		if ("JPG" !== r && "PNG" !== r && "GIF" !== r && "BMP" !== r) throw new r$2("image-aux-raster:open", "the data is not a supported format");
		this._set("datasetFormat", r);
		const a = r.toLowerCase(), o = "gif" === a || "bmp" === a || !has("ios"), i = await this.decodePixelBlock(s, {
			format: a,
			useCanvas: o,
			hasNoZlibMask: !0
		});
		if (null == i) throw new r$2("image-aux-raster:open", "the data cannot be decoded");
		return i;
	}
	async _fetchAuxiliaryData(t) {
		const e = t?.signal, { skipExtensions: a = [], skipMapInfo: o } = this.ioConfig, i = o || a.includes("aux.xml") ? null : this.request(this.url + ".aux.xml", {
			responseType: "xml",
			signal: e
		}), n = this.datasetFormat, l = "JPG" === n ? "jgw" : "PNG" === n ? "pgw" : "BMP" === n ? "bpw" : null, p = await j([i, l && a.includes(l) ? null : this.request(this.url.slice(0, this.url.lastIndexOf(".")) + "." + l, {
			responseType: "text",
			signal: e
		})]);
		if (e?.aborted) throw u$3();
		const c = S$2(p[0].value?.data);
		if (!c.transform) {
			const t = p[1].value ? p[1].value.data.split("\n").slice(0, 6).map((t) => Number(t)) : null;
			c.transform = 6 === t?.length ? new y$2({ forwardCoefficients: [
				t[4],
				t[5],
				t[0],
				-t[1],
				t[2],
				-t[3]
			] }) : null;
		}
		return c;
	}
};
__decorate([a({
	type: String,
	json: { write: !0 }
})], h.prototype, "datasetFormat", void 0), h = __decorate([c$2("esri.layers.raster.datasets.ImageAuxRaster")], h);
var d = h;
//#endregion
//#region node_modules/@arcgis/core/layers/raster/datasets/ImageServerRaster.js
var g = class extends Z {
	constructor() {
		super(...arguments), this._levelOffset = 0, this._tilemapCache = null, this._slices = null, this.datasetFormat = "RasterTileServer", this.tileType = null;
	}
	async fetchRawTile(e, t, i, s = {}) {
		const { storageInfo: r, extent: a } = this.rasterInfo, { transposeInfo: l } = r, n = null != l && !!s.transposedVariableName;
		if (this._slices && !n && null == s.sliceId) return null;
		const o = n ? 0 : r.maximumPyramidLevel - e + this._levelOffset, c = `${this.url}/tile/${o}/${t}/${i}`, u = {
			...this._slices ? n ? { variable: s.transposedVariableName } : { sliceId: s.sliceId || 0 } : null,
			...s.refreshParameters
		};
		let h, f;
		if (r.isBsqTile) {
			const e = (s.bandIds?.length ? s.bandIds : [
				0,
				1,
				2
			]).map((e) => this.request(c, {
				query: {
					...u,
					bandId: e
				},
				responseType: "array-buffer",
				signal: s.signal
			})), t = await Promise.all(e), i = t.map((e) => e.data.byteLength).reduce((e, t) => e + t), r = new Uint8Array(i);
			f = [];
			let a = 0;
			for (const { data: s } of t) f.push(a), r.set(new Uint8Array(s), a), a += s.byteLength;
			h = r.buffer;
		} else h = (await this.request(c, {
			query: u,
			responseType: "array-buffer",
			signal: s.signal
		})).data;
		if (!h) return null;
		const p = n ? l.tileSize : r.tileInfo.size, d = await this.decodePixelBlock(h, {
			width: p[0],
			height: p[1],
			planes: f?.length,
			offsets: f,
			pixelType: null,
			isPoint: "Elevation" === this.tileType,
			returnInterleaved: n,
			noDataValue: this.rasterInfo.noDataValue
		});
		if (null == d) return null;
		const y = r.blockBoundary[e];
		if ("jpg" !== r.compression || i > y.minCol && i < y.maxCol && t > y.minRow && t < y.maxRow) return d;
		const { origin: x, blockWidth: g, blockHeight: v } = r, { x: S, y: I } = this.getPyramidPixelSize(e), w = Math.round((a.xmin - x.x) / S) % g, b = Math.round((a.xmax - x.x) / S) % g || g, T = Math.round((x.y - a.ymax) / I) % v, M = Math.round((x.y - a.ymin) / I) % v || v, _ = i === y.minCol ? w : 0, j = t === y.minRow ? T : 0, O = i === y.maxCol ? b : g, R = t === y.maxRow ? M : v;
		return C$2(d, {
			x: _,
			y: j
		}, {
			width: O - _,
			height: R - j
		}), d;
	}
	getSliceIndex(e) {
		if (!this._slices || null == e || 0 === e.length) return null;
		const t = e;
		for (let i = 0; i < this._slices.length; i++) {
			const e = this._slices[i].multidimensionalDefinition;
			if (e.length === t.length && !e.some((e) => {
				const i = t.find((t) => e.variableName === t.variableName && t.dimensionName === e.dimensionName);
				if (!i) return !0;
				return (Array.isArray(e.values[0]) ? `${e.values[0][0]}-${e.values[0][1]}` : e.values[0]) !== (Array.isArray(i.values[0]) ? `${i.values[0][0]}-${i.values[0][1]}` : i.values[0]);
			})) return i;
		}
		return null;
	}
	async fetchVariableStatisticsHistograms(e, t) {
		const i = this.request(this.url + "/statistics", {
			query: {
				variable: e,
				f: "json"
			},
			signal: t
		}).then((e) => e.data?.statistics), s = this.request(this.url + "/histograms", {
			query: {
				variable: e,
				f: "json"
			},
			signal: t
		}).then((e) => e.data?.histograms), r = await Promise.all([i, s]);
		return r[0] && r[0].forEach((e) => {
			e.avg = e.mean, e.stddev = e.standardDeviation;
		}), r[1]?.[0]?.counts?.length || (r[1] = null), {
			statistics: r[0] || null,
			histograms: r[1] || null
		};
	}
	async computeBestPyramidLevelForLocation(e, t = {}) {
		if (!this._tilemapCache) return 0;
		let i = this.identifyPixelLocation(e, 0, t.datumTransformation);
		if (null === i) return null;
		let s = 0;
		const { maximumPyramidLevel: r } = this.rasterInfo.storageInfo;
		let a = r - s + this._levelOffset;
		const l = i.srcLocation;
		for (; a >= 0;) {
			try {
				if ("available" === await this._tilemapCache.fetchAvailability(a, i.row, i.col, t)) break;
			} catch {}
			if (a--, s++, i = this.identifyPixelLocation(l, s, t.datumTransformation), null === i) return null;
		}
		return -1 === a || null == i ? null : s;
	}
	async _open(e) {
		const r = e?.signal, a = this.sourceJSON ? { data: this.sourceJSON } : await this.request(this.url, {
			query: { f: "json" },
			signal: r
		});
		a.ssl && (this.url = this.url.replace(/^http:/i, "https:"));
		const l = a.data;
		if (this.sourceJSON = l, !l) throw new r$2("imageserverraster:open", "cannot initialize tiled image service, missing service info");
		if (!l.tileInfo) throw new r$2("imageserverraster:open", "use ImageryLayer to open non-tiled image services");
		this._fixScaleInServiceInfo();
		this.tileType = l.cacheType, this.tileType ?? ([
			"jpg",
			"jpeg",
			"png",
			"png8",
			"png24",
			"png32",
			"mixed"
		].includes(l.tileInfo.format.toLowerCase()) ? this.tileType = "Map" : "lerc" === l.tileInfo.format.toLowerCase() ? this.tileType = "Elevation" : this.tileType = "Raster"), this.datasetName = l.name?.slice(l.name.indexOf("/") + 1) ?? "";
		const o = await this._fetchRasterInfo({ signal: r });
		if (null == o) throw new r$2("image-server-raster:open", "cannot initialize image service");
		d$3(o, l);
		const c = "Map" === this.tileType ? v$1(l.tileInfo, l) : z$1.fromJSON(l.tileInfo);
		n$2(c);
		const [m, u] = this._computeMinMaxLOD(o, c), { extent: h, pixelSize: x } = o, g = .001 * Math.min(x.x, x.y);
		(x.x !== x.y || Math.abs(m.resolution - x.x) > g) && (x.x = x.y = m.resolution, o.width = Math.ceil((h.xmax - h.xmin) / x.x - .1), o.height = Math.ceil((h.ymax - h.ymin) / x.y - .1));
		const I = m.level - u.level, [w, b] = c.size, T$1 = [], M = [], { lods: _ } = c;
		_.forEach((e, t) => {
			e.level >= u.level && e.level <= m.level && T$1.push({
				x: e.resolution,
				y: e.resolution
			}), t < _.length - 1 && M.push(Math.round(10 * e.resolution / _[t + 1].resolution) / 10);
		}), T$1.sort((e, t) => e.x - t.x);
		const j = this.computeBlockBoundary(h, w, b, c.origin, T$1, I), O = T$1.length > 1 ? T$1.slice(1) : null;
		let R;
		l.transposeInfo && (R = {
			tileSize: [l.transposeInfo.rows, l.transposeInfo.cols],
			packetSize: o.keyProperties?._yxs.PacketSize ?? 0
		});
		const z = M.length <= 1 || M.length >= 3 && M.slice(0, -1).every((e) => e === M[0]) ? M[0] ?? 2 : Math.round(10 / (u.resolution / m.resolution) ** (-1 / I)) / 10, N = !!l.bsq && "Raster" === this.tileType;
		if (o.storageInfo = new p$3({
			blockWidth: c.size[0],
			blockHeight: c.size[1],
			pyramidBlockWidth: c.size[0],
			pyramidBlockHeight: c.size[1],
			pyramidResolutions: O,
			pyramidScalingFactor: z,
			compression: c.format,
			origin: c.origin,
			firstPyramidLevel: 1,
			maximumPyramidLevel: I,
			tileInfo: c,
			isBsqTile: N,
			transposeInfo: R,
			blockBoundary: j
		}), S$1(o), this._set("rasterInfo", o), l.capabilities.toLowerCase().includes("tilemap")) this._tilemapCache = new T({ layer: {
			tileInfo: o.storageInfo.tileInfo,
			parsedUrl: I$2(this.url),
			url: this.url,
			tileServers: []
		} });
	}
	async _fetchRasterInfo(e) {
		const t = this.sourceJSON;
		if ("Map" === this.tileType) {
			const e = t.fullExtent || t.extent, i = Math.ceil((e.xmax - e.xmin) / t.pixelSizeX - .1), s = Math.ceil((e.ymax - e.ymin) / t.pixelSizeY - .1), r = S$3.fromJSON(t.spatialReference || e.spatialReference), a = new _({
				x: t.pixelSizeX,
				y: t.pixelSizeY,
				spatialReference: r
			});
			return new m$2({
				width: i,
				height: s,
				bandCount: 3,
				extent: z.fromJSON(e),
				spatialReference: r,
				pixelSize: a,
				pixelType: "u8",
				statistics: null,
				keyProperties: { DataType: "processed" }
			});
		}
		const i = {
			...e,
			query: {
				...this.ioConfig.customFetchParameters,
				f: "json"
			}
		}, s = f$2(this.url, this.sourceJSON, i), r = t.hasMultidimensions ? this.request(`${this.url}/slices`, i).then((e) => e.data?.slices).catch(() => null) : null, a = await Promise.all([s, r]);
		return this._slices = a[1], a[0];
	}
	_fixScaleInServiceInfo() {
		const { sourceJSON: e } = this;
		e.minScale && e.minScale < 0 && (e.minScale = 0), e.maxScale && e.maxScale < 0 && (e.maxScale = 0);
	}
	_computeMinMaxLOD(e, t) {
		const { pixelSize: i } = e, s = .5 / e.width * i.x, { lods: r } = t, a = t.lodAt(Math.max.apply(null, r.map((e) => e.level))), l = t.lodAt(Math.min.apply(null, r.map((e) => e.level))), { tileType: n } = this;
		if ("Map" === n) return this._levelOffset = r[0].level, [a, l];
		if ("Raster" === n) return [r.find((e) => e.resolution === i.x) ?? a, l];
		const { minScale: o, maxScale: c } = this.sourceJSON;
		let m = a;
		c > 0 && (m = r.find((e) => Math.abs(e.scale - c) < s), m || (m = r.filter((e) => e.scale > c).sort((e, t) => e.scale > t.scale ? 1 : -1)[0] ?? a));
		let u = l;
		return o > 0 && (u = r.find((e) => Math.abs(e.scale - o) < s) ?? l, this._levelOffset = u.level - l.level), [m, u];
	}
};
function v$1(e, t) {
	if (!e) return null;
	const { minScale: i, maxScale: s, minLOD: r, maxLOD: a } = t;
	if (null != r && null != a) return z$1.fromJSON({
		...e,
		lods: e.lods.filter(({ level: e }) => null != e && e >= r && e <= a)
	});
	if (0 !== i && 0 !== s) {
		const t = (e) => Math.round(1e4 * e) / 1e4, r = i ? t(i) : Infinity, a = s ? t(s) : -Infinity;
		return z$1.fromJSON({
			...e,
			lods: e.lods.filter((e) => {
				const i = t(e.scale);
				return i <= r && i >= a;
			})
		});
	}
	return z$1.fromJSON(e);
}
function S$1(e) {
	const { extent: t, spatialReference: i } = e;
	t.xmin > -1 && t.xmax > 181 && i?.wkid && i.isGeographic && (e.nativeExtent = e.extent, e.transform = new a$1(), e.extent = e.transform.forwardTransform(t));
}
__decorate([a({
	type: String,
	json: { write: !0 }
})], g.prototype, "datasetFormat", void 0), __decorate([a()], g.prototype, "tileType", void 0), g = __decorate([c$2("esri.layers.raster.datasets.ImageServerRaster")], g);
var I$1 = g;
//#endregion
//#region node_modules/@arcgis/core/layers/raster/datasets/MRFRaster.js
var x = new Map([
	["byte", "u8"],
	["signedbyte", "s8"],
	["int8", "s8"],
	["uint8", "u8"],
	["int16", "s16"],
	["uint16", "u16"],
	["int32", "s32"],
	["uint32", "u32"],
	["float32", "f32"],
	["float64", "f64"],
	["double64", "f64"]
]), y = /* @__PURE__ */ new Map();
y.set("none", {
	blobExtension: ".til",
	isOneSegment: !0,
	decoderFormat: "bip"
}), y.set("lerc", {
	blobExtension: ".lrc",
	isOneSegment: !1,
	decoderFormat: "lerc"
}), y.set("deflate", {
	blobExtension: ".pzp",
	isOneSegment: !0,
	decoderFormat: "deflate"
}), y.set("jpeg", {
	blobExtension: ".pjg",
	isOneSegment: !0,
	decoderFormat: "jpg"
}), y.set("qb3", {
	blobExtension: ".pq3",
	isOneSegment: !0,
	decoderFormat: "qb3"
});
var b$1 = class extends Z {
	constructor() {
		super(...arguments), this._files = null, this._storageIndex = null, this.datasetFormat = "MRF";
	}
	async fetchRawTile(t, e, r, s = {}) {
		const { blockWidth: i, blockHeight: o, blockBoundary: a } = this.rasterInfo.storageInfo, n = a[t];
		if (!n || n.maxRow < e || n.maxCol < r || n.minRow > e || n.minCol > r) return null;
		const { bandCount: l, pixelType: f } = this.rasterInfo, { ranges: u, actualTileWidth: h, actualTileHeight: c } = this._getTileLocation(t, e, r);
		if (!u || 0 === u.length) return null;
		if (u.some((t) => t.from > t.to)) return new c$3({
			width: i,
			height: o,
			pixels: void 0,
			mask: new Uint8Array(i * o),
			validPixelCount: 0
		});
		const { bandIds: p } = this.ioConfig, g = this._getBandSegmentCount(), d = [];
		let x = 0;
		for (x = 0; x < g; x++) p && !p.includes(x) || d.push(this.request(this._files.data, {
			range: {
				from: u[x].from,
				to: u[x].to
			},
			responseType: "array-buffer",
			signal: s.signal
		}));
		const b = await Promise.all(d), w = b.map((t) => t.data.byteLength).reduce((t, e) => t + e), I = new Uint8Array(w), A = [];
		let F = 0;
		for (x = 0; x < g; x++) A.push(F), I.set(new Uint8Array(b[x].data), F), F += b[x].data.byteLength;
		const R = y.get(this.rasterInfo.storageInfo.compression).decoderFormat, _ = await this.decodePixelBlock(I.buffer, {
			width: i,
			height: o,
			format: R,
			planes: p?.length || l,
			offsets: A,
			pixelType: f
		}).catch(() => null);
		if (null == _) return null;
		let { noDataValue: S } = this.rasterInfo;
		if (null != S && "lerc" !== R && !_.mask && (S = S[0], null != S)) {
			const t = _.width * _.height, e = new Uint8Array(t);
			if (Math.abs(S) > 1e24) for (x = 0; x < t; x++) Math.abs((_.pixels[0][x] - S) / S) > 1e-6 && (e[x] = 1);
			else for (x = 0; x < t; x++) _.pixels[0][x] !== S && (e[x] = 1);
			_.mask = e;
		}
		let M = 0, k = 0;
		if (h !== i || c !== o) {
			let t = _.mask;
			if (t) for (x = 0; x < o; x++) if (k = x * i, x < c) for (M = h; M < i; M++) t[k + M] = 0;
			else for (M = 0; M < i; M++) t[k + M] = 0;
			else for (t = new Uint8Array(i * o), _.mask = t, x = 0; x < c; x++) for (k = x * i, M = 0; M < h; M++) t[k + M] = 1;
		}
		return _;
	}
	async _open(t) {
		this.datasetName = this.url.slice(this.url.lastIndexOf("/") + 1);
		const e = t ? t.signal : null, r = await this.request(this.url, {
			responseType: "xml",
			signal: e
		}), { rasterInfo: s, files: i } = this._parseHeader(r.data), { skipMapInfo: o, skipExtensions: a = [] } = this.ioConfig;
		if (!a.includes("aux.xml") && !o) {
			const e = await this._fetchAuxiliaryData(t);
			null != e && (s.statistics = e.statistics ?? s.statistics, s.histograms = e.histograms, e.histograms && null == s.statistics && (s.statistics = v$3(e.histograms)));
		}
		o && this.updateImageSpaceRasterInfo(s), this._set("rasterInfo", s), this._files = i;
		this._storageIndex = w((await this.request(i.index, {
			responseType: "array-buffer",
			signal: e
		})).data);
		const { blockWidth: l, blockHeight: f } = this.rasterInfo.storageInfo, u = this.rasterInfo.storageInfo.pyramidScalingFactor, { width: h, height: c } = this.rasterInfo, m = [], g = this._getBandSegmentCount();
		let d = 0, x = -1;
		for (; d < this._storageIndex.length;) {
			x++;
			const t = Math.ceil(h / l / u ** x) - 1, e = Math.ceil(c / f / u ** x) - 1;
			d += (t + 1) * (e + 1) * g * 4, m.push({
				maxRow: e,
				maxCol: t,
				minCol: 0,
				minRow: 0
			});
		}
		this.rasterInfo.storageInfo.blockBoundary = m, x > 0 && (this.rasterInfo.storageInfo.firstPyramidLevel = 1, this.rasterInfo.storageInfo.maximumPyramidLevel = x), this.updateTileInfo();
	}
	_getBandSegmentCount() {
		return y.get(this.rasterInfo.storageInfo.compression).isOneSegment ? 1 : this.rasterInfo.bandCount;
	}
	_getTileLocation(t, e, r) {
		const { blockWidth: s, blockHeight: i, pyramidScalingFactor: o } = this.rasterInfo.storageInfo, { width: a, height: n } = this.rasterInfo, l = this._getBandSegmentCount();
		let f, u, h, c = 0, p = 0;
		for (h = 0; h < t; h++) p = o ** h, f = Math.ceil(a / s / p), u = Math.ceil(n / i / p), c += f * u;
		p = o ** t, f = Math.ceil(a / s / p), u = Math.ceil(n / i / p), c += e * f + r, c *= 4 * l;
		const m = this._storageIndex.subarray(c, c + 4 * l);
		let g = 0, d = 0;
		const x = [];
		for (let y = 0; y < l; y++) g = m[4 * y] * 2 ** 32 + m[4 * y + 1], d = g + m[4 * y + 2] * 2 ** 32 + m[4 * y + 3] - 1, x.push({
			from: g,
			to: d
		});
		return {
			ranges: x,
			actualTileWidth: r < f - 1 ? s : Math.ceil(a / p) - s * (f - 1),
			actualTileHeight: e < u - 1 ? i : Math.ceil(n / p) - i * (u - 1)
		};
	}
	_parseHeader(t) {
		const r = e$1(t, "MRF_META/Raster");
		if (!r) throw new r$2("mrf:open", "not a valid MRF format");
		const s = e$1(r, "Size"), n = parseInt(s.getAttribute("x"), 10), f = parseInt(s.getAttribute("y"), 10), c = parseInt(s.getAttribute("c"), 10), p = (t$2(r, "Compression") || "none").toLowerCase();
		if (!y.has(p)) throw new r$2("mrf:open", "currently does not support compression " + p);
		const m = t$2(r, "DataType") || "UInt8", b = x.get(m.toLowerCase());
		if (null == b) throw new r$2("mrf:open", "currently does not support pixel type " + m);
		const w = e$1(r, "PageSize"), I = parseInt(w.getAttribute("x"), 10), A = parseInt(w.getAttribute("y"), 10), F = e$1(r, "DataValues");
		let R, _$1;
		F && (_$1 = F.getAttribute("NoData"), null != _$1 && (R = _$1.trim().split(" ").map((t) => parseFloat(t))));
		if (e$1(t, "MRF_META/CachedSource")) throw new r$2("mrf:open", "currently does not support MRF referencing other data files");
		const S = e$1(t, "MRF_META/GeoTags"), M = e$1(S, "BoundingBox");
		let k, C = !1;
		if (null != M) {
			const t = parseFloat(M.getAttribute("minx")), e = parseFloat(M.getAttribute("miny")), r = parseFloat(M.getAttribute("maxx")), s = parseFloat(M.getAttribute("maxy")), o = t$2(S, "Projection") || "";
			let n = S$3.WGS84;
			if ("LOCAL_CS[]" !== o) if (o.toLowerCase().startsWith("epsg:")) {
				const t = Number(o.slice(5));
				isNaN(t) || 0 === t || (n = new S$3({ wkid: t }));
			} else n = d$1(o) ?? S$3.WGS84;
			else C = !0, n = new S$3({ wkid: 3857 });
			k = new z(t, e, r, s), k.spatialReference = n;
		} else C = !0, k = new z({
			xmin: -.5,
			ymin: .5 - f,
			xmax: n - .5,
			ymax: .5,
			spatialReference: new S$3({ wkid: 3857 })
		});
		const T = e$1(t, "MRF_META/Rsets"), j = parseInt(T?.getAttribute("scale") || "2", 10), L = k.spatialReference, B = new p$3({
			origin: new _({
				x: k.xmin,
				y: k.ymax,
				spatialReference: L
			}),
			blockWidth: I,
			blockHeight: A,
			pyramidBlockWidth: I,
			pyramidBlockHeight: A,
			compression: p,
			pyramidScalingFactor: j
		}), E = new _({
			x: k.width / n,
			y: k.height / f,
			spatialReference: L
		}), P = new m$2({
			width: n,
			height: f,
			extent: k,
			isPseudoSpatialReference: C,
			spatialReference: L,
			bandCount: c,
			pixelType: b,
			pixelSize: E,
			noDataValue: R,
			storageInfo: B
		}), U = this._getAuxFullPath(t$2(r, "DataFile")), W = this._getAuxFullPath(t$2(r, "IndexFile"));
		return {
			rasterInfo: P,
			files: {
				mrf: this.url,
				index: W || this.url.replace(".mrf", ".idx"),
				data: U || this.url.replace(".mrf", y.get(p).blobExtension)
			}
		};
	}
	_getAuxFullPath(t) {
		if (!t) return null;
		if (t.includes("\\")) return null;
		if (t.includes("/")) return t.startsWith(new URL(this.url).origin) ? t : null;
		return `${this.url.slice(0, this.url.lastIndexOf("/"))}/${t}`;
	}
	async _fetchAuxiliaryData(t) {
		try {
			const { data: e } = await this.request(this.url + ".aux.xml", {
				responseType: "xml",
				signal: t?.signal
			});
			return S$2(e);
		} catch {
			return null;
		}
	}
};
function w(t) {
	if (t.byteLength % 16 > 0) throw new Error("invalid array buffer must be multiples of 16");
	let e, r, s, i, o, a;
	if (r$3) {
		for (r = new Uint8Array(t), i = new ArrayBuffer(t.byteLength), s = new Uint8Array(i), o = 0; o < t.byteLength / 4; o++) for (a = 0; a < 4; a++) s[4 * o + a] = r[4 * o + 3 - a];
		e = new Uint32Array(i);
	} else e = new Uint32Array(t);
	return e;
}
__decorate([a()], b$1.prototype, "_files", void 0), __decorate([a()], b$1.prototype, "_storageIndex", void 0), __decorate([a({
	type: String,
	json: { write: !0 }
})], b$1.prototype, "datasetFormat", void 0), b$1 = __decorate([c$2("esri.layers.raster.datasets.MRFRaster")], b$1);
var I = b$1;
//#endregion
//#region node_modules/@arcgis/core/layers/raster/datasets/DBFParser.js
function t(e) {
	const t = e.fields, r = e.records, n = t.some((e) => "oid" === e.name.toLowerCase()) ? "OBJECTID" : "OID", i = [{
		name: n,
		type: "esriFieldTypeOID",
		alias: "OID"
	}].concat(t.map((e) => ({
		name: e.name,
		type: "esriFieldType" + e.typeName,
		alias: e.name
	}))), a = i.map((e) => e.name), s = [];
	let o = 0, l = 0;
	return r.forEach((e) => {
		const t = {};
		for (t[n] = o++, l = 1; l < a.length; l++) t[a[l]] = e[l - 1];
		s.push({ attributes: t });
	}), {
		displayFieldName: "",
		fields: i,
		features: s
	};
}
var r = class {
	static get supportedVersions() {
		return [5];
	}
	static parse(r) {
		const n = new DataView(r), i = 3 & n.getUint8(0);
		if (3 !== i) return {
			header: { version: i },
			recordSet: null
		};
		const a = n.getUint32(4, !0), s = n.getUint16(8, !0), o = n.getUint16(10, !0), l = {
			version: i,
			recordCount: a,
			headerByteCount: s,
			recordByteCount: o
		};
		let p = 32;
		const d = [], g = [];
		let h;
		if (3 === i) {
			for (; 13 !== n.getUint8(p);) h = String.fromCharCode(n.getUint8(p + 11)).trim(), d.push({
				name: r$4(new Uint8Array(r, p, 11)),
				type: h,
				typeName: [
					"String",
					"Date",
					"Double",
					"Boolean",
					"String",
					"Integer"
				][[
					"C",
					"D",
					"F",
					"L",
					"M",
					"N"
				].indexOf(h)],
				length: n.getUint8(p + 16)
			}), p += 32;
			if (p += 1, d.length > 0) for (; g.length < a && r.byteLength - p > o;) {
				const t = [];
				32 === n.getUint8(p) ? (p += 1, d.forEach((n) => {
					if ("C" === n.type) t.push(r$4(new Uint8Array(r, p, n.length)).trim());
					else if ("N" === n.type) t.push(parseInt(String.fromCharCode.apply(null, new Uint8Array(r, p, n.length)).trim(), 10));
					else if ("F" === n.type) t.push(parseFloat(String.fromCharCode.apply(null, new Uint8Array(r, p, n.length)).trim()));
					else if ("D" === n.type) {
						const e = String.fromCharCode.apply(null, new Uint8Array(r, p, n.length)).trim();
						t.push(new Date(parseInt(e.slice(0, 4), 10), parseInt(e.slice(4, 6), 10) - 1, parseInt(e.slice(6, 8), 10)));
					}
					p += n.length;
				}), g.push(t)) : p += o;
			}
		}
		return {
			header: l,
			fields: d,
			records: g,
			recordSet: t({
				fields: d,
				records: g
			})
		};
	}
};
//#endregion
//#region node_modules/@arcgis/core/layers/raster/datasets/TIFFRaster.js
var S = (e, t) => e.get(t)?.values, b = (e, t) => e.get(t)?.values?.[0];
var E = class extends Z {
	constructor() {
		super(...arguments), this._files = null, this._headerInfo = null, this._bufferSize = 1048576, this._chunkSize = 10485760, this.datasetFormat = "TIFF";
	}
	async fetchRawTile(e, t, i, r = {}) {
		if (!this._headerInfo?.isSupported || this.isBlockOutside(e, t, i)) return null;
		const s = await this._fetchRawTiffTile(e, t, i, !1, r);
		if (null != s && this._headerInfo.hasMaskBand) {
			const a = await this._fetchRawTiffTile(e, t, i, !0, r);
			null != a && a.pixels[0] instanceof Uint8Array && (s.mask = a.pixels[0]);
		}
		return s;
	}
	async _open(e) {
		const r = e ? e.signal : null, { data: s } = await this.request(this.url, {
			range: {
				from: 0,
				to: this._bufferSize
			},
			responseType: "array-buffer",
			signal: r
		});
		if (!s) throw new r$2("tiffraster:open", "failed to open url " + this.url);
		this.datasetName = this.url.slice(this.url.lastIndexOf("/") + 1, this.url.lastIndexOf("."));
		const { littleEndian: a, firstIFDPos: n, isBigTiff: o } = H(s), f = [], l = {
			fileChunk: s,
			posIFD: n,
			fileOffset: 0
		};
		await this._readIFDs(f, l, a, o ? 8 : 4, r);
		const { imageInfo: u, rasterInfo: c } = k(f);
		if (this._headerInfo = {
			littleEndian: a,
			isBigTiff: o,
			ifds: f,
			pyramidIFDs: G(f),
			maskIFDs: v$2(f),
			...u
		}, this._set("rasterInfo", c), !u.isSupported) throw new r$2("tiffraster:open", "this tiff is not supported: " + u.message);
		if (!u.tileWidth) throw new r$2("tiffraster:open", "none-tiled tiff is not optimized for access, convert to COG and retry.");
		c.isPseudoSpatialReference && n$1.getLogger(this).warn("The spatial reference for this tiff is unsupported. Only EPSG spatial reference codes and Esri WKTs are supported.");
		const g = f[0].get("PREDICTOR")?.values?.[0];
		if (3 === f[0].get("SAMPLEFORMAT")?.values?.[0] && 2 === g) throw new r$2("tiffraster:open", "unsupported horizontal difference encoding. Predictor=3 is supported for floating point data");
		const { skipMapInfo: w, skipExtensions: I = [] } = this.ioConfig;
		if (!I.includes("aux.xml") && !w) {
			const t = await this._fetchAuxiliaryMetaData(e);
			null != t && v(t, c);
		}
		I.includes("vat.dbf") || 1 !== c.bandCount || "u8" !== c.pixelType || w || (c.attributeTable = await this._fetchAuxiliaryTable(e), null != c.attributeTable && (c.keyProperties.DataType = "thematic")), w && this.updateImageSpaceRasterInfo(c), this.updateTileInfo();
	}
	async _validateOrFetchHeaderBuffer(e, t) {
		let { fileChunk: i, fileOffset: r, posIFD: s } = e;
		if (s + 8 >= i.byteLength || s < 0) {
			r = s + r;
			i = (await this.request(this.url, {
				range: {
					from: r,
					to: r + this._bufferSize
				},
				responseType: "array-buffer",
				signal: t
			})).data, s = 0;
		}
		return {
			fileChunk: i,
			fileOffset: r,
			posIFD: s
		};
	}
	async _readIFDs(e$3, i, r, s = 4, a) {
		if (!i.posIFD) return null;
		i = await this._validateOrFetchHeaderBuffer(i, a);
		const n = await this._readIFD(i, r, e, s, a);
		if (!n?.ifd) throw new r$2("tiffraster:open", "cannot parse tiff header. failed to open url " + this.url);
		if (e$3.push(n.ifd), !n.nextIFD) return null;
		i.posIFD = n.nextIFD - i.fileOffset, await this._readIFDs(e$3, i, r, s, a);
	}
	async _readIFD(e$2, t, i = e, r = 4, s) {
		let { fileChunk: a, posIFD: n, fileOffset: o } = e$2;
		if (!e$2.fileChunk) return null;
		const f = Y(a, t, n, o, i, r);
		if (f.success) {
			const e = [];
			if (f.ifd?.forEach((t) => {
				t.values || e.push(t);
			}), e.length > 0 && await this._fillOffsets(e, t, f.nextIFD, s), f.ifd?.has("GEOKEYDIRECTORY")) {
				const e = f.ifd.get("GEOKEYDIRECTORY"), i = e?.values;
				if (i && i.length > 4) {
					const r = i[0] + "." + i[1] + "." + i[2];
					n = e.valueOffset + 6 - o;
					const f = await this._validateOrFetchHeaderBuffer({
						fileChunk: a,
						posIFD: n,
						fileOffset: o
					}, s);
					e.data = (await this._readIFD(f, t, t$1, 2, s))?.ifd, e.data && e.data.set("GEOTIFFVersion", {
						id: 0,
						type: 2,
						valueCount: 1,
						valueOffset: null,
						values: [r]
					});
				}
			}
			return f;
		}
		if (f.requiredBufferSize) return a = (await this.request(this.url, {
			range: {
				from: o,
				to: o + n + f.requiredBufferSize + 8
			},
			responseType: "array-buffer",
			signal: s
		})).data, a.byteLength < n + f.requiredBufferSize ? null : (e$2.fileChunk = a, e$2.fileOffset = o, this._readIFD(e$2, t, i, r, s));
		return null;
	}
	async _fillOffsets(e, t, i, r) {
		const s = e.filter((e) => null != e.offlineOffsetSize);
		if (0 === s.length) return;
		const a = s.map((e) => e.offlineOffsetSize), n = Math.min.apply(null, a.map((e) => e[0])), o = Math.max.apply(null, a.map((e) => e[0] + e[1]));
		let f = 1 === a.length || o - n <= this._bufferSize;
		if (!f && a.length > 1) {
			a.sort((e, t) => e[0] - t[0]);
			f = a.reduce((e, t) => e === t[0] ? t[0] + t[1] : 0, a[0][0]) === o;
		}
		if (f) {
			const e = await this._fetchOffsets(n, Math.max(o, n + this._bufferSize), r);
			s.forEach((i) => N$1(e, t, i, n));
			return;
		}
		const l = s.map(async (e) => {
			const i = e.offlineOffsetSize;
			N$1(await this._fetchOffsets(i[0], i[1] + i[0], r), t, e, i[0]);
		});
		await Promise.all(l);
	}
	async _fetchOffsets(e, t, i) {
		const r = [], s = this._chunkSize, a = Math.ceil((t - e) / s);
		let n = e;
		for (let l = 0; l < a; l++) r.push(this.request(this.url, {
			range: {
				from: n,
				to: l === a - 1 ? t : n + s - 1
			},
			responseType: "array-buffer",
			signal: i
		})), n += s;
		const o = await Promise.all(r);
		if (1 === a) return o[0].data;
		const f = new Uint8Array(t - e + 1);
		for (let l = 0; l < a; l++) f.set(new Uint8Array(o[l].data), l * s);
		return f.buffer;
	}
	async _fetchRawTiffTile(e, t, i, r, s = {}) {
		const a = this._getTileLocation(e, t, i, r);
		if (!a) return null;
		const { ranges: n, actualTileWidth: o, actualTileHeight: f, ifd: l } = a, u = n.map((e) => this.request(this.url, {
			range: e,
			responseType: "array-buffer",
			signal: s.signal
		})), c = await Promise.all(u), h = c.map((e) => e.data.byteLength).reduce((e, t) => e + t), p = 1 === c.length ? c[0].data : new ArrayBuffer(h), d = [0], m = [0];
		if (c.length > 1) {
			const e = new Uint8Array(p);
			for (let t = 0, i = 0; t < c.length; t++) {
				const r = c[t].data;
				e.set(new Uint8Array(r), i), d[t] = i, i += r.byteLength, m[t] = r.byteLength;
			}
		}
		const { blockWidth: y, blockHeight: g } = this.getBlockWidthHeight(e), x = await this.decodePixelBlock(p, {
			format: "tiff",
			customOptions: {
				headerInfo: this._headerInfo,
				ifd: l,
				offsets: d,
				sizes: m
			},
			width: y,
			height: g,
			planes: null,
			pixelType: null
		});
		if (null == x) return null;
		if (o !== y || f !== g) {
			let e = x.mask;
			if (e) for (let t = 0; t < g; t++) {
				const i = t * y;
				if (t < f) for (let t = o; t < y; t++) e[i + t] = 0;
				else for (let t = 0; t < y; t++) e[i + t] = 0;
			}
			else {
				e = new Uint8Array(y * g), x.mask = e;
				for (let t = 0; t < f; t++) {
					const i = t * y;
					for (let t = 0; t < o; t++) e[i + t] = 1;
				}
			}
		}
		return x;
	}
	_getTileLocation(e, t, i, r = !1) {
		const { firstPyramidLevel: s, blockBoundary: a } = this.rasterInfo.storageInfo, n = 0 === e ? 0 : e - (s - 1), { _headerInfo: o } = this;
		if (!o) return null;
		const f = r ? o.maskIFDs[n] : 0 === n ? o?.ifds[0] : o?.pyramidIFDs[n - 1];
		if (!f) return null;
		const l = C$1(f, o), u = S(f, "TILEOFFSETS");
		if (void 0 === u) return null;
		const c = S(f, "TILEBYTECOUNTS"), { minRow: h, minCol: p, maxRow: d, maxCol: m } = a[n];
		if (t > d || i > m || t < h || i < p) return null;
		const y = b(f, "IMAGEWIDTH"), x = b(f, "IMAGELENGTH"), w = b(f, "TILEWIDTH"), I = b(f, "TILELENGTH"), T = [];
		if (l) {
			const { bandCount: e } = this.rasterInfo;
			for (let r = 0; r < e; r++) {
				const e = r * (d + 1) * (m + 1) + t * (m + 1) + i;
				T[r] = {
					from: u[e],
					to: u[e] + c[e] - 1
				};
			}
		} else {
			const e = t * (m + 1) + i;
			T.push({
				from: u[e],
				to: u[e] + c[e] - 1
			});
		}
		for (let g = 0; g < T.length; g++) if (null == T[g].from || !T[g].to || T[g].to < 0) return null;
		return {
			ranges: T,
			ifd: f,
			actualTileWidth: i === m && y % w || w,
			actualTileHeight: t === d && x % I || I
		};
	}
	async _fetchAuxiliaryMetaData(e) {
		try {
			const { data: t } = await this.request(this.url + ".aux.xml", {
				responseType: "xml",
				signal: e?.signal
			});
			return S$2(t);
		} catch {
			return null;
		}
	}
	async _fetchAuxiliaryTable(e) {
		try {
			const { data: t } = await this.request(this.url + ".vat.dbf", {
				responseType: "array-buffer",
				signal: e?.signal
			}), i = r.parse(t);
			return i?.recordSet ? g$2.fromJSON(i.recordSet) : null;
		} catch {
			return null;
		}
	}
};
function k(e) {
	const t = k$1(e), { width: i, height: r, tileWidth: s, tileHeight: f, planes: l, pixelType: u, compression: h, firstPyramidLevel: p, maximumPyramidLevel: d, pyramidBlockWidth: m, pyramidBlockHeight: y, pyramidResolutions: g, tileBoundary: w, affine: I, metadata: T } = t;
	let E = d$1(t.extent.spatialReference?.wkt || t.extent.spatialReference?.wkid), k = !!t.isPseudoGeographic;
	E ?? (k = !0, E = new S$3({ wkid: 3857 }));
	const v = new z({
		...t.extent,
		spatialReference: E
	}), P = new p$3({
		blockWidth: s,
		blockHeight: f,
		pyramidBlockWidth: m,
		pyramidBlockHeight: y,
		compression: h,
		origin: new _(v ? {
			x: v.xmin,
			y: v.ymax,
			spatialReference: E
		} : {
			x: 0,
			y: 0
		}),
		firstPyramidLevel: p,
		maximumPyramidLevel: d,
		pyramidResolutions: g,
		blockBoundary: w
	}), B = new _({
		x: (v.xmax - v.xmin) / i,
		y: (v.ymax - v.ymin) / r,
		spatialReference: E
	}), z$2 = T ? {
		BandProperties: T.bandProperties,
		DataType: T.dataType
	} : {};
	let C = null;
	const L = b(e[0], "PHOTOMETRICINTERPRETATION"), j = S(e[0], "COLORMAP");
	if (L <= 3 && j?.length > 3 && j.length % 3 == 0) {
		C = [];
		const e = j.length / 3;
		for (let t = 0; t < e; t++) C.push([
			t,
			j[t] >>> 8,
			j[t + e] >>> 8,
			j[t + 2 * e] >>> 8
		]);
	}
	const A = new m$2({
		width: i,
		height: r,
		bandCount: l,
		pixelType: u,
		pixelSize: B,
		storageInfo: P,
		spatialReference: E,
		isPseudoSpatialReference: k,
		keyProperties: z$2,
		extent: v,
		colormap: C,
		statistics: T ? T.statistics : null
	});
	if (I?.length && (A.nativeExtent = new z({
		xmin: -.5,
		ymin: .5 - r,
		xmax: i - .5,
		ymax: .5,
		spatialReference: E
	}), A.transform = new y$2({
		polynomialOrder: 1,
		forwardCoefficients: [
			I[2] + I[0] / 2,
			I[5] - I[3] / 2,
			I[0],
			I[3],
			-I[1],
			-I[4]
		]
	}), A.extent = A.transform.forwardTransform(A.nativeExtent), A.pixelSize = new _({
		x: (v.xmax - v.xmin) / i,
		y: (v.ymax - v.ymin) / r,
		spatialReference: E
	}), P.origin.x = -.5, P.origin.y = .5), g) {
		const { x: e, y: t } = A.pixelSize;
		g.forEach((i) => {
			i.x *= e, i.y *= t;
		});
	}
	return {
		imageInfo: t,
		rasterInfo: A
	};
}
function v(e, t) {
	if (t.statistics = e.statistics ?? t.statistics, t.histograms = e.histograms, e.histograms && null == t.statistics && (t.statistics = v$3(e.histograms)), e.transform && null == t.transform) {
		t.transform = e.transform, t.nativeExtent = t.extent;
		const i = t.transform.forwardTransform(t.nativeExtent);
		t.pixelSize = new _({
			x: (i.xmax - i.xmin) / t.width,
			y: (i.ymax - i.ymin) / t.height,
			spatialReference: t.spatialReference
		}), t.extent = i;
	}
	t.isPseudoSpatialReference && e.spatialReference && (t.spatialReference = e.spatialReference, t.extent.spatialReference = t.nativeExtent.spatialReference = t.storageInfo.origin.spatialReference = t.spatialReference);
}
__decorate([a()], E.prototype, "_files", void 0), __decorate([a()], E.prototype, "_headerInfo", void 0), __decorate([a()], E.prototype, "_bufferSize", void 0), __decorate([a()], E.prototype, "_chunkSize", void 0), __decorate([a({
	type: String,
	json: { write: !0 }
})], E.prototype, "datasetFormat", void 0), E = __decorate([c$2("esri.layers.raster.datasets.TIFFRaster")], E);
var D = E;
//#endregion
//#region node_modules/@arcgis/core/layers/raster/datasets/RasterFactory.js
var n = /* @__PURE__ */ new Map();
n.set("MRF", {
	desc: "Meta Raster Format",
	constructor: I
}), n.set("TIFF", {
	desc: "GeoTIFF",
	constructor: D
}), n.set("RasterTileServer", {
	desc: "Raster Tile Server",
	constructor: I$1
}), n.set("JPG", {
	desc: "JPG Raster Format",
	constructor: d
}), n.set("PNG", {
	desc: "PNG Raster Format",
	constructor: d
}), n.set("GIF", {
	desc: "GIF Raster Format",
	constructor: d
}), n.set("BMP", {
	desc: "BMP Raster Format",
	constructor: d
}), n.set("CovJSON", {
	desc: "COVJSON Raster Format",
	constructor: h$1
}), n.set("MEMORY", {
	desc: "In Memory Raster Format",
	constructor: x$1
});
var u = class {
	static get supportedFormats() {
		const t = /* @__PURE__ */ new Set();
		return n.forEach((r, e) => t.add(e)), t;
	}
	static async open(r) {
		const { url: e, ioConfig: s, source: o, sourceJSON: a } = r;
		let c = r.datasetFormat ?? s?.datasetFormat;
		c ?? (e.includes(".") ? c = e.slice(e.lastIndexOf(".") + 1).toUpperCase() : "coverage" === o?.type?.toLowerCase() ? c = "CovJSON" : o?.extent && o.pixelblocks && (c = "MEMORY")), "OVR" === c || "TIF" === c ? c = "TIFF" : "JPG" === c || "JPEG" === c || "JFIF" === c ? c = "JPG" : "COVJSON" === c && (c = "CovJSON"), e.toLowerCase().includes("/imageserver") && !e.toLowerCase().includes("/wcsserver") && (c = "RasterTileServer");
		const u = {
			url: e,
			source: o,
			sourceJSON: a,
			datasetFormat: c,
			ioConfig: s ?? {
				bandIds: null,
				sampling: null
			}
		};
		if (Object.keys(u).forEach((t) => {
			u[t] ?? delete u[t];
		}), c) {
			if (!this.supportedFormats.has(c)) throw new r$2("rasterfactory:open", "not a supported format " + c);
			if ("CRF" === c) throw new r$2("rasterfactory:open", `cannot open raster: ${e}`);
			const s = new (n.get(c)).constructor(u);
			return await s.open({ signal: r.signal }), s;
		}
		const i = Array.from(n.keys()).filter((t) => "CovJSON" !== t && "Memory" !== t);
		let l = 0;
		const m = () => {
			if (c = i[l++], !c) return null;
			if ("CRF" === c) return null;
			const t = new (n.get(c)).constructor(u);
			return t.open({ signal: r.signal }).then(() => t).catch(() => m());
		};
		return m();
	}
	static register(t, r, e) {
		n.has(t.toUpperCase()) || n.set(t.toUpperCase(), {
			desc: r,
			constructor: e
		});
	}
};
//#endregion
export { u as default };

//# sourceMappingURL=RasterFactory-Bx9FiZmI.js.map