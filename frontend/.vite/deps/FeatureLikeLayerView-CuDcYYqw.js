import { r as __decorate } from "./tslib.es6-DlxpVI88.js";
import { A as has, n as n$8, o as l$6, t as r$6, w as a$3, y as i$6 } from "./Error-CzxduO2m.js";
import { T as N } from "./typedArrayUtil-BAuNmygZ.js";
import { o as S } from "./request-CuG5cxow.js";
import { C as y$3, L as e$6, U as t$4, b as s$6, f as d$6, j as u$8, t as $, v as m$4, x as u$9 } from "./promiseUtils-DhYhergm.js";
import { a as o$10, n as c$8, t as a$4 } from "./decorators-DE7S5xmd.js";
import { t as b$2 } from "./Accessor-kDoDKy4v.js";
import { t as i$7 } from "./Evented-GLJbxWO5.js";
import { t as q } from "./Collection-BAJSKCip.js";
import { A as re } from "./units-Dg-cK1vO.js";
import { t as S$1 } from "./SpatialReference-rIfb2LrD.js";
import { t as z$1 } from "./Extent-CquIzaXp.js";
import { t as A$1 } from "./layerUtils-sQ-3wxAB.js";
import { a as h, c as w$1, n as U$1, r as a$5, s as l$7 } from "./reactiveUtils-DRpp6Nmg.js";
import { h as m$5, y as r$7 } from "./mathUtils-hEBUcrMa.js";
import { d as t$5 } from "./curveUtils-CfkOAT4m.js";
import { S as u$10 } from "./aaBoundingRect-CgUWvAgv.js";
import { f as u$11 } from "./screenUtils-BR-xd7ya.js";
import { a as o$11, i as d$7, o as p$2, r as c$9 } from "./sql-Cyp7eZa9.js";
import { M as h$1, O as Z, P as j$1, S as T, T as Ue, V as x$1, W as z$2, _ as L$1, a as D, i as C$1, p as I$1, y as R$1, z as w$2 } from "./fieldUtils-CC2YSmV6.js";
import { t as j$2 } from "./Graphic-D2G0Ykqt.js";
import { r as t$6 } from "./getPopupProvider-CZza_7Ci.js";
import { r as d$8, t as u$12 } from "./SimpleMarkerSymbol-BjFFaoyw.js";
import { t as m$6 } from "./SimpleFillSymbol-CbXKKnxp.js";
import { t as r$8 } from "./workers-Nrqav2LG.js";
import { t as g$2 } from "./FeatureSet-Sjrap7hf.js";
import { r as u$13 } from "./displayFilterUtils-DQYkMjND.js";
import { t as R$2 } from "./Query-aOayEcb1.js";
import { t as p$3 } from "./StatisticDefinition-DCvGQn-e.js";
import { t as d$9 } from "./FeatureFilter-Bd2SB4Jt.js";
import { t as p$4 } from "./FeatureEffect-CWwUJtCe.js";
import { t as m$7 } from "./lengthUtils-DrG-JkjU.js";
import { b as _, y as Z$1 } from "./featureLayerUtils-4Rc-m6fm.js";
import { a as u$14, n as m$8, r as p$5, t as c$10 } from "./relativeTimeQueryUtils-BHOVTSHF.js";
import { t as c$11 } from "./labelingInfo-BvxiOw9s.js";
import { i as f$3 } from "./heatmapUtils-CKd_Sdiu.js";
import { t as p$6 } from "./OrderByInfo-Daf0eByc.js";
import { t as e$7 } from "./networkFieldUtils-CvN2K9lH.js";
import { t as h$2 } from "./UpdatingHandles-BpejPsAZ.js";
import { t as e$8 } from "./ReactiveMap-B1BORGbU.js";
import { i as p$7, r as l$8 } from "./util-xsku_21L.js";
import { t as t$7 } from "./capabilities-C6OeTqnP.js";
import { t as e$9 } from "./TileKey-CWP4O_FK.js";
import { n as B$1 } from "./featureConversionUtils-BQ5ifpAj.js";
import { n as s$7, t as n$9 } from "./featurePopupQueryUtils-DrOVJjEC.js";
import { n as p$8, t as n$10 } from "./popupUtils-CvHyPUNh.js";
import { a as l$9, t as O$1 } from "./query-IgT1qZDA.js";
import { t as E } from "./constants-DW1wVucs.js";
import { n as o$12 } from "./floorFilterUtils-BpyhDh2E.js";
import { t as d$10 } from "./parquetUtils-BveepdDi.js";
import { t as i$8 } from "./AttributeBinsFeatureSet-C9_UVRGK.js";
import { n as L$2 } from "./densifyCurvedGeometry-LJustJq_.js";
import { t as i$9 } from "./FeatureMetadata-BHJp6_d8.js";
import { n as R$3 } from "./AttributeBinsQuery-BIYjAjK6.js";
import { i as _$1, p as a$6, r as Z$2 } from "./CIMSymbolHelper-BFA0d3St.js";
import { t as h$3 } from "./EffectView-R15HeTXu.js";
import { t as S$2 } from "./LayerView2D-CjEvsXMB.js";
import { t as I$2 } from "./LayerView-BX1wZnFy.js";
import { t as i$10 } from "./RefreshableLayerView-36iUmJ9V.js";
import { t as g$3 } from "./rotate-TnnlaUOT.js";
import { n as n$11, t as i$11 } from "./timeSupport-COlvgLeh.js";
import { n as r$9, r as s$8, t as i$12 } from "./TechniqueInstance-CEoDFwU6.js";
import { n as b$3 } from "./TileInfoPrograms-DBJ0RhGd.js";
import { C as m$9, S as z$3, T as F$2, b as b$4, i as t$9, l as c$12, n as p$9, r as s$9, t as o$13, v as A$2, w as t$8, x as x$2, y as e$10 } from "./FeatureCommandQueue-O3CDY0lQ.js";
import { t as t$10 } from "./CircularArray-Daxpyh3i.js";
import { t as e$11 } from "./featureReductionUtils-DWiY_E_L.js";
import { n as u$15, t as e$12 } from "./FeatureIdInfo-B0YGalvX.js";
import { i as s$10 } from "./SDFHelper-4toB0lWY.js";
import { t as r$10 } from "./highlightOptionsUtils-xZAbXXbo.js";
//#region node_modules/@arcgis/core/AggregateGraphic.js
var s$5 = class extends j$2 {
	constructor() {
		super(...arguments), this.isAggregate = !0, this.origin = null;
	}
	getObjectId() {
		return this.attributes.aggregateId;
	}
};
__decorate([a$4({ type: Boolean })], s$5.prototype, "isAggregate", void 0), __decorate([a$4({ clonable: "reference" })], s$5.prototype, "origin", void 0), s$5 = __decorate([c$8("esri.AggregateGraphic")], s$5);
var i$5 = s$5;
//#endregion
//#region node_modules/@arcgis/core/TrackGraphic.js
var s$4 = class extends j$2 {
	constructor() {
		super(...arguments), this.isAggregate = !0, this.origin = null;
	}
	getObjectId() {
		return this.attributes.aggregateId;
	}
};
__decorate([a$4({ type: Boolean })], s$4.prototype, "isAggregate", void 0), __decorate([a$4({ clonable: "reference" })], s$4.prototype, "origin", void 0), s$4 = __decorate([c$8("esri.TrackGraphic")], s$4);
var i$4 = s$4;
//#endregion
//#region node_modules/@arcgis/core/layers/effects/FeatureEffectView.js
var c$7 = class extends b$2 {
	constructor(e) {
		super(e), this._filter = null, this.duration = has("mapview-transitions-duration"), this._excludedEffectView = new h$3(e), this._includedEffectView = new h$3(e);
	}
	get excludedEffects() {
		return this._excludedEffectView.effects;
	}
	set featureEffect(e) {
		this._get("featureEffect") !== e && this._transitionTo(e);
	}
	get filter() {
		return this._filter || this.featureEffect?.filter || null;
	}
	get hasEffects() {
		return this._excludedEffectView.hasEffects || this._includedEffectView.hasEffects;
	}
	get includedEffects() {
		return this._includedEffectView.effects;
	}
	set scale(e) {
		this._set("scale", e), this._excludedEffectView.scale = e, this._includedEffectView.scale = e;
	}
	get transitioning() {
		return this._excludedEffectView.transitioning || this._includedEffectView.transitioning;
	}
	get transitioningToEmpty() {
		return !this._excludedEffectView.final && !this._includedEffectView.final;
	}
	transitionStep(e, t) {
		this._set("scale", t), this.transitioning ? (this._includedEffectView.transitionStep(e, t), this._excludedEffectView.transitionStep(e, t), this.transitioning || (this._filter = null)) : (this._excludedEffectView.scale = t, this._includedEffectView.scale = t);
	}
	endTransition() {
		this._includedEffectView.endTransition(), this._excludedEffectView.endTransition(), this._filter = null;
	}
	_transitionTo(e) {
		const t = this._get("featureEffect"), i = e, f = i?.includedEffect, s = i?.excludedEffect, c = this._includedEffectView.canTransitionTo(f) && this._excludedEffectView.canTransitionTo(s);
		this._includedEffectView.effect = f, this._excludedEffectView.effect = s, this._set("featureEffect", i), this._filter = i?.filter || t?.filter || null, c || this.endTransition();
	}
};
__decorate([a$4()], c$7.prototype, "_filter", void 0), __decorate([a$4()], c$7.prototype, "_excludedEffectView", void 0), __decorate([a$4()], c$7.prototype, "_includedEffectView", void 0), __decorate([a$4()], c$7.prototype, "duration", void 0), __decorate([a$4()], c$7.prototype, "excludedEffects", null), __decorate([a$4()], c$7.prototype, "featureEffect", null), __decorate([a$4()], c$7.prototype, "filter", null), __decorate([a$4()], c$7.prototype, "hasEffects", null), __decorate([a$4()], c$7.prototype, "includedEffects", null), __decorate([a$4({ value: 0 })], c$7.prototype, "scale", null), __decorate([a$4()], c$7.prototype, "transitioning", null), __decorate([a$4()], c$7.prototype, "transitioningToEmpty", null), c$7 = __decorate([c$8("esri.layers.effects.FeatureEffectView")], c$7);
//#endregion
//#region node_modules/@arcgis/core/rest/support/AggregateFeatureSet.js
var c$6 = class extends g$2 {
	constructor() {
		super(...arguments), this.features = [];
	}
	readFeatures(e, t) {
		const o = S$1.fromJSON(t.spatialReference), a = [];
		for (let s = 0; s < e.length; s++) {
			const t = e[s], p = i$5.fromJSON(t), c = t.geometry?.spatialReference;
			null == p.geometry || c || (p.geometry.spatialReference = o);
			const i = t.aggregateGeometries, f = p.aggregateGeometries;
			if (i && null != f) for (const e in f) {
				const r = f[e], s = i[e]?.spatialReference;
				null == r || s || (r.spatialReference = o);
			}
			a.push(p);
		}
		return a;
	}
};
__decorate([a$4({
	type: [i$5],
	json: { write: !0 }
})], c$6.prototype, "features", void 0), __decorate([o$10("features")], c$6.prototype, "readFeatures", null), c$6 = __decorate([c$8("esri.rest.support.AggregateFeatureSet")], c$6);
//#endregion
//#region node_modules/@arcgis/core/views/2d/engine/webgl/shaderGraph/techniques/FeatureInstanceStore.js
var n$7 = class {
	constructor() {
		this._instanceById = /* @__PURE__ */ new Map();
	}
	destroy() {
		this._instanceById.clear();
	}
	get size() {
		return this._instanceById.size;
	}
	entries() {
		return this._instanceById.entries();
	}
	find(t) {
		for (const e of this.values()) if (e.techniqueRef.type === t) return e;
		return null;
	}
	updateStart(t) {
		t && (this._instanceByIdNext = /* @__PURE__ */ new Map(), this._shaderCountByMesh = /* @__PURE__ */ new Map(), this._shaderIndices = /* @__PURE__ */ new Map());
	}
	updateEnd(t) {
		if (t) {
			if (!this._instanceByIdNext) throw new Error("InternalError: Found updateEnd call without corresponding updateStart");
			for (const t of this._instanceById.keys()) this._instanceByIdNext.has(t) || this._instanceById.delete(t);
			for (const [t, e] of this._instanceByIdNext.entries()) {
				const s = this._instanceById.get(t);
				s ? s.setInput(e.getInput()) : this._instanceById.set(t, e);
			}
			this._instanceByIdNext = null, this._shaderCountByMesh = null, this._shaderIndices = null;
		}
	}
	values() {
		return this._instanceById.values();
	}
	ensureInstance(n, i) {
		let r;
		if ("object" == typeof i && "optionalAttributes" in i && "uniforms" in i) {
			const t = `${n.type}.${JSON.stringify(i.optionalAttributes)}`, e = t + `.${JSON.stringify(i.uniforms)}`;
			let s = 0;
			null != this._instanceByIdNext && (this._shaderIndices.has(e) ? s = this._shaderIndices.get(e) : (s = this._shaderCountByMesh.get(t) ?? 0, this._shaderCountByMesh.set(t, s + 1), this._shaderIndices.set(e, s))), r = t + `.${s}`;
		} else r = `${n.type}.${JSON.stringify(i)}`;
		const a = l$6(r);
		if (this._instanceByIdNext) {
			const t = new i$12(r$9(a), n, i);
			return this._instanceByIdNext.set(a, t), t;
		}
		if (!this._instanceById.has(a)) {
			const t = new i$12(r$9(a), n, i);
			this._instanceById.set(a, t);
		}
		return this._instanceById.get(a);
	}
	getInstance(t) {
		const e = this._instanceById.get(t);
		if (null == e) throw new Error(`InternalError: Unable to get instance for ${t}`);
		return e;
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/RenderState.js
var o$9 = 1e3;
var d$5 = class {
	constructor(t, i, s, r, n, d) {
		this.getStage = t, this.getSubscriptionVersion = i, this.version = s, this._fader = r, this._container = n, this._tileInfoView = d, this._pendingUpdates = new t$10(o$9), this._locked = !1, this._tiles = /* @__PURE__ */ new Map();
	}
	destroy() {
		for (const e of this.tiles()) this._fader.unregisterFeatureTile(e), e.destroy();
		this._pendingUpdates.clear(), this._tiles.clear(), this._container = null, this._fader = null;
	}
	tiles() {
		return this._tiles.values();
	}
	size() {
		return this._tiles.size;
	}
	get usedMemory() {
		let e = 0;
		for (const t of this._tiles.values()) e += t.usedMemory;
		for (const t of this._pendingUpdates.entries) t && (e += l$5(t.inner));
		return e;
	}
	get requiresRefresh() {
		for (const e of this._tiles.values()) if (e.requiresRefresh) return !0;
		return !1;
	}
	getTile(e) {
		return this._tiles.get(e);
	}
	setTiles(e) {
		this._tiles.clear();
		for (const t of e) this._tiles.set(t.key.id, t);
	}
	lockUploads() {
		this._locked = !0;
	}
	unlockUploads() {
		this._locked = !1, this.flush();
	}
	enqueueUpdate(e) {
		this._pendingUpdates.enqueue(e);
	}
	update(e) {
		if (!this._locked) for (; this._pendingUpdates.size;) {
			const t = this._pendingUpdates.peek();
			if (null == t || t.inner.attributeEpoch > e) break;
			this._updateTile(t), this._pendingUpdates.dequeue();
		}
	}
	removeTile(e) {
		const t = this._tiles.get(e);
		has("esri-2d-update-debug") && console.debug(`Tile[${e}] RenderState.removeTile`), t && (this._fader.unregisterFeatureTile(t), t.destroy()), this._tiles.delete(e);
	}
	isTileDone(e) {
		const t = this._tiles.get(e.id);
		return !!t && t.isReady && t.decluttered;
	}
	flush() {
		for (; this._pendingUpdates.size;) {
			const e = this._pendingUpdates.dequeue();
			null != e && this._updateTile(e);
		}
		for (const e of this._tiles.values()) e.upload();
	}
	_updateTile(e) {
		const { inner: t, objectIdMap: i } = e, s = this.getSubscriptionVersion(t.id);
		if (s !== t.subscriptionVesrion) {
			if (has("esri-2d-update-debug")) {
				const e = `${t.subscriptionVesrion} != ${s}`;
				console.debug(`Version[${e}] Tile[${t.id}] FeatureContainer - Dropping message, outdated version]`, t);
			}
			return;
		}
		if (has("esri-2d-update-debug")) {
			const e = t.debugInfo?.chunkId ?? "<EnsureEnd>";
			console.debug(`Version[${t.version}] Tile[${t.id}] Chunk[${e}] RenderState.updateTile [${t.type}]`, t);
		}
		const r = this._ensureTile(t.id);
		if ("update" === t.type) {
			const [e, ...s] = t.modify;
			r.onMessage({
				type: "update",
				modify: e,
				remove: t.remove,
				end: t.end,
				attributeEpoch: t.attributeEpoch,
				objectIdMap: i
			});
			for (const r of s) {
				const e = this._tiles.get(r.tileId);
				e && e.onMessage({
					type: "update",
					modify: r,
					remove: t.remove,
					end: !1,
					isPixelBuffer: !0,
					attributeEpoch: null,
					objectIdMap: i
				});
			}
			return;
		}
		if (null == t.append) return void r.onMessage({
			type: "append",
			clear: t.clear,
			debugInfo: t.debugInfo,
			end: t.end,
			attributeEpoch: t.attributeEpoch,
			objectIdMap: i
		});
		const [n, ...o] = t.append;
		r.onMessage({
			type: "append",
			clear: t.clear,
			append: n,
			debugInfo: t.debugInfo,
			end: t.end,
			attributeEpoch: t.attributeEpoch,
			objectIdMap: i
		});
		for (const d of o) {
			const e = this._tiles.get(d.tileId);
			e && e.onMessage({
				type: "update",
				modify: d,
				remove: [],
				sort: !1,
				end: !1,
				isPixelBuffer: !0,
				attributeEpoch: null,
				objectIdMap: i
			});
		}
	}
	_ensureTile(e) {
		if (!this._tiles.has(e)) {
			const t = this._createTile(e);
			this._copyPixelBufferedEntitiesInto(t), this._tiles.set(e, t);
		}
		return this._tiles.get(e);
	}
	_createTile(e) {
		has("esri-2d-update-debug") && console.debug(`Version[${this.version}] Tile[${e}] RenderState.createTile`);
		const o = new e$9(e), d = this._tileInfoView.getTileBounds(u$10(), o), l = d[0], u = d[3], a = this._tileInfoView.getTileResolution(o.level), p = this._container.instanceStore.find(19)?.instanceId, c = new b$3(o, a, l, u, this._fader, p, !0);
		if (this._fader.registerFeatureTile(c), c.stage = this.getStage(), !c.stage) {
			const e = new r$6("featurelayerview:webgl", "Cannot create tile with empty stage");
			n$8.getLogger("esri.views.2d.layers.features.RenderState").error(e);
		}
		return c;
	}
	_copyPixelBufferedEntitiesInto(e) {
		let t = 7;
		const i = this._tileInfoView.getLODInfoAt(e.key);
		for (let s = -1; s <= 1; s++) for (let r = -1; r <= 1; r++) {
			if (0 === s && 0 === r) continue;
			const n = e.key.getNormalizedNeighbor(r, s, i).id, o = this._tiles.get(n);
			if (null != o) {
				const i = 1 << t;
				e.copyPixelBufferedEntitesFrom(o, i, r, s);
			}
			t--;
		}
	}
};
function l$5(e) {
	switch (e.type) {
		case "update": return e.modify.reduce((e, t) => e + u$7(t), 0);
		case "append": return e.append ? e.append.reduce((e, t) => e + u$7(t), 0) : 0;
	}
}
function u$7(e) {
	return e.entities.byteLength + e.data.reduce((e, t) => e + a$2(t), 0);
}
function a$2(e) {
	return e.indices.byteLength + e.vertices.byteLength + (e.metrics?.byteLength ?? 0);
}
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/FeatureContainer.js
var u$6 = class {
	constructor(e, t) {
		this.id = e, this.version = t, this._resolver = $(), this._done = !1;
	}
	get done() {
		return this._done;
	}
	get promise() {
		return this._resolver.promise;
	}
	end() {
		this._resolver.resolve(), this._done = !0;
	}
	destroy() {
		this._resolver.reject();
	}
};
var c$5 = class extends s$8 {
	constructor(e) {
		super(e.view.featuresTilingScheme), this.updatingHandles = new h$2(), this._hitTestsRequests = [], this._store = new n$7(), this._visibleTiles = /* @__PURE__ */ new Set(), this._subscriptions = /* @__PURE__ */ new Map(), this._updateStatisticsRequests = [], this._lockStatisticUpdates = !1, this._shouldUnlockAttributeView = !1, this._layerView = e, this.addTransitionable(this._layerView.featureEffectView);
	}
	destroy() {
		this.updatingHandles.destroy(), super.destroy(), this._renderState = u$8(this._renderState), this._renderStateNext = u$8(this._renderStateNext);
	}
	renderChildren(e) {
		if (this._updateAttributeView(), this._renderState?.update(this.attributeView.currentEpoch), this._layerView.requestUpdate(), this._renderState) {
			const e = Array.from(this._renderState.tiles()).filter((e) => e.needsUpload);
			if (e.length) {
				e[Math.floor(Math.random() * e.length)].upload(), e.length >= 2 && this.requestRender();
				for (const t of e.filter((e) => e.hasMetrics)) t.upload();
			}
			for (const t of this._renderState.tiles()) t.tryReady(this.attributeView.currentEpoch) && (this._subscriptions.get(t.key.id)?.end(), this._layerView.requestUpdate(), this.hasLabels && this._layerView.view.labelManager.requestUpdate(), this._layerView.view.labelManager.symbolFader.restartDeclutter(), this.requestRender());
		}
		const t = this._layerView.subscriptionManager.updateVisibility();
		this.setVisibleTiles(t);
		for (const s of this.children) s.setTransform(e.state);
		switch (super.renderChildren(e), e.drawPhase) {
			case 1: return this._renderMapPhase(e);
			case 16: return this._renderHighlightPhase(e);
			case 2: return this._renderLabelPhase(e);
		}
	}
	subscriptions() {
		return this._subscriptions.values();
	}
	get hasLabels() {
		return this._layerView.labelingCollisionInfos.length > 0;
	}
	get hasHighlight() {
		return this._layerView.hasHighlight;
	}
	get children() {
		return this._renderState ? Array.from(this._renderState.tiles()).filter((e) => this._visibleTiles.has(e.key.id)) : [];
	}
	get usedMemory() {
		let e = 0;
		return this._renderState && (e += this._renderState.usedMemory), this._renderStateNext && (e += this._renderStateNext.usedMemory), e += this.attributeView.usedMemory, e;
	}
	get instanceStore() {
		return this._store;
	}
	get labelingCollisionInfos() {
		return this._layerView.labelingCollisionInfos;
	}
	get layerOrder() {
		return this._layerView.view.allLayerViews.items.indexOf(this._layerView);
	}
	get tiles() {
		return this._renderState?.tiles();
	}
	get requiresRefresh() {
		return !!this._renderState?.requiresRefresh;
	}
	get _instanceStore() {
		return this._store;
	}
	_getHeatmapInstance(e) {
		if (null == this._instanceStore || !(e.drawPhase & F$2.heatmap.drawPhase)) return null;
		for (const t of this._instanceStore.values()) if (f$2(t)) return t;
		return null;
	}
	updateAttributeView(e) {
		this.requestRender(), this.attributeView.requestUpdate(e), this.hasLabels && (this._layerView.view.labelManager.requestUpdate(), this._layerView.view.labelManager.symbolFader.restartDeclutter());
	}
	updateSubscriptions(e) {
		for (const { tileId: t, version: s } of e.subscribe) {
			if (!this._subscriptions.has(t)) {
				const e = new u$6(t, s);
				this._subscriptions.set(t, e), this.updatingHandles.addPromise(e.promise);
				continue;
			}
			this._subscriptions.get(t).version = s;
		}
		for (const t of e.unsubscribe) this._subscriptions.get(t)?.destroy(), this._subscriptions.delete(t), this.removeTile(t);
	}
	isDone(e) {
		return !!this._renderState && this._renderState.isTileDone(e);
	}
	isFeatureFiltered(e) {
		const t = this.attributeView.getFilterFlags(e), s = t & 64, r = null == this._layerView.featureEffectView.featureEffect || this._layerView.featureEffectView.featureEffect.excludedLabelsVisible || t & 128;
		return !(s && r);
	}
	async updateRenderState(e) {
		has("esri-2d-update-debug") && console.debug(`Version[${e}] FeatureContainer.updateRenderState`), this._renderStateNext = new d$5(() => this._stage, (e) => this._subscriptions.get(e)?.version, e, this._layerView.view.labelManager.symbolFader, this, this.tilingScheme);
	}
	getDisplayStatistics(e, t) {
		const s = this._statisticsByLevel.get(e);
		return s ? s.get(t) : null;
	}
	updateStatistics(e, t) {
		if (this._lockStatisticUpdates) return void this._updateStatisticsRequests.push({
			level: e,
			statistics: t
		});
		let s = this._statisticsByLevel.get(e);
		s || (s = /* @__PURE__ */ new Map(), this._statisticsByLevel.set(e, s));
		for (const r of t) s.set(r.fieldName, {
			minValue: r.minValue,
			maxValue: r.maxValue
		});
	}
	lockForOverrides() {
		this._renderState?.lockUploads(), this._lockStatisticUpdates = !0, this.attributeView.locked || (this.attributeView.lockTextureUploads(), this._shouldUnlockAttributeView = !0);
	}
	unlockForOverrides() {
		this._renderState?.unlockUploads(), this._shouldUnlockAttributeView && (this.attributeView.unlockTextureUploads(), this._shouldUnlockAttributeView = !1), this._lockStatisticUpdates = !1;
		for (const e of this._updateStatisticsRequests) this.updateStatistics(e.level, e.statistics);
		this._updateStatisticsRequests = [], this._renderState?.flush(), this.requestRender();
	}
	trySwapRenderState() {
		if (this._renderStateNext) {
			has("esri-2d-update-debug") && console.debug(`Version[${this._renderStateNext.version}] FeatureContainer.update.swapRenderState`);
			const e = /* @__PURE__ */ new Map();
			for (const t of this._renderState?.tiles() || []) e.set(t.id, t.metricsVisibility);
			this._renderState?.destroy(), this._renderState = this._renderStateNext, this._renderState.flush();
			for (const t of this._renderState.tiles()) t.copyMetricsVisibility(e.get(t.id) || /* @__PURE__ */ new Set());
			this._renderStateNext = null;
		}
		this.requestRender();
	}
	setVisibleTiles(e) {
		this._visibleTiles = e;
		for (const t of this.tiles ?? []) t.rendering = e.has(t.key.id);
	}
	async onMessage(e, s) {
		s$6(s);
		const r = e.inner;
		if (!this._subscriptions.has(r.id)) return;
		const i = this._subscriptions.get(r.id);
		if (i.version !== r.subscriptionVesrion) {
			if (has("esri-2d-update-debug")) {
				const e = `${r.subscriptionVesrion} != ${i.version}`;
				console.debug(`Version[${e}] Tile[${r.id}] FeatureContainer - Dropping message, outdated version]`, r);
			}
			return;
		}
		const n = this._renderStateNext ?? this._renderState;
		if (!n) throw new Error("InternalError: No renderState defined");
		n.version !== r.version && console.error(`InternalError: Version mismatch. [renderState: ${n.version}, message: ${r.version}]`), n.enqueueUpdate(e), this.requestRender(), this._layerView.view.labelManager.requestUpdate(), this._layerView.requestUpdate();
	}
	removeTile(e) {
		(this._renderState || this._renderStateNext) && (this._renderState && this._renderState.removeTile(e), this._renderStateNext && this._renderStateNext.removeTile(e));
	}
	hitTest(e) {
		let t = this._hitTestsRequests.find(({ x: t, y: s }) => t === e.x && s === e.y);
		const r = $();
		return t ? t.resolvers.push(r) : (t = {
			x: e.x,
			y: e.y,
			resolvers: [r]
		}, this._hitTestsRequests.push(t)), this.requestRender(), r.promise;
	}
	getSortKeys(e) {
		const t = new Set(e), s = /* @__PURE__ */ new Map();
		for (const r of this.children) if (r.getSortKeys(t).forEach((e, t) => s.set(t, e)), s.size === t.size) break;
		return s;
	}
	get hasAnimation() {
		return this.hasLabels;
	}
	doRender(e) {
		const { minScale: t, maxScale: s } = this._layerView.layer.effectiveScaleRange, r = e.state.scale;
		r <= (t || Infinity) && r >= s && super.doRender(e);
	}
	afterRender(e) {
		super.afterRender(e), this._hitTestsRequests.length && this.requestRender();
	}
	setStencilReference(e) {
		if (null == this._getHeatmapInstance(e)) super.setStencilReference(e);
		else for (const t of this.children) t.stencilRef = F$2.heatmap.getStencilReference(t);
	}
	_renderMapPhase(e) {
		this._layerView.featureEffectView.hasEffects ? (this._renderOutsideEffect(e), this._renderInsideEffect(e)) : this._renderFeatures(e, 0), this._hitTestsRequests.length > 0 && this._renderHittest(e);
	}
	_renderHighlightPhase(e) {
		this.hasHighlight && p$7(e, !1, (e) => {
			this._renderFeatures(e, 1);
		});
	}
	_renderLabelPhase(e) {
		this._renderFeatures(e, 0);
	}
	_renderInsideEffect(e) {
		const t = e.painter.effects.insideEffect;
		t.bind(e), this._renderFeatures(e, 2), t.draw(e, this._layerView.featureEffectView.includedEffects), t.unbind();
	}
	_renderOutsideEffect(e) {
		const t = e.painter.effects.outsideEffect;
		t.bind(e), this._renderFeatures(e, 3), t.draw(e, this._layerView.featureEffectView.excludedEffects), t.unbind();
	}
	_renderHittest(e) {
		const { context: t } = e, s = e.painter.effects.hittest, r = t.getBoundFramebufferObject(), i = t.getViewport(), n = e.passOptions, a = e.drawPhase;
		s.bind(e), e.passOptions = s.createOptions(e, this._hitTestsRequests), e.drawPhase = 8;
		const { distance: o, smallSymbolDistance: h } = e.passOptions, d = Math.max(o, h);
		for (const l of this.children) l.visible && l.containsScreenPoint(e.state, e.passOptions.position, 2 * d) && this._renderTile(l, e, 0);
		s.draw(e), s.unbind(), t.bindFramebuffer(r), t.restoreViewport(i), e.passOptions = n, e.drawPhase = a;
	}
	_renderFeatures(e, t) {
		const s = this._getHeatmapInstance(e);
		null != s ? this._renderHeatmapFeatures(e, t, s) : this._renderGeometryFeatures(e, t);
	}
	_renderGeometryFeatures(e, t) {
		for (const s of this.children) s.visible && this._renderTile(s, e, t);
	}
	_renderHeatmapFeatures(e, t, s) {
		for (const r of this.children) r.visible && this._renderTile(r, e, t, 18);
		s.techniqueRef.renderResolvePass(e, s);
	}
	_renderTile(e, t, s, r) {
		const i = has("featurelayer-strict-draw-order") ? 1 : has("featurelayer-force-marker-text-draw-order") ? 2 : 0, n = e.getDisplayList(this._instanceStore, i);
		t.selection = s, n?.render(t, r);
	}
};
function f$2(e) {
	return 18 === e.techniqueRef.type;
}
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/FeaturePipelineWorkerProxy.js
async function t$3(t) {
	return new n$6(await r$8("FeaturePipelineWorker", {
		client: t,
		strategy: "dedicated"
	}));
}
var n$6 = class {
	constructor(e) {
		this._connection = e, this.pipeline = this._connection.createInvokeProxy(), this.features = this._connection.createInvokeProxy("features"), this.aggregates = this._connection.createInvokeProxy("aggregates"), this.streamMessenger = this._connection.createInvokeProxy("streamMessenger");
	}
	destroy() {
		this._connection.destroy();
	}
	get closed() {
		return this._connection.closed;
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/FeatureSourceEventLog.js
var l$4 = 10;
var u$5 = class extends b$2 {
	constructor() {
		super(...arguments), this.events = new i$7(), this._updatingStrategy = !0, this._tileToEvent = new e$8(), this._fetchStatus = {
			outstanding: 0,
			done: 0
		}, this._pipelineStatistics = {
			type: "performance",
			usedMemory: 0
		};
	}
	get hasAllData() {
		return !this._updatingStrategy && this._hasAllTileData();
	}
	get pipelineStatistics() {
		return this._pipelineStatistics;
	}
	get willQueryAllFeatures() {
		return this._strategyInfo?.willQueryAllFeatures ?? !1;
	}
	get willQueryFullResolutionGeometry() {
		return this._strategyInfo?.willQueryAllFeatures ?? !1;
	}
	onEvent(t) {
		switch (t.type) {
			case "subscribe":
			case "unsubscribe":
			case "loaded":
			case "error":
				this._handleTileEvent(t);
				break;
			case "updateStrategyStart":
				this._updatingStrategy = !0, this._fetchStatus = {
					done: 0,
					outstanding: 0
				}, this._strategyInfo = t.about;
				break;
			case "updateStrategyEnd":
				this._updatingStrategy = !1;
				break;
			case "updateFieldsStart":
				this._fetchStatus = {
					done: 0,
					outstanding: 0
				};
				break;
			case "updateFieldsEnd": break;
			case "updateFieldsError":
				this.events.emit("error", t);
				break;
			case "fetchStart":
				this._fetchStatus.outstanding += 1, this.events.emit("status", this._fetchStatus);
				break;
			case "fetchEnd":
				this._fetchStatus.done += 1, this.events.emit("status", this._fetchStatus), t.done && (this._fetchStatus = {
					done: 0,
					outstanding: 0
				});
				break;
			case "performance": this._pipelineStatistics = t;
		}
	}
	_hasAllTileData() {
		for (const t of this._tileToEvent.values()) if ("loaded" !== t.peekLast()?.type) return !1;
		return !0;
	}
	_handleTileEvent(t) {
		switch (t.type) {
			case "subscribe": {
				const e = new t$10(l$4);
				e.enqueue(t), this._tileToEvent.set(t.tile, e);
				break;
			}
			case "unsubscribe":
				this._tileToEvent.delete(t.tile);
				break;
			case "loaded": {
				const e = this._tileToEvent.get(t.tile);
				if (!e) return;
				e.enqueue(t), this._tileToEvent.set(t.tile, e);
				break;
			}
			case "error": {
				const e = this._tileToEvent.get(t.tile);
				if (!e) return;
				e.enqueue(t), this._tileToEvent.set(t.tile, e), this.events.emit("error", t), n$8.getLogger(this).error(`Failed to load tile ${t.tile}.`, { error: t.error });
				break;
			}
		}
	}
};
__decorate([a$4({ readOnly: !0 })], u$5.prototype, "hasAllData", null), __decorate([a$4()], u$5.prototype, "pipelineStatistics", null), __decorate([a$4()], u$5.prototype, "willQueryAllFeatures", null), __decorate([a$4()], u$5.prototype, "willQueryFullResolutionGeometry", null), __decorate([a$4()], u$5.prototype, "_updatingStrategy", void 0), __decorate([a$4()], u$5.prototype, "_strategyInfo", void 0), __decorate([a$4()], u$5.prototype, "_tileToEvent", void 0), __decorate([a$4()], u$5.prototype, "_pipelineStatistics", void 0), u$5 = __decorate([c$8("esri.views.2d.layers.features.FeatureSourceEventLog")], u$5);
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/layerAdapters/geometryUtils.js
function e$5(e) {
	switch (e.geometryType) {
		case "point": return "esriGeometryPoint";
		case "polyline": return "esriGeometryPolyline";
		case "mesh":
		case "polygon": return "esriGeometryPolygon";
		case "multipatch": return "esriGeometryMultiPatch";
		case "multipoint": return "esriGeometryMultipoint";
		default: return null;
	}
}
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/support/visualVariableSimpleUtils.js
var e$4 = Math.PI;
function r$5(t, n) {
	switch (n.transformationType) {
		case "additive": return a$1(t, n);
		case "constant": return s$3(n, t);
		case "clamped-linear": return u$4(t, n);
		case "proportional": return o$8(t, n);
		case "stops": return c$4(t, n);
		case "real-world-size": return l$3(t, n);
		case "identity": return t;
		case "unknown": return null;
	}
}
function i$3(t, n) {
	return "number" == typeof t ? t : r$5(n, t);
}
function a$1(t, n) {
	return t + (i$3(n.minSize, t) || n.minDataValue);
}
function s$3(t, n) {
	const e = t.stops;
	let r = e?.length && e[0].size;
	return r ??= t.minSize, i$3(r, n);
}
function u$4(t, n) {
	const e = n.minDataValue, r = n.maxDataValue, a = (t - e) / (r - e), s = i$3(n.minSize, t), u = i$3(n.maxSize, t);
	return t <= e ? s : t >= r ? u : s + a * (u - s);
}
function o$8(n, e) {
	const r = n / e.minDataValue, a = i$3(e.minSize, n), s = i$3(e.maxSize, n);
	let u = null;
	return u = r * a, r$7(u, a, s);
}
function c$4(t, n) {
	const [e, r, a] = m$3(t, n.cache.ipData);
	if (e === r) return i$3(n.stops[e].size, t);
	{
		const s = i$3(n.stops[e].size, t);
		return s + (i$3(n.stops[r].size, t) - s) * a;
	}
}
function l$3(t, r) {
	const i = m$7[r.valueUnit], { valueRepresentation: a } = r;
	let s = null;
	return s = "area" === a ? 2 * Math.sqrt(t / e$4) / i : "radius" === a || "distance" === a ? 2 * t / i : t / i, s;
}
function m$3(t, n) {
	if (!n) return;
	let e = 0, r = n.length - 1;
	return n.some((n, i) => t < n ? (r = i, !0) : (e = i, !1)), [
		e,
		r,
		(t - n[e]) / (n[r] - n[e])
	];
}
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/layerAdapters/labelingUtils.js
function l$2(n) {
	return (n.labelsVisible && n.labelingInfo?.every((n) => "none" !== n.deconflictionStrategy)) ?? !1;
}
function r$4(n, i) {
	const l = e$11(n, i);
	if (l?.labelsVisible && l.labelingInfo?.length) return l.labelingInfo.every((n) => "none" !== n.deconflictionStrategy);
}
function t$2(e) {
	return (l) => u$11(r$5(l, e));
}
function o$7(n) {
	const e = null != n && "visualVariables" in n && n.visualVariables;
	if (!e) return null;
	for (const i of e) if ("size" === i.type) return t$2(i);
	return null;
}
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/schema/ExceedsLimitCache.js
var o$6 = class {
	constructor() {
		this._cache = /* @__PURE__ */ new Map();
	}
	async executeExceedsLimitQuery(e, t, r) {
		const o = i$9.createFeature(e.metadata), i = u$15.create(e, t, o).createQuery();
		i.inner.orderByFields = [], i.inner.returnGeometry = !1, i.inner.cacheHint = !0;
		const c = "esriGeometryPoint" !== e.metadata.geometryType, u = JSON.stringify({
			source: e.source,
			query: i.inner.toJSON(),
			customParameters: i.customParameters,
			snapshotInfo: r,
			isPoints: c
		});
		let m = this._cache.get(u);
		return m ?? (m = await n$5(e.source, c, i, r), this._cache.set(u, m)), m;
	}
};
async function n$5(t, a, s, o) {
	if (!o.supportsExceedsLimit) return i$2(t, a, s, o);
	try {
		const a = s.inner.clone(), n = new p$3();
		n.statisticType = "exceedslimit", n.maxPointCount = o.maxFeatureCount, n.maxRecordCount = o.maxFeatureCount, n.outStatisticFieldName = "exceedslimit", o.maxVertexCount && (n.maxVertexCount = o.maxVertexCount), a.outStatistics = [n];
		return !(0 === (await l$9(t, a, void 0, { query: s.customParameters })).features[0]?.attributes.exceedslimit);
	} catch (n) {
		return i$2(t, a, s, o);
	}
}
async function i$2(e, r, a, s) {
	if (r) return !0;
	const o = a.inner.clone();
	try {
		return await O$1(e, o, { query: a.customParameters }) > s.maxFeatureCount;
	} catch (n) {
		return !0;
	}
}
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/schema/SourceSchema.js
async function e$3(e, r, i) {
	return {
		type: "feature",
		service: e,
		strategy: await t$1(e, i, r, i.cache)
	};
}
async function t$1(e, t, r, i) {
	const s = t.customParameters ?? {};
	t.apiKey && (s.token = t.apiKey);
	const o = t.displayFilterEnabled ? t.displayFilterInfo?.toJSON() : null, n = t.historicMoment?.getTime(), a = t.timeExtent?.toJSON(), l = {
		...t,
		displayFilterInfo: o,
		customParameters: s,
		historicMoment: n,
		timeExtent: a
	};
	if ("feature-service" === e.type && null != r && !await i.executeExceedsLimitQuery(e, l, r)) return {
		type: "snapshot",
		snapshotInfo: r,
		partial: { availableFields: t.availableFields },
		full: {
			displayFilterInfo: null,
			queryScaleRanges: null,
			sourceRefreshVersion: t.sourceRefreshVersion,
			definitionExpression: t.definitionExpression,
			postQueryFilter: t.postQueryFilter,
			customParameters: s,
			gdbVersion: t.gdbVersion,
			historicMoment: n,
			timeExtent: a
		}
	};
	return "feature-service" === e.type && e.isSourceHosted || "memory" === e.type || "ogc" === e.type ? {
		type: "paged-tile",
		partial: { availableFields: t.availableFields },
		full: {
			sourceRefreshVersion: t.sourceRefreshVersion,
			definitionExpression: t.definitionExpression,
			postQueryFilter: t.postQueryFilter,
			customParameters: s,
			gdbVersion: t.gdbVersion,
			historicMoment: n,
			queryScaleRanges: t.queryScaleRanges,
			timeExtent: a,
			displayFilterInfo: o
		}
	} : {
		type: "drill-down",
		partial: { availableFields: t.availableFields },
		full: {
			sourceRefreshVersion: t.sourceRefreshVersion,
			definitionExpression: t.definitionExpression,
			postQueryFilter: t.postQueryFilter,
			customParameters: s,
			gdbVersion: t.gdbVersion,
			historicMoment: n,
			queryScaleRanges: t.queryScaleRanges,
			timeExtent: a,
			displayFilterInfo: o
		}
	};
}
function r$3(e, t, r, a, l, u) {
	const p = n$4(a);
	if (!(!!has("featurelayer-snapshot-enabled") && r?.query.supportsPagination && !t)) return null;
	if (r?.operations.supportsEditing && !has("featurelayer-snapshot-allow-editable")) return null;
	const c = o$5(l, u), { min: y, max: m } = p, h = c ? m : y, f = s$2(a);
	let d = has("featurelayer-snapshot-initial-tolerance");
	return "esriGeometryPoint" !== a && "esriGeometryMultipoint" !== a || (d = null), {
		supportsExceedsLimit: i$1(e, r),
		initialTolerance: d,
		maxFeatureCount: h,
		maxVertexCount: f
	};
}
function i$1(e, t) {
	return !(!e && !has("featurelayer-snapshot-non-hosted-exceedslimit-enabled")) && t?.operations.supportsExceedsLimitStatistics;
}
function s$2(e) {
	switch (e) {
		case "esriGeometryPoint":
		case "esriGeometryPolyline":
		case "esriGeometryPolygon":
		case "esriGeometryMultiPatch":
		case "esriGeometryMultipoint": return has("featurelayer-snapshot-max-vertex-count");
	}
}
function o$5(e, t) {
	const r = t?.clone().intersection(e), i = null != r ? r.width * r.height : 0, s = t ? t.width * t.height : 0, o = 0 === s ? 0 : i / s, n = has("featurelayer-snapshot-coverage");
	return !isNaN(o) && o >= n;
}
function n$4(e) {
	switch (e) {
		case "esriGeometryMultipoint": return {
			min: has("featurelayer-snapshot-multipoint-min-threshold"),
			max: has("featurelayer-snapshot-multipoint-max-threshold")
		};
		case "esriGeometryPoint": return {
			min: has("featurelayer-snapshot-point-min-threshold"),
			max: has("featurelayer-snapshot-point-max-threshold")
		};
		case "esriGeometryMultiPatch":
		case "esriGeometryPolygon": return {
			min: has("featurelayer-snapshot-polygon-min-threshold"),
			max: has("featurelayer-snapshot-polygon-max-threshold")
		};
		case "esriGeometryPolyline": return {
			min: has("featurelayer-snapshot-polyline-min-threshold"),
			max: has("featurelayer-snapshot-polyline-max-threshold")
		};
	}
}
//#endregion
//#region node_modules/@arcgis/core/views/2d/engine/webgl/color.js
function u$3(n, r, t = 0) {
	if (null == r) return n[t] = 0, n[t + 1] = 0, n[t + 2] = 0, void (n[t + 3] = 0);
	const { r: o, g: u, b: i, a: c } = r;
	n[t] = o * c / 255, n[t + 1] = u * c / 255, n[t + 2] = i * c / 255, n[t + 3] = c;
}
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/schema/processor/symbols/SymbolSchema.js
async function z(i, a) {
	if (!i) return [];
	switch (i.type) {
		case "simple-fill": return U(i, a);
		case "picture-fill": return j(i, a);
		case "simple-marker": return C(i, a);
		case "picture-marker": return I(i, a);
		case "simple-line": return F$1(i, a, !1);
		case "text": return O(i, a);
		case "label": return A(i, a);
		case "cim": return c$12(i.data, a);
		case "web-style": return z(await i.fetchSymbol({ acceptedFormats: ["cim", "web"] }), a);
		case "line-3d": return n$8.getLogger("esri.views.layers.FeatureLayerView").warn("unsupported-symbol", `Symbol of type "${i.type}" unsupported in MapView. Defaulting to simple-line`), F$1(new d$8(), a, !1);
		case "point-3d": return n$8.getLogger("esri.views.layers.FeatureLayerView").warn("unsupported-symbol", `Symbol of type "${i.type}" unsupported in MapView. Defaulting to simple-marker`), C(new u$12(), a);
		case "polygon-3d": return n$8.getLogger("esri.views.layers.FeatureLayerView").warn("unsupported-symbol", `Symbol of type "${i.type}" unsupported in MapView. Defaulting to simple-fill`), U(new m$6(), a);
		case "mesh-3d":
		case "label-3d": return n$8.getLogger("esri.views.layers.FeatureLayerView").warn("unsupported-symbol", `Symbol of type "${i.type}" unsupported in MapView. Ignoring`), [];
		case "CIMSymbolReference": throw new Error("InternalError: CIMSymbolReference should already be resolved");
	}
}
async function d$4(e, i) {
	const { schemaOptions: a } = i, { store: r } = a, t = new Array(8), o = new Array(8 / 4);
	for (let l = 0; l < 8; l++) {
		const i = l < e.attributes.length ? e.attributes[l].color : null;
		t[l] = [
			0,
			0,
			0,
			0
		], u$3(t[l], i);
	}
	for (let l = 0; l < 8 / 4; l++) o[l] = [
		0,
		0,
		0,
		0
	], o[l][0] = 4 * l < e.attributes.length ? 1 : 0, o[l][1] = 4 * l + 1 < e.attributes.length ? 1 : 0, o[l][2] = 4 * l + 2 < e.attributes.length ? 1 : 0, o[l][3] = 4 * l + 3 < e.attributes.length ? 1 : 0;
	const s = {
		uniforms: {
			isActive: o,
			colors: t,
			dotValue: e.dotValue,
			dotScale: e.referenceScale,
			blending: e.dotBlendingEnabled,
			dotSize: e.dotSize,
			seed: e.seed
		},
		optionalAttributes: {}
	}, n = r.ensureInstance(F$2.dotDensity, s).createMeshInfo({ effects: null }), u = [], m = await z(new m$6({
		color: e.backgroundColor ?? [
			0,
			0,
			0,
			0
		],
		outline: null
	}), i);
	if (u.push(...m), u.push(n), e.outline) {
		const a = F$1(e.outline, i, !0);
		u.push(...a);
	}
	return u;
}
async function g$1(e, l) {
	const { store: r } = l, { radius: t, minDensity: o, maxDensity: s, referenceScale: n, field: u, valueExpression: c, colorStops: p } = e, b = f$3(p);
	return [r.ensureInstance(F$2.heatmap, {
		uniforms: {
			radius: u$11(t),
			minDensity: o,
			maxDensity: s,
			referenceScale: n,
			isFieldActive: !(!u && !c),
			gradient: b,
			gradientHash: b.join(",")
		},
		optionalAttributes: {}
	}).createMeshInfo({ effects: null })];
}
async function M(e, a) {
	const { store: l } = a, r = e.outline?.width || 0, t = x$2(e), o = l.ensureInstance(F$2.pieChart, {
		uniforms: {
			shader: {
				outlineWidth: Math.round(u$11(r)),
				defaultColor: t$8(e.defaultColor),
				outlineColor: t$8(e.outline?.color),
				othersColor: t$8(e.othersCategory?.color),
				donutRatio: e.holePercentage,
				sectorThreshold: e.othersCategory?.threshold || 0,
				colors: e.attributes.map((e) => t$8(e.color)),
				visualVariableOpacity: t.visualVariableOpacity,
				visualVariableSizeMinMaxValue: t.visualVariableSizeMinMaxValue,
				visualVariableSizeScaleStops: t.visualVariableSizeScaleStops,
				visualVariableSizeStops: t.visualVariableSizeStops,
				visualVariableSizeUnitValue: t.visualVariableSizeUnitValue,
				hittestUniforms: null,
				highlightUniforms: null
			},
			numberOfFields: e.attributes.length
		},
		optionalAttributes: {}
	}).createMeshInfo({
		size: e.size,
		outlineWidth: r,
		effects: null,
		scaleInfo: null,
		minPixelBuffer: z$3(t)
	});
	return [...e.backgroundFillSymbol ? await U(e.backgroundFillSymbol, {
		schemaOptions: a,
		uniforms: b$4
	}) : [], o];
}
function x(e) {
	if ("path" === e.style) {
		if (null == e.path) throw new Error("Symbol with a style of type path must define a path");
		return {
			type: "sprite-rasterization-param",
			overrides: [],
			resource: {
				type: "path",
				path: e.path,
				asFill: !0
			}
		};
	}
	const i = _$1.fromSimpleMarker(e);
	if ("outline" in e && e.outline && "none" !== e.outline.style) {
		if ("solid" !== e.outline.style) {
			if (!i || !i.symbolLayers) throw new Error("Error handling marker! ");
			return {
				type: "sprite-rasterization-param",
				resource: i.symbolLayers[0],
				overrides: []
			};
		}
	}
	return {
		type: "sprite-rasterization-param",
		resource: s$10(i),
		overrides: []
	};
}
async function C(e, i) {
	const { uniforms: a, schemaOptions: l } = i, { store: r } = l;
	if ("path" === e.style || e.outline && "solid" !== e.outline.style && "none" !== e.outline.style) {
		const l = _$1.fromSimpleMarker(e);
		if (!l || !l.symbolLayers) throw new Error("Error handling marker! ");
		if (a.visualVariableRotation && (l.angleAlignment = "Map"), "path" !== e.style) {
			const e = l.symbolLayers[0];
			if (A$2(i.uniforms)) {
				const a = z$3(i.uniforms, 0, 1);
				if (a > e.size) {
					const i = a / e.size;
					e.size = a;
					const l = e.markerGraphics?.[0].symbol;
					(l.symbolLayers && l.symbolLayers[0]).width *= i;
				}
			}
		}
		return c$12({
			type: "CIMSymbolReference",
			symbol: l
		}, i);
	}
	const t = r.ensureInstance(F$2.marker, {
		uniforms: {
			visualVariableColor: a.visualVariableColor,
			visualVariableOpacity: a.visualVariableOpacity,
			visualVariableSizeMinMaxValue: a.visualVariableSizeMinMaxValue,
			visualVariableSizeScaleStops: a.visualVariableSizeScaleStops,
			visualVariableSizeStops: a.visualVariableSizeStops,
			visualVariableSizeUnitValue: a.visualVariableSizeUnitValue,
			visualVariableRotation: a.visualVariableRotation
		},
		optionalAttributes: { zoomRange: !1 }
	}), s = x(e);
	let n = e.color?.toArray() ?? [
		0,
		0,
		0,
		0
	];
	"CIMVectorMarker" === s.resource.type && (n = [
		255,
		255,
		255,
		255
	]);
	const u = "triangle" === e.style ? 124 / 116 : 1, c = e.size, p = c * u, b = null != a.visualVariableColor && ("cross" === e.style || "x" === e.style);
	return [t.createMeshInfo({
		type: "simple",
		color: n,
		height: c,
		width: p,
		offsetX: e.xoffset,
		offsetY: e.yoffset,
		angle: e.angle,
		alignment: e$10(a) ? 1 : 0,
		outlineColor: e.outline?.color?.toArray() ?? [
			0,
			0,
			0,
			0
		],
		outlineSize: e.outline?.width ?? 1,
		referenceSize: c,
		sprite: s,
		overrideOutlineColor: b,
		hasSizeVV: A$2(a),
		placement: null,
		effects: null,
		transforms: null,
		scaleInfo: null,
		minPixelBuffer: z$3(a)
	})];
}
function I(e, i) {
	const { uniforms: a, schemaOptions: l } = i, { store: r } = l, t = r.ensureInstance(F$2.marker, {
		uniforms: {
			visualVariableColor: a.visualVariableColor,
			visualVariableOpacity: a.visualVariableOpacity,
			visualVariableSizeMinMaxValue: a.visualVariableSizeMinMaxValue,
			visualVariableSizeScaleStops: a.visualVariableSizeScaleStops,
			visualVariableSizeStops: a.visualVariableSizeStops,
			visualVariableSizeUnitValue: a.visualVariableSizeUnitValue,
			visualVariableRotation: a.visualVariableRotation
		},
		optionalAttributes: { zoomRange: !1 }
	}), s = _$1.createPictureMarkerRasterizationParam(e);
	if (!s) return [];
	return [t.createMeshInfo({
		type: "picture",
		color: [
			255,
			255,
			255,
			255
		],
		height: e.height,
		width: e.width,
		offsetX: e.xoffset,
		offsetY: e.yoffset,
		angle: e.angle,
		alignment: e$10(a) ? 1 : 0,
		outlineColor: null,
		outlineSize: 0,
		referenceSize: e.height,
		sprite: s,
		overrideOutlineColor: !1,
		hasSizeVV: A$2(a),
		placement: null,
		effects: null,
		transforms: null,
		scaleInfo: null,
		minPixelBuffer: z$3(a)
	})];
}
function w(e, i, a) {
	const { uniforms: l, schemaOptions: r } = a, { store: t } = r, o = t.ensureInstance(F$2.marker, {
		uniforms: {
			visualVariableColor: l.visualVariableColor,
			visualVariableOpacity: l.visualVariableOpacity,
			visualVariableSizeMinMaxValue: l.visualVariableSizeMinMaxValue,
			visualVariableSizeScaleStops: l.visualVariableSizeScaleStops,
			visualVariableSizeStops: l.visualVariableSizeStops,
			visualVariableSizeUnitValue: l.visualVariableSizeUnitValue,
			visualVariableRotation: l.visualVariableRotation
		},
		optionalAttributes: { zoomRange: !1 }
	}), s = x(e), n = 6, u = n * i.width, c = u, p = e.color?.toArray() ?? i.color?.toArray() ?? [
		0,
		0,
		0,
		0
	], b = "cross" === e.style || "x" === e.style;
	let m;
	switch (e.placement) {
		case "begin-end":
			m = "Both";
			break;
		case "begin":
			m = "JustBegin";
			break;
		case "end":
			m = "JustEnd";
			break;
		default: m = "None";
	}
	const V = {
		type: "cim-marker-placement-param",
		placement: {
			type: "CIMMarkerPlacementAtExtremities",
			angleToLine: !0,
			offset: 0,
			extremityPlacement: m,
			offsetAlongLine: 0
		},
		overrides: []
	};
	return [o.createMeshInfo({
		type: "simple",
		color: p,
		height: c,
		width: u,
		offsetX: 0,
		offsetY: 0,
		angle: 0,
		alignment: e$10(l) ? 1 : 0,
		outlineColor: p,
		outlineSize: b ? i.width : 0,
		referenceSize: c / n,
		sprite: s,
		overrideOutlineColor: b && null != l.visualVariableColor,
		hasSizeVV: A$2(l),
		placement: V,
		transforms: null,
		effects: null,
		scaleInfo: null,
		minPixelBuffer: z$3(l)
	})];
}
function O(e, i) {
	const { uniforms: a, schemaOptions: l } = i, { store: r } = l;
	return [r.ensureInstance(F$2.text, {
		uniforms: {
			visualVariableColor: a.visualVariableColor,
			visualVariableOpacity: a.visualVariableOpacity,
			visualVariableRotation: a.visualVariableRotation,
			visualVariableSizeMinMaxValue: a.visualVariableSizeMinMaxValue,
			visualVariableSizeScaleStops: a.visualVariableSizeScaleStops,
			visualVariableSizeStops: a.visualVariableSizeStops,
			visualVariableSizeUnitValue: a.visualVariableSizeUnitValue
		},
		optionalAttributes: {
			zoomRange: !1,
			clipAngle: !1,
			referenceSymbol: !1,
			visibility: !1
		}
	}).createMeshInfo({
		alignment: 0,
		boxBackgroundColor: e.backgroundColor?.toArray(),
		boxBorderLineColor: e.borderLineColor?.toArray(),
		boxBorderLineSize: e.borderLineSize ?? 0,
		color: e.color?.toArray() ?? [
			0,
			0,
			0,
			0
		],
		offsetX: e.xoffset,
		offsetY: e.yoffset,
		postAngle: e.angle,
		fontSize: e.font.size,
		decoration: e.font.decoration,
		haloColor: e.haloColor?.toArray() ?? [
			0,
			0,
			0,
			0
		],
		haloSize: e.haloSize ?? 0,
		outlineColor: [
			0,
			0,
			0,
			0
		],
		outlineSize: 0,
		lineWidth: e.lineWidth,
		lineHeightRatio: e.lineHeight,
		horizontalAlignment: e.horizontalAlignment,
		verticalAlignment: e.verticalAlignment,
		useCIMAngleBehavior: !1,
		glyphs: {
			type: "text-rasterization-param",
			resource: {
				type: "text",
				font: e.font.toJSON(),
				textString: e.text,
				symbol: _$1.createCIMTextSymbolfromTextSymbol(e)
			},
			overrides: []
		},
		referenceSize: null,
		effects: null,
		placement: null,
		scaleInfo: null,
		transforms: null,
		scaleFactor: 1,
		minPixelBuffer: z$3(a),
		repeatLabel: null,
		repeatLabelDistance: null,
		allowOverrun: null,
		labelPosition: null,
		layerId: null,
		labelClassId: null
	})];
}
function A(e, a) {
	const { schemaOptions: l, uniforms: r } = a, { store: t } = l, s = e.symbol, { allowOverrun: n, repeatLabel: c, repeatLabelDistance: p } = e, b = {
		maxScale: e.maxScale ?? 0,
		minScale: e.minScale ?? 0
	}, m = t.ensureInstance(F$2.label, {
		uniforms: {
			visualVariableColor: null,
			visualVariableOpacity: null,
			visualVariableRotation: r.visualVariableRotation,
			visualVariableSizeMinMaxValue: r.visualVariableSizeMinMaxValue,
			visualVariableSizeScaleStops: r.visualVariableSizeScaleStops,
			visualVariableSizeStops: r.visualVariableSizeStops,
			visualVariableSizeUnitValue: r.visualVariableSizeUnitValue
		},
		optionalAttributes: {
			zoomRange: !0,
			clipAngle: !0,
			referenceSymbol: !0,
			visibility: !0
		}
	}), V = e.labelPlacement, [y, v] = a$6(V);
	return [m.createMeshInfo({
		alignment: 0,
		boxBackgroundColor: s.backgroundColor?.toArray(),
		boxBorderLineColor: s.borderLineColor?.toArray(),
		boxBorderLineSize: s.borderLineSize ?? 0,
		color: s.color?.toArray() ?? [
			0,
			0,
			0,
			0
		],
		offsetX: s.xoffset,
		offsetY: s.yoffset,
		postAngle: s.angle,
		fontSize: s.font.size,
		decoration: s.font.decoration,
		outlineColor: [
			0,
			0,
			0,
			0
		],
		outlineSize: 0,
		haloColor: s.haloColor?.toArray() ?? [
			0,
			0,
			0,
			0
		],
		haloSize: s.haloSize ?? 0,
		lineWidth: s.lineWidth,
		lineHeightRatio: s.lineHeight,
		horizontalAlignment: y,
		verticalAlignment: v,
		repeatLabel: c,
		repeatLabelDistance: u$11(p),
		allowOverrun: n,
		labelPosition: e.labelPosition,
		scaleInfo: b,
		minPixelBuffer: z$3(r),
		useCIMAngleBehavior: !1,
		glyphs: {
			type: "text-rasterization-param",
			resource: {
				type: "text",
				font: s.font.toJSON(),
				textString: s.text,
				symbol: _$1.createCIMTextSymbolfromTextSymbol(s),
				primitiveName: "label-override"
			},
			useLegacyLabelEvaluationRules: null == e.labelExpressionInfo?.expression,
			overrides: [{
				valueExpressionInfo: {
					type: "CIMExpressionInfo",
					expression: e.labelExpressionInfo?.expression ?? e.labelExpression,
					returnType: "String"
				},
				primitiveName: "label-override",
				propertyName: "textString",
				defaultValue: ""
			}]
		},
		referenceSize: null,
		effects: null,
		placement: null,
		transforms: null,
		scaleFactor: 1,
		layerId: a.layerId,
		labelClassId: a.labelClassId
	})];
}
function L(e, i) {
	const a = e.width;
	return {
		outlineColor: e.color?.toArray() || [
			0,
			0,
			0,
			1
		],
		width: a,
		referenceWidth: a,
		capType: e.cap ?? "round",
		joinType: e.join ?? "round",
		miterLimit: e.miterLimit,
		hasSizeVV: i,
		outlineUsesColorVV: !1
	};
}
function R(e, i) {
	const { uniforms: a, schemaOptions: l } = i, { store: r } = l, t = e.color?.toArray() ?? [
		0,
		0,
		0,
		0
	], o = {
		type: "sprite-rasterization-param",
		resource: {
			type: "fill-style",
			style: e.style
		},
		overrides: []
	};
	if ("solid" === e.outline?.style) return [r.ensureInstance(F$2.patternOutlineFill, {
		uniforms: {
			visualVariableColor: a.visualVariableColor,
			visualVariableOpacity: a.visualVariableOpacity,
			visualVariableSizeScaleStops: a.visualVariableSizeOutlineScaleStops,
			visualVariableSizeMinMaxValue: null,
			visualVariableSizeStops: null,
			visualVariableSizeUnitValue: null
		},
		optionalAttributes: { zoomRange: !1 }
	}).createMeshInfo({
		color: t,
		...L(e.outline, !!a.visualVariableSizeOutlineScaleStops),
		sprite: o,
		scaleInfo: null,
		effects: null
	})];
	const s = [], n = r.ensureInstance(F$2.patternFill, {
		uniforms: {
			visualVariableColor: a.visualVariableColor,
			visualVariableOpacity: a.visualVariableOpacity
		},
		optionalAttributes: { zoomRange: !1 }
	}).createMeshInfo({
		color: e.color?.toArray() ?? [
			0,
			0,
			0,
			0
		],
		sprite: o,
		scaleInfo: null,
		effects: null
	});
	return s.push(n), e.outline && s.push(...F$1(e.outline, i, !0)), s;
}
function k(e, i) {
	const { uniforms: a, schemaOptions: l } = i, { store: r } = l, t = e.color?.toArray() ?? [
		0,
		0,
		0,
		0
	];
	if ("none" !== e.style && "solid" === e.outline?.style) return [r.ensureInstance(F$2.outlineFill, {
		uniforms: {
			visualVariableColor: a.visualVariableColor,
			visualVariableOpacity: a.visualVariableOpacity,
			visualVariableSizeScaleStops: a.visualVariableSizeOutlineScaleStops,
			visualVariableSizeMinMaxValue: null,
			visualVariableSizeStops: null,
			visualVariableSizeUnitValue: null
		},
		optionalAttributes: { zoomRange: !1 }
	}).createMeshInfo({
		color: t,
		...L(e.outline, !!a.visualVariableSizeOutlineScaleStops),
		scaleInfo: null,
		effects: null
	})];
	const o = [];
	if ("none" !== e.style) {
		const e = r.ensureInstance(F$2.fill, {
			uniforms: {
				visualVariableColor: a.visualVariableColor,
				visualVariableOpacity: a.visualVariableOpacity
			},
			optionalAttributes: { zoomRange: !1 }
		}).createMeshInfo({
			color: t,
			scaleInfo: null,
			effects: null
		});
		o.push(e);
	}
	return e.outline && o.push(...F$1(e.outline, i, !0)), o;
}
async function U(e, i) {
	if ("cim" === e.type) return c$12(e.data, i);
	const { style: a } = e;
	return a && "none" !== a && "solid" !== a ? R(e, i) : k(e, i);
}
function j(e, i) {
	const { outline: a } = e, { uniforms: l, schemaOptions: r } = i, { store: t } = r, s = [], n = _$1.createPictureFillRasterizationParam(e);
	if (!n) return [];
	const { width: u, height: c, xoffset: p, yoffset: b, xscale: m, yscale: S } = e, V = {
		color: [
			255,
			255,
			255,
			255
		],
		sprite: n,
		height: c,
		aspectRatio: u / c,
		offsetX: p,
		offsetY: b,
		scaleX: m,
		scaleY: S,
		angle: 0,
		applyRandomOffset: !1,
		sampleAlphaOnly: !1,
		scaleProportionally: !1,
		effects: null,
		scaleInfo: null
	};
	if ("solid" === a?.style) return [t.ensureInstance(F$2.complexOutlineFill, {
		uniforms: {
			visualVariableColor: l.visualVariableColor,
			visualVariableOpacity: l.visualVariableOpacity,
			visualVariableSizeScaleStops: l.visualVariableSizeOutlineScaleStops,
			visualVariableSizeMinMaxValue: null,
			visualVariableSizeStops: null,
			visualVariableSizeUnitValue: null
		},
		optionalAttributes: { zoomRange: !1 }
	}).createMeshInfo({
		...V,
		...L(a, !!l.visualVariableSizeOutlineScaleStops)
	})];
	const y = t.ensureInstance(F$2.complexFill, {
		uniforms: {
			visualVariableColor: l.visualVariableColor,
			visualVariableOpacity: l.visualVariableOpacity
		},
		optionalAttributes: { zoomRange: !1 }
	});
	return s.push(y.createMeshInfo(V)), a && s.push(...F$1(a, i, !0)), s;
}
function F$1(e, i, a) {
	const { color: l, style: r, width: t, cap: o, join: n } = e, { schemaOptions: u } = i, { store: c } = u, p = [], b = a ? {
		...b$4,
		visualVariableSizeScaleStops: i.uniforms.visualVariableSizeOutlineScaleStops
	} : i.uniforms, m = {
		uniforms: {
			visualVariableColor: b.visualVariableColor,
			visualVariableOpacity: b.visualVariableOpacity,
			visualVariableSizeMinMaxValue: b.visualVariableSizeMinMaxValue,
			visualVariableSizeScaleStops: b.visualVariableSizeScaleStops,
			visualVariableSizeStops: b.visualVariableSizeStops,
			visualVariableSizeUnitValue: b.visualVariableSizeUnitValue
		},
		optionalAttributes: { zoomRange: !1 }
	}, S = {
		color: l?.toArray() ?? [
			0,
			0,
			0,
			0
		],
		width: t,
		referenceWidth: t,
		capType: o,
		joinType: n,
		miterLimit: e.miterLimit,
		hasSizeVV: A$2(b),
		effects: null,
		scaleInfo: null
	};
	if (null == r || "solid" === r) {
		const e = c.ensureInstance(F$2.line, m).createMeshInfo(S);
		p.push(e);
	} else if ("none" !== r) {
		const e = c.ensureInstance(F$2.texturedLine, m).createMeshInfo({
			...S,
			offsetAlongLine: 0,
			shouldScaleDash: !0,
			shouldSampleAlphaOnly: !1,
			isSDF: !0,
			sprite: {
				type: "sprite-rasterization-param",
				resource: {
					type: "dash",
					dashTemplate: Z$2(r, o)
				},
				overrides: []
			}
		});
		p.push(e);
	}
	return null != e.marker && p.push(...w(e.marker, e, i)), p;
}
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/schema/processor/LabelMatcherSchema.js
async function o$4(e, a, o) {
	const r = a.labelsVisible && a.labelingInfo || [], i = e$5(a), t = c$11(r, i);
	return {
		type: "label",
		classes: await Promise.all(t.map((l, s) => n$3(e, l, a.labelsVisible, a.labelingInfoSource, s, i, o)))
	};
}
async function n$3(l, s, o, n, r, i, t) {
	const m = l$6(`${n} ${r}`), c = n, p = await z(s, {
		schemaOptions: l,
		uniforms: t,
		layerId: c,
		labelClassId: m
	});
	return {
		maxScale: s.maxScale,
		minScale: s.minScale,
		deconflictionEnabled: "none" !== s.deconflictionStrategy && o,
		expression: s.labelExpressionInfo?.expression ?? s.labelExpression,
		where: s.where,
		meshes: p,
		layerId: c,
		labelClassId: m,
		geometryType: i
	};
}
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/schema/processor/MatcherSchema.js
async function l$1(e, i) {
	if (!i) return {
		type: "simple",
		meshes: []
	};
	switch (i.type) {
		case "simple": return o$3(e, i);
		case "dot-density": return r$2(e, i);
		case "class-breaks": return u$2(e, i);
		case "unique-value": return m$2(e, i);
		case "dictionary": return c$3(i);
		case "heatmap": return p$1(e, i);
		case "pie-chart": return f$1(e, i);
	}
}
async function o$3(i, a) {
	const n = a.symbols;
	return {
		type: "simple",
		meshes: await z(n.length ? n[0] : null, {
			schemaOptions: i,
			uniforms: x$2(a)
		})
	};
}
async function r$2(i, a) {
	return {
		type: "dot-density",
		meshes: await d$4(a, {
			schemaOptions: i,
			uniforms: x$2(a)
		})
	};
}
async function u$2(a, n) {
	const t = x$2(n), l = n.backgroundFillSymbol, o = n.normalizationType, r = "log" === o ? "esriNormalizeByLog" : "percent-of-total" === o ? "esriNormalizeByPercentOfTotal" : "field" === o ? "esriNormalizeByField" : null, u = n.classBreakInfos.map(async (e) => ({
		meshes: await z(e.symbol, {
			path: `renderer-stop-${e.minValue}-${e.maxValue}`,
			schemaOptions: a,
			uniforms: t
		}),
		min: e.minValue,
		max: e.maxValue
	})), m = (await Promise.all(u)).sort((e, i) => e.min - i.min), c = await z(l, {
		schemaOptions: a,
		uniforms: {
			...b$4,
			visualVariableSizeOutlineScaleStops: t.visualVariableSizeOutlineScaleStops
		}
	}), p = await z(n.defaultSymbol, {
		schemaOptions: a,
		uniforms: t
	});
	return {
		type: "interval",
		field: n.field,
		expression: n.valueExpression,
		backgroundFill: c,
		defaultSymbol: p,
		intervals: m,
		normalizationField: n.normalizationField,
		normalizationTotal: n.normalizationTotal,
		normalizationType: r,
		isMaxInclusive: n.isMaxInclusive
	};
}
async function m$2(a, n) {
	const t = [], l = x$2(n), o = await z(n.backgroundFillSymbol, {
		schemaOptions: a,
		uniforms: {
			...b$4,
			visualVariableSizeOutlineScaleStops: l.visualVariableSizeOutlineScaleStops
		}
	}), r = await z(n.defaultSymbol, {
		schemaOptions: a,
		uniforms: l
	});
	for (const e of n.uniqueValueInfos ?? []) {
		const i = await z(e.symbol, {
			path: `renderer-unique-value-${e.value}`,
			schemaOptions: a,
			uniforms: l
		});
		t.push({
			value: "" + e.value,
			symbol: i
		});
	}
	return {
		type: "map",
		field: n.field,
		expression: n.valueExpression,
		field2: n.field2,
		field3: n.field3,
		fieldDelimiter: n.fieldDelimiter,
		backgroundFill: o,
		defaultSymbol: r,
		map: t
	};
}
async function c$3(i) {
	const a = x$2(i), n = await i.getDictionaryInfo(), s = i.scaleExpression, t = null != s && "1" !== s ? {
		valueExpressionInfo: {
			type: "CIMExpressionInfo",
			expression: i.scaleExpression,
			returnType: "Numeric"
		},
		defaultValue: 1
	} : void 0;
	return {
		type: "dictionary",
		dictionaryInfo: n,
		userConfig: i.config ?? {},
		fieldMap: i.fieldMap ?? {},
		scaleExpression: t,
		visualVariableUniforms: a
	};
}
async function p$1(e, i) {
	return {
		type: "heatmap",
		meshes: await g$1(i, e)
	};
}
async function f$1(e, i) {
	return {
		type: "pie-chart",
		meshes: await M(i, e)
	};
}
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/schema/processor/SimpleProcessorSchema.js
async function l(e, r) {
	const s = r.renderer, n = x$2(s);
	return {
		symbology: await l$1(e, s),
		labels: await o$4(e, r, n)
	};
}
async function o$2(e, r, t, i) {
	const n = t.featureReduction;
	if (n) switch (n.type) {
		case "binning": return f(n, e, r, t, i);
		case "cluster": return b$1(n, e, r, t, i);
	}
	if (t.trackInfo?.enabled) return p(t.trackInfo, e, r, t, i);
	const a = y$2(t.orderBy, t.renderer, t.objectIdField), o = t$9(t.renderer, r.filters), c = await l(e, t), u = m$1(c.symbology);
	return {
		storage: o,
		mesh: {
			properties: {
				sortKey: a,
				timeZone: r.timeZone,
				returnMeshObjectId: u,
				displayRefreshVersion: i,
				currentUser: r.currentUser
			},
			strategy: { type: "feature" },
			factory: c
		},
		expressionProperties: { timeExtent: r.timeExtent?.toJSON() }
	};
}
function c$2(e, r) {
	return e.fields.map((e) => ({
		...e.toJSON(),
		type: u$1(e, r)
	}));
}
function u$1(e, r) {
	const { onStatisticExpression: t, onStatisticField: i, statisticType: s } = e;
	switch (s) {
		case "min":
		case "max":
		case "avg":
		case "avg_angle":
		case "sum":
		case "count": return "esriFieldTypeDouble";
		case "mode": {
			if (t) {
				const { returnType: e } = t;
				return e ? "string" === e ? "esriFieldTypeString" : "esriFieldTypeDouble" : "esriFieldTypeString";
			}
			const e = r.find((e) => e.name === i);
			return e ? e.type : "esriFieldTypeString";
		}
	}
}
async function f(r, n, l, o, u) {
	const f = c$2(r, o.fields), b = r.renderer, p = await l$1(n, b), y = t$9(b, [null, null]), d = x$2(b), g = await o$4(n, {
		geometryType: "polygon",
		labelingInfoSource: o.labelingInfoSource + "-binning",
		labelingInfo: r.labelingInfo,
		labelsVisible: r.labelsVisible
	}, d), v = m$1(p), h = "geohash" === r.binType ? {
		type: "geohash",
		fixBinLevel: r.fixedBinLevel ?? 3
	} : {
		type: "grid",
		size: u$11(r.size),
		fixedBinLevel: r.fixedBinLevel
	};
	return {
		storage: y,
		mesh: {
			properties: {
				sortKey: null,
				timeZone: l.timeZone,
				returnMeshObjectId: v,
				displayRefreshVersion: u,
				currentUser: l.currentUser
			},
			strategy: {
				type: "binning",
				fields: f,
				index: h,
				featureFilter: l.filters[0]
			},
			factory: {
				labels: g,
				symbology: p
			}
		},
		expressionProperties: { timeExtent: l.timeExtent?.toJSON() }
	};
}
async function b$1(r, n, l, o, u) {
	const f = c$2(r, o.fields), b = {
		type: "cluster",
		feature: await l$1(n, r.effectiveFeatureRenderer),
		cluster: await l$1(n, r.effectiveClusterRenderer)
	}, p = x$2(r.effectiveFeatureRenderer), y = {
		type: "cluster",
		feature: await o$4(n, o, p),
		cluster: await o$4(n, {
			geometryType: "point",
			labelingInfoSource: o.labelingInfoSource + "-clusters",
			labelingInfo: r.labelingInfo,
			labelsVisible: r.labelsVisible
		}, p)
	}, d = t$9(r.effectiveFeatureRenderer, [null, null]), g = m$1(b);
	return {
		storage: d,
		mesh: {
			properties: {
				sortKey: null,
				timeZone: l.timeZone,
				displayRefreshVersion: u,
				returnMeshObjectId: g,
				currentUser: l.currentUser
			},
			strategy: {
				type: "cluster",
				fields: f,
				featureFilter: l.filters[0],
				clusterRadius: u$11(r.clusterRadius / 2)
			},
			factory: {
				labels: y,
				symbology: b
			}
		},
		expressionProperties: { timeExtent: l.timeExtent?.toJSON() }
	};
}
async function p(e, r, s, l, o) {
	const u = c$2(e, l.fields), f = {
		type: "track",
		previousObservation: await l$1(r, e.previousObservations.renderer),
		latestObservation: await l$1(r, e.latestObservations.renderer),
		trackLine: await l$1(r, e.trackLines.renderer)
	}, b = {
		type: "track",
		previousObservation: await o$4(r, {
			geometryType: l.geometryType,
			labelingInfoSource: l.labelingInfoSource + "-track-prev",
			labelingInfo: e.previousObservations.labelingInfo,
			labelsVisible: e.previousObservations.labelsVisible
		}, x$2(e.previousObservations.renderer)),
		latestObservation: await o$4(r, {
			geometryType: l.geometryType,
			labelingInfoSource: l.labelingInfoSource + "-track-latest",
			labelingInfo: e.latestObservations.labelingInfo,
			labelsVisible: e.latestObservations.labelsVisible
		}, x$2(e.latestObservations.renderer)),
		trackLine: await o$4(r, {
			geometryType: "polyline",
			labelingInfoSource: l.labelingInfoSource + "-track-line",
			labelingInfo: e.trackLines.labelingInfo,
			labelsVisible: e.trackLines.labelsVisible
		}, x$2(e.trackLines.renderer))
	}, p = s$9(e, [null, null]), y = m$1(f);
	return {
		storage: p,
		mesh: {
			properties: {
				sortKey: null,
				timeZone: s.timeZone,
				returnMeshObjectId: y,
				displayRefreshVersion: o,
				currentUser: s.currentUser
			},
			strategy: {
				type: "track",
				featureFilter: s.filters[0],
				fields: u,
				maxDisplayDuration: e.maxDisplayDuration?.toMilliseconds() ?? 0,
				maxDisplayObservationsPerTrack: e.maxDisplayObservationsPerTrack,
				showLatestObservation: e.latestObservations.visible,
				showPreviousObservations: e.previousObservations.visible,
				showTrackLine: e.trackLines.visible,
				timeField: e.timeField
			},
			factory: {
				labels: b,
				symbology: f
			}
		},
		expressionProperties: { timeExtent: s.timeExtent?.toJSON() }
	};
}
function y$2(e, t, i) {
	const s = null != t && "unique-value" === t.type && t.orderByClassesEnabled;
	if ("default" !== e || s || (e = [new p$6({
		field: i,
		order: "descending"
	})]), "default" !== e && e?.length) {
		e.length;
		const r = e[0], t = "ascending" === r.order ? "asc" : "desc";
		return r.field ? {
			field: r.field,
			order: t
		} : r.valueExpression ? {
			expression: r.valueExpression,
			order: t
		} : null;
	}
	if (s) return {
		byRenderer: !0,
		order: "asc"
	};
	return null;
}
function d$3(e) {
	return 2 === e.techniqueType;
}
function m$1(e) {
	if ("simple" === e.type && e.meshes.some(d$3)) return !0;
	if ("interval" === e.type) {
		if (e.intervals.some((e) => e.meshes.some(d$3))) return !0;
		if (e.backgroundFill.some(d$3)) return !0;
	}
	if ("map" === e.type) {
		if (e.map.some((e) => e.symbol.some(d$3))) return !0;
		if (e.backgroundFill.some(d$3)) return !0;
	}
	return !1;
}
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/layerAdapters/CatalogFootprintLayerAdapter.js
var d$2 = class {
	constructor(e) {
		this.layer = e, this._cache = new o$6();
	}
	getLabelingDeconflictionInfo(e) {
		const r = this.layer, t = l$2(r);
		return [{
			vvEvaluators: { 0: o$7(r.renderer) },
			deconflictionEnabled: t
		}];
	}
	async createSourceSchema(e, r) {
		return e$3(this._createServiceInfo(e), null, {
			definitionExpression: this.layer.definitionExpression,
			queryScaleRanges: [],
			displayFilterEnabled: e.displayFilterEnabled,
			displayFilterInfo: this.layer.displayFilterInfo,
			customParameters: this.layer.customParameters,
			gdbVersion: null,
			historicMoment: null,
			postQueryFilter: null,
			timeExtent: this.layer.timeExtent,
			apiKey: this.layer.apiKey,
			sourceRefreshVersion: r,
			availableFields: e.availableFields,
			cache: this._cache
		});
	}
	createProcessorSchema(e, r, t) {
		const { fields: i, geometryType: o, orderBy: s, objectIdField: a, renderer: n, labelingInfo: d, labelsVisible: p } = this.layer;
		return o$2(e, r, {
			featureReduction: null,
			labelingInfoSource: this.layer.id,
			fields: i.map((e) => e.toJSON()),
			geometryType: o,
			labelingInfo: d,
			labelsVisible: p,
			objectIdField: a,
			orderBy: s ?? "default",
			renderer: n?.clone()
		}, t);
	}
	get hasRequiredSupport() {
		return m$9(this.layer.renderer);
	}
	get timeOptions() {
		return this.layer;
	}
	getUpdateHashProperties(e) {
		return [
			() => this.layer.apiKey,
			() => this.layer.customParameters,
			() => this.layer.definitionExpression,
			() => this.layer.outFields,
			() => this.layer.renderer,
			() => this.layer.labelsVisible ? this.layer.labelingInfo : null,
			() => this.layer.orderBy
		];
	}
	setGraphicOrigin(e) {
		e.origin = this.layer.graphicOrigin;
	}
	_createServiceInfo(i) {
		const o = this.layer, { capabilities: s, editingInfo: a, objectIdField: l, globalIdField: n, datesInUnknownTimezone: d, dateFieldsTimeZone: p, orderBy: u, parsedUrl: c } = o, y = o.fieldsIndex.toJSON(), m = e$5(o), f = o.timeInfo?.toJSON(), h = o.spatialReference.toJSON(), g = a$3(c);
		let b = l;
		if (u?.length) {
			const e = !u[0].valueExpression && u[0].field;
			e && (b = e);
		}
		const F = S(g.path), I = i.spatialReference.toJSON();
		return {
			type: "feature-service",
			source: g,
			isSourceHosted: F,
			orderByFields: b,
			outSpatialReference: I,
			metadata: {
				timeReferenceUnknownClient: d,
				dateFieldsTimeZone: p,
				globalIdField: n,
				fieldsIndex: y,
				geometryType: m,
				featureIdInfo: {
					type: "object-id",
					fieldName: l
				},
				timeInfo: f,
				spatialReference: h,
				outSpatialReference: I,
				subtypeField: null,
				subtypes: null,
				typeIdField: null,
				types: null
			},
			queryMetadata: {
				maxRecordCount: s.query.maxRecordCount,
				tileMaxRecordCount: s.query.tileMaxRecordCount,
				supportsCompactGeometry: s.query.supportsCompactGeometry,
				supportsDefaultSpatialReference: s.query.supportsDefaultSpatialReference,
				supportsFormatPBF: s.query.supportsFormatPBF,
				supportsMaxRecordCountFactor: s.query.supportsMaxRecordCountFactor,
				supportsQuantization: s.query.supportsQuantization,
				supportsCentroidOnDegeneratedQuantizedGeometry: s.query.supportsCentroidOnDegeneratedQuantizedGeometry,
				supportsDegeneratedQuantizedGeometry: s.query.supportsDegeneratedQuantizedGeometry,
				lastEditDate: a?.lastEditDate?.getTime()
			}
		};
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/layerAdapters/featureServiceUtils.js
function e$2(e) {
	const { objectIdField: i, uniqueIdFields: t } = e;
	return t?.length ? t.length >= 2 ? {
		type: "unique-id-composite",
		fieldNames: t
	} : {
		type: "unique-id-simple",
		fieldName: t[0]
	} : {
		type: "object-id",
		fieldName: i
	};
}
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/layerAdapters/floorFilterUtils.js
function o$1(e, l) {
	return null != e.floorInfo && (e.floorInfo.viewAllLevelIds.length > 0 || l.floors.length > 0);
}
function r$1(e, o, r) {
	const t = n$2(e, o?.where, r);
	return t ? (o ??= new d$9(), o.where = t, o) : o;
}
function n$2(l, o, r) {
	if (null == l.floorInfo || !r.floors?.length) return o;
	let n = r.floors;
	const { floorField: t, viewAllLevelIds: f } = l.floorInfo;
	f.length && (n = f);
	const s = n.filter((e) => "" !== e).map((e) => "'" + e + "'");
	if (s.push("''"), o?.includes(t)) {
		let e = new RegExp("AND \\(" + t + ".*NULL\\)", "g");
		o = o.replace(e, ""), e = new RegExp("\\(" + t + ".*NULL\\)", "g"), o = (o = o.replace(e, "")).replaceAll(/\s+/g, " ").trim();
	}
	let i = "(" + t + " IN ({ids}) OR " + t + " IS NULL)";
	return i = i.replace("{ids}", s.join(", ")), o$11(o, i);
}
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/layerAdapters/FeatureLayerAdapter.js
var F = class {
	constructor(e) {
		this.layer = e, this._cache = new o$6();
	}
	getLabelingDeconflictionInfo(e) {
		const r = this.layer, t = r$4(r, e) ?? l$2(r), i = e$11(r, e), s = [...r.labelingInfo || [], ...i?.labelingInfo || []];
		return [{
			vvEvaluators: { 0: o$7(r.renderer) },
			deconflictionEnabled: t,
			labelingInfo: s
		}];
	}
	async createSourceSchema(e, t) {
		const a = this._createServiceInfo(e), o = null != this.layer.editingInfo?.lastEditDate, l = this.layer.refreshInterval > 0, n = !o && l, y = r$3(a.isSourceHosted, n, this.layer.capabilities, a.metadata.geometryType, e.extent, this.layer.fullExtent), d = null != this.layer.subtypeCode ? `${this.layer.subtypeField} = ${this.layer.subtypeCode}` : null, p = o$11(this.layer.definitionExpression, d), u = u$14(p) ? await m$8(this.layer.fieldsIndex, p, this.layer.capabilities.query.relativeTimeBinWindow) : p;
		return e$3(a, y, {
			definitionExpression: u,
			postQueryFilter: p !== u ? { where: p } : null,
			queryScaleRanges: [],
			displayFilterEnabled: e.displayFilterEnabled,
			displayFilterInfo: this.layer.displayFilterInfo,
			customParameters: this.layer.customParameters,
			gdbVersion: this.layer.gdbVersion,
			historicMoment: this.layer.historicMoment,
			timeExtent: this.layer.timeExtent,
			apiKey: this.layer.apiKey,
			sourceRefreshVersion: t,
			availableFields: e.availableFields,
			cache: this._cache
		});
	}
	createProcessorSchema(e, r, t) {
		const { fields: i, renderer: s, geometryType: a, labelingInfo: l, labelsVisible: n, orderBy: y, objectIdField: d, trackInfo: p } = this.layer;
		return o$2(e, r, {
			fields: i.map((e) => e.toJSON()),
			renderer: s?.clone(),
			labelingInfoSource: this.layer.id,
			featureReduction: e$11(this.layer, r),
			geometryType: a,
			labelingInfo: l,
			labelsVisible: n,
			objectIdField: d,
			orderBy: y ?? "default",
			trackInfo: p
		}, t);
	}
	get hasRequiredSupport() {
		return m$9(this.layer.renderer);
	}
	get timeOptions() {
		return this.layer;
	}
	addFilters(e, r) {
		return r$1(this.layer, e, r);
	}
	getUpdateHashProperties(e) {
		return this.layer.destroyed ? [] : [
			() => this.layer.apiKey,
			() => this.layer.customParameters,
			() => this.layer.definitionExpression,
			() => e$11(this.layer, e),
			() => o$1(this.layer, e) ? e.floors : null,
			() => this.layer.gdbVersion,
			() => this.layer.historicMoment,
			() => this.layer.labelsVisible ? this.layer.labelingInfo : null,
			() => this.layer.orderBy,
			() => this.layer.outFields,
			() => this.layer.renderer,
			() => this.layer.subtypeCode,
			() => this.layer.trackInfo,
			() => e?.requiredFieldsOptions?.featureTitleFields && this.layer.featureTitleFields,
			() => e?.requiredFieldsOptions?.utilityNetworkFields && e$7(e, this.layer)
		];
	}
	setGraphicOrigin(e) {
		e.origin = this.layer.graphicOrigin;
	}
	setAggregateGraphicOrigin(e) {
		e.origin = this.layer.aggregateGraphicOrigin;
	}
	setTrackGraphicOrigin(e) {
		e.origin = this.layer.trackGraphicOrigin;
	}
	_createServiceInfo(r) {
		const i = this.layer, { capabilities: s, editingInfo: a, typeIdField: o, globalIdField: n, datesInUnknownTimezone: y, dateFieldsTimeZone: p, orderBy: u, subtypeField: c } = i, m = i.fieldsIndex.toJSON(), h = e$5(i), f = i.timeInfo?.toJSON(), g = i.spatialReference.toJSON(), b = i.types?.map((e) => e.toJSON()), F = a$3(this.layer.parsedUrl);
		this.layer.dynamicDataSource && (F.query = { layer: JSON.stringify({ source: this.layer.dynamicDataSource }) });
		let I = this.layer.objectIdField;
		if (u?.length) {
			const e = !u[0].valueExpression && u[0].field;
			e && (I = e);
		}
		const S$4 = S(F.path), x = r.spatialReference.toJSON();
		return {
			type: "feature-service",
			source: F,
			isSourceHosted: S$4,
			orderByFields: I,
			outSpatialReference: x,
			metadata: {
				typeIdField: o ?? void 0,
				types: b,
				timeReferenceUnknownClient: y,
				dateFieldsTimeZone: p,
				subtypeField: c,
				globalIdField: n,
				fieldsIndex: m,
				geometryType: h,
				featureIdInfo: e$2(i),
				timeInfo: f,
				spatialReference: g,
				outSpatialReference: x,
				subtypes: this.layer.subtypes?.map((e) => e.toJSON())
			},
			queryMetadata: {
				maxRecordCount: s.query.maxRecordCount,
				tileMaxRecordCount: s.query.tileMaxRecordCount,
				supportsCompactGeometry: s.query.supportsCompactGeometry,
				supportsDefaultSpatialReference: s.query.supportsDefaultSpatialReference,
				supportsFormatPBF: s.query.supportsFormatPBF,
				supportsMaxRecordCountFactor: s.query.supportsMaxRecordCountFactor,
				supportsQuantization: s.query.supportsQuantization,
				supportsCentroidOnDegeneratedQuantizedGeometry: s.query.supportsCentroidOnDegeneratedQuantizedGeometry,
				supportsDegeneratedQuantizedGeometry: s.query.supportsDegeneratedQuantizedGeometry,
				lastEditDate: a?.lastEditDate?.getTime()
			}
		};
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/layerAdapters/InMemoryLayerAdapter.js
function c$1(r) {
	if (!("openPorts" in r)) throw new r$6("featurelayer:source-not-supported", "source is not supported");
}
var d$1 = class {
	constructor(e) {
		this.layer = e, this._cache = new o$6();
	}
	openMessagePorts() {
		return c$1(this.layer.source), this.layer.source.openPorts();
	}
	getLabelingDeconflictionInfo(e) {
		const r = this.layer, i = r$4(r, e) ?? l$2(r), s = e$11(r, e), l = [...r.labelingInfo || [], ...s?.labelingInfo || []];
		return [{
			vvEvaluators: { 0: o$7(r.renderer) },
			deconflictionEnabled: i,
			labelingInfo: l
		}];
	}
	async createSourceSchema(e, r) {
		return e$3(this._createServiceInfo(e), null, {
			definitionExpression: this.layer.definitionExpression,
			queryScaleRanges: [],
			displayFilterEnabled: e.displayFilterEnabled,
			displayFilterInfo: this.layer.displayFilterInfo,
			customParameters: this.layer.customParameters,
			gdbVersion: null,
			historicMoment: null,
			timeExtent: "timeExtent" in this.layer ? this.layer.timeExtent : null,
			apiKey: null,
			sourceRefreshVersion: r,
			availableFields: e.availableFields,
			cache: this._cache,
			postQueryFilter: null
		});
	}
	createProcessorSchema(e, r, i) {
		const { fields: o, renderer: n, geometryType: a, labelingInfo: s, labelsVisible: l, orderBy: p, objectIdField: c } = this.layer, d = "trackInfo" in this.layer ? this.layer.trackInfo : null;
		return o$2(e, r, {
			fields: o.map((e) => e.toJSON()),
			renderer: n?.clone(),
			labelingInfoSource: this.layer.id,
			featureReduction: e$11(this.layer, r),
			geometryType: a,
			labelingInfo: s,
			labelsVisible: l,
			objectIdField: c,
			orderBy: p ?? "default",
			trackInfo: d
		}, i);
	}
	get hasRequiredSupport() {
		return m$9(this.layer.renderer);
	}
	get timeOptions() {
		return n$11(this.layer) ? this.layer : null;
	}
	getUpdateHashProperties(e) {
		return [
			() => this.layer.definitionExpression,
			() => this.layer.displayFilterInfo,
			() => this.layer.orderBy,
			() => "outFields" in this.layer ? this.layer.outFields : null,
			() => this.layer.renderer,
			() => this.layer.labelsVisible ? this.layer.labelingInfo : null,
			() => e$11(this.layer, e),
			() => "trackInfo" in this.layer ? this.layer.trackInfo : null
		];
	}
	setGraphicOrigin(e) {
		e.origin = this.layer.graphicOrigin;
	}
	setAggregateGraphicOrigin(e) {
		e.origin = this.layer.aggregateGraphicOrigin;
	}
	setTrackGraphicOrigin(e) {
		"trackGraphicOrigin" in this.layer && (e.origin = this.layer.trackGraphicOrigin);
	}
	_createServiceInfo(e) {
		const r = this.layer, { capabilities: t, objectIdField: o } = r, n = r.fieldsIndex.toJSON(), a = e$5(r), s = "timeInfo" in r ? r.timeInfo?.toJSON() : null, l = r.spatialReference.toJSON();
		c$1(r.source);
		const u = o, p = e.spatialReference.toJSON();
		return {
			type: "memory",
			orderByFields: u,
			outSpatialReference: p,
			metadata: {
				fieldsIndex: n,
				geometryType: a,
				featureIdInfo: {
					type: "object-id",
					fieldName: r.objectIdField
				},
				timeInfo: s,
				spatialReference: l,
				outSpatialReference: p,
				subtypes: null,
				subtypeField: null,
				globalIdField: null,
				typeIdField: null,
				types: null,
				timeReferenceUnknownClient: "datesInUnknownTimezone" in r ? r.datesInUnknownTimezone : null,
				dateFieldsTimeZone: "dateFieldsTimeZone" in r ? r.dateFieldsTimeZone : null
			},
			queryMetadata: {
				maxRecordCount: t.query.maxRecordCount,
				tileMaxRecordCount: t.query.tileMaxRecordCount,
				supportsCompactGeometry: t.query.supportsCompactGeometry,
				supportsDefaultSpatialReference: t.query.supportsDefaultSpatialReference,
				supportsFormatPBF: t.query.supportsFormatPBF,
				supportsMaxRecordCountFactor: t.query.supportsMaxRecordCountFactor,
				supportsQuantization: t.query.supportsQuantization,
				supportsCentroidOnDegeneratedQuantizedGeometry: t.query.supportsCentroidOnDegeneratedQuantizedGeometry,
				supportsDegeneratedQuantizedGeometry: t.query.supportsDegeneratedQuantizedGeometry,
				lastEditDate: null
			}
		};
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/layerAdapters/KnowledgeGraphSublayerAdapter.js
var d = class {
	constructor(e) {
		this.layer = e, this._cache = new o$6();
	}
	getLabelingDeconflictionInfo(e) {
		const r = this.layer, t = r$4(r, e) ?? l$2(r);
		return [{
			vvEvaluators: { 0: o$7(r.renderer) },
			deconflictionEnabled: t
		}];
	}
	async createSourceSchema(e, r) {
		return e$3(this._createServiceInfo(e), null, {
			definitionExpression: this.layer.definitionExpression,
			queryScaleRanges: [],
			displayFilterEnabled: e.displayFilterEnabled,
			displayFilterInfo: this.layer.displayFilterInfo,
			customParameters: null,
			gdbVersion: null,
			historicMoment: null,
			timeExtent: this.layer.timeExtent,
			apiKey: null,
			sourceRefreshVersion: r,
			availableFields: e.availableFields,
			cache: this._cache,
			postQueryFilter: null
		});
	}
	createProcessorSchema(e, r, t) {
		const { fields: o, renderer: a, geometryType: s, labelingInfo: l, labelsVisible: n, objectIdField: p } = this.layer;
		return o$2(e, r, {
			fields: o.map((e) => e.toJSON()),
			renderer: a?.clone(),
			labelingInfoSource: this.layer.id,
			featureReduction: e$11(this.layer, r),
			geometryType: s,
			labelingInfo: l,
			labelsVisible: n,
			objectIdField: p,
			orderBy: "default"
		}, t);
	}
	get hasRequiredSupport() {
		return m$9(this.layer.renderer);
	}
	get timeOptions() {
		return this.layer;
	}
	getUpdateHashProperties(e) {
		return [
			() => this.layer.definitionExpression,
			() => this.layer.renderer,
			() => this.layer.labelsVisible ? this.layer.labelingInfo : null,
			() => e$11(this.layer, e),
			() => y$1(this.layer)
		];
	}
	hasFilters(e) {
		return y$1(this.layer);
	}
	addFilters(i, o) {
		if (y$1(this.layer)) {
			const o = o$11(i?.where, `${E}=1`);
			if (!o) return i;
			i ??= new d$9(), i.where = o;
		}
		return i;
	}
	setGraphicOrigin(e) {
		e.origin = this.layer.graphicOrigin;
	}
	setAggregateGraphicOrigin(e) {
		e.origin = this.layer.aggregateGraphicOrigin;
	}
	openMessagePorts() {
		return this.layer.source.openPorts();
	}
	_createServiceInfo(e) {
		const r = this.layer, { capabilities: t, objectIdField: i } = r, a = r.fieldsIndex.toJSON(), s = e$5(r), l = r.spatialReference.toJSON(), n = i, p = e.spatialReference.toJSON();
		return {
			type: "memory",
			orderByFields: n,
			outSpatialReference: p,
			metadata: {
				fieldsIndex: a,
				geometryType: s,
				featureIdInfo: {
					type: "object-id",
					fieldName: r.objectIdField
				},
				spatialReference: l,
				outSpatialReference: p,
				globalIdField: null,
				subtypeField: null,
				subtypes: null,
				timeInfo: r.timeInfo?.toJSON(),
				timeReferenceUnknownClient: null,
				dateFieldsTimeZone: null,
				typeIdField: null,
				types: null
			},
			queryMetadata: {
				maxRecordCount: t.query.maxRecordCount,
				tileMaxRecordCount: t.query.tileMaxRecordCount,
				supportsCompactGeometry: t.query.supportsCompactGeometry,
				supportsDefaultSpatialReference: t.query.supportsDefaultSpatialReference,
				supportsFormatPBF: t.query.supportsFormatPBF,
				supportsMaxRecordCountFactor: t.query.supportsMaxRecordCountFactor,
				supportsQuantization: t.query.supportsQuantization,
				supportsCentroidOnDegeneratedQuantizedGeometry: t.query.supportsCentroidOnDegeneratedQuantizedGeometry,
				supportsDegeneratedQuantizedGeometry: t.query.supportsDegeneratedQuantizedGeometry,
				lastEditDate: null
			}
		};
	}
};
function y$1(e) {
	return "link-chart" === e.parentCompositeLayer.type && "hidden" === e.parentCompositeLayer.linkChart?.linkChartProperties.nonspatialDataDisplay?.mode;
}
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/layerAdapters/OGCFeatureLayerAdapter.js
var c = class {
	constructor(e) {
		this.layer = e, this._cache = new o$6();
	}
	getLabelingDeconflictionInfo(e) {
		const r = this.layer, t = r$4(r, e) ?? l$2(r);
		return [{
			vvEvaluators: { 0: o$7(r.renderer) },
			deconflictionEnabled: t,
			labelingInfo: r.labelingInfo
		}];
	}
	async createSourceSchema(e, r) {
		return e$3(this._createServiceInfo(e), null, {
			definitionExpression: null,
			queryScaleRanges: [],
			displayFilterEnabled: e.displayFilterEnabled,
			displayFilterInfo: null,
			customParameters: this.layer.customParameters,
			gdbVersion: null,
			historicMoment: null,
			timeExtent: this.layer.timeExtent,
			apiKey: this.layer.apiKey,
			sourceRefreshVersion: r,
			availableFields: e.availableFields,
			cache: this._cache,
			postQueryFilter: null
		});
	}
	createProcessorSchema(e, t, i) {
		const { fields: a, renderer: o, geometryType: s, labelingInfo: l, labelsVisible: u, orderBy: c, objectIdField: p } = this.layer;
		return o$2(e, t, {
			fields: a.map((e) => e.toJSON()),
			renderer: o?.clone(),
			labelingInfoSource: this.layer.id,
			featureReduction: e$11(this.layer, t),
			geometryType: s,
			labelingInfo: l,
			labelsVisible: u,
			objectIdField: p,
			orderBy: c ?? "default"
		}, i);
	}
	get hasRequiredSupport() {
		return m$9(this.layer.renderer);
	}
	get timeOptions() {
		return this.layer;
	}
	getUpdateHashProperties(e) {
		return [
			() => this.layer.apiKey,
			() => this.layer.customParameters,
			() => e$11(this.layer, e),
			() => this.layer.labelsVisible ? this.layer.labelingInfo : null,
			() => this.layer.orderBy,
			() => this.layer.renderer
		];
	}
	setGraphicOrigin(e) {
		e.origin = this.layer.graphicOrigin;
	}
	setAggregateGraphicOrigin(e) {
		e.origin = this.layer.aggregateGraphicOrigin;
	}
	setTrackGraphicOrigin(e) {
		e.origin = this.layer.trackGraphicOrigin;
	}
	_createServiceInfo(r) {
		const i = this.layer, { capabilities: a } = i, o = i.fieldsIndex.toJSON(), s = e$5(i), l = i.timeInfo?.toJSON(), n = i.spatialReference.toJSON(), u = i.source.getSource(), c = this.layer.objectIdField, p = a$3(a);
		p.query.maxRecordCount = u.maxRecordCount;
		const d = r.spatialReference.toJSON();
		return {
			type: "ogc",
			source: u,
			orderByFields: c,
			outSpatialReference: d,
			metadata: {
				fieldsIndex: o,
				geometryType: s,
				featureIdInfo: {
					type: "object-id",
					fieldName: i.objectIdField
				},
				timeInfo: l,
				spatialReference: n,
				outSpatialReference: d,
				globalIdField: null,
				subtypeField: null,
				subtypes: null,
				timeReferenceUnknownClient: null,
				dateFieldsTimeZone: null,
				typeIdField: null,
				types: null
			},
			queryMetadata: {
				maxRecordCount: p.query.maxRecordCount,
				tileMaxRecordCount: p.query.tileMaxRecordCount,
				supportsCompactGeometry: p.query.supportsCompactGeometry,
				supportsDefaultSpatialReference: p.query.supportsDefaultSpatialReference,
				supportsFormatPBF: p.query.supportsFormatPBF,
				supportsMaxRecordCountFactor: p.query.supportsMaxRecordCountFactor,
				supportsQuantization: p.query.supportsQuantization,
				supportsCentroidOnDegeneratedQuantizedGeometry: p.query.supportsCentroidOnDegeneratedQuantizedGeometry,
				supportsDegeneratedQuantizedGeometry: p.query.supportsDegeneratedQuantizedGeometry,
				lastEditDate: null
			}
		};
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/layerAdapters/OrientedImageryLayerAdapter.js
var m = class {
	constructor(e) {
		this.layer = e, this._cache = new o$6();
	}
	getLabelingDeconflictionInfo(e) {
		const r = this.layer, t = r$4(r, e) ?? l$2(r);
		return [{
			vvEvaluators: { 0: o$7(r.renderer) },
			deconflictionEnabled: t
		}];
	}
	async createSourceSchema(e, r) {
		const t = this._createServiceInfo(e), i = null != this.layer.editingInfo?.lastEditDate, a = this.layer.refreshInterval > 0, s = !i && a;
		return e$3(t, r$3(t.isSourceHosted, s, this.layer.capabilities, t.metadata.geometryType, e.extent, this.layer.fullExtent), {
			definitionExpression: this.layer.definitionExpression,
			queryScaleRanges: [],
			displayFilterEnabled: e.displayFilterEnabled,
			displayFilterInfo: this.layer.displayFilterInfo,
			customParameters: this.layer.customParameters,
			gdbVersion: this.layer.gdbVersion,
			historicMoment: this.layer.historicMoment,
			timeExtent: this.layer.timeExtent,
			apiKey: this.layer.apiKey,
			sourceRefreshVersion: r,
			availableFields: e.availableFields,
			cache: this._cache,
			postQueryFilter: null
		});
	}
	createProcessorSchema(e, r, i) {
		const { fields: a, renderer: s, geometryType: o, labelingInfo: l, labelsVisible: n, orderBy: d, objectIdField: p } = this.layer;
		return o$2(e, r, {
			fields: a.map((e) => e.toJSON()),
			renderer: s?.clone(),
			labelingInfoSource: this.layer.id,
			featureReduction: e$11(this.layer, r),
			geometryType: o,
			labelingInfo: l,
			labelsVisible: n,
			objectIdField: p,
			orderBy: d ?? "default"
		}, i);
	}
	get hasRequiredSupport() {
		return m$9(this.layer.renderer);
	}
	get timeOptions() {
		return this.layer;
	}
	addFilters(e, r) {
		return r$1(this.layer, e, r);
	}
	getUpdateHashProperties(e) {
		return [
			() => this.layer.outFields,
			() => this.layer.orderBy,
			() => this.layer.definitionExpression,
			() => this.layer.renderer,
			() => this.layer.labelsVisible ? this.layer.labelingInfo : null,
			() => e$11(this.layer, e),
			() => this.layer.customParameters,
			() => o$1(this.layer, e) ? e.floors : null
		];
	}
	setGraphicOrigin(e) {
		e.origin = this.layer.graphicOrigin;
	}
	setAggregateGraphicOrigin(e) {
		e.origin = this.layer.aggregateGraphicOrigin;
	}
	setTrackGraphicOrigin(e) {
		e.origin = this.layer.trackGraphicOrigin;
	}
	_createServiceInfo(t) {
		const i = this.layer, { capabilities: a, globalIdField: o, orderBy: l } = i, n = i.fieldsIndex.toJSON(), d = e$5(i), p = i.timeInfo?.toJSON(), c = i.spatialReference.toJSON(), u = a$3(this.layer.parsedUrl);
		let y = this.layer.objectIdField;
		if (l?.length) {
			const e = !l[0].valueExpression && l[0].field;
			e && (y = e);
		}
		const m = S(u.path), h = t.spatialReference.toJSON();
		return {
			type: "feature-service",
			source: u,
			isSourceHosted: m,
			orderByFields: y,
			outSpatialReference: h,
			metadata: {
				globalIdField: o,
				fieldsIndex: n,
				geometryType: d,
				featureIdInfo: {
					type: "object-id",
					fieldName: i.objectIdField
				},
				timeInfo: p,
				spatialReference: c,
				outSpatialReference: h,
				timeReferenceUnknownClient: i.datesInUnknownTimezone,
				dateFieldsTimeZone: i.dateFieldsTimeZone,
				subtypeField: null,
				subtypes: null,
				typeIdField: null,
				types: null
			},
			queryMetadata: {
				maxRecordCount: a.query.maxRecordCount,
				tileMaxRecordCount: a.query.tileMaxRecordCount,
				supportsCompactGeometry: a.query.supportsCompactGeometry,
				supportsDefaultSpatialReference: a.query.supportsDefaultSpatialReference,
				supportsFormatPBF: a.query.supportsFormatPBF,
				supportsMaxRecordCountFactor: a.query.supportsMaxRecordCountFactor,
				supportsQuantization: a.query.supportsQuantization,
				supportsCentroidOnDegeneratedQuantizedGeometry: a.query.supportsCentroidOnDegeneratedQuantizedGeometry,
				supportsDegeneratedQuantizedGeometry: a.query.supportsDegeneratedQuantizedGeometry,
				lastEditDate: null
			}
		};
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/layerAdapters/ParquetLayerAdapter.js
var y = class {
	constructor(e) {
		this.layer = e;
	}
	get hasRequiredSupport() {
		return m$9(this.layer.renderer);
	}
	get timeOptions() {
		return null;
	}
	getLabelingDeconflictionInfo(e) {
		const r = this.layer, i = r$4(r, e) ?? l$2(r), n = e$11(r, e), o = [...r.labelingInfo || [], ...n?.labelingInfo || []];
		return [{
			vvEvaluators: { 0: o$7(r.renderer) },
			deconflictionEnabled: i,
			labelingInfo: o
		}];
	}
	getUpdateHashProperties(e) {
		return [
			() => this.layer.urls.join(","),
			() => this.layer.outFields,
			() => this.layer.definitionExpression,
			() => this.layer.labelsVisible ? this.layer.labelingInfo : null,
			() => e$11(this.layer, e),
			() => this.layer.customParameters,
			() => this.layer.orderBy,
			() => this.layer.renderer,
			() => this.layer.popupTemplate
		];
	}
	openMessagePorts() {
		return this.layer.source.openPorts();
	}
	async createSourceSchema(e, r) {
		const t = this._createServiceInfo(e), i = e.availableFields.includes("*") ? this.layer.fields.map((e) => e.name) : e.availableFields, l = {
			partial: {
				urls: [...this.layer.urls.items],
				availableFields: i
			},
			full: {
				definitionExpression: this.layer.definitionExpression ?? null,
				sourceRefreshVersion: r,
				customParameters: this.layer.customParameters ?? null
			}
		};
		return {
			type: "parquet",
			service: t,
			strategy: this._createStrategy(t, l)
		};
	}
	_createStrategy(e, r) {
		return e.geometryInfo.displayOptimization ? {
			type: "xz",
			...r
		} : {
			type: "snapshot",
			...r
		};
	}
	createProcessorSchema(e, r, i) {
		return o$2(e, r, {
			fields: this.layer.fields.map((e) => e.toJSON()),
			renderer: this.layer.renderer?.clone(),
			labelingInfoSource: this.layer.id,
			featureReduction: e$11(this.layer, r),
			geometryType: this.layer.geometryType,
			labelingInfo: this.layer.labelingInfo,
			labelsVisible: this.layer.labelsVisible,
			objectIdField: this.layer.objectIdField,
			orderBy: this.layer.orderBy
		}, i);
	}
	setGraphicOrigin(e) {
		e.origin = this.layer.graphicOrigin;
	}
	setAggregateGraphicOrigin(e) {
		e.origin = this.layer.aggregateGraphicOrigin;
	}
	_createServiceInfo(t) {
		const l = e$5(this.layer), a = t.spatialReference.toJSON(), s = this.layer.geometryEncoding;
		if (null == s && null == this.layer.displayOptimization) throw new r$6("parquet-layer:unsupported", "creating a parquet layer view requires an encoding", { layer: this.layer });
		return {
			type: "parquet",
			outSpatialReference: a,
			geometryInfo: {
				geometry: s ? {
					geometryType: d$10(this.layer.geometryType),
					spatialReference: this.layer.spatialReference.toJSON(),
					encoding: s.toJSON()
				} : null,
				displayOptimization: this.layer.displayOptimization
			},
			metadata: {
				spatialReference: this.layer.spatialReference.toJSON(),
				outSpatialReference: a,
				fieldsIndex: this.layer.fieldsIndex.toJSON(),
				featureIdInfo: {
					type: "object-id",
					fieldName: this.layer.objectIdField
				},
				geometryType: l,
				types: null,
				subtypes: null,
				timeInfo: null,
				typeIdField: null,
				subtypeField: null,
				globalIdField: null,
				timeReferenceUnknownClient: null,
				dateFieldsTimeZone: null
			}
		};
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/layerAdapters/StreamLayerAdapter.js
var s$1 = class {
	constructor(e) {
		this.layer = e;
	}
	getLabelingDeconflictionInfo(r) {
		const a = this.layer, n = r$4(a, r) ?? l$2(a), s = e$11(a, r), o = [...a.labelingInfo || [], ...s?.labelingInfo || []];
		return [{
			vvEvaluators: { 0: o$7(a.renderer) },
			deconflictionEnabled: n,
			labelingInfo: o
		}];
	}
	async createSourceSchema(e, r) {
		return {
			type: "stream",
			service: this._createServiceInfo(e),
			strategy: {
				type: "stream",
				partial: { sourceRefreshVersion: r },
				full: {
					availableFields: e.availableFields,
					geometryDefinition: this.layer.geometryDefinition?.toJSON(),
					definitionExpression: this.layer.definitionExpression,
					customParameters: this.layer.customParameters,
					maxReconnectionAttempts: this.layer.maxReconnectionAttempts,
					maxReconnectionInterval: this.layer.maxReconnectionInterval,
					purgeOptions: this.layer.purgeOptions.toJSON()
				}
			}
		};
	}
	createProcessorSchema(r, t, i) {
		const { fields: l, renderer: n, geometryType: s, labelingInfo: o, labelsVisible: c, objectIdField: y, trackInfo: p } = this.layer;
		return o$2(r, t, {
			fields: l.map((e) => e.toJSON()),
			renderer: n?.clone(),
			labelingInfoSource: this.layer.id,
			featureReduction: e$11(this.layer, t),
			geometryType: s,
			labelingInfo: o,
			labelsVisible: c,
			objectIdField: y,
			orderBy: "default",
			trackInfo: p
		}, i);
	}
	get hasRequiredSupport() {
		return m$9(this.layer.renderer);
	}
	get timeOptions() {
		return this.layer;
	}
	getUpdateHashProperties(r) {
		return [
			() => this.layer.definitionExpression,
			() => this.layer.renderer,
			() => this.layer.labelsVisible ? this.layer.labelingInfo : null,
			() => e$11(this.layer, r),
			() => this.layer.customParameters,
			() => this.layer.geometryDefinition,
			() => this.layer.definitionExpression,
			() => this.layer.trackInfo
		];
	}
	setGraphicOrigin(e) {
		e.origin = this.layer.graphicOrigin;
	}
	setAggregateGraphicOrigin(e) {
		e.origin = this.layer.aggregateGraphicOrigin;
	}
	setTrackGraphicOrigin(e) {
		e.origin = this.layer.trackGraphicOrigin;
	}
	_createServiceInfo(e) {
		const t = e$5(this.layer), i = this.layer.timeInfo?.toJSON() || null, l = this.layer.spatialReference ? this.layer.spatialReference.toJSON() : null, a = e.spatialReference.toJSON();
		return {
			type: "stream",
			source: this.layer.parsedUrl,
			outSpatialReference: a,
			metadata: {
				fieldsIndex: this.layer.fieldsIndex.toJSON(),
				geometryType: t,
				featureIdInfo: {
					type: "object-id",
					fieldName: this.layer.objectIdField
				},
				timeInfo: i,
				timeReferenceUnknownClient: null,
				dateFieldsTimeZone: null,
				spatialReference: l,
				outSpatialReference: a,
				subtypeField: null,
				subtypes: null,
				globalIdField: null,
				typeIdField: null,
				types: null
			}
		};
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/schema/processor/SubtypeProcessorSchema.js
async function a(e, { subtypeField: t, sublayers: s }) {
	const i = await Promise.all(s.map(({ renderer: t }) => l$1(e, t)));
	return {
		type: "subtype",
		subtypeField: t,
		renderers: s.reduce((e, { subtypeCode: r }, t) => ({
			...e,
			[r]: i[t]
		}), {})
	};
}
function n$1(e, r) {
	const s = t$7();
	return {
		type: "multi",
		filters: e.filters,
		capabilities: { maxTextureSize: s.maxTextureSize },
		keyField: r.subtypeField,
		target: "feature",
		bindings: r.sublayers.reduce((e, { renderer: r, subtypeCode: s }) => {
			const i = p$9(r);
			return {
				...e,
				[s]: i
			};
		}, {})
	};
}
async function o(r, { subtypeField: t, sublayers: i, labelingInfoSource: a }) {
	const n = await Promise.all(i.map((t) => {
		const i = x$2(t.renderer);
		return o$4(r, {
			...t,
			geometryType: t.geometryType ?? null,
			labelingInfoSource: a
		}, i);
	}));
	return {
		type: "subtype",
		subtypeField: t,
		renderers: i.reduce((e, { subtypeCode: r }, t) => ({
			...e,
			[r]: n[t]
		}), {})
	};
}
async function u(e, r, t, s) {
	return {
		storage: n$1(r, t),
		mesh: {
			properties: {
				timeZone: r.timeZone,
				displayRefreshVersion: s,
				returnMeshObjectId: !1,
				sortKey: null,
				currentUser: r.currentUser
			},
			strategy: { type: "feature" },
			factory: {
				symbology: await a(e, t),
				labels: await o(e, t)
			}
		},
		expressionProperties: { timeExtent: r.timeExtent?.toJSON() }
	};
}
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/features/layerAdapters/SubtypeGroupLayerAdapter.js
var b = class {
	constructor(e) {
		this.layer = e, this._cache = new o$6();
	}
	getLabelingDeconflictionInfo(e) {
		const t = this.layer;
		return [{
			vvEvaluators: {},
			deconflictionEnabled: t.sublayers.some((e) => l$2(e)),
			labelingInfo: t.sublayers.toArray().filter((e) => !!e.labelingInfo).flatMap((e) => e.labelingInfo)
		}];
	}
	async createSourceSchema(e, t) {
		const { definitionExpression: r, customParameters: i, gdbVersion: s, historicMoment: o, timeExtent: n, apiKey: u, displayFilterInfo: p } = this.layer, y = this._createServiceInfo(e), c = null != this.layer.editingInfo?.lastEditDate, d = this.layer.refreshInterval > 0, h = !c && d, b = r$3(y.isSourceHosted, h, this.layer.capabilities, y.metadata.geometryType, e.extent, this.layer.fullExtent), g = u$14(r) ? await m$8(this.layer.fieldsIndex, r, this.layer.capabilities.query.relativeTimeBinWindow) : r;
		return e$3(y, b, {
			definitionExpression: g,
			postQueryFilter: r !== g ? { where: r } : null,
			queryScaleRanges: this.layer.sublayers.items.map((e) => ({
				subtypeCode: e.subtypeCode,
				minScale: e.minScale,
				maxScale: e.maxScale
			})),
			displayFilterEnabled: e.displayFilterEnabled,
			displayFilterInfo: p,
			customParameters: i,
			gdbVersion: s,
			historicMoment: o,
			timeExtent: n,
			apiKey: u,
			sourceRefreshVersion: t,
			availableFields: e.availableFields,
			cache: this._cache
		});
	}
	createProcessorSchema(e, t, r) {
		return u(e, t, {
			labelingInfoSource: this.layer.id,
			subtypeField: this.layer.subtypeField,
			sublayers: Array.from(this.layer.sublayers, (e, t) => ({
				featureReduction: null,
				geometryType: this.layer.geometryType,
				labelingInfoSource: this.layer.id + `-${t}`,
				labelingInfo: e.labelingInfo,
				labelsVisible: e.labelsVisible,
				renderer: e.renderer,
				subtypeCode: e.subtypeCode,
				orderBy: null
			}))
		}, r);
	}
	addFilters(e, t) {
		e = r$1(this.layer, e, t);
		const i = this.layer.sublayers.filter((e) => !g(e, t)).map((e) => e.subtypeCode);
		if (!i.length) return e;
		e ??= new d$9();
		const a = `NOT ${this.layer.subtypeField} IN (${i.join(",")})`;
		return e.where = o$11(e.where, a), e;
	}
	get hasRequiredSupport() {
		return !0;
	}
	get timeOptions() {
		return this.layer;
	}
	getUpdateHashProperties(e) {
		return [
			() => this.layer.apiKey,
			() => this.layer.customParameters,
			() => this.layer.definitionExpression,
			() => o$1(this.layer, e) ? e.floors : null,
			() => this.layer.outFields,
			() => this.layer.gdbVersion,
			() => this.layer.historicMoment,
			() => e?.requiredFieldsOptions?.featureTitleFields && this.layer.featureTitleFields,
			() => e?.requiredFieldsOptions?.utilityNetworkFields && e$7(e, this.layer),
			() => this.layer.sublayers.map(({ renderer: e, labelsVisible: t, labelingInfo: r, visible: i, minScale: s, maxScale: a }) => ({
				renderer: e,
				labelsVisible: t,
				labelingInfo: r,
				visible: i,
				minScale: s,
				maxScale: a
			}))
		];
	}
	setGraphicOrigin(e) {
		const t = this.layer.findSublayerForFeature(e);
		e.layer = e.sourceLayer = t, e.origin = t?.graphicOrigin;
	}
	_createServiceInfo(t) {
		const { capabilities: r, datesInUnknownTimezone: s, dateFieldsTimeZone: a, editingInfo: l, globalIdField: o, objectIdField: u, subtypeField: p } = this.layer, c = this.layer.fieldsIndex.toJSON(), d = e$5(this.layer), m = this.layer.timeInfo?.toJSON(), f = this.layer.spatialReference.toJSON(), h = a$3(this.layer.parsedUrl), b = u, g = S(h.path), S$3 = t.spatialReference.toJSON();
		return {
			type: "feature-service",
			source: h,
			isSourceHosted: g,
			orderByFields: b,
			outSpatialReference: S$3,
			metadata: {
				timeReferenceUnknownClient: s,
				dateFieldsTimeZone: a,
				subtypeField: p,
				globalIdField: o,
				fieldsIndex: c,
				geometryType: d,
				featureIdInfo: e$2(this.layer),
				timeInfo: m,
				spatialReference: f,
				outSpatialReference: S$3,
				subtypes: this.layer.subtypes?.map((e) => e.toJSON()),
				typeIdField: null,
				types: null
			},
			queryMetadata: {
				maxRecordCount: r.query.maxRecordCount,
				tileMaxRecordCount: r.query.tileMaxRecordCount,
				supportsCompactGeometry: r.query.supportsCompactGeometry,
				supportsDefaultSpatialReference: r.query.supportsDefaultSpatialReference,
				supportsFormatPBF: r.query.supportsFormatPBF,
				supportsMaxRecordCountFactor: r.query.supportsMaxRecordCountFactor,
				supportsQuantization: r.query.supportsQuantization,
				supportsCentroidOnDegeneratedQuantizedGeometry: r.query.supportsCentroidOnDegeneratedQuantizedGeometry,
				supportsDegeneratedQuantizedGeometry: r.query.supportsDegeneratedQuantizedGeometry,
				lastEditDate: l?.lastEditDate?.getTime()
			}
		};
	}
};
function g(e, r) {
	return e.visible && (0 === e.minScale || m$5(r.scale, e.minScale) || r.scale < e.minScale) && (0 === e.maxScale || m$5(r.scale, e.maxScale) || r.scale > e.maxScale);
}
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/support/DebouncedFeatureOverride.js
var e$1 = class {
	constructor() {
		this._commands = /* @__PURE__ */ new Map(), this._historicMoment = null;
	}
	tryAdd(e) {
		if (!this._canDebounce(e)) return !1;
		switch (e.type) {
			case "override": return this._addOverride(e), !0;
			case "override-by-id": return this._addOverrideById(e), !0;
		}
	}
	toMessage() {
		const e = {
			historicMoment: this._historicMoment,
			commands: {
				updateByIdWeak: [],
				updateWeak: [],
				removeWeak: [],
				update: [],
				remove: [],
				release: []
			}
		};
		for (const [s, o] of this._commands.entries()) switch (o.type) {
			case "override-update-by-id":
				e.commands.updateByIdWeak.push(s);
				break;
			case "override-update":
				o.isWeak ? e.commands.updateWeak.push(o.feature) : e.commands.update.push(o.feature);
				break;
			case "override-remove":
				o.isWeak ? e.commands.removeWeak.push(s) : e.commands.remove.push(s);
				break;
			case "override-release": e.commands.release.push(s);
		}
		return e;
	}
	_canDebounce(e) {
		switch (e.type) {
			case "override": {
				const { updates: s, removed: o, release: t } = e;
				return !s.some((e) => this._commands.has(e.objectId)) && !o.some((e) => this._commands.has(e)) && !t.some((e) => this._commands.has(e));
			}
			case "override-by-id": {
				const { updates: s, removed: o } = e;
				return !s.some((e) => this._commands.has(e)) && !o.some((e) => this._commands.has(e));
			}
		}
	}
	_addOverrideById(e) {
		this._historicMoment = e.historicMoment;
		for (const s of e.updates) this._commands.set(s, {
			type: "override-update-by-id",
			isWeak: e.isWeak
		});
		for (const s of e.removed) this._commands.set(s, {
			type: "override-remove",
			isWeak: e.isWeak
		});
	}
	_addOverride(e) {
		this._historicMoment = e.historicMoment;
		for (const s of e.updates) this._commands.set(s.objectId, {
			type: "override-update",
			feature: s,
			isWeak: e.isWeak
		});
		for (const s of e.removed) this._commands.set(s, {
			type: "override-remove",
			isWeak: e.isWeak
		});
		for (const s of e.release) this._commands.set(s, { type: "override-release" });
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/support/handleNoEngineError.js
async function n(n, r) {
	try {
		return await n;
	} catch (t) {
		if ("no-queryEngine" !== t.name) throw t;
		return r;
	}
}
//#endregion
//#region node_modules/@arcgis/core/views/2d/tiling/FeatureTileSubscriptionManager.js
function e(s, e) {
	const i = /* @__PURE__ */ new Set();
	for (const t of s instanceof Set ? s.values() : s.keys()) e.has(t) || i.add(t);
	return i;
}
var i = class {
	constructor(s, e, i) {
		this.target = /* @__PURE__ */ new Map(), this.previous = /* @__PURE__ */ new Map();
		const t = i ? s.getTileCoverage(i, 0, !0, "closest") : null, r = s.getTileCoverage(e, 0, !0, "closest");
		if (this.target.clear(), this.previous.clear(), t) for (const o of t.keys()) this.previous.set(o.id, o);
		if (r) for (const o of r.keys()) this.target.set(o.id, o);
	}
	*values() {
		yield* this.target.values(), yield* this.previous.values();
	}
	*keys() {
		yield* this.target.keys(), yield* this.previous.keys();
	}
};
var t = class {
	constructor(s) {
		this.version = s;
	}
};
var r = class {
	constructor(s) {
		this._subscriptions = /* @__PURE__ */ new Map(), this._visible = /* @__PURE__ */ new Set(), this._version = 0, this._strategy = "eager", this._config = s;
	}
	destroy() {}
	get coverage() {
		return this._coverage;
	}
	get subscriptions() {
		return new Set(this._subscriptions.keys());
	}
	setVisibilityStrategy(s) {
		this._strategy = s;
	}
	suspend() {
		this._suspendedCoverage = this._coverage, this._coverage = null, this._updateSubscriptions();
	}
	resume() {
		this._coverage ?? (this._coverage = this._suspendedCoverage, this._suspendedCoverage = null, this._updateSubscriptions());
	}
	update(s, e) {
		return this._version = (this._version + 1) % Number.MAX_SAFE_INTEGER, this._coverage = new i(this._config.tileInfoView, s, e), this._updateSubscriptions(), new Set(this._visible);
	}
	updateVisibility() {
		return this._visible = this._updateVisibility(), this._visible;
	}
	_updateSubscriptions() {
		if (!this._coverage) return;
		const s = this._updateVisibility(), i = new Set(this._coverage.keys()), r = e(this._subscriptions, s), o = e(i, this._subscriptions), n = e(r, i);
		this._visible = s;
		for (const e of o.values()) this._subscriptions.set(e, new t(this._version));
		for (const e of n.values()) this._subscriptions.delete(e);
		(o.size || n.size) && this._sendUpdateSubscriptions(o, n);
	}
	_sendUpdateSubscriptions(s, e) {
		const i = Array.from(s.values()).map((s) => ({
			tileId: s,
			version: this._subscriptions.get(s).version
		}));
		this._config.updateSubscriptions({
			subscribe: i,
			unsubscribe: Array.from(e.values())
		});
	}
	_updateVisibility() {
		if (!this._coverage) return /* @__PURE__ */ new Set();
		switch (this._strategy) {
			case "eager": return this._updateVisibilityEager(this._coverage);
			case "target-defer": return this._updateVisibilityTargetDefer(this._coverage);
		}
	}
	_updateVisibilityEager(s) {
		const e = /* @__PURE__ */ new Set();
		for (const i of s.values()) {
			if (this._config.isDone(i)) {
				e.add(i.id);
				continue;
			}
			if (this._addVisibleParent(e, i)) continue;
			this._addVisibleChildren(e, i) || e.add(i.id);
		}
		return e;
	}
	_updateVisibilityTargetDefer(s) {
		const e = /* @__PURE__ */ new Set(), i = Array.from(s.target.values());
		if (!i.some((s) => !this._config.isDone(s))) {
			for (const s of i) e.add(s.id);
			return e;
		}
		for (const t of s.values()) {
			if (this._addVisibleParent(e, t)) continue;
			this._addVisibleChildren(e, t) || e.add(t.id);
		}
		return e;
	}
	_addVisibleParent(e, i) {
		let t = !1;
		for (const r of this._visible.values()) new e$9(r).containsChild(i) && (e.add(r), t = !0);
		return t;
	}
	_addVisibleChildren(e, i) {
		let t = !1;
		for (const r of this._visible.values()) {
			const o = new e$9(r);
			i.containsChild(o) && (e.add(r), t = !0);
		}
		return t;
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/2d/layers/FeatureLikeLayerView2D.js
function ue(e, t) {
	const r = /* @__PURE__ */ new Set();
	return e && e.forEach((e) => r.add(e)), t && t.forEach((e) => r.add(e)), r.has("*") ? ["*"] : Array.from(r);
}
var he = 4294967294;
function ce(e, t) {
	return t$4(e.map((e) => l$7(() => {
		const t = e();
		return t && "object" == typeof t ? "getTime" in t && "function" == typeof t.getTime ? t.getTime() : JSON.stringify(t) : t;
	}, t, U$1)));
}
function pe(e) {
	if (!e.geometry) return e;
	if (!t$5(e.geometry)) return e;
	const t = .001 / re(e.geometry.spatialReference);
	return {
		geometry: L$2(e.geometry, {
			maxSegmentsPerCurve: 12e3,
			maxSegmentLength: 100 * t,
			maxDeviation: 0
		}),
		attributes: e.attributes
	};
}
var de = (l) => {
	const v = i$10(S$2(l));
	let b$5 = class extends v {
		constructor() {
			super(...arguments), this._commandsQueue = new o$13({ process: (e) => {
				switch (e.type) {
					case "override-batch": return this._doOverride(e);
					case "update": return this._doUpdate();
					case "highlight": return this._updateHighlights();
				}
			} }), this._visibilityOverrides = /* @__PURE__ */ new Set(), this._lastAvailableFields = [], this._lastTargetState = null, this.eventLog = new u$5(), this._sourceRefreshVersion = 1, this._displayRefreshVersion = 1, this._pipelineUpdating = !1, this._editUpdatingHandles = new h$2(), this._fields = null, this._sourceUpdating = !1, this.featureEffectView = new c$7(), this._lastUpdate = 0, this._lastRequiredLevel = 0;
		}
		destroy() {
			this._editUpdatingHandles.destroy(), this._workerProxy?.destroy(), this._workerAttached.reject(u$9()), this._commandsQueue.destroy();
		}
		initialize() {
			this._workerAttached = $(), y$3(this._workerAttached.promise), this.addResolvingPromise(this._initProxy()), this.featureEffectView.featureEffect = this.featureEffect, this.featureEffectView.endTransition();
		}
		async _initProxy() {
			const e = this.layer;
			if ("isTable" in e && e.isTable) throw new r$6("featurelayerview:table-not-supported", "table feature layer can't be displayed", { layer: e });
			if (("feature" === e.type || "subtype-group" === e.type) && !1 === A$1(e)?.operations.supportsQuery) throw new r$6("featurelayerview:query-not-supported", "layer view requires a layer with query capability", { layer: e });
			this._workerProxy && this._workerProxy.destroy();
			this._workerProxy = await t$3(this._createClientOptions());
		}
		async _attachProxy() {
			const e = g$3(this.view.extent, this.view.rotation, this.view.center), t = this.view.featuresTilingScheme.getClosestInfoForScale(this.view.scale).level;
			t !== this._lastRequiredLevel && (this._lastRequiredLevel = t);
			const r = {
				tileInfoJSON: this.view?.featuresTilingScheme?.tileInfo?.toJSON(),
				initialViewExtent: e.toJSON(),
				requiredLevel: t,
				requiredScale: this.view.scale
			};
			try {
				await this._workerProxy.pipeline.onAttach(r), this._workerAttached.resolve();
			} catch (i) {
				this._workerAttached.reject(u$9()), m$4(i);
			}
		}
		async _detachProxy() {
			return this._workerProxy.pipeline.onDetach();
		}
		async getWorker() {
			return await this._workerAttached.promise, this._workerProxy;
		}
		get dataUpdating() {
			return this._sourceUpdating || this._editUpdatingHandles.updating;
		}
		get hasAllFeatures() {
			return this.layer.visible && !this.suspended && this.eventLog.hasAllData && this.eventLog.willQueryAllFeatures;
		}
		get hasAllFeaturesInView() {
			const e = this.effectiveDisplayFilter?.where || null, t = !this.eventLog.willQueryAllFeatures && null != e && "1=1" !== e;
			return this.layer.visible && !this.suspended && this.eventLog.hasAllData && !t;
		}
		get hasFullGeometries() {
			return this.layer.visible && !this.suspended && this.eventLog.hasAllData && this.eventLog.willQueryFullResolutionGeometry;
		}
		get labelingCollisionInfos() {
			const e = this.layerAdapter.getLabelingDeconflictionInfo(this.view), t = this.layer.geometryType, r = !this.suspended;
			return e.map(({ vvEvaluators: e, deconflictionEnabled: i, labelingInfo: s }) => ({
				labelingInfo: s,
				container: this.featureContainer,
				vvEvaluators: e,
				deconflictionEnabled: i,
				geometryType: t,
				visible: r
			}));
		}
		get layerAdapter() {
			switch (this.layer.type) {
				case "feature": return "memory" === this.layer.source.type ? new d$1(this.layer) : new F(this.layer);
				case "geojson":
				case "csv":
				case "wfs": return new d$1(this.layer);
				case "parquet": return new y(this.layer);
				case "subtype-group": return new b(this.layer);
				case "ogc-feature": return new c(this.layer);
				case "stream": return new s$1(this.layer);
				case "oriented-imagery": return new m(this.layer);
				case "knowledge-graph-sublayer": return new d(this.layer);
				case "catalog-footprint": return new d$2(this.layer);
				default: this.layer;
			}
			return null;
		}
		get timeExtent() {
			return i$11(this.layerAdapter.timeOptions, this.view?.timeExtent, this._get("timeExtent"));
		}
		get usedMemory() {
			return this.container.usedMemory + this.eventLog.pipelineStatistics.usedMemory;
		}
		getDisplayStatistics(e, t) {
			return this.featureContainer?.getDisplayStatistics(e, t);
		}
		highlight(e, t) {
			let i;
			e instanceof j$2 ? i = [e.getObjectId()] : "number" == typeof e || "string" == typeof e ? i = [e] : q.isCollection(e) && e.length > 0 ? i = e.map((e) => e?.getObjectId()).toArray() : Array.isArray(e) && e.length > 0 && (i = "number" == typeof e[0] || "string" == typeof e[0] ? e : e.map((e) => e?.getObjectId()));
			const o = i?.filter(N);
			if (!o?.length) return e$6();
			const l = r$10(t);
			return this._addHighlights(o, l), e$6(() => !this.destroyed && this._removeHighlights(o, l));
		}
		async hitTest(e, s) {
			const a = await this.featureContainer.hitTest(s);
			if (0 === a.length) return null;
			const { features: n, aggregates: l, tracks: u } = await (await this.getWorker()).pipeline.getDisplayFeatures(a), h = this.featureContainer.getSortKeys(a), c = ({ displayId: e }, { displayId: t }) => h.has(e) && h.has(t) ? h.get(e) - h.get(t) : e - t;
			n.sort(c).reverse(), l.sort(c).reverse();
			const p = has("parquetlayer-hittest-max-feature-count") ?? 1;
			return "parquet" === this.layer.type && n.length > p && (n.length = p), [
				...l.map((r) => this._createAggregateGraphicHit(e, i$5.fromJSON(r))),
				...u.map((t) => this._createTrackGraphicHit(e, i$4.fromJSON(t))),
				...n.map((t) => this._createFeatureGraphicHit(e, j$2.fromJSON(t)))
			];
		}
		async queryAggregateSummaryStatistics(e, t, r) {
			const i = {
				...t,
				scale: this.view.scale
			};
			return n((await this.getWorker()).aggregates.executeQueryForSummaryStatistics(this._cleanUpAggregateQuery(e), i, r), {});
		}
		async queryUniqueValues(e, t, r) {
			const i = await this.getWorker(), s = {
				...t,
				scale: this.view.scale
			};
			return n(i.features.executeQueryForUniqueValues(this._cleanUpQuery(e), s, r), { uniqueValueInfos: [] });
		}
		async queryAggregateUniqueValues(e, t, r) {
			const i = await this.getWorker(), s = {
				...t,
				scale: this.view.scale
			};
			return n(i.aggregates.executeQueryForUniqueValues(this._cleanUpAggregateQuery(e), s, r), { uniqueValueInfos: [] });
		}
		async queryAggregateClassBreaks(e, t, r) {
			const i = await this.getWorker(), s = {
				...t,
				scale: this.view.scale
			};
			return n(i.aggregates.executeQueryForClassBreaks(this._cleanUpAggregateQuery(e), s, r), { classBreakInfos: [] });
		}
		async queryHistogram(e, t, r) {
			const i = await this.getWorker(), s = {
				...t,
				scale: this.view.scale
			};
			return n(i.features.executeQueryForHistogram(this._cleanUpQuery(e), s, r), {
				bins: [],
				maxValue: null,
				minValue: null,
				normalizationTotal: null
			});
		}
		async queryAggregateCount(e, t) {
			return n((await this.getWorker()).aggregates.executeQueryForCount(this._cleanUpAggregateQuery(e), t), 0);
		}
		async queryAggregateHistogram(e, t, r) {
			const i = await this.getWorker(), s = {
				...t,
				scale: this.view.scale
			};
			return n(i.aggregates.executeQueryForHistogram(this._cleanUpAggregateQuery(e), s, r), {
				bins: [],
				maxValue: null,
				minValue: null,
				normalizationTotal: null
			});
		}
		async queryAggregateIds(e, t) {
			return n((await this.getWorker()).aggregates.executeQueryForIds(this._cleanUpAggregateQuery(e), t), []);
		}
		async queryAggregateJSON(e, t) {
			return n((await this.getWorker()).aggregates.executeQuery(this._cleanUpAggregateQuery(e), t), { features: [] });
		}
		async queryAttributeBins(e, t) {
			const i = await n((await this.getWorker()).features.executeAttributeBinsQuery(this._cleanUpAttributeBinsQuery(e), t), { features: [] });
			return i$8.fromJSON(i);
		}
		async queryAggregates(e, t) {
			const i = await n((await this.getWorker()).aggregates.executeQuery(this._cleanUpAggregateQuery(e), t), { features: [] }), s = c$6.fromJSON(i);
			return s.features.forEach((e) => this.layerAdapter.setAggregateGraphicOrigin?.(e)), s;
		}
		async queryClassBreaks(e, t, r) {
			const i = await this.getWorker(), s = {
				...t,
				scale: this.view.scale
			};
			return n(i.features.executeQueryForClassBreaks(this._cleanUpQuery(e), s, r), { classBreakInfos: [] });
		}
		async queryExtent(e, t) {
			const i = await n((await this.getWorker()).features.executeQueryForExtent(this._cleanUpQuery(e), t), {
				count: 0,
				extent: null
			});
			return {
				count: i.count,
				extent: z$1.fromJSON(i.extent)
			};
		}
		async queryFeatureCount(e, t) {
			return n((await this.getWorker()).features.executeQueryForCount(this._cleanUpQuery(e), t), 0);
		}
		queryFeatures(e, t) {
			return this.queryFeaturesJSON(e, t).then((e) => {
				const t = g$2.fromJSON(e);
				return t.features.forEach((e) => this._setOriginForFeature(e)), t;
			});
		}
		async queryFeaturesJSON(e, t) {
			return n((await this.getWorker()).features.executeQuery(this._cleanUpQuery(e), t), { features: [] });
		}
		async queryHeatmapStatistics(e) {
			return (await this.getWorker()).pipeline.queryHeatmapStatistics(e);
		}
		async queryObjectIds(e, t) {
			return n((await this.getWorker()).features.executeQueryForIds(this._cleanUpQuery(e), t), []);
		}
		async queryStatistics() {
			return n((await this.getWorker()).pipeline.queryStatistics(), {
				featureCount: 0,
				ringCount: 0,
				vertexCount: 0
			});
		}
		async querySummaryStatistics(e, t, r) {
			const i = await this.getWorker(), s = {
				...t,
				scale: this.view.scale
			};
			return n(i.features.executeQueryForSummaryStatistics(this._cleanUpQuery(e), s, r), {});
		}
		async queryVisibleFeatures(e, t) {
			const i = await n((await this.getWorker()).pipeline.queryVisibleFeatures(this._cleanUpQuery(e), t), { features: [] }), s = g$2.fromJSON(i);
			return s.features.forEach((e) => this._setOriginForFeature(e)), s;
		}
		async getSampleFeatures(e) {
			return (await this.getWorker()).pipeline.getSampleFeatures(e);
		}
		setVisibility(e, t) {
			t ? this._visibilityOverrides.delete(e) : this._visibilityOverrides.add(e), this._update();
		}
		update(e) {
			const t = performance.now();
			if (t - this._lastUpdate < 200) return;
			if (this._lastUpdate = t, !this.subscriptionManager) return;
			this.view.animation && !this._lastTargetState && (this._lastTargetState = e.state.clone()), !this.view.animation && this._lastTargetState && (this._lastTargetState = null);
			const r = this.subscriptionManager.update(e.targetState, this._lastTargetState), i = new Set(this.subscriptionManager.coverage?.target.keys());
			for (const s of this.featureContainer.tiles || []) s.isCoverage = i.has(s.id);
			this.featureContainer.setVisibleTiles(r);
		}
		attach() {
			has("esri-2d-update-debug") && console.debug("FeatureLayerView2D.attach"), y$3(this._updatingHandles.addPromise(this._workerAttached.promise)), y$3(this._attachProxy()), this.featureContainer = new c$5(this), this.container.addChild(this.featureContainer), this.view.timeline.record(`${this.layer.title} (FeatureLayer) Attach`), this.subscriptionManager = new r({
				tileInfoView: this.view.featuresTilingScheme,
				updateSubscriptions: (e) => {
					this.featureContainer.updateSubscriptions(e), y$3(this._updatingHandles.addPromise(this.getWorker().then((t) => t.pipeline.updateSubscriptions(e))));
				},
				isDone: (e) => this.featureContainer.isDone(e)
			}), this.requestUpdate(), this.addAttachHandles([
				ce([
					() => this._displayRefreshVersion,
					() => "displayFilterInfo" in this.layer && this.layer.displayFilterInfo,
					() => this.timeExtent,
					() => this.clips,
					() => this.filter,
					() => this.effectiveDisplayFilterClause,
					() => this.featureEffect,
					() => this._sourceRefreshVersion,
					() => this.view.timeZone,
					() => this.view.timeExtent,
					...this.layerAdapter.getUpdateHashProperties(this.view)
				], () => this._update()),
				l$7(() => this.updateSuspended, (e) => {
					e || (this.subscriptionManager.resume(), this.view.labelManager.requestUpdate());
				}),
				l$7(() => this.visible, (e) => {
					this.view.labelManager.symbolFader.restartDeclutter(), this.view.labelManager.requestUpdate();
				}),
				l$7(() => this.view.scale, async (e) => {
					const t = this.view.featuresTilingScheme.getClosestInfoForScale(e).level;
					if (t !== this._lastRequiredLevel) {
						this._lastRequiredLevel = t;
						(await this.getWorker()).pipeline.updateRequiredLevel(t, e);
					}
				}, h),
				l$7(() => this.view.stationary, async (e) => {
					if (e) {
						const e = await this.getWorker(), t = g$3(this.view.extent, this.view.rotation, this.view.center);
						e.pipeline.updateStationaryExtent(t.toJSON(), this.featureContainer.requiresRefresh);
					}
				})
			]), this._update(), "stream" !== this.layer.type && "parquet" !== this.layer.type && "catalog-footprint" !== this.layer.type && this.addAttachHandles(this.layer.on("edits", (e) => this._editUpdatingHandles.addPromise(this._edit(e))));
		}
		detach() {
			has("esri-2d-update-debug") && console.debug("FeatureLayerView2D.detach"), this._detachProxy(), this._fields = null, this.view.labelManager.removeContainer(this.featureContainer), this.featureContainer.destroy(), this.featureContainer = null, this._commandsQueue.clear(), this.container.removeAllChildren(), this.subscriptionManager = u$8(this.subscriptionManager), this._workerProxy.pipeline.onDetach(), this._workerAttached = $(), y$3(this._workerAttached.promise), this._lastAvailableFields = [], this._lastSchema = null;
		}
		viewChange() {
			this.requestUpdate();
		}
		moveEnd() {
			this.requestUpdate();
		}
		addOverrides(e) {
			return this._commandsQueue.push({
				type: "override",
				options: {
					...e,
					release: []
				}
			});
		}
		removeOverrides(e) {
			for (const r of e) if (null == r) throw new r$6("featurelayerview:bad-override", "Tried to remove an override for an invalid objectId", { objectId: r });
			const t = {
				added: [],
				updated: [],
				removed: [],
				release: e,
				isWeak: !1,
				historicMoment: null
			};
			return this._commandsQueue.push({
				type: "override",
				options: t
			});
		}
		onProxyClientEvent(e) {}
		isUpdating() {
			const e = "renderer" in this.layer && null != this.layer.renderer, t = this._commandsQueue.updateTracking.updating, r = null != this._updatingRequiredPromise, i = this.featureContainer.updatingHandles.updating, s = this.updateRequested || e && (t || r) || i || this._pipelineUpdating || this.dataUpdating;
			if (has("esri-2d-log-updating")) {
				console.log(`Updating FLV2D (${this.layer.id}): ${s}\n  -> updateRequested ${this.updateRequested}\n  -> hasRenderer ${e}\n  -> updatingRequiredFields ${r}\n  -> hasPendingCommand ${t}\n  -> dataUpdating ${this.dataUpdating}\n  -> processing ${this._pipelineUpdating}\n  -> updatingContainer ${i}\n`);
				for (const e of this.featureContainer.subscriptions()) console.log(`    -> Tile[${e.id}] Done: ${e.done}`);
			}
			return s;
		}
		_createClientOptions() {
			const e = this;
			return {
				openMemoryPorts: async () => {
					if (this.layerAdapter?.openMessagePorts) {
						const e = await this.layerAdapter.openMessagePorts();
						return {
							result: e,
							transferList: e
						};
					}
					throw new Error("InternalError: Layer adapter does not support opening message ports");
				},
				get container() {
					return e.featureContainer;
				},
				setUpdating: (e) => {
					this._set("_pipelineUpdating", e.pipeline), this._set("_sourceUpdating", e.source);
				},
				emitEvent: (e) => {
					this.onProxyClientEvent(e);
				},
				get eventLog() {
					return e.eventLog;
				},
				fetch: async (t) => {
					if (has("esri-2d-stabilize-glyphs")) {
						const r = [];
						for (const i of t) r.push(await e.view.stage.painter.textureManager.rasterizeItem(i));
						return r;
					}
					return Promise.all(t.map((t) => e.view.stage.painter.textureManager.rasterizeItem(t)));
				},
				fetchDictionary: (e) => Promise.all(e.map((e) => this._fetchDictionaryRequest(e)))
			};
		}
		async _fetchDictionaryRequest(e) {
			try {
				if ("subtype-group" === this.layer.type) throw new Error("InternalError: SubtypeGroupLayer does not support dictionary renderer");
				const t = this.layer.renderer;
				if (!t || "dictionary" !== t.type) throw new Error("InternalError: Expected layer to have a DictionaryRenderer");
				const r = this._lastSchema.processor.mesh.factory.symbology;
				if ("dictionary" !== r.type) throw new Error("InternalError: Expected schema to be of type 'dictionary'");
				const i = {
					cimAnalyzer: this.view.stage.cimAnalyzer,
					cimResourceManager: this.view.stage.painter.textureManager.resourceManager,
					store: this.featureContainer.instanceStore,
					scaleExpression: r.scaleExpression
				};
				this._fields || (this._fields = this.layer.fields.map((e) => e.toJSON()));
				const s = r.visualVariableUniforms, a = l$8(this.layer.geometryType), o = await t.getSymbolForControlString(e.controlString, a, !1);
				if (!o || !o.data) return {
					type: "dictionary-response",
					meshes: []
				};
				return {
					type: "dictionary-response",
					meshes: await c$12({
						...o.data,
						hasTextStringTemplates: !0
					}, {
						uniforms: s,
						path: "renderer",
						schemaOptions: i
					})
				};
			} catch (t) {
				return {
					type: "dictionary-response",
					meshes: []
				};
			}
		}
		_cleanUpQuery(e) {
			const t = R$2.from(e) || this.createQuery();
			return t.outSpatialReference || (t.outSpatialReference = this.view.spatialReference), t.toJSON();
		}
		_cleanUpAttributeBinsQuery(e) {
			const t = R$3.from(e);
			return t.outSpatialReference || (t.outSpatialReference = this.view.spatialReference), t.toJSON();
		}
		_cleanUpAggregateQuery(e) {
			const t = R$2.from(e) || this.createAggregateQuery();
			t.outSpatialReference || (t.outSpatialReference = this.view.spatialReference);
			const r = t.objectIds ?? [];
			for (const i of t.aggregateIds ?? []) r.push(i);
			return t.objectIds = r, t.aggregateIds = [], t.toJSON();
		}
		async _update() {
			return this._commandsQueue.push({ type: "update" });
		}
		_edit(e) {
			return this._commandsQueue.push({
				type: "edit",
				event: e
			});
		}
		async doRefresh(e) {
			this.attached && (this.updateSuspended && e || (e ? this.incrementSourceRefreshVersion() : this.incrementDisplayRefreshVersion()));
		}
		incrementSourceRefreshVersion() {
			this._sourceRefreshVersion = (this._sourceRefreshVersion + 1) % he + 1;
		}
		incrementDisplayRefreshVersion() {
			this._displayRefreshVersion = (this._displayRefreshVersion + 1) % he + 1;
		}
		async _resolveIdenifiers(e) {
			const t = [], r = [];
			for (const a of e) null == a.objectId || -1 === a.objectId ? null == a.globalId ? n$8.getLogger(this).warn("mapview-apply-edits", "A feature identifier must contain either a GlobalId or ObjectId. Ignoring", { identifier: a }) : r.push(a.globalId) : t.push(a.objectId);
			const i = "globalIdField" in this.layer && this.layer.globalIdField, s = i && this.availableFields.includes(i);
			if (r.length && !s) return n$8.getLogger(this).error(new r$6("mapview-apply-edits", `Editing the specified service requires the layer's globalIdField, ${i} to be included the layer's outFields for updates to be reflected on the map`)), t;
			if (r.length) {
				const e = await this._workerProxy.pipeline.getObjectIdsFromGlobalIds(r);
				for (const r of e) t.push(r);
			}
			return t;
		}
		_resolveOverrides(e) {
			const t = !1, r = !1, i = l$8(this.layer.geometryType), s = e$2(this.layer), a = [];
			for (const n of e.added) {
				const e = B$1(pe(n), i, t, r, s);
				if (null == e.objectId) throw new r$6("featurelayerview:bad-override", "Feature does not have an objectId", { feature: n });
				a.push(e);
			}
			for (const n of e.updated) {
				const e = B$1(pe(n), i, t, r, s);
				if (null == e.objectId) throw new r$6("featurelayerview:bad-override", "Feature does not have an objectId", { feature: n });
				a.push(e);
			}
			for (const n of e.removed) if (null == n) throw new r$6("featurelayerview:bad-override", "Tried to remove an invalid objectId", { objectId: n });
			return {
				type: "override",
				updates: a,
				removed: e.removed,
				release: e.release,
				isWeak: e.isWeak ?? !1,
				historicMoment: e.historicMoment ?? null
			};
		}
		async _resolveEdit(e) {
			const t = this.layer, r = e.historicMoment?.getTime() ?? null, i = "layerId" in t && e.editedFeatures?.find((e) => e.layerId === t.layerId);
			if (i && this._canEditByFeature(i)) {
				const { adds: e, deletes: t, updates: s } = i.editedFeatures, a = this.layer.objectIdField, o = s.map((e) => e.current), n = t.map((e) => "attributes" in e ? { objectId: a ? e.attributes[a] : null } : e), l = await this._resolveIdenifiers(n);
				return this._resolveOverrides({
					added: e,
					updated: o,
					removed: l,
					historicMoment: r,
					isWeak: !0,
					release: []
				});
			}
			const [s, a, o] = await Promise.all([
				this._resolveIdenifiers(e.addedFeatures),
				this._resolveIdenifiers(e.updatedFeatures),
				this._resolveIdenifiers(e.deletedFeatures)
			]);
			return {
				type: "override-by-id",
				updates: [...s, ...a],
				removed: o,
				historicMoment: r,
				isWeak: !0
			};
		}
		_canEditByFeature(e) {
			const { adds: t, updates: r } = e.editedFeatures;
			return t.every((e) => this.view.spatialReference.equals(e.geometry?.spatialReference)) && r.every((e) => this.view.spatialReference.equals(e.current.geometry?.spatialReference));
		}
		async _doUpdate() {
			"featureReduction" in this.layer && this.layer.featureReduction && this.layer.featureReduction !== this._lastFeatureReduction && (this.layer.featureReduction = this.layer.featureReduction?.clone(), this._lastFeatureReduction = this.layer.featureReduction);
			try {
				if (await Promise.allSettled([this._handleChange(), _(this.layer)]), this.destroyed || !this.layerAdapter?.hasRequiredSupport || !this.subscriptionManager) return;
				const e = this.featureContainer.instanceStore;
				this.featureContainer.attributeView.lockTextureUploads();
				const t = this._lastSchema?.processor.mesh.factory.symbology?.type;
				let r = !0;
				"subtype-group" !== this.layer.type && "dictionary" === this.layer.renderer?.type && "dictionary" === t && (r = !1), e.updateStart(r);
				const i = this.featureEffect, s = {
					store: e,
					cimAnalyzer: this.view.stage.cimAnalyzer,
					cimResourceManager: this.view.stage.painter.textureManager.resourceManager,
					scaleExpression: void 0
				}, a = await this._createViewSchemaConfig(), o = {
					source: await this.layerAdapter.createSourceSchema(a, this._sourceRefreshVersion),
					processor: await this.layerAdapter.createProcessorSchema(s, a, this._displayRefreshVersion)
				}, n = o.processor.mesh.factory.labels;
				n && this.view.labelManager.setLabelSchemaStyles(n, this.featureContainer);
				const l = i$6(this._lastSchema?.source.strategy, o.source.strategy) || i$6(this._lastSchema?.processor, o.processor);
				if (!l) return this.featureContainer.requestRender(), this.featureContainer.attributeView.unlockTextureUploads(), e.updateEnd(r), void (this.featureEffectView.featureEffect = i);
				this._lastSchema = o, this._fields = null;
				const u = Math.round(performance.now());
				has("esri-2d-update-debug") && console.debug(`Id[${this.layer.uid}] Version[${u}] FeatureLayerView2D._doUpdate`, { changes: l });
				await (await this.getWorker()).pipeline.updateSchema(o, u), e.updateEnd(r), this.featureEffectView.featureEffect = i, this.featureEffectView.endTransition(), this.featureContainer.restartAllAnimations(), this.featureContainer.attributeView.unlockTextureUploads(), this.featureContainer.trySwapRenderState(), this.featureContainer.requestRender();
				const p = o.processor.mesh.strategy, d = "cluster" === p.type || "binning" === p.type, y = "heatmap" === o.processor.mesh.factory.symbology.type, g = d || y ? "target-defer" : "eager";
				this.subscriptionManager.setVisibilityStrategy(g), has("esri-2d-update-debug") && console.debug(`Version[${u}] FeatureLayerView2D.updateEnd`), this.requestUpdate();
			} catch (e) {}
		}
		async _doOverride(e) {
			const t = await this.getWorker();
			try {
				let r = new e$1();
				for (const i of e.messages) switch (i.type) {
					case "edit":
						r.tryAdd(await this._resolveEdit(i.event)) || (await t.pipeline.onOverride(r.toMessage()), r = new e$1(), r.tryAdd(await this._resolveEdit(i.event)));
						break;
					case "override":
						r.tryAdd(this._resolveOverrides(i.options)) || (await t.pipeline.onOverride(r.toMessage()), r = new e$1(), r.tryAdd(this._resolveOverrides(i.options)));
						break;
				}
				await t.pipeline.onOverride(r.toMessage());
			} catch (r) {
				d$6(r);
			}
		}
		_getEffectiveAvailableFields(e) {
			const t = ue(this._lastAvailableFields, e);
			return this._lastAvailableFields = t, h$1(this.layer.fieldsIndex, t);
		}
		async _createViewSchemaConfig() {
			const e = this.requiresCurrentUser ? await this.signedInUser : null, t = [U(this.view, this.layerAdapter, this.timeExtent, this._visibilityOverrides, this.filter, this.effectiveDisplayFilterClause), this.featureEffect?.filter?.toJSON() ?? null];
			return {
				availableFields: this._getEffectiveAvailableFields(this.availableFields),
				displayFilterEnabled: this.displayFilterEnabled,
				filters: t,
				scale: this.view.scale,
				timeZone: this.view.timeZone,
				timeExtent: this.view.timeExtent,
				currentUser: e,
				spatialReference: this.view.spatialReference,
				extent: this.view.extent
			};
		}
		_processHighlight() {
			this._commandsQueue.push({ type: "highlight" });
		}
		async _updateHighlights() {
			const e = this._getHighlights(), t = await this.getWorker();
			if (this.destroyed) return;
			const r = t.pipeline.updateHighlight({ highlights: e }).catch((e) => {
				d$6(e) || n$8.getLogger(this).error(e);
			});
			this._updatingHandles.addPromise(r);
		}
		_setOriginForFeature(e) {
			e.layer = e.sourceLayer = this.layer, this.layerAdapter.setGraphicOrigin(e);
		}
		_createFeatureGraphicHit(e, t) {
			return this._setOriginForFeature(t), this._createGraphicHit(e, t);
		}
		_createTrackGraphicHit(e, t) {
			return t.layer = t.sourceLayer = this.layer, this.layerAdapter.setTrackGraphicOrigin?.(t), this._createGraphicHit(e, t);
		}
		_createAggregateGraphicHit(e, t) {
			return t.layer = t.sourceLayer = this.layer, this.layerAdapter.setAggregateGraphicOrigin?.(t), this._createGraphicHit(e, t);
		}
		_createGraphicHit(e, t) {
			return null != t.geometry && (t.geometry.spatialReference = this.view.spatialReference), {
				type: "graphic",
				graphic: t,
				layer: this.layer,
				mapPoint: e
			};
		}
	};
	function U(e, t, r, i, s, a) {
		s && (s = s.clone());
		const o = null != s ? s.timeExtent : null, n = null != r && null != o ? r.intersection(o) : r || o;
		n && (s ??= new d$9(), s.timeExtent = n), s = t.addFilters?.(s, e) ?? s, a && (s ??= new d$9(), s.where = o$11(s.where, a));
		let l = s?.toJSON() ?? null;
		return i.size && (l ??= new d$9().toJSON(), l.hiddenIds = Array.from(i)), l;
	}
	return __decorate([a$4()], b$5.prototype, "_commandsQueue", void 0), __decorate([a$4()], b$5.prototype, "_sourceRefreshVersion", void 0), __decorate([a$4()], b$5.prototype, "_displayRefreshVersion", void 0), __decorate([a$4({ readOnly: !0 })], b$5.prototype, "_pipelineUpdating", void 0), __decorate([a$4()], b$5.prototype, "_sourceUpdating", void 0), __decorate([a$4({ readOnly: !0 })], b$5.prototype, "dataUpdating", null), __decorate([a$4({ readOnly: !0 })], b$5.prototype, "hasAllFeatures", null), __decorate([a$4({ readOnly: !0 })], b$5.prototype, "hasAllFeaturesInView", null), __decorate([a$4({ readOnly: !0 })], b$5.prototype, "hasFullGeometries", null), __decorate([a$4()], b$5.prototype, "featureEffectView", void 0), __decorate([a$4()], b$5.prototype, "labelingCollisionInfos", null), __decorate([a$4()], b$5.prototype, "layerAdapter", null), __decorate([a$4({ readOnly: !0 })], b$5.prototype, "timeExtent", null), b$5 = __decorate([c$8("esri.views.2d.layers.FeatureLikeLayerView2D")], b$5), b$5;
};
//#endregion
//#region node_modules/@arcgis/core/views/layers/support/WhereClauseVisitor.js
var s = class {
	constructor(i) {
		this._fieldsIndex = i, this._clauses = [];
	}
	async finish() {
		return { requiresCurrentUser: (await Promise.all(this._clauses)).some((i) => i?.currentUserRequired) };
	}
	visitClientWhereClause(e) {
		e && this._clauses.push(c$9(e, this._fieldsIndex));
	}
	visitFeatureReduction(i) {
		if (i) switch (i.type) {
			case "binning":
			case "cluster": this.visitLabelingInfo(i.labelsVisible, i.labelingInfo);
		}
	}
	visitLabelingInfo(i, e) {
		if (i && null != e) for (const s of e) this.visitClientWhereClause(s.where);
	}
	visitDisplayFilter(i, e) {
		if (i) for (const s of e?.filters ?? []) this.visitClientWhereClause(s.where);
	}
	visitFilter(i) {
		this.visitClientWhereClause(i?.where);
	}
	visitRelativeTimeQuery(s) {
		this._clauses.push(c$10(this._fieldsIndex, s).then((e) => s && e.length > 0 ? c$9(s, this._fieldsIndex) : null));
	}
	visitTrackInfo(i) {
		null != i && (this.visitLabelingInfo(i?.latestObservations.labelsVisible, i?.latestObservations.labelingInfo), this.visitLabelingInfo(i?.previousObservations.labelsVisible, i?.previousObservations.labelingInfo), this.visitLabelingInfo(i?.trackLines.labelsVisible, i?.trackLines.labelingInfo));
	}
};
//#endregion
//#region node_modules/@arcgis/core/views/layers/FeatureLikeLayerView.js
var B = class extends I$2 {
	constructor() {
		super(...arguments), this._updatingRequiredPromise = null, this.filter = null, this.layer = null, this.requiresCurrentUser = !1, this.requiredFields = [], this.view = null;
	}
	initialize() {
		this.addHandles([
			l$7(() => {
				const e = this.layer, i = this.view;
				return [
					e && "elevationInfo" in e ? e.elevationInfo?.featureExpressionInfo : null,
					e && "displayField" in e ? e.displayField : null,
					e && "timeInfo" in e && e.timeInfo,
					e && "renderer" in e && e.renderer,
					e && "labelingInfo" in e && e.labelingInfo,
					e && "floorInfo" in e && e.floorInfo,
					i?.requiredFieldsOptions?.featureTitleFields && e && "featureTitleFields" in e && e.featureTitleFields,
					i?.requiredFieldsOptions?.utilityNetworkFields && e$7(i, e),
					e && "displayFilterInfo" in e && e.displayFilterInfo,
					this.displayFilterEnabled,
					this.filter,
					this.featureEffect,
					this.timeExtent,
					"knowledge-graph-sublayer" === e?.type && "link-chart" === e.parentCompositeLayer.type && e.parentCompositeLayer.linkChart?.linkChartProperties.nonspatialDataDisplay?.mode
				];
			}, () => this._handleChange(), w$1),
			a$5(() => this.view?.floors, "change", () => this._handleChange()),
			a$5(() => (this.layer && "displayFilterInfo" in this.layer ? this.layer.displayFilterInfo : null)?.filters, "change", () => this._handleChange()),
			a$5(() => this.layer && "sublayers" in this.layer ? this.layer.sublayers : null, "change", () => this._handleChange())
		]);
	}
	get availableFields() {
		if (!this.layer) return [];
		const { layer: e, layer: { fieldsIndex: i }, requiredFields: t } = this;
		return "outFields" in e && e.outFields ? I$1(i, [...T(i, e.outFields), ...t]) : I$1(i, t);
	}
	get displayFilterEnabled() {
		return (this.view?.displayFilterEnabled ?? !0) && (!("displayFilterEnabled" in this.layer) || (this.layer?.displayFilterEnabled ?? !0));
	}
	get effectiveDisplayFilter() {
		const e = this.layer;
		return "displayFilterInfo" in e && this.displayFilterEnabled && e.displayFilterInfo ? u$13(e.displayFilterInfo, this.view) : null;
	}
	get effectiveDisplayFilterClause() {
		const e = this.effectiveDisplayFilter?.where ?? null;
		return e && this.hasHighlight ? d$7(e, p$2(this.layer.objectIdField, this.highlightIds)) : e;
	}
	get featureEffect() {
		return this.layer && "featureEffect" in this.layer ? this.layer.featureEffect : null;
	}
	set featureEffect(e) {
		this._override("featureEffect", e);
	}
	get highlightOptions() {}
	set highlightOptions(e) {}
	get maximumNumberOfFeatures() {
		return 0;
	}
	set maximumNumberOfFeatures(e) {
		n$8.getLogger(this).error("#maximumNumberOfFeatures=", "Setting maximum number of features is not supported");
	}
	get maximumNumberOfFeaturesExceeded() {
		return !1;
	}
	get signedInUser() {
		return this.layer?.url ? Z$1(this.layer.url) : Promise.resolve(null);
	}
	createQuery() {
		const e = {
			outFields: ["*"],
			returnGeometry: !0,
			outSpatialReference: this.view.spatialReference
		}, i = null != this.filter ? this.filter.createQuery(e) : new R$2(e);
		return "floorInfo" in this.layer && this.layer.floorInfo && (i.where = o$11(i.where, o$12(this))), this.displayFilterEnabled && (i.where = o$11(i.where, this.effectiveDisplayFilter?.where)), null != this.timeExtent && (i.timeExtent = null != i.timeExtent ? i.timeExtent.intersection(this.timeExtent) : this.timeExtent.clone()), i;
	}
	createAggregateQuery() {
		return new R$2({
			outFields: ["*"],
			returnGeometry: !0,
			outSpatialReference: this.view.spatialReference
		});
	}
	async fetchPopupFeaturesFromGraphics(e, i) {
		const t = [], r = /* @__PURE__ */ new Set();
		for (const s of e) {
			const l = n$10(t$6(s.origin), {
				...i,
				checkPopupEnabled: !0
			});
			l && (t.push(s), r.add(l));
		}
		if (0 === t.length || 0 === r.size) return [];
		const l = await this._createPopupQuery(r, i);
		return await s$7(this.layer, t, l, {
			hasRequiredFields: (e, i) => this._popupFeatureHasRequiredFields(e, i),
			...i
		});
	}
	_handleChange() {
		const e = Promise.all([this._updateRequiredFields(), this._updateClientWhereClauseRequirements()]).then(() => {});
		return this._updatingRequiredPromise = e, e.then(() => {
			this._updatingRequiredPromise === e && (this._updatingRequiredPromise = null);
		}), e;
	}
	async _updateClientWhereClauseRequirements() {
		if (!this.layer || !this.view) return;
		const { layer: e } = this, t = new s(e.fieldsIndex);
		if (t.visitFilter(this.filter), "featureReduction" in e && t.visitFeatureReduction(e.featureReduction), "labelingInfo" in e && t.visitLabelingInfo(e.labelsVisible, e.labelingInfo), "trackInfo" in e && t.visitTrackInfo(e.trackInfo), "2d" === this.view.type && (t.visitFilter(this.featureEffect?.filter), "featureReduction" in e && t.visitFeatureReduction(e.featureReduction), "displayFilterInfo" in e && t.visitDisplayFilter(this.displayFilterEnabled, e.displayFilterInfo)), "subtype-group" === e.type) for (const i of e.sublayers) t.visitLabelingInfo(i.labelsVisible, i.labelingInfo);
		"definitionExpression" in e && "capabilities" in e && e.capabilities?.query.relativeTimeBinWindow && t.visitRelativeTimeQuery(e.definitionExpression);
		try {
			const e = await t.finish();
			this._set("requiresCurrentUser", e.requiresCurrentUser);
		} catch (r) {
			n$8.getLogger(this).error(r);
		}
	}
	async _updateRequiredFields() {
		if (!this.layer || !this.view) return;
		const e = "3d" === this.view.type, { layer: t, layer: { fieldsIndex: r } } = this, l = "renderer" in t && t.renderer, s = "orderBy" in t && t.orderBy, n = "featureReduction" in t ? t.featureReduction : null, o = /* @__PURE__ */ new Set(), a = [
			l ? l.collectRequiredFields(o, r) : null,
			Z(o, t),
			e && "elevationInfo" in t ? L$1(o, t) : null,
			null != this.filter ? C$1(o, t, this.filter) : null,
			e || null == this.featureEffect ? null : C$1(o, t, this.featureEffect.filter),
			!e && n ? j$1(o, t, n) : null,
			!e && s ? R$1(o, t, s) : null,
			"definitionExpression" in t && "capabilities" in t && t.capabilities?.query.relativeTimeBinWindow ? p$5(o, t.fieldsIndex, t.definitionExpression) : null
		];
		if ("timeInfo" in t && t.timeInfo && this.timeExtent && w$2(o, t.fieldsIndex, [t.timeInfo.startField, t.timeInfo.endField]), "timeInfo" in t && t.timeInfo && "trackInfo" in t && t.trackInfo) {
			const { trackInfo: e } = t;
			w$2(o, t.fieldsIndex, [t.timeInfo.trackIdField]), "feature" !== t.type && "startTimeField" !== e.timeField || w$2(o, t.fieldsIndex, [t.timeInfo.startField]), "endTimeField" === e.timeField && w$2(o, t.fieldsIndex, [t.timeInfo.endField]), await D(o, t);
		}
		if ("floorInfo" in t && t.floorInfo && w$2(o, t.fieldsIndex, [t.floorInfo.floorField]), "featureTitleFields" in t && this.view?.requiredFieldsOptions?.featureTitleFields && t.featureTitleFields && w$2(o, t.fieldsIndex, t.featureTitleFields), "feature" === t.type && t.globalIdField && this.view?.requiredFieldsOptions?.globalIdField && w$2(o, t.fieldsIndex, [t.globalIdField]), "feature" !== t.type && "subtype-group" !== t.type || !this.view?.requiredFieldsOptions?.utilityNetworkFields || w$2(o, t.fieldsIndex, e$7(this.view, t)), this.displayFilterEnabled && "displayFilterInfo" in t && a.push(z$2(o, t, t.displayFilterInfo)), "feature" === t.type && e && null != t.infoFor3D && (t.globalIdField ?? n$8.getLogger(this).error("globalIdField missing on 3DObjectFeatureLayer"), w$2(o, t.fieldsIndex, [t.globalIdField])), "subtype-group" === t.type) {
			x$1(o, r, t.subtypeField);
			const e = t.sublayers.map((e) => Promise.all([e.renderer?.collectRequiredFields(o, r), Z(o, e)]));
			a.push(Promise.all(e));
		}
		if ("catalog-footprint" === t.type && t.parent) {
			const e = t.parent;
			w$2(o, r, [
				e.itemNameField,
				e.itemSourceField,
				e.itemTypeField,
				e.maxScaleField,
				e.minScaleField
			]);
		}
		"knowledge-graph-sublayer" === t.type && "link-chart" === t.parentCompositeLayer.type && x$1(o, r, "LC.ESRI__IsSpatial");
		const u = await Promise.allSettled(a);
		if (e) x$1(o, r, t.objectIdField);
		else for (const i of e$12(e$2(t))) x$1(o, r, i);
		e && "displayField" in t && t.displayField && x$1(o, r, t.displayField);
		for (const p of u) "rejected" === p.status && n$8.getLogger(this).error(p.reason);
		const d = Array.from(o).sort();
		this._set("requiredFields", d);
	}
	_popupFeatureHasRequiredFields(e, i) {
		return Ue(e, i);
	}
	async _createPopupQuery(e, i) {
		const r = this.layer.createQuery(), l = /* @__PURE__ */ new Set();
		let s = "point" === this.layer.geometryType;
		for (const n of e) {
			if (!s) {
				const e = await n$9(n);
				s$6(i), s = (e && e.arcadeUtils.hasGeometryOperations(n)) ?? !1;
			}
			const e = await p$8(this.layer, n);
			s$6(i);
			for (const i of e) l.add(i);
		}
		return r.where = "1=1", r.returnGeometry = s, r.returnZ = s, r.returnM = s, r.outFields = Array.from(l), r.outSpatialReference = this.view.spatialReference, "floorInfo" in this.layer && this.layer.floorInfo && (r.where = o$11(r.where, o$12(this))), r;
	}
	canResume() {
		return !!super.canResume() && (null == this.timeExtent || !this.timeExtent.isEmpty);
	}
	getTest() {}
	get test() {}
};
__decorate([a$4()], B.prototype, "_updatingRequiredPromise", void 0), __decorate([a$4({ readOnly: !0 })], B.prototype, "availableFields", null), __decorate([a$4({ readOnly: !0 })], B.prototype, "displayFilterEnabled", null), __decorate([a$4({ readOnly: !0 })], B.prototype, "effectiveDisplayFilter", null), __decorate([a$4({ readOnly: !0 })], B.prototype, "effectiveDisplayFilterClause", null), __decorate([a$4({ type: p$4 })], B.prototype, "featureEffect", null), __decorate([a$4({ type: d$9 })], B.prototype, "filter", void 0), __decorate([a$4()], B.prototype, "layer", void 0), __decorate([a$4({ type: Number })], B.prototype, "maximumNumberOfFeatures", null), __decorate([a$4({
	readOnly: !0,
	type: Boolean
})], B.prototype, "maximumNumberOfFeaturesExceeded", null), __decorate([a$4()], B.prototype, "requiresCurrentUser", void 0), __decorate([a$4({ readOnly: !0 })], B.prototype, "requiredFields", void 0), __decorate([a$4({ readOnly: !0 })], B.prototype, "signedInUser", null), __decorate([a$4()], B.prototype, "view", void 0), B = __decorate([c$8("esri.views.layers.FeatureLikeLayerView")], B);
//#endregion
export { de as n, n as r, B as t };

//# sourceMappingURL=FeatureLikeLayerView-CuDcYYqw.js.map